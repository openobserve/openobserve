name: Security audit
on:
  push:
    paths-ignore:
      - "**.md"
      - "**.yml"
      - "**.yaml"

jobs:
  security_audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: taiki-e/install-action@v2
        with:
          tool: cargo-audit
      - name: run cargo audit
        run: cargo audit
