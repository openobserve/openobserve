{
  "feature_slug": "workflows",
  "feature_title": "Workflows E2E Coverage (branch node, canvas actions, draft lifecycle, test drawer)",
  "area": "Workflows",
  "edition": "enterprise",
  "needs_e2e": true,
  "needs_api": false,
  "spec_filename": "workflows-branch.spec.js",
  "spec_path": "tests/ui-testing/playwright-tests/Workflows/workflows-branch.spec.js",
  "playwright_group": "Workflows",
  "source_files": [
    "tests/ui-testing/pages/workflowsPages/workflowsPage.js",
    "tests/ui-testing/playwright-tests/Workflows/workflows-branch.spec.js",
    "tests/ui-testing/playwright-tests/Workflows/workflows-canvas.spec.js",
    "tests/ui-testing/playwright-tests/Workflows/workflows-lifecycle.spec.js",
    "tests/ui-testing/playwright-tests/Workflows/workflows-test-dialog.spec.js"
  ],
  "existing_tests_in_diff": true,
  "skip": false,
  "skip_reason": "",
  "rationale": {
    "edition": "All six changed paths sit under tests/ui-testing/ — no enterprise/ directory — but the four new specs are explicitly tagged @enterprise and document the Workflows feature as 'Enterprise-only', and the only non-spec file is the enterprise matrix overlay tests/ui-testing/ci-matrix/ci_matrix.ent.json. There are no web/src or backend production changes. Workflows is an enterprise-gated feature, so the change is classified enterprise.",
    "skip": "Not skipped. The trigger comment and feature hint are empty and no tests-added label was passed, so there is no explicit opt-out. The diff is not docs/CI-only: it adds four new Playwright E2E specs under tests/ui-testing/playwright-tests/Workflows/, so the dev-authored-tests rule applies and the pipeline continues on the enhance route instead of bailing out.",
    "e2e": "E2E is warranted. The change adds end-to-end coverage for user-facing Workflows UI flows — the branch node editor, canvas/node-card actions, draft lifecycle and list operations, and the test drawer — that shipped earlier without any coverage. Although this diff changes no production web/src code, the entire substance of the PR is E2E verification of user-facing behavior, so the Architect reviews and enhances the existing specs.",
    "area": "Workflows. The bulk of the change lives in tests/ui-testing/playwright-tests/Workflows/ (four new specs) plus the Workflows page object, matching the existing Workflows E2E area and the Workflows shard already present in the CI matrix.",
    "group": "Workflows. The enterprise matrix (ci_matrix.ent.json) defines a single 'Workflows' shard whose run_files already list the sibling workflows-*.spec.js files, and this PR appends the four new specs to that same list — so 'Workflows' is the shard they join.",
    "existing_tests": "Yes — four new Playwright E2E specs under tests/ui-testing/playwright-tests/Workflows/ are added (workflows-branch, workflows-canvas, workflows-lifecycle, workflows-test-dialog), covering previously untested Workflows behavior, so the Architect should enhance rather than generate from scratch. The modified workflowsPage.js is a page object, not an E2E spec, and no Vitest or Rust unit tests are involved."
  }
}
