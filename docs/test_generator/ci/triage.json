{
  "feature_slug": "alert-template-roundtrip",
  "feature_title": "Alert Template Mode Tab Roundtrip",
  "area": "Alerts",
  "edition": "oss",
  "needs_e2e": true,
  "needs_api": false,
  "spec_filename": "alertTemplateRoundtrip.spec.js",
  "spec_path": "tests/ui-testing/playwright-tests/Alerts/alertTemplateRoundtrip.spec.js",
  "playwright_group": "Alerts",
  "source_files": [
    "web/src/components/alerts/AddTemplate.vue"
  ],
  "existing_tests_in_diff": false,
  "skip": false,
  "skip_reason": "",
  "rationale": {
    "edition": "OSS only — both changed files (web/src/components/alerts/AddTemplate.spec.ts and web/src/components/alerts/AddTemplate.vue) live under web/src/, the open-source frontend tree. No enterprise paths (enterprise/, o2_enterprise/, src/enterprise/) appear in the file list, and there are no enterprise-gated keywords in the paths. Feature hint and trigger comment are empty.",
    "skip": "Not skipped — the change modifies AddTemplate.vue, a user-facing alert template editor component, fixing a P0 bug where switching between Template and Raw payload tabs destroyed the custom template body. No explicit opt-out markers (no tests-added label, empty trigger comment). The change is not docs-only or CI-only.",
    "e2e": "E2E is warranted — the fix addresses a user-facing UI interaction in the alert template editor's mode-tab switching (Template ↔ Raw payload). A user visible state corruption bug that destroys a custom template's body during tab navigation needs verification that the roundtrip preserves payloads, including unsaved edits and the existing template body.",
    "area": "Alerts — the changed component lives in web/src/components/alerts/ and is part of the alert template creation/editing workflow. Existing Playwright specs live under tests/ui-testing/playwright-tests/Alerts/ (alerts-e2e-flow.spec.js, alerts-ui-operations.spec.js, etc.), confirming Alerts as the established test area.",
    "group": "Alerts — the CI matrix (ci_matrix_firefox.json, ci_matrix.ent.json) defines a testfolder named 'Alerts' with actual_folder 'Alerts' containing the existing alert E2E specs. A new alert template spec should join this shard.",
    "existing_tests": "No Playwright E2E specs in the diff — the only test file changed is web/src/components/alerts/AddTemplate.spec.ts, which is a Vitest unit test (not under tests/ui-testing/playwright-tests/). This does not count as existing E2E coverage for the feature."
  }
}
