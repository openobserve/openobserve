{
  "feature_slug": "logstream-timezone-display",
  "feature_title": "Log Stream Timezone Display",
  "area": "Logs",
  "edition": "oss",
  "needs_e2e": true,
  "needs_api": false,
  "spec_filename": "timezoneDisplay.spec.js",
  "spec_path": "tests/ui-testing/playwright-tests/Logs/timezoneDisplay.spec.js",
  "playwright_group": "Logs-Features",
  "source_files": [
    "web/src/components/logstream/schema.vue",
    "web/src/utils/date.ts"
  ],
  "existing_tests_in_diff": false,
  "skip": false,
  "skip_reason": "",
  "rationale": {
    "edition": "OSS — changed files are under web/src/ (web/src/components/logstream/schema.vue, web/src/utils/date.ts); no enterprise-owned paths (enterprise/, o2_enterprise/, src/enterprise/) are touched, and no enterprise-only keywords (SSO, SAML, RBAC, SDR, cipher, logo-management) appear in file paths or symbol names.",
    "skip": "Not skipped — the change modifies a user-facing UI component (the logstream schema panel's timezone label and timestamp formatting); no skip marker or label was provided, and both the feature hint and trigger comment inputs are empty.",
    "e2e": "E2E warranted — the diff replaces a hardcoded 'UTC' label with a dynamic timezone display and switches timestamp formatting from formatTimestamp to formatTimestampInTimezone, making the schema panel render timestamps in the user's configured timezone. A user interacting with the log stream schema panel will see different labels and timestamp values depending on their selected timezone, which needs verification across timezone configurations.",
    "area": "Logs — the changed component (web/src/components/logstream/schema.vue) is part of the Logs/Logstream view displayed when browsing log streams. The logstream is the primary logs interface, and sibling E2E specs (logspage.spec.js, logshistogram.spec.js, shareLink.spec.js, etc.) live under tests/ui-testing/playwright-tests/Logs/.",
    "group": "Logs-Features — this is a specific feature enhancement (timezone-aware display in the schema panel) rather than core page functionality (which belongs in Logs-Core) or query behavior (which belongs in Logs-Queries). Logs-Features already hosts similar feature-specific specs such as indexquery.spec.js and logsQueryBuilder.spec.js.",
    "existing_tests": "No E2E specs in the diff — the diff only touches web/src/components/logstream/schema.vue and web/src/utils/date.ts; no files under tests/ui-testing/playwright-tests/ were added or modified."
  }
}
