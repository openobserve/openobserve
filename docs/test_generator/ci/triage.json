{
  "feature_slug": "status-page-brand-link",
  "feature_title": "Status Page Powered-by Brand Link",
  "area": "GeneralTests",
  "edition": "oss",
  "needs_e2e": true,
  "needs_api": false,
  "spec_filename": "statusPageBrandLink.spec.js",
  "spec_path": "tests/ui-testing/playwright-tests/GeneralTests/statusPageBrandLink.spec.js",
  "playwright_group": "GeneralTests",
  "source_files": [
    "web/src/views/synthetics/status-pages/StatusPagePreview.vue",
    "src/api/management/src/request/status_pages/status_page.html"
  ],
  "existing_tests_in_diff": false,
  "skip": false,
  "skip_reason": "",
  "rationale": {
    "edition": "Both changed files live in open-source-owned paths — web/src/views/synthetics/status-pages/StatusPagePreview.vue and src/api/management/src/request/status_pages/status_page.html — neither under enterprise/, o2_enterprise/, or src/enterprise/. No enterprise keywords (cipher/SSO/SAML/RBAC/SDR/logo-management) appear in the paths or symbols, so this is an OSS change.",
    "skip": "No skip marker is present: the trigger comment and feature hint are both empty and no 'tests-added' label was passed. The change alters the rendered UI (wraps the 'Powered by OpenObserve' branding in a hyperlink and reflows the logo/title layout), so it is user-facing and does not qualify for a docs/CI-only skip.",
    "e2e": "The change adds a clickable 'Powered by OpenObserve' link (a new anchor with hover/focus styling) and a flex layout for the logo + page title in both the Synthetics status-page preview and the public status-page HTML. That is a user-visible interaction worth verifying: the brand mark is present, is an anchor pointing to https://openobserve.ai/ with target=_blank, and the logo/title still render side-by-side.",
    "area": "There is no dedicated Synthetics or StatusPages folder under playwright-tests/, so the change lands in GeneralTests, the catch-all area. The most analogous existing spec is logoManagement.spec.js, which already lives in GeneralTests and covers branding/logo UI, making this a natural sibling.",
    "group": "GeneralTests is the existing matrix shard whose run_files already hold a branding/logo spec (logoManagement.spec.js), so the new spec joins it and reuses the same shard without requiring a new matrix group.",
    "existing_tests": "No E2E specs in the diff — the changed files are StatusPagePreview.vue and status_page.html, neither under tests/ui-testing/playwright-tests/, so there is no dev-authored Playwright coverage to extend."
  }
}
