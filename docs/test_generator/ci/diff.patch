===== COMPLETE LIST OF CHANGED FILES (27 files) =====
# AUTHORITATIVE: this list is never truncated. The diff BODY below may be trimmed for
# very large PRs, so decide WHICH files/areas changed (e.g. web/src frontend) from THIS list.
scripts/translations/.translation_state.json
src/api/common/src/auth/jwt.rs
src/api/management/src/request/organization/settings.rs
src/common/src/meta/organization.rs
src/config/src/meta/user.rs
web/src/components/settings/DomainOrgMappingDialog.vue
web/src/components/settings/DomainOrgMappings.schema.ts
web/src/components/settings/DomainOrgMappings.vue
web/src/components/settings/OrganizationSettings.spec.ts
web/src/components/settings/OrganizationSettings.vue
web/src/layouts/MainLayout.vue
web/src/locales/languages/ar-SA.json
web/src/locales/languages/de-DE.json
web/src/locales/languages/en-US.json
web/src/locales/languages/es-ES.json
web/src/locales/languages/fr-FR.json
web/src/locales/languages/it-IT.json
web/src/locales/languages/ja-JP.json
web/src/locales/languages/ko-KR.json
web/src/locales/languages/nl-NL.json
web/src/locales/languages/pl-PL.json
web/src/locales/languages/pt-PT.json
web/src/locales/languages/ru-RU.json
web/src/locales/languages/tr-TR.json
web/src/locales/languages/vi-VN.json
web/src/locales/languages/zh-CN.json
web/src/locales/languages/zh-TW.json
===== END CHANGED FILES =====

diff --git a/scripts/translations/.translation_state.json b/scripts/translations/.translation_state.json
index bea6c6061ea..db3da37e7f0 100644
--- a/scripts/translations/.translation_state.json
+++ b/scripts/translations/.translation_state.json
@@ -15118,6 +15118,27 @@
     "domainManagementDesc": "88398e3ac40ce4d7",
     "domainNeedsBlockedEmails": "53170f72948b23a8",
     "domainNeedsEmails": "c68d07b51aa28f5a",
+    "domainOrgMappings": {
+      "addMapping": "160cc87a108cd97f",
+      "baseRole": "33551d66efcd283e",
+      "baseRoleHelp": "50482be96fa489a0",
+      "confirmRemove": "f10b126b0e82cab8",
+      "deleteTitle": "06380e0419eca2ef",
+      "domain": "f865f1ee55b26840",
+      "domainHelp": "c927b335c093577b",
+      "duplicateDomain": "197ff12f947386be",
+      "editMapping": "d5ef1883268e1264",
+      "empty": "df0e529517206088",
+      "organization": "d764d42592fc9687",
+      "organizationHelp": "783574eea65c82ad",
+      "organizationPlaceholder": "d7f6cb5385e83aca",
+      "organizationRequired": "7d12b1ae42d8cc36",
+      "subtitle": "031a53b7414811e0",
+      "title": "ac852cfa9baf6fa9",
+      "userGroup": "ccd983e3c5253d45",
+      "userGroupHelp": "30b9ca455711411b",
+      "userGroupPlaceholder": "762ebb70ef0ea2e8"
+    },
     "domainPlaceholder": "cc921aaa05754257",
     "domainRemoved": "2984d48362fadf0b",
     "domainRequired": "025f3d105c5c8f7c",
diff --git a/src/api/common/src/auth/jwt.rs b/src/api/common/src/auth/jwt.rs
index f76a6495656..79dd3ea4afe 100644
--- a/src/api/common/src/auth/jwt.rs
+++ b/src/api/common/src/auth/jwt.rs
@@ -13,8 +13,6 @@
 // You should have received a copy of the GNU Affero General Public License
 // along with this program.  If not, see <http://www.gnu.org/licenses/>.
 
-#[cfg(feature = "cloud")]
-use config::DEFAULT_ORG;
 #[cfg(all(feature = "enterprise", not(feature = "cloud")))]
 use openobserve_core::{organization, users};
 #[cfg(feature = "cloud")]
@@ -45,6 +43,13 @@ use {
     std::collections::HashMap,
     std::sync::LazyLock as Lazy,
 };
+#[cfg(feature = "cloud")]
+use {
+    config::{DEFAULT_ORG, META_ORG_ID, meta::user::UserRole, utils::rand::generate_random_string},
+    db::{org_users, organization::get_org_setting},
+    o2_openfga::authorizer::{authz::get_add_user_to_org_tuples, groups::update_group},
+    std::{collections::HashSet, str::FromStr as _},
+};
 
 #[cfg(feature = "cloud")]
 use crate::common::meta::{
@@ -829,6 +834,70 @@ async fn publish_org_not_found_error(org_id: &str, user_email: &str) {
     usage_reporting::publish_error(error_data).await;
 }
 
+#[cfg(feature = "cloud")]
+pub async fn process_domain_org_mapping(user_email: &str) -> Result<bool, anyhow::Error> {
+    let meta_settings = get_org_setting(META_ORG_ID).await?;
+    let mappings = meta_settings.domain_org_mappings;
+    // split email to get the domain
+    if let Some((_, domain)) = user_email.to_lowercase().split_once("@") {
+        if let Some(mapped) = mappings
+            .into_iter()
+            .find(|m| m.domain.to_lowercase() == domain)
+        {
+            log::info!("found domain org mapping for user {user_email}, processing");
+            let base_role = UserRole::from_str(&mapped.base_role).map_err(|e| {
+                anyhow::anyhow!(
+                    "error parsing domain org role {} to user role : {e:?}",
+                    mapped.base_role
+                )
+            })?;
+            // first add the user entry itself to the ofga, as a member of the mapped org with
+            // mapped base role
+            let mut new_tuples = Vec::new();
+            get_add_user_to_org_tuples(
+                &mapped.org_id,
+                user_email,
+                &mapped.base_role,
+                &mut new_tuples,
+            );
+            update_tuples(new_tuples, vec![]).await?;
+            // then if there is some user group to be added, add user to that group
+            if let Some(group) = mapped.user_group {
+                let mut user_set = HashSet::new();
+                user_set.insert(user_email.to_string());
+                update_group(&mapped.org_id, &group, Some(user_set), None, None, None).await?;
+            }
+            // finally add the org user entry, this is last
+            // because ofga is in a way idempotent, so if if it gets retried again, it will work
+            // but having org user entry but not ofga mapping can cause a lot of issues with 401s
+            // which would be difficult to resolve, so try it the last
+            org_users::add(
+                &mapped.org_id,
+                user_email,
+                base_role,
+                &generate_random_string(16),
+                Some(format!("rum{}", generate_random_string(16))),
+            )
+            .await
+            .map_err(|e| {
+                anyhow::anyhow!(
+                    "Failed to add user {user_email} to org in domain org mapping processing : {e}"
+                )
+            })?;
+            log::info!("domain org mapping for user {user_email} successfully processed");
+            return Ok(true);
+        } else {
+            log::info!("no domain org mapping found for user {user_email}, continuing normally");
+            return Ok(false);
+        }
+    } else {
+        log::warn!(
+            "user email {user_email} could not be split correctly at @, skipping org domain mapping"
+        );
+        return Ok(false);
+    }
+}
+
 #[cfg(feature = "cloud")]
 pub async fn check_and_add_to_org(
     user_email: &str,
@@ -886,6 +955,21 @@ pub async fn check_and_add_to_org(
         }
     }
 
+    // if user is a new user, process org mappings for them
+    if is_new_user {
+        match process_domain_org_mapping(user_email).await {
+            Ok(processed) => {
+                if processed {
+                    // domain based adding is done, so do not treat as a new user
+                    return Ok((false, pending_invites));
+                }
+            }
+            Err(e) => log::error!(
+                "error processing domain org mapping for user {user_email}, continuing normally : {e}"
+            ),
+        }
+    }
+
     // Check if the user is part of any organization. Exclude orgs that are
     // pending_deletion/deleting: they are hidden + blocked everywhere, so if a
     // user's ONLY org is being deleted we must treat them as having no org and
@@ -959,7 +1043,7 @@ pub async fn check_and_add_to_org(
                     log::info!("User updated to the openfga");
                 }
                 Err(e) => {
-                    log::error!("Error updating user to the openfga: {}", e);
+                    log::error!("Error updating user {user_email} to the openfga: {}", e);
                 }
             }
         }
diff --git a/src/api/management/src/request/organization/settings.rs b/src/api/management/src/request/organization/settings.rs
index 68ef0238cde..5db855c69fc 100644
--- a/src/api/management/src/request/organization/settings.rs
+++ b/src/api/management/src/request/organization/settings.rs
@@ -19,6 +19,8 @@ use axum::{
     http::StatusCode,
     response::{IntoResponse, Response},
 };
+#[cfg(feature = "cloud")]
+use config::META_ORG_ID;
 use db::organization::{get_org_setting, set_org_setting};
 use infra::errors::{DbError, Error};
 #[cfg(feature = "enterprise")]
@@ -151,6 +153,59 @@ pub async fn create(
         data.cross_links = cross_links;
     }
 
+    // ignore this for all non _meta orgs
+    #[cfg(feature = "cloud")]
+    if org_id == META_ORG_ID
+        && let Some(mappings) = settings.domain_org_mappings
+    {
+        field_found = true;
+        for mapping in &mappings {
+            use o2_openfga::authorizer::groups::get_all_groups;
+
+            if openobserve_core::organization::get_org(&mapping.org_id)
+                .await
+                .is_none()
+            {
+                return MetaHttpResponse::bad_request(format!(
+                    "No org with org id {} found",
+                    mapping.org_id
+                ));
+            }
+
+            if !matches!(
+                mapping.base_role.as_str(),
+                "admin" | "editor" | "viewer" | "allowed_user"
+            ) {
+                return MetaHttpResponse::bad_request(format!(
+                    "base role {} for org {} is not a valid base role - only admin, editor, viewer, allowed_user are supported",
+                    mapping.base_role, mapping.org_id
+                ));
+            }
+            if let Some(group) = mapping.user_group.as_ref() {
+                let all_groups = match get_all_groups(&mapping.org_id, None).await {
+                    Ok(v) => v,
+                    Err(e) => {
+                        log::error!(
+                            "error getting all groups for {} when updating domain org mappings : {e}",
+                            mapping.org_id
+                        );
+                        return MetaHttpResponse::bad_request(format!(
+                            "error getting groups for org {} : {e}",
+                            mapping.org_id
+                        ));
+                    }
+                };
+                if !all_groups.contains(&group) {
+                    return MetaHttpResponse::bad_request(format!(
+                        "custom group {group} not found in org {}",
+                        mapping.org_id
+                    ));
+                }
+            }
+        }
+        data.domain_org_mappings = mappings;
+    }
+
     if !field_found {
         return MetaHttpResponse::bad_request("No valid field found");
     }
diff --git a/src/common/src/meta/organization.rs b/src/common/src/meta/organization.rs
index 426df05bc53..d857e9d70b9 100644
--- a/src/common/src/meta/organization.rs
+++ b/src/common/src/meta/organization.rs
@@ -470,6 +470,14 @@ fn default_claim_parser_function() -> String {
     "".to_string()
 }
 
+#[derive(Deserialize, Serialize, Debug, Clone, ToSchema)]
+pub struct DomainOrgMapping {
+    pub domain: String,
+    pub org_id: String,
+    pub base_role: String,
+    pub user_group: Option<String>,
+}
+
 #[derive(Serialize, ToSchema, Deserialize, Debug, Clone)]
 pub struct OrganizationSettingPayload {
     /// Ideally this should be the same as prometheus-scrape-interval (in
@@ -501,6 +509,8 @@ pub struct OrganizationSettingPayload {
     pub claim_parser_function: Option<String>,
     #[serde(skip_serializing_if = "Option::is_none")]
     pub cross_links: Option<Vec<config::meta::stream::CrossLink>>,
+    #[serde(skip_serializing_if = "Option::is_none")]
+    pub domain_org_mappings: Option<Vec<DomainOrgMapping>>,
 }
 
 #[derive(Serialize, ToSchema, Deserialize, Debug, Clone)]
@@ -540,6 +550,9 @@ pub struct OrganizationSetting {
     pub cross_links: Vec<config::meta::stream::CrossLink>,
     #[serde(default)]
     pub org_storage_enabled: bool,
+    #[cfg(feature = "cloud")]
+    #[serde(default)]
+    pub domain_org_mappings: Vec<DomainOrgMapping>,
 }
 
 impl Default for OrganizationSetting {
@@ -573,6 +586,8 @@ impl Default for OrganizationSetting {
             claim_parser_function: default_claim_parser_function(),
             cross_links: Vec::new(),
             org_storage_enabled: false,
+            #[cfg(feature = "cloud")]
+            domain_org_mappings: Vec::new(),
         }
     }
 }
@@ -1442,6 +1457,7 @@ mod tests {
             #[cfg(feature = "enterprise")]
             claim_parser_function: None,
             cross_links: None,
+            domain_org_mappings: None,
         };
         let json = serde_json::to_value(&payload).unwrap();
         let obj = json.as_object().unwrap();
@@ -1518,6 +1534,8 @@ mod tests {
             claim_parser_function: String::new(),
             cross_links: vec![],
             org_storage_enabled: false,
+            #[cfg(feature = "cloud")]
+            domain_org_mappings: vec![],
         };
         let json = serde_json::to_value(&setting).unwrap();
         let obj = json.as_object().unwrap();
@@ -1546,6 +1564,8 @@ mod tests {
             claim_parser_function: String::new(),
             cross_links: vec![],
             org_storage_enabled: false,
+            #[cfg(feature = "cloud")]
+            domain_org_mappings: vec![],
         };
         let json = serde_json::to_value(&setting).unwrap();
         let obj = json.as_object().unwrap();
diff --git a/src/config/src/meta/user.rs b/src/config/src/meta/user.rs
index 58fe9665641..aca00f3837e 100644
--- a/src/config/src/meta/user.rs
+++ b/src/config/src/meta/user.rs
@@ -159,7 +159,7 @@ impl FromStr for UserRole {
             "root" => Ok(UserRole::Root),
             "viewer" => Ok(UserRole::Viewer),
             "editor" => Ok(UserRole::Editor),
-            "user" => Ok(UserRole::User),
+            "user" | "allowed_user" => Ok(UserRole::User),
             "service_account" => Ok(UserRole::ServiceAccount),
             "sre_agent" => Ok(UserRole::SreAgent),
             _ => Ok(UserRole::Admin),
diff --git a/web/src/components/settings/DomainOrgMappingDialog.vue b/web/src/components/settings/DomainOrgMappingDialog.vue
new file mode 100644
index 00000000000..fd933c35a4c
--- /dev/null
+++ b/web/src/components/settings/DomainOrgMappingDialog.vue
@@ -0,0 +1,179 @@
+<!-- Copyright 2026 OpenObserve Inc.
+
+This program is free software: you can redistribute it and/or modify
+it under the terms of the GNU Affero General Public License as published by
+the Free Software Foundation, either version 3 of the License, or
+(at your option) any later version.
+
+This program is distributed in the hope that it will be useful
+but WITHOUT ANY WARRANTY; without even the implied warranty of
+MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
+GNU Affero General Public License for more details.
+
+You should have received a copy of the GNU Affero General Public License
+along with this program.  If not, see <http://www.gnu.org/licenses/>.
+-->
+
+<template>
+  <ODialog
+    data-test="settings-domain-org-mapping-dialog"
+    v-model:open="dialogOpen"
+    persistent
+    size="md"
+    :form-id="FORM_ID"
+    :title="
+      isEditing
+        ? t('settings.domainOrgMappings.editMapping')
+        : t('settings.domainOrgMappings.addMapping')
+    "
+    :secondary-button-label="t('common.cancel')"
+    :primary-button-label="isEditing ? t('common.save') : t('common.add')"
+    @click:secondary="onCancel"
+  >
+    <OForm :id="FORM_ID" :form="form" class="flex flex-col gap-5">
+      <OFormInput
+        data-test="settings-domain-org-mapping-domain-input"
+        name="domain"
+        :label="t('settings.domainOrgMappings.domain')"
+        required
+        :placeholder="t('settings.domainPlaceholder', { example: raw('example.com') })"
+        :help-text="t('settings.domainOrgMappings.domainHelp', { at_sign: '@' })"
+      />
+
+      <OFormInput
+        data-test="settings-domain-org-mapping-org-input"
+        name="org_id"
+        :label="t('settings.domainOrgMappings.organization')"
+        required
+        :placeholder="t('settings.domainOrgMappings.organizationPlaceholder')"
+        :help-text="t('settings.domainOrgMappings.organizationHelp')"
+      />
+
+      <OFormSelect
+        data-test="settings-domain-org-mapping-role-select"
+        name="base_role"
+        :label="t('settings.domainOrgMappings.baseRole')"
+        :options="roleOptions"
+        :searchable="false"
+        required
+        :help-text="t('settings.domainOrgMappings.baseRoleHelp')"
+      />
+
+      <OFormInput
+        data-test="settings-domain-org-mapping-group-input"
+        name="user_group"
+        :label="t('settings.domainOrgMappings.userGroup')"
+        :placeholder="t('settings.domainOrgMappings.userGroupPlaceholder')"
+        :help-text="t('settings.domainOrgMappings.userGroupHelp')"
+      />
+    </OForm>
+  </ODialog>
+</template>
+
+<script setup lang="ts">
+import { computed, watch } from "vue";
+import { raw, useI18nTyped } from "@/types/i18n";
+import ODialog from "@/lib/overlay/Dialog/ODialog.vue";
+import OForm from "@/lib/forms/Form/OForm.vue";
+import OFormInput from "@/lib/forms/Input/OFormInput.vue";
+import OFormSelect from "@/lib/forms/Select/OFormSelect.vue";
+import { useOForm } from "@/lib/forms/Form/useOForm";
+import { toast } from "@/lib/feedback/Toast/useToast";
+import {
+  DOMAIN_ORG_BASE_ROLES,
+  domainOrgMappingDefaults,
+  makeDomainOrgMappingSchema,
+  type DomainOrgMapping,
+  type DomainOrgMappingForm,
+} from "./DomainOrgMappings.schema";
+
+const FORM_ID = "domain-org-mapping-form";
+
+const props = withDefaults(
+  defineProps<{
+    open: boolean;
+    mapping?: DomainOrgMapping | null;
+    /** Domains already mapped, excluding the one being edited — blocks duplicates. */
+    takenDomains?: string[];
+  }>(),
+  { mapping: null, takenDomains: () => [] },
+);
+
+const emit = defineEmits<{
+  (_e: "update:open", _value: boolean): void;
+  (_e: "save", _value: DomainOrgMapping): void;
+}>();
+
+const { t } = useI18nTyped();
+
+// A dynamic `t()` key is untypable, so the role→label mapping is a literal map.
+// `allowed_user` is the OpenFGA relation for the User role, so it reuses that label.
+const ROLE_LABEL_KEYS = {
+  admin: "components.badge.userRole.admin",
+  editor: "components.badge.userRole.editor",
+  viewer: "components.badge.userRole.viewer",
+  allowed_user: "components.badge.userRole.user",
+} as const;
+
+const dialogOpen = computed({
+  get: () => props.open,
+  set: (value: boolean) => emit("update:open", value),
+});
+
+const isEditing = computed(() => !!props.mapping?.domain);
+
+const roleOptions = computed(() =>
+  DOMAIN_ORG_BASE_ROLES.map((role) => ({
+    label: t(ROLE_LABEL_KEYS[role]),
+    value: role,
+  })),
+);
+
+// Edit-prefill defaults → a typed computed, re-applied via form.reset on open.
+const mappingDefaults = computed((): DomainOrgMappingForm => {
+  const base = domainOrgMappingDefaults();
+  if (!props.mapping) return base;
+  return {
+    domain: props.mapping.domain ?? "",
+    org_id: props.mapping.org_id ?? "",
+    base_role: props.mapping.base_role ?? base.base_role,
+    user_group: props.mapping.user_group ?? "",
+  };
+});
+
+const form = useOForm<DomainOrgMappingForm>({
+  defaultValues: mappingDefaults.value,
+  schema: makeDomainOrgMappingSchema(t),
+  onSubmit,
+});
+
+// The form is created here so it survives the dialog body's unmount, so it is
+// re-seeded from the current mapping on every open.
+watch(
+  () => props.open,
+  (visible) => {
+    if (visible) form.reset(mappingDefaults.value);
+  },
+);
+
+function onSubmit(value: DomainOrgMappingForm) {
+  const domain = value.domain.trim().toLowerCase();
+  if (props.takenDomains.includes(domain)) {
+    toast({ variant: "error", message: t("settings.domainOrgMappings.duplicateDomain") });
+    return;
+  }
+
+  emit("save", {
+    domain,
+    org_id: value.org_id.trim(),
+    base_role: value.base_role,
+    // Omitted rather than "" so the payload matches the optional backend field.
+    user_group: value.user_group?.trim() || undefined,
+  });
+  emit("update:open", false);
+}
+
+function onCancel() {
+  emit("update:open", false);
+}
+</script>
diff --git a/web/src/components/settings/DomainOrgMappings.schema.ts b/web/src/components/settings/DomainOrgMappings.schema.ts
new file mode 100644
index 00000000000..31bdc3f41a4
--- /dev/null
+++ b/web/src/components/settings/DomainOrgMappings.schema.ts
@@ -0,0 +1,46 @@
+// Copyright 2026 OpenObserve Inc.
+//
+// Validation schema for the add/edit dialog behind the Domain → organization
+// mappings section of OrganizationSettings.vue. Only the dialog is
+// OForm-validated; the rendered mapping list is display state merged into the
+// org-settings payload at submit (the same composite exception CrossLinkManager
+// takes). Validation TIMING is owned by OForm; this file describes WHAT is valid.
+
+import { z } from "zod";
+import { isValidDomain } from "./DomainManagement.schema";
+
+// The only values the backend accepts. `allowed_user` is the OpenFGA relation
+// name for the User role, not a separate role — see user_fga_role() in core.
+export const DOMAIN_ORG_BASE_ROLES = ["admin", "editor", "viewer", "allowed_user"] as const;
+
+export type DomainOrgBaseRole = (typeof DOMAIN_ORG_BASE_ROLES)[number];
+
+export interface DomainOrgMapping {
+  domain: string;
+  org_id: string;
+  base_role: DomainOrgBaseRole;
+  user_group?: string;
+}
+
+export const makeDomainOrgMappingSchema = (t: (_key: string) => string) =>
+  z.object({
+    // NO .trim(): OForm validates with the schema but saves the raw value, so a
+    // trim here would let " acme.com" pass and persist the space.
+    domain: z
+      .string()
+      .min(1, t("settings.domainRequired"))
+      .refine((v) => isValidDomain(v), { message: t("settings.invalidDomain") }),
+    org_id: z.string().min(1, t("settings.domainOrgMappings.organizationRequired")),
+    base_role: z.enum(DOMAIN_ORG_BASE_ROLES),
+    // "" is the cleared state of the group select; normalized away at submit.
+    user_group: z.string().optional(),
+  });
+
+export type DomainOrgMappingForm = z.infer<ReturnType<typeof makeDomainOrgMappingSchema>>;
+
+export const domainOrgMappingDefaults = (): DomainOrgMappingForm => ({
+  domain: "",
+  org_id: "",
+  base_role: "admin",
+  user_group: "",
+});
diff --git a/web/src/components/settings/DomainOrgMappings.vue b/web/src/components/settings/DomainOrgMappings.vue
new file mode 100644
index 00000000000..64af111b498
--- /dev/null
+++ b/web/src/components/settings/DomainOrgMappings.vue
@@ -0,0 +1,207 @@
+<!-- Copyright 2026 OpenObserve Inc.
+
+This program is free software: you can redistribute it and/or modify
+it under the terms of the GNU Affero General Public License as published by
+the Free Software Foundation, either version 3 of the License, or
+(at your option) any later version.
+
+This program is distributed in the hope that it will be useful
+but WITHOUT ANY WARRANTY; without even the implied warranty of
+MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
+GNU Affero General Public License for more details.
+
+You should have received a copy of the GNU Affero General Public License
+along with this program.  If not, see <http://www.gnu.org/licenses/>.
+-->
+
+<template>
+  <div class="domain-org-mappings">
+    <div class="mb-3 flex items-center justify-between gap-2">
+      <div class="min-w-0">
+        <div class="text-base font-bold">{{ t("settings.domainOrgMappings.title") }}</div>
+        <div class="text-text-muted text-xs">
+          {{ t("settings.domainOrgMappings.subtitle") }}
+        </div>
+      </div>
+      <OButton
+        data-test="settings-domain-org-mappings-add-btn"
+        variant="outline"
+        size="sm-action"
+        icon-left="add"
+        class="shrink-0"
+        @click="openAdd"
+      >
+        {{ t("settings.domainOrgMappings.addMapping") }}
+      </OButton>
+    </div>
+
+    <div v-if="mappings.length > 0" data-test="settings-domain-org-mappings-list">
+      <div
+        v-for="(mapping, index) in mappings"
+        :key="index"
+        :data-test="`settings-domain-org-mappings-item-${index}`"
+        class="border-card-glass-border rounded-default mb-1 border p-2"
+      >
+        <div class="flex items-start justify-between">
+          <div class="min-w-0 flex-1">
+            <div class="flex min-w-0 items-center gap-1 text-sm">
+              <span
+                class="text-text-heading truncate font-bold"
+                :data-test="`settings-domain-org-mappings-item-domain-${index}`"
+                >{{ atSign }}{{ mapping.domain }}</span
+              >
+              <OIcon name="arrow-forward" size="xs" class="text-text-muted shrink-0" />
+              <span
+                class="text-text-body truncate"
+                :data-test="`settings-domain-org-mappings-item-org-${index}`"
+                >{{ mapping.org_id }}</span
+              >
+            </div>
+            <div class="mt-1 flex flex-wrap items-center gap-1">
+              <OTag type="userRole" :value="badgeRole(mapping.base_role)" />
+              <OTag
+                v-if="mapping.user_group"
+                type="fieldTag"
+                :data-test="`settings-domain-org-mappings-item-group-${index}`"
+              >
+                <span class="truncate text-xs">{{ mapping.user_group }}</span>
+              </OTag>
+            </div>
+          </div>
+          <div class="ms-2 flex shrink-0 items-center gap-1">
+            <OButton
+              :data-test="`settings-domain-org-mappings-edit-${index}`"
+              variant="ghost"
+              size="icon-sm"
+              icon-left="edit"
+              :title="t('common.edit')"
+              @click="openEdit(index)"
+            />
+            <OButton
+              :data-test="`settings-domain-org-mappings-delete-${index}`"
+              variant="ghost-destructive"
+              size="icon-sm"
+              icon-left="delete"
+              :title="t('common.delete')"
+              @click="confirmRemove(index)"
+            />
+          </div>
+        </div>
+      </div>
+    </div>
+    <div
+      v-else
+      data-test="settings-domain-org-mappings-empty"
+      class="text-text-muted py-4 text-center text-sm"
+    >
+      {{
+        t("settings.domainOrgMappings.empty", {
+          addLabel: t("settings.domainOrgMappings.addMapping"),
+        })
+      }}
+    </div>
+
+    <DomainOrgMappingDialog
+      v-model:open="dialogOpen"
+      :mapping="editingMapping"
+      :taken-domains="takenDomains"
+      @save="onSaveMapping"
+    />
+
+    <ConfirmDialog
+      v-model="confirmRemoveOpen"
+      :title="t('settings.domainOrgMappings.deleteTitle')"
+      :message="
+        t('settings.domainOrgMappings.confirmRemove', {
+          domain: pendingRemoveIndex !== null ? mappings[pendingRemoveIndex]?.domain : '',
+        })
+      "
+      @update:ok="doRemove"
+      @update:cancel="pendingRemoveIndex = null"
+    />
+  </div>
+</template>
+
+<script setup lang="ts">
+import { computed, ref } from "vue";
+import { raw, useI18nTyped } from "@/types/i18n";
+import OButton from "@/lib/core/Button/OButton.vue";
+import OIcon from "@/lib/core/Icon/OIcon.vue";
+import OTag from "@/lib/core/Badge/OTag.vue";
+import ConfirmDialog from "@/components/ConfirmDialog.vue";
+import DomainOrgMappingDialog from "./DomainOrgMappingDialog.vue";
+import type { DomainOrgMapping } from "./DomainOrgMappings.schema";
+
+const props = withDefaults(defineProps<{ modelValue?: DomainOrgMapping[] }>(), {
+  modelValue: () => [],
+});
+
+const emit = defineEmits<{
+  (_e: "update:modelValue", _value: DomainOrgMapping[]): void;
+  (_e: "change"): void;
+}>();
+
+const { t } = useI18nTyped();
+
+const atSign = raw("@");
+
+const mappings = computed(() => props.modelValue);
+
+// The badge registry keys the User role as `user`; `allowed_user` is its OpenFGA relation name.
+const badgeRole = (role: string) => (role === "allowed_user" ? "user" : role);
+
+const dialogOpen = ref(false);
+const editingIndex = ref<number | null>(null);
+const editingMapping = ref<DomainOrgMapping | null>(null);
+
+// The edited row's own domain must stay available to it, so it is excluded.
+const takenDomains = computed(() =>
+  mappings.value
+    .filter((_, index) => index !== editingIndex.value)
+    .map((mapping) => mapping.domain.toLowerCase()),
+);
+
+const confirmRemoveOpen = ref(false);
+const pendingRemoveIndex = ref<number | null>(null);
+
+function openAdd() {
+  editingIndex.value = null;
+  editingMapping.value = null;
+  dialogOpen.value = true;
+}
+
+function openEdit(index: number) {
+  editingIndex.value = index;
+  editingMapping.value = { ...mappings.value[index] };
+  dialogOpen.value = true;
+}
+
+function commit(next: DomainOrgMapping[]) {
+  emit("update:modelValue", next);
+  emit("change");
+}
+
+function onSaveMapping(mapping: DomainOrgMapping) {
+  const next = [...mappings.value];
+  if (editingIndex.value !== null) {
+    next[editingIndex.value] = mapping;
+  } else {
+    next.push(mapping);
+  }
+  commit(next);
+  editingIndex.value = null;
+  editingMapping.value = null;
+}
+
+function confirmRemove(index: number) {
+  pendingRemoveIndex.value = index;
+  confirmRemoveOpen.value = true;
+}
+
+function doRemove() {
+  const index = pendingRemoveIndex.value;
+  if (index === null) return;
+  commit(mappings.value.filter((_, i) => i !== index));
+  pendingRemoveIndex.value = null;
+}
+</script>
diff --git a/web/src/components/settings/OrganizationSettings.spec.ts b/web/src/components/settings/OrganizationSettings.spec.ts
index 409a18351be..055c54187d4 100644
--- a/web/src/components/settings/OrganizationSettings.spec.ts
+++ b/web/src/components/settings/OrganizationSettings.spec.ts
@@ -312,6 +312,21 @@ describe("OrganizationSettings", () => {
       });
     });
 
+    it("should prefer the server's response message over the HTTP status text", async () => {
+      const wrapper = createWrapper();
+      mockPostOrganizationSettings.mockRejectedValue({
+        message: "Request failed with status code 400",
+        response: { data: { message: "No org with org id abc found" } },
+      });
+
+      await wrapper.vm.saveOrgSettings(validValue);
+
+      expect(mockToast).toHaveBeenCalledWith({
+        message: "No org with org id abc found",
+        variant: "error",
+      });
+    });
+
     it("should fall back to a default message when the API error has none", async () => {
       const wrapper = createWrapper();
       mockPostOrganizationSettings.mockRejectedValue({});
diff --git a/web/src/components/settings/OrganizationSettings.vue b/web/src/components/settings/OrganizationSettings.vue
index 85c23860059..35b64d0e2e9 100644
--- a/web/src/components/settings/OrganizationSettings.vue
+++ b/web/src/components/settings/OrganizationSettings.vue
@@ -77,6 +77,12 @@
         />
       </template>
 
+      <!-- Domain -> organization mappings: cloud-only, and stored on the meta org. -->
+      <template v-if="showDomainOrgMappings">
+        <OSeparator class="mt-6 mb-4" />
+        <DomainOrgMappings v-model="domainOrgMappings" @change="formDirty = true" />
+      </template>
+
       <div class="mt-3 flex gap-2">
         <!-- <OButton
         data-test="add-alert-cancel-btn"
@@ -99,12 +105,15 @@
 
 <script setup lang="ts">
 import { computed, ref, watch } from "vue";
-import { useI18nTyped } from "@/types/i18n";
+import { raw, useI18nTyped } from "@/types/i18n";
 import { updateOrgSettingsMutation } from "@/services/organizations.queries";
 import { useMutation } from "@tanstack/vue-query";
 import { useOrgId } from "@/composables/query";
 import { useStore } from "vuex";
 import CrossLinkManager from "@/components/cross-linking/CrossLinkManager.vue";
+import DomainOrgMappings from "./DomainOrgMappings.vue";
+import useIsMetaOrg from "@/composables/useIsMetaOrg";
+import type { DomainOrgMapping } from "./DomainOrgMappings.schema";
 import OButton from "@/lib/core/Button/OButton.vue";
 import OForm from "@/lib/forms/Form/OForm.vue";
 import OFormInput from "@/lib/forms/Input/OFormInput.vue";
@@ -131,6 +140,14 @@ const organizationSettingsSchema = makeOrganizationSettingsSchema(t);
 const crossLinks = ref(store.state?.organizationData?.organizationSettings?.cross_links || []);
 const formDirty = ref(false);
 
+// Domain mappings are read and written on the meta org only, and the backend
+// ignores the field outside cloud — so the section renders under both conditions.
+const { isMetaOrg } = useIsMetaOrg();
+const showDomainOrgMappings = computed(() => config.isCloud === "true" && isMetaOrg.value);
+const domainOrgMappings = ref<DomainOrgMapping[]>(
+  store.state?.organizationData?.organizationSettings?.domain_org_mappings || [],
+);
+
 // Dynamic defaults (edit-prefill from the store) → a typed computed. The trace/
 // span/toggle values are form-owned (OFormInput / OFormSwitch).
 const organizationSettingsDefaults = computed((): OrganizationSettingsForm => {
@@ -152,6 +169,15 @@ watch(
   },
 );
 
+watch(
+  () => store.state?.organizationData?.organizationSettings?.domain_org_mappings,
+  (newVal) => {
+    if (!formDirty.value) {
+      domainOrgMappings.value = newVal || [];
+    }
+  },
+);
+
 // @submit fires only once the schema passes (both field names required + regex),
 // so the old validateOrgSettings()/error refs are gone. Awaited by OForm so the
 // inline Save button's spinner spans the POST.
@@ -165,6 +191,12 @@ const saveOrgSettings = async (value: OrganizationSettingsForm) => {
       usage_stream_enabled: value.usageStreamEnabled,
     };
 
+    // Only sent when the section rendered: an org that never showed it must not
+    // post an empty list and wipe mappings another admin saved.
+    if (showDomainOrgMappings.value) {
+      payload.domain_org_mappings = domainOrgMappings.value;
+    }
+
     await updateOrgSettings.mutateAsync(payload);
 
     const updatedSettings: any = {
@@ -174,6 +206,7 @@ const saveOrgSettings = async (value: OrganizationSettingsForm) => {
       toggle_ingestion_logs: value.toggleIngestionLogs,
       cross_links: crossLinks.value,
       usage_stream_enabled: value.usageStreamEnabled,
+      ...(showDomainOrgMappings.value ? { domain_org_mappings: domainOrgMappings.value } : {}),
     };
 
     store.dispatch("setOrganizationSettings", updatedSettings);
@@ -186,7 +219,10 @@ const saveOrgSettings = async (value: OrganizationSettingsForm) => {
     });
   } catch (e: any) {
     toast({
-      message: e?.message || t("settings.organizationSettings.settingsSaveError"),
+      message:
+        raw(e?.response?.data?.message) ||
+        raw(e?.message) ||
+        t("settings.organizationSettings.settingsSaveError"),
       variant: "error",
     });
   }
@@ -195,6 +231,8 @@ const saveOrgSettings = async (value: OrganizationSettingsForm) => {
 // Exposed for unit tests that exercise the submit handler directly.
 defineExpose({
   crossLinks,
+  domainOrgMappings,
+  showDomainOrgMappings,
   formDirty,
   saveOrgSettings,
   organizationSettingsSchema,
diff --git a/web/src/layouts/MainLayout.vue b/web/src/layouts/MainLayout.vue
index ac404f78ca6..f7190014df7 100644
--- a/web/src/layouts/MainLayout.vue
+++ b/web/src/layouts/MainLayout.vue
@@ -1159,6 +1159,7 @@ export default defineComponent({
         dark_mode_theme_color: undefined,
         claim_parser_function: "",
         org_storage_enabled: false,
+        domain_org_mappings: [],
       };
 
       try {
@@ -1202,6 +1203,7 @@ export default defineComponent({
           cross_links: orgSettings?.data?.data?.cross_links ?? [],
           org_storage_enabled:
             orgSettings?.data?.data?.org_storage_enabled ?? defaultSettings.org_storage_enabled,
+          domain_org_mappings: orgSettings?.data?.data?.domain_org_mappings ?? [],
         });
 
         // Load the org's home dashboard (settings/v2 KV) alongside the legacy org
diff --git a/web/src/locales/languages/ar-SA.json b/web/src/locales/languages/ar-SA.json
index 8829ef4c5ae..28000867a2a 100644
--- a/web/src/locales/languages/ar-SA.json
+++ b/web/src/locales/languages/ar-SA.json
@@ -9362,7 +9362,28 @@
     "listConjunctionAnd": "{list}، و {last}",
     "runsIn": "تشغيل في",
     "recommendedByFieldCoverage": "موصى به بناءً على تغطية الحقول في التدفقات الخاصة بك.",
-    "listConjunctionAndTwo": "{first} و {last}"
+    "listConjunctionAndTwo": "{first} و {last}",
+    "domainOrgMappings": {
+      "title": "تعيينات النطاق إلى المؤسسة",
+      "subtitle": "يُضاف المستخدمون الجدد الذين يسجّلون الدخول بنطاق بريد إلكتروني مطابق إلى المؤسسة تلقائيًا.",
+      "addMapping": "إضافة تعيين",
+      "editMapping": "تعديل التعيين",
+      "empty": "لا توجد تعيينات بعد. استخدم «{addLabel}» لإضافة تعيين.",
+      "domain": "نطاق البريد الإلكتروني",
+      "domainHelp": "الجزء الذي يلي {at_sign} في عنوان البريد الإلكتروني للمستخدم.",
+      "organization": "المؤسسة",
+      "organizationPlaceholder": "معرّف المؤسسة",
+      "organizationHelp": "معرّف المؤسسة التي يُضاف إليها المستخدمون المطابقون.",
+      "organizationRequired": "المؤسسة مطلوبة",
+      "baseRole": "الدور الأساسي",
+      "baseRoleHelp": "الدور الذي يُمنح للمستخدمين المطابقين في تلك المؤسسة.",
+      "userGroup": "مجموعة المستخدمين",
+      "userGroupPlaceholder": "اسم المجموعة",
+      "userGroupHelp": "اختياري. ينضم المستخدمون المطابقون أيضًا إلى هذه المجموعة في تلك المؤسسة.",
+      "duplicateDomain": "يوجد تعيين لهذا النطاق بالفعل",
+      "confirmRemove": "هل أنت متأكد من حذف التعيين الخاص بـ {domain}؟",
+      "deleteTitle": "حذف التعيين"
+    }
   },
   "reports": {
     "frequencyUnits": {
diff --git a/web/src/locales/languages/de-DE.json b/web/src/locales/languages/de-DE.json
index 456a75a87f4..579d42f6c51 100644
--- a/web/src/locales/languages/de-DE.json
+++ b/web/src/locales/languages/de-DE.json
@@ -9362,7 +9362,28 @@
     "listConjunctionAnd": "{list} und {last}",
     "runsIn": "Läuft in",
     "recommendedByFieldCoverage": "Empfohlen basierend auf der Feldabdeckung in Ihren Streams.",
-    "listConjunctionAndTwo": "{first} und {last}"
+    "listConjunctionAndTwo": "{first} und {last}",
+    "domainOrgMappings": {
+      "title": "Zuordnungen von Domänen zu Organisationen",
+      "subtitle": "Neue Benutzer, die sich mit einer passenden E-Mail-Domain anmelden, werden automatisch zur Organisation hinzugefügt.",
+      "addMapping": "Zuordnung hinzufügen",
+      "editMapping": "Zuordnung bearbeiten",
+      "empty": "Noch keine Zuordnungen. Verwenden Sie „{addLabel}“, um eine hinzuzufügen.",
+      "domain": "E-Mail-Domain",
+      "domainHelp": "Der Teil nach {at_sign} in der E-Mail-Adresse des Benutzers.",
+      "organization": "Organisation",
+      "organizationPlaceholder": "Organisationskennung",
+      "organizationHelp": "Die Kennung der Organisation, zu der passende Benutzer hinzugefügt werden.",
+      "organizationRequired": "Organisation ist erforderlich",
+      "baseRole": "Basisrolle",
+      "baseRoleHelp": "Die Rolle, die passende Benutzer in dieser Organisation erhalten.",
+      "userGroup": "Benutzergruppe",
+      "userGroupPlaceholder": "Gruppenname",
+      "userGroupHelp": "Optional. Passende Benutzer treten in dieser Organisation auch dieser Gruppe bei.",
+      "duplicateDomain": "Für diese Domain existiert bereits eine Zuordnung",
+      "confirmRemove": "Sind Sie sicher, dass Sie die Zuordnung für {domain} löschen möchten?",
+      "deleteTitle": "Zuordnung löschen"
+    }
   },
   "reports": {
     "frequencyUnits": {
diff --git a/web/src/locales/languages/en-US.json b/web/src/locales/languages/en-US.json
index c6b1d71a3f0..eebc314d551 100644
--- a/web/src/locales/languages/en-US.json
+++ b/web/src/locales/languages/en-US.json
@@ -9362,7 +9362,28 @@
     "listConjunctionAnd": "{list}, and {last}",
     "runsIn": "Runs in",
     "recommendedByFieldCoverage": "Recommended based on field coverage in your streams.",
-    "listConjunctionAndTwo": "{first} and {last}"
+    "listConjunctionAndTwo": "{first} and {last}",
+    "domainOrgMappings": {
+      "title": "Domain to organization mappings",
+      "subtitle": "New users signing in with a matching email domain are added to the organization automatically.",
+      "addMapping": "Add mapping",
+      "editMapping": "Edit mapping",
+      "empty": "No mappings yet. Use “{addLabel}” to add one.",
+      "domain": "Email domain",
+      "domainHelp": "The part after {at_sign} in the user's email address.",
+      "organization": "Organization",
+      "organizationPlaceholder": "Organization identifier",
+      "organizationHelp": "The identifier of the organization matching users are added to.",
+      "organizationRequired": "Organization is required",
+      "baseRole": "Base role",
+      "baseRoleHelp": "The role matching users are given in that organization.",
+      "userGroup": "User group",
+      "userGroupPlaceholder": "Group name",
+      "userGroupHelp": "Optional. Matching users also join this group in that organization.",
+      "duplicateDomain": "A mapping for this domain already exists",
+      "confirmRemove": "Are you sure you want to delete the mapping for {domain}?",
+      "deleteTitle": "Delete Mapping"
+    }
   },
   "reports": {
     "frequencyUnits": {
diff --git a/web/src/locales/languages/es-ES.json b/web/src/locales/languages/es-ES.json
index a9acd668d2b..471b5111ae8 100644
--- a/web/src/locales/languages/es-ES.json
+++ b/web/src/locales/languages/es-ES.json
@@ -9362,7 +9362,28 @@
     "listConjunctionAnd": "{list} y {last}",
     "runsIn": "Se ejecuta en",
     "recommendedByFieldCoverage": "Recomendado según la cobertura de campos en sus flujos.",
-    "listConjunctionAndTwo": "{first} y {last}"
+    "listConjunctionAndTwo": "{first} y {last}",
+    "domainOrgMappings": {
+      "title": "Asignaciones de dominio a organización",
+      "subtitle": "Los nuevos usuarios que inicien sesión con un dominio de correo coincidente se añaden a la organización automáticamente.",
+      "addMapping": "Agregar asignación",
+      "editMapping": "Editar asignación",
+      "empty": "Aún no hay asignaciones. Use “{addLabel}” para agregar una.",
+      "domain": "Dominio de correo",
+      "domainHelp": "La parte después de {at_sign} en la dirección de correo del usuario.",
+      "organization": "Organización",
+      "organizationPlaceholder": "Identificador de la organización",
+      "organizationHelp": "El identificador de la organización a la que se añaden los usuarios coincidentes.",
+      "organizationRequired": "La organización es obligatoria",
+      "baseRole": "Rol base",
+      "baseRoleHelp": "El rol que se asigna a los usuarios coincidentes en esa organización.",
+      "userGroup": "Grupo de usuarios",
+      "userGroupPlaceholder": "Nombre del grupo",
+      "userGroupHelp": "Opcional. Los usuarios coincidentes también se unen a este grupo en esa organización.",
+      "duplicateDomain": "Ya existe una asignación para este dominio",
+      "confirmRemove": "¿Está seguro de que desea eliminar la asignación de {domain}?",
+      "deleteTitle": "Eliminar asignación"
+    }
   },
   "reports": {
     "frequencyUnits": {
diff --git a/web/src/locales/languages/fr-FR.json b/web/src/locales/languages/fr-FR.json
index ead6dee9545..b7616e36013 100644
--- a/web/src/locales/languages/fr-FR.json
+++ b/web/src/locales/languages/fr-FR.json
@@ -9362,7 +9362,28 @@
     "listConjunctionAnd": "{list}, et {last}",
     "runsIn": "S'exécute dans",
     "recommendedByFieldCoverage": "Recommandé en fonction de la couverture des champs dans vos flux.",
-    "listConjunctionAndTwo": "{first} et {last}"
+    "listConjunctionAndTwo": "{first} et {last}",
+    "domainOrgMappings": {
+      "title": "Domaines vers les organisations",
+      "subtitle": "Les nouveaux utilisateurs se connectant avec un domaine de messagerie correspondant sont ajoutés automatiquement à l'organisation.",
+      "addMapping": "Ajouter une correspondance",
+      "editMapping": "Modifier la correspondance",
+      "empty": "Aucune correspondance pour l'instant. Utilisez « {addLabel} » pour en ajouter une.",
+      "domain": "Domaine de messagerie",
+      "domainHelp": "La partie après {at_sign} dans l'adresse e-mail de l'utilisateur.",
+      "organization": "Organisation",
+      "organizationPlaceholder": "Identifiant de l'organisation",
+      "organizationHelp": "L'identifiant de l'organisation à laquelle les utilisateurs correspondants sont ajoutés.",
+      "organizationRequired": "L'organisation est requise",
+      "baseRole": "Rôle de base",
+      "baseRoleHelp": "Le rôle attribué aux utilisateurs correspondants dans cette organisation.",
+      "userGroup": "Groupe d'utilisateurs",
+      "userGroupPlaceholder": "Nom du groupe",
+      "userGroupHelp": "Facultatif. Les utilisateurs correspondants rejoignent également ce groupe dans cette organisation.",
+      "duplicateDomain": "Une correspondance pour ce domaine existe déjà",
+      "confirmRemove": "Êtes-vous sûr de vouloir supprimer la correspondance pour {domain} ?",
+      "deleteTitle": "Supprimer la correspondance"
+    }
   },
   "reports": {
     "frequencyUnits": {
diff --git a/web/src/locales/languages/it-IT.json b/web/src/locales/languages/it-IT.json
index b0acc3b5eb7..0e0e9b1a13a 100644
--- a/web/src/locales/languages/it-IT.json
+++ b/web/src/locales/languages/it-IT.json
@@ -9362,7 +9362,28 @@
     "listConjunctionAnd": "{list} e {last}",
     "runsIn": "Esecuzione in",
     "recommendedByFieldCoverage": "Consigliato in base alla copertura dei campi nei tuoi stream.",
-    "listConjunctionAndTwo": "{first} e {last}"
+    "listConjunctionAndTwo": "{first} e {last}",
+    "domainOrgMappings": {
+      "title": "Mappature da dominio a organizzazione",
+      "subtitle": "I nuovi utenti che accedono con un dominio email corrispondente vengono aggiunti automaticamente all'organizzazione.",
+      "addMapping": "Aggiungi mappatura",
+      "editMapping": "Modifica mappatura",
+      "empty": "Nessuna mappatura. Usa “{addLabel}” per aggiungerne una.",
+      "domain": "Dominio email",
+      "domainHelp": "La parte dopo {at_sign} nell'indirizzo email dell'utente.",
+      "organization": "Organizzazione",
+      "organizationPlaceholder": "Identificatore dell'organizzazione",
+      "organizationHelp": "L'identificatore dell'organizzazione a cui vengono aggiunti gli utenti corrispondenti.",
+      "organizationRequired": "L'organizzazione è obbligatoria",
+      "baseRole": "Ruolo di base",
+      "baseRoleHelp": "Il ruolo assegnato agli utenti corrispondenti in tale organizzazione.",
+      "userGroup": "Gruppo di utenti",
+      "userGroupPlaceholder": "Nome del gruppo",
+      "userGroupHelp": "Facoltativo. Gli utenti corrispondenti vengono aggiunti anche a questo gruppo in tale organizzazione.",
+      "duplicateDomain": "Esiste già una mappatura per questo dominio",
+      "confirmRemove": "Sei sicuro di voler eliminare la mappatura per {domain}?",
+      "deleteTitle": "Elimina mappatura"
+    }
   },
   "reports": {
     "frequencyUnits": {
diff --git a/web/src/locales/languages/ja-JP.json b/web/src/locales/languages/ja-JP.json
index 97d5d29a24b..3bd474f270e 100644
--- a/web/src/locales/languages/ja-JP.json
+++ b/web/src/locales/languages/ja-JP.json
@@ -9362,7 +9362,28 @@
     "listConjunctionAnd": "{list}、および {last}",
     "runsIn": "実行環境",
     "recommendedByFieldCoverage": "ストリームのフィールドカバレッジに基づく推奨です。",
-    "listConjunctionAndTwo": "{first} と {last}"
+    "listConjunctionAndTwo": "{first} と {last}",
+    "domainOrgMappings": {
+      "title": "ドメインと組織のマッピング",
+      "subtitle": "一致するメールドメインでサインインした新規ユーザーは、自動的に組織に追加されます。",
+      "addMapping": "マッピングを追加",
+      "editMapping": "マッピングを編集",
+      "empty": "マッピングはまだありません。「{addLabel}」を使用して追加してください。",
+      "domain": "メールドメイン",
+      "domainHelp": "ユーザーのメールアドレスで {at_sign} より後の部分。",
+      "organization": "組織",
+      "organizationPlaceholder": "組織識別子",
+      "organizationHelp": "一致するユーザーが追加される組織の識別子。",
+      "organizationRequired": "組織は必須です",
+      "baseRole": "基本ロール",
+      "baseRoleHelp": "一致するユーザーがその組織で付与されるロール。",
+      "userGroup": "ユーザーグループ",
+      "userGroupPlaceholder": "グループ名",
+      "userGroupHelp": "任意。一致するユーザーはその組織内でこのグループにも参加します。",
+      "duplicateDomain": "このドメインのマッピングはすでに存在します",
+      "confirmRemove": "{domain} のマッピングを削除してもよろしいですか？",
+      "deleteTitle": "マッピングを削除"
+    }
   },
   "reports": {
     "frequencyUnits": {
diff --git a/web/src/locales/languages/ko-KR.json b/web/src/locales/languages/ko-KR.json
index 451f547f3fe..8561534c51c 100644
--- a/web/src/locales/languages/ko-KR.json
+++ b/web/src/locales/languages/ko-KR.json
@@ -9362,7 +9362,28 @@
     "listConjunctionAnd": "{list}, 그리고 {last}",
     "runsIn": "실행 위치",
     "recommendedByFieldCoverage": "스트림의 필드 커버리지를 기반으로 추천됩니다.",
-    "listConjunctionAndTwo": "{first} 및 {last}"
+    "listConjunctionAndTwo": "{first} 및 {last}",
+    "domainOrgMappings": {
+      "title": "도메인-조직 매핑",
+      "subtitle": "일치하는 이메일 도메인으로 로그인하는 신규 사용자는 조직에 자동으로 추가됩니다.",
+      "addMapping": "매핑 추가",
+      "editMapping": "매핑 편집",
+      "empty": "아직 매핑이 없습니다. “{addLabel}”을 사용하여 매핑을 추가하세요.",
+      "domain": "이메일 도메인",
+      "domainHelp": "사용자 이메일 주소에서 {at_sign} 뒤에 오는 부분입니다.",
+      "organization": "조직",
+      "organizationPlaceholder": "조직 식별자",
+      "organizationHelp": "일치하는 사용자가 추가되는 조직의 식별자입니다.",
+      "organizationRequired": "조직은 필수입니다",
+      "baseRole": "기본 역할",
+      "baseRoleHelp": "해당 조직에서 일치하는 사용자에게 부여되는 역할입니다.",
+      "userGroup": "사용자 그룹",
+      "userGroupPlaceholder": "그룹 이름",
+      "userGroupHelp": "선택 사항입니다. 일치하는 사용자는 해당 조직에서 이 그룹에도 가입됩니다.",
+      "duplicateDomain": "이 도메인에 대한 매핑이 이미 존재합니다",
+      "confirmRemove": "{domain}에 대한 매핑을 삭제하시겠습니까?",
+      "deleteTitle": "매핑 삭제"
+    }
   },
   "reports": {
     "frequencyUnits": {
diff --git a/web/src/locales/languages/nl-NL.json b/web/src/locales/languages/nl-NL.json
index dcf5d3b2be8..395f5300ce8 100644
--- a/web/src/locales/languages/nl-NL.json
+++ b/web/src/locales/languages/nl-NL.json
@@ -9362,7 +9362,28 @@
     "listConjunctionAnd": "{list} en {last}",
     "runsIn": "Draait in",
     "recommendedByFieldCoverage": "Aanbevolen op basis van velddekking in uw streams.",
-    "listConjunctionAndTwo": "{first} en {last}"
+    "listConjunctionAndTwo": "{first} en {last}",
+    "domainOrgMappings": {
+      "title": "Domein naar organisatie-toewijzingen",
+      "subtitle": "Nieuwe gebruikers die inloggen met een overeenkomend e-maildomein worden automatisch aan de organisatie toegevoegd.",
+      "addMapping": "Toewijzing toevoegen",
+      "editMapping": "Toewijzing bewerken",
+      "empty": "Nog geen toewijzingen. Gebruik “{addLabel}” om er een toe te voegen.",
+      "domain": "E-maildomein",
+      "domainHelp": "Het deel na {at_sign} in het e-mailadres van de gebruiker.",
+      "organization": "Organisatie",
+      "organizationPlaceholder": "Organisatie-identificatie",
+      "organizationHelp": "De identificatie van de organisatie waaraan overeenkomende gebruikers worden toegevoegd.",
+      "organizationRequired": "Organisatie is vereist",
+      "baseRole": "Basisrol",
+      "baseRoleHelp": "De rol die overeenkomende gebruikers in die organisatie krijgen.",
+      "userGroup": "Gebruikersgroep",
+      "userGroupPlaceholder": "Groepsnaam",
+      "userGroupHelp": "Optioneel. Overeenkomende gebruikers worden ook lid van deze groep in die organisatie.",
+      "duplicateDomain": "Er bestaat al een toewijzing voor dit domein",
+      "confirmRemove": "Weet je zeker dat je de toewijzing voor {domain} wilt verwijderen?",
+      "deleteTitle": "Toewijzing verwijderen"
+    }
   },
   "reports": {
     "frequencyUnits": {
diff --git a/web/src/locales/languages/pl-PL.json b/web/src/locales/languages/pl-PL.json
index 79d11c456a7..45e228191d0 100644
--- a/web/src/locales/languages/pl-PL.json
+++ b/web/src/locales/languages/pl-PL.json
@@ -9362,7 +9362,28 @@
     "listConjunctionAnd": "{list} i {last}",
     "runsIn": "Działa w",
     "recommendedByFieldCoverage": "Zalecane na podstawie pokrycia pól w Twoich strumieniach.",
-    "listConjunctionAndTwo": "{first} i {last}"
+    "listConjunctionAndTwo": "{first} i {last}",
+    "domainOrgMappings": {
+      "title": "Mapowania domen na organizacje",
+      "subtitle": "Nowi użytkownicy logujący się z pasującą domeną e-mail są automatycznie dodawani do organizacji.",
+      "addMapping": "Dodaj mapowanie",
+      "editMapping": "Edytuj mapowanie",
+      "empty": "Brak mapowań. Użyj „{addLabel}”, aby dodać mapowanie.",
+      "domain": "Domena e-mail",
+      "domainHelp": "Część po {at_sign} w adresie e-mail użytkownika.",
+      "organization": "Organizacja",
+      "organizationPlaceholder": "Identyfikator organizacji",
+      "organizationHelp": "Identyfikator organizacji, do której dodawani są pasujący użytkownicy.",
+      "organizationRequired": "Organizacja jest wymagana",
+      "baseRole": "Rola podstawowa",
+      "baseRoleHelp": "Rola przyznawana pasującym użytkownikom w tej organizacji.",
+      "userGroup": "Grupa użytkowników",
+      "userGroupPlaceholder": "Nazwa grupy",
+      "userGroupHelp": "Opcjonalne. Pasujący użytkownicy dołączają także do tej grupy w tej organizacji.",
+      "duplicateDomain": "Mapowanie dla tej domeny już istnieje",
+      "confirmRemove": "Czy na pewno chcesz usunąć mapowanie dla {domain}?",
+      "deleteTitle": "Usuń mapowanie"
+    }
   },
   "reports": {
     "frequencyUnits": {
diff --git a/web/src/locales/languages/pt-PT.json b/web/src/locales/languages/pt-PT.json
index 4e5dfba0f41..4c7ff173691 100644
--- a/web/src/locales/languages/pt-PT.json
+++ b/web/src/locales/languages/pt-PT.json
@@ -9362,7 +9362,28 @@
     "listConjunctionAnd": "{list} e {last}",
     "runsIn": "Executa em",
     "recommendedByFieldCoverage": "Recomendado com base na cobertura de campos em seus fluxos.",
-    "listConjunctionAndTwo": "{first} e {last}"
+    "listConjunctionAndTwo": "{first} e {last}",
+    "domainOrgMappings": {
+      "title": "Mapeamentos de domínio para organização",
+      "subtitle": "Novos usuários que fazem login com um domínio de e-mail correspondente são adicionados à organização automaticamente.",
+      "addMapping": "Adicionar mapeamento",
+      "editMapping": "Editar mapeamento",
+      "empty": "Nenhum mapeamento ainda. Use “{addLabel}” para adicionar um.",
+      "domain": "Domínio de e-mail",
+      "domainHelp": "A parte após {at_sign} no endereço de e-mail do usuário.",
+      "organization": "Organização",
+      "organizationPlaceholder": "Identificador da organização",
+      "organizationHelp": "O identificador da organização à qual os usuários correspondentes são adicionados.",
+      "organizationRequired": "A organização é obrigatória",
+      "baseRole": "Função base",
+      "baseRoleHelp": "A função atribuída aos usuários correspondentes nessa organização.",
+      "userGroup": "Grupo de usuários",
+      "userGroupPlaceholder": "Nome do grupo",
+      "userGroupHelp": "Opcional. Os usuários correspondentes também entram nesse grupo nessa organização.",
+      "duplicateDomain": "Já existe um mapeamento para este domínio",
+      "confirmRemove": "Tem certeza de que deseja excluir o mapeamento para {domain}?",
+      "deleteTitle": "Excluir mapeamento"
+    }
   },
   "reports": {
     "frequencyUnits": {
diff --git a/web/src/locales/languages/ru-RU.json b/web/src/locales/languages/ru-RU.json
index cabc8f7878f..bd2baf7805d 100644
--- a/web/src/locales/languages/ru-RU.json
+++ b/web/src/locales/languages/ru-RU.json
@@ -9362,7 +9362,28 @@
     "listConjunctionAnd": "{list} и {last}",
     "runsIn": "Выполняется в",
     "recommendedByFieldCoverage": "Рекомендуется на основе покрытия полей в ваших потоках.",
-    "listConjunctionAndTwo": "{first} и {last}"
+    "listConjunctionAndTwo": "{first} и {last}",
+    "domainOrgMappings": {
+      "title": "Сопоставления доменов и организаций",
+      "subtitle": "Новые пользователи, входящие в систему с подходящим доменом электронной почты, автоматически добавляются в организацию.",
+      "addMapping": "Добавить сопоставление",
+      "editMapping": "Изменить сопоставление",
+      "empty": "Сопоставлений пока нет. Используйте «{addLabel}», чтобы добавить.",
+      "domain": "Домен электронной почты",
+      "domainHelp": "Часть после {at_sign} в адресе электронной почты пользователя.",
+      "organization": "Организация",
+      "organizationPlaceholder": "Идентификатор организации",
+      "organizationHelp": "Идентификатор организации, в которую добавляются подходящие пользователи.",
+      "organizationRequired": "Организация обязательна",
+      "baseRole": "Базовая роль",
+      "baseRoleHelp": "Роль, которая назначается подходящим пользователям в этой организации.",
+      "userGroup": "Группа пользователей",
+      "userGroupPlaceholder": "Название группы",
+      "userGroupHelp": "Необязательно. Подходящие пользователи также присоединяются к этой группе в указанной организации.",
+      "duplicateDomain": "Сопоставление для этого домена уже существует",
+      "confirmRemove": "Вы уверены, что хотите удалить сопоставление для {domain}?",
+      "deleteTitle": "Удалить сопоставление"
+    }
   },
   "reports": {
     "frequencyUnits": {
diff --git a/web/src/locales/languages/tr-TR.json b/web/src/locales/languages/tr-TR.json
index 340f34dc8e0..d99ff1e99cb 100644
--- a/web/src/locales/languages/tr-TR.json
+++ b/web/src/locales/languages/tr-TR.json
@@ -9362,7 +9362,28 @@
     "listConjunctionAnd": "{list} ve {last}",
     "runsIn": "Çalışır",
     "recommendedByFieldCoverage": "Akışlarınızdaki alan kapsamına göre önerilir.",
-    "listConjunctionAndTwo": "{first} ve {last}"
+    "listConjunctionAndTwo": "{first} ve {last}",
+    "domainOrgMappings": {
+      "title": "Alan adı - organizasyon eşlemeleri",
+      "subtitle": "Eşleşen e-posta alan adıyla oturum açan yeni kullanıcılar organizasyona otomatik olarak eklenir.",
+      "addMapping": "Eşleme ekle",
+      "editMapping": "Eşlemeyi düzenle",
+      "empty": "Henüz eşleme yok. Eklemek için \"{addLabel}\" kullanın.",
+      "domain": "E-posta alan adı",
+      "domainHelp": "Kullanıcının e-posta adresinde {at_sign} sonrasındaki kısım.",
+      "organization": "Organizasyon",
+      "organizationPlaceholder": "Organizasyon tanımlayıcısı",
+      "organizationHelp": "Eşleşen kullanıcıların ekleneceği organizasyonun tanımlayıcısı.",
+      "organizationRequired": "Organizasyon gereklidir",
+      "baseRole": "Temel rol",
+      "baseRoleHelp": "Eşleşen kullanıcılara bu organizasyonda verilen rol.",
+      "userGroup": "Kullanıcı grubu",
+      "userGroupPlaceholder": "Grup adı",
+      "userGroupHelp": "İsteğe bağlı. Eşleşen kullanıcılar bu organizasyondaki bu gruba da katılır.",
+      "duplicateDomain": "Bu alan adı için bir eşleme zaten mevcut",
+      "confirmRemove": "{domain} için eşlemeyi silmek istediğinizden emin misiniz?",
+      "deleteTitle": "Eşlemeyi Sil"
+    }
   },
   "reports": {
     "frequencyUnits": {
diff --git a/web/src/locales/languages/vi-VN.json b/web/src/locales/languages/vi-VN.json
index b6f59c9e1a9..ec48c3d9749 100644
--- a/web/src/locales/languages/vi-VN.json
+++ b/web/src/locales/languages/vi-VN.json
@@ -9362,7 +9362,28 @@
     "listConjunctionAnd": "{list} và {last}",
     "runsIn": "Chạy trong",
     "recommendedByFieldCoverage": "Được đề xuất dựa trên mức độ bao phủ trường trong các luồng của bạn.",
-    "listConjunctionAndTwo": "{first} và {last}"
+    "listConjunctionAndTwo": "{first} và {last}",
+    "domainOrgMappings": {
+      "title": "Ánh xạ tên miền đến tổ chức",
+      "subtitle": "Người dùng mới đăng nhập bằng tên miền email khớp sẽ tự động được thêm vào tổ chức.",
+      "addMapping": "Thêm ánh xạ",
+      "editMapping": "Chỉnh sửa ánh xạ",
+      "empty": "Chưa có ánh xạ nào. Sử dụng “{addLabel}” để thêm.",
+      "domain": "Tên miền email",
+      "domainHelp": "Phần sau {at_sign} trong địa chỉ email của người dùng.",
+      "organization": "Tổ chức",
+      "organizationPlaceholder": "Mã định danh tổ chức",
+      "organizationHelp": "Mã định danh của tổ chức mà người dùng khớp được thêm vào.",
+      "organizationRequired": "Tổ chức là bắt buộc",
+      "baseRole": "Vai trò cơ bản",
+      "baseRoleHelp": "Vai trò được gán cho người dùng khớp trong tổ chức đó.",
+      "userGroup": "Nhóm người dùng",
+      "userGroupPlaceholder": "Tên nhóm",
+      "userGroupHelp": "Tùy chọn. Người dùng khớp cũng sẽ tham gia nhóm này trong tổ chức đó.",
+      "duplicateDomain": "Đã tồn tại ánh xạ cho tên miền này",
+      "confirmRemove": "Bạn có chắc chắn muốn xóa ánh xạ cho {domain} không?",
+      "deleteTitle": "Xóa ánh xạ"
+    }
   },
   "reports": {
     "frequencyUnits": {
diff --git a/web/src/locales/languages/zh-CN.json b/web/src/locales/languages/zh-CN.json
index 631617647dc..59b888121a9 100644
--- a/web/src/locales/languages/zh-CN.json
+++ b/web/src/locales/languages/zh-CN.json
@@ -9362,7 +9362,28 @@
     "listConjunctionAnd": "{list} 和 {last}",
     "runsIn": "运行于",
     "recommendedByFieldCoverage": "根据您的数据流中的字段覆盖率推荐。",
-    "listConjunctionAndTwo": "{first} 和 {last}"
+    "listConjunctionAndTwo": "{first} 和 {last}",
+    "domainOrgMappings": {
+      "title": "域名到组织映射",
+      "subtitle": "使用匹配邮箱域名登录的新用户将自动加入该组织。",
+      "addMapping": "添加映射",
+      "editMapping": "编辑映射",
+      "empty": "暂无映射。使用“{addLabel}”添加一个。",
+      "domain": "邮箱域名",
+      "domainHelp": "用户邮箱地址中 {at_sign} 之后的部分。",
+      "organization": "组织",
+      "organizationPlaceholder": "组织标识符",
+      "organizationHelp": "匹配用户所加入组织的标识符。",
+      "organizationRequired": "组织为必填项",
+      "baseRole": "基础角色",
+      "baseRoleHelp": "匹配用户在该组织中被赋予的角色。",
+      "userGroup": "用户组",
+      "userGroupPlaceholder": "组名称",
+      "userGroupHelp": "可选。匹配用户还会加入该组织中的此组。",
+      "duplicateDomain": "该域名的映射已存在",
+      "confirmRemove": "确定要删除 {domain} 的映射吗？",
+      "deleteTitle": "删除映射"
+    }
   },
   "reports": {
     "frequencyUnits": {
diff --git a/web/src/locales/languages/zh-TW.json b/web/src/locales/languages/zh-TW.json
index a2c690fa288..30a9ceda25a 100644
--- a/web/src/locales/languages/zh-TW.json
+++ b/web/src/locales/languages/zh-TW.json
@@ -9357,7 +9357,28 @@
     "listConjunctionAnd": "{list} 和 {last}",
     "runsIn": "執行於",
     "recommendedByFieldCoverage": "根據您串流中的欄位覆蓋率推薦。",
-    "listConjunctionAndTwo": "{first} 和 {last}"
+    "listConjunctionAndTwo": "{first} 和 {last}",
+    "domainOrgMappings": {
+      "title": "網域至組織的對應",
+      "subtitle": "使用符合電子郵件網域登入的新使用者會自動加入該組織。",
+      "addMapping": "新增對應",
+      "editMapping": "編輯對應",
+      "empty": "尚無對應。使用「{addLabel}」新增一筆。",
+      "domain": "電子郵件網域",
+      "domainHelp": "使用者電子郵件地址中 {at_sign} 之後的部分。",
+      "organization": "組織",
+      "organizationPlaceholder": "組織識別碼",
+      "organizationHelp": "符合的使用者所加入之組織的識別碼。",
+      "organizationRequired": "必須填寫組織",
+      "baseRole": "基本角色",
+      "baseRoleHelp": "符合的使用者在該組織中被賦予的角色。",
+      "userGroup": "使用者群組",
+      "userGroupPlaceholder": "群組名稱",
+      "userGroupHelp": "選填。符合的使用者也會加入該組織中的此群組。",
+      "duplicateDomain": "此網域的對應已存在",
+      "confirmRemove": "您確定要刪除 {domain} 的對應嗎？",
+      "deleteTitle": "刪除對應"
+    }
   },
   "reports": {
     "frequencyUnits": {
