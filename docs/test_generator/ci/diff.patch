===== COMPLETE LIST OF CHANGED FILES (107 files) =====
# AUTHORITATIVE: this list is never truncated. The diff BODY below may be trimmed for
# very large PRs, so decide WHICH files/areas changed (e.g. web/src frontend) from THIS list.
.claude/skills/ui-architect/SKILL.md
.claude/skills/ui-architect/references/core-display.md
.claude/skills/ui-architect/references/creating-components.md
.claude/skills/ui-architect/references/design-tokens.md
.claude/skills/ui-architect/references/house-rules.md
.claude/skills/ui-architect/references/navigation-menus.md
.claude/skills/ui-architect/references/overlay-navigation.md
CLAUDE.md
scripts/translations/.translation_state.json
tests/ui-testing/pages/alertsPages/alertsPage.js
web/src/components/alertLibrary/LibraryCard.spec.ts
web/src/components/alertLibrary/LibraryCard.vue
web/src/components/alertLibrary/LibraryDrawer.vue
web/src/components/alertLibrary/LibraryRail.vue
web/src/components/alertLibrary/libraryFacets.ts
web/src/components/alerts/AddAlert.vue
web/src/components/alerts/AlertGroupsTable.vue
web/src/components/alerts/AlertHistoryDrawer.vue
web/src/components/alerts/AlertList.spec.ts
web/src/components/alerts/AlertList.vue
web/src/components/alerts/AlertSettingsHelpDrawer.vue
web/src/components/alerts/AlertsDestinationList.vue
web/src/components/alerts/CreateAlertFromSourceDialog.vue
web/src/components/alerts/DependencyImpactDialog.vue
web/src/components/alerts/DependencyUsageCell.vue
web/src/components/alerts/DestinationTestResult.vue
web/src/components/alerts/ImportAlert.vue
web/src/components/alerts/ImportDestination.vue
web/src/components/alerts/ImportTemplate.vue
web/src/components/alerts/IncidentDetailDrawer.vue
web/src/components/alerts/IncidentRCAAnalysis.vue
web/src/components/alerts/TemplateList.vue
web/src/components/alerts/template-content/TemplatePreviewPanel.vue
web/src/components/dbm/DbmCheckList.vue
web/src/components/dbm/DbmCoverageLine.vue
web/src/components/dbm/DbmDeadlockCycle.vue
web/src/components/dbm/DbmDeadlockSide.vue
web/src/components/dbm/DbmDeltaCell.spec.ts
web/src/components/dbm/DbmDeltaCell.vue
web/src/components/dbm/DbmDisclosureLine.spec.ts
web/src/components/dbm/DbmDisclosureLine.vue
web/src/components/dbm/DbmHistoryPanel.spec.ts
web/src/components/dbm/DbmHistoryPanel.vue
web/src/components/dbm/DbmInstanceHealthCell.vue
web/src/components/dbm/DbmLoadCell.vue
web/src/components/dbm/DbmMetricTiles.spec.ts
web/src/components/dbm/DbmMetricTiles.vue
web/src/components/dbm/DbmOverlapValue.vue
web/src/components/dbm/DbmPageChrome.spec.ts
web/src/components/dbm/DbmPageChrome.vue
web/src/components/dbm/DbmQueryCell.spec.ts
web/src/components/dbm/DbmQueryCell.vue
web/src/components/dbm/DbmRefreshButton.spec.ts
web/src/components/dbm/DbmRefreshButton.vue
web/src/components/dbm/DbmRowActions.vue
web/src/components/dbm/DbmScopeFilters.vue
web/src/components/dbm/DbmSection.spec.ts
web/src/components/dbm/DbmSection.vue
web/src/components/dbm/DbmTerminateSql.spec.ts
web/src/components/dbm/DbmTerminateSql.vue
web/src/components/slos/SloAlertCondition.vue
web/src/components/slos/SloAlertsPanel.vue
web/src/components/slos/SloBurndownChart.vue
web/src/composables/dbm/useDbmChartTheme.spec.ts
web/src/composables/dbm/useDbmChartTheme.ts
web/src/lib/core/Badge/badgeGroups.ts
web/src/lib/core/PageLayout/OPageLayout.vue
web/src/lib/core/RefreshButton/ORefreshButton.types.ts
web/src/lib/core/RefreshButton/ORefreshButton.vue
web/src/lib/core/Table/OTable.types.ts
web/src/lib/core/Table/OTable.vue
web/src/lib/core/Table/sub-components/OTableHeader.vue
web/src/lib/forms/Select/OSelect.vue
web/src/lib/navigation/Tabs/OTabPanel.types.ts
web/src/lib/navigation/Tabs/OTabPanel.vue
web/src/lib/styles/tokens/component.css
web/src/lib/styles/tokens/dark.css
web/src/locales/languages/de-DE.json
web/src/locales/languages/en-US.json
web/src/locales/languages/es-ES.json
web/src/locales/languages/fr-FR.json
web/src/locales/languages/it-IT.json
web/src/locales/languages/ja-JP.json
web/src/locales/languages/ko-KR.json
web/src/locales/languages/nl-NL.json
web/src/locales/languages/pl-PL.json
web/src/locales/languages/pt-PT.json
web/src/locales/languages/ru-RU.json
web/src/locales/languages/tr-TR.json
web/src/locales/languages/vi-VN.json
web/src/locales/languages/zh-CN.json
web/src/locales/languages/zh-TW.json
web/src/utils/dbm/historyChart.spec.ts
web/src/utils/dbm/historyChart.ts
web/src/views/AlertLibrary/AlertLibrary.vue
web/src/views/DatabaseMonitoring/ActivityPage.vue
web/src/views/DatabaseMonitoring/BlockedQueriesPage.vue
web/src/views/DatabaseMonitoring/DatabasesPage.vue
web/src/views/DatabaseMonitoring/DeadlocksPage.vue
web/src/views/DatabaseMonitoring/QueriesPage.vue
web/src/views/DatabaseMonitoring/QueryDetailPage.vue
web/src/views/DatabaseMonitoring/SamplesPage.vue
web/src/views/DatabaseMonitoring/TableHealthPage.vue
web/src/views/DatabaseMonitoring/dbmZeroTraceQueryDetail.spec.ts
web/src/views/slos/AddSlo.vue
web/src/views/slos/SloDetail.vue
web/src/views/slos/SloList.vue
===== END CHANGED FILES =====

diff --git a/.claude/skills/ui-architect/SKILL.md b/.claude/skills/ui-architect/SKILL.md
index 74ab6f01426..b5ce474dafd 100644
--- a/.claude/skills/ui-architect/SKILL.md
+++ b/.claude/skills/ui-architect/SKILL.md
@@ -77,7 +77,9 @@ read it once, it is the backbone of everything below.
 
 1. **Every page/module header is `OPageHeader`** — never a hand-rolled
    `<div class="header">…<h1>` or a `q-toolbar`. One header contract keeps the
-   title in the same place across list → detail → edit.
+   title in the same place across list → detail → edit. Peer/section tabs need
+   **`tabs-below`** (the slot alone renders them inline beside the title), and the
+   header `icon` must be the SAME `IconName` the page's nav entry declares.
 2. **Build from O2 components in `web/src/lib`** — never a bare HTML control
    (`<button>`, `<input>`) or a third-party UI primitive when an `O*` equivalent
    exists. Drive them by **intent**
@@ -195,6 +197,23 @@ read it once, it is the backbone of everything below.
      Default to `text-sm` for body. Go smaller only for genuinely dense/secondary UI,
      larger only for titles. If a design needs a size not on the scale, snap to the
      nearest step — do **not** reintroduce an arbitrary `text-[..]`.
+   - **Casing — never uppercase anywhere in the app, except an established short
+     form.** This is app-wide and strict. No shouting: don't bake caps into a string
+     (`"FIRST APPEARED AT {time}"`, `"NEW TO THIS LIST"`) and don't force it with the
+     `uppercase` utility. Write copy in **sentence case** (`"First appeared at {time}"`)
+     — capitalize the first word and proper nouns only, not Every Word.
+     **The one exception is an established short form** — an acronym, initialism, or
+     abbreviation that is conventionally written in caps (`SQL`, `API`, `URL`, `ID`,
+     `CPU`, `AI`, `HTTP`, `JSON`) and metric tokens (`p95`, `p99`) — which keep their
+     canonical casing inside otherwise-sentence-case copy. A full word is never a short
+     form: `DELETE`, `SAVE`, `NEW`, `SERVER` are violations; `Delete`, `Save`, `New`,
+     `Server` are correct. This applies everywhere text renders: micro-labels,
+     stat/tile captions, table headers, chips, badges, buttons, tooltips, empty states.
+     Make a label quiet with `text-text-label`/`text-xs` weight + colour, not caps.
+     `tracking-wide uppercase` is **not** the house label style. (The transform is a
+     legibility cost — caps runs slow the reader and break at small sizes — and a
+     baked-in caps string also can't be sentence-cased per locale.) `capitalize` is
+     acceptable only for a single data token that must render title-cased.
    - **Corner radius — exactly two tiers + circle, never an arbitrary value:**
      `rounded-default` (**4px** — controls: buttons, inputs, chips, small icon
      buttons), `rounded-surface` (**12px** — surfaces: dialogs, drawers, cards,
@@ -504,7 +523,8 @@ and each domain has its own reference below.
 | Decision | The rule | Detail |
 | --- | --- | --- |
 | **Tabular data** | `OTable` + `OTableColumnDef[]`; client-side pagination unless the backend paginates a set too large to fetch whole | [core-controls-table](references/core-controls-table.md) |
-| **Whole-page layout** | **Every routed view is a `OPageLayout`.** It's the ONE page component — it owns the full-height column, the header (from `:title`/`:icon`/`:subtitle`/`:back` props + `#actions`/`#header-tabs`), an optional `#subnav` strip, an optional `#sidebar` rail (fixed or `resizable`), and the body's inset. You plug in data; there's no place to hand-roll a padded `<div>`. Body is inset to the page-edge grid by default — pass **`bleed`** for a full-bleed body (an `OTable`, a chart, a `router-view` shell), or **`constrained`** for a centered reading column (forms). The `#header` slot is a rare escape hatch only. | [page-recipes](references/page-recipes.md) |
+| **Charts / graphs** | **Every data chart renders through the shared dashboard engine — never mount a charting lib in a feature page.** Time-series, category, scatter, geo/map, gauge, pie → **`PanelSchemaRenderer`** (`web/src/components/dashboards/PanelSchemaRenderer.vue`) with a panel schema: it runs the query, applies the app's unit/theme/annotation formatting, and owns the loading/error ladder. **Banned in feature code:** `echarts.init` / a raw `<v-chart>` / ApexCharts / D3 / Chart.js / a hand-rolled `<canvas>` or `<svg>` plot. The low-level **`panels/ChartRenderer.vue`** (raw ECharts option) is the ONLY sanctioned escape hatch, and ONLY when you need chart-`@click` forwarding `PanelSchemaRenderer` doesn't re-emit — annotate the site with why, and convert once the schema renderer forwards clicks. **Not charts** (do NOT force these through the renderer): in-row trend lines are **`OSparkline`**, single-value share bars are **`OProgressBar`**, in-cell data bars are the table's **`ODataBarCell`**, and a decorative topology/diagram is bespoke SVG. | [core-display](references/core-display.md) |
+| **Whole-page layout** | **Every routed view is a `OPageLayout`.** It's the ONE page component — it owns the full-height column, the header (from `:title`/`:icon`/`:subtitle`/`:back` props + `#actions`/`#header-tabs`, the latter needing **`tabs-below`** to land in row 2 instead of inline), an optional `#subnav` strip, an optional `#sidebar` rail (fixed or `resizable`), and the body's inset. You plug in data; there's no place to hand-roll a padded `<div>`. Body is inset to the page-edge grid by default — pass **`bleed`** for a full-bleed body (an `OTable`, a chart, a `router-view` shell), or **`constrained`** for a centered reading column (forms). The `#header` slot is a rare escape hatch only. | [page-recipes](references/page-recipes.md) |
 | **Content inset** | `OPageLayout` already insets the body. Anywhere else (a panel, a dialog section, one tab's content) wrap it in **`OContent`** (bakes the one `px-page-edge` grid line, the primitive `OPageLayout` uses internally) instead of hand-picking `px-2`/`px-4`/`p-2.5`; pass `bleed` (or `bleed-x`/`bleed-y`) for full-bleed content that owns its own edge — same escape-hatch idea as `ODrawer`/`ODialog` `bleed`. Never hand-roll a content inset. | [conventions](references/conventions.md) |
 | **Tab strips** | an `OTabs` strip needs **no** horizontal wrapper padding — the first tab's label self-aligns to the `px-page-edge` grid, so it lines up with the `OContent` body below it. Put the strip's bottom divider on the strip (`border-b`) and give it no `px-*`; wrapping a tab strip in `px-page-edge` double-insets the labels. | [conventions](references/conventions.md) |
 | **Listing toolbar** | every list carries three affordances — search + filters (`#toolbar`), refresh (`#toolbar-trailing`), and the auto-injected column-visibility toggle; empty state is one `OEmptyState` with `:filtered` | [page-recipes](references/page-recipes.md) |
@@ -554,6 +574,13 @@ Run this in your head before writing template markup, and again before
 considering the UI done:
 
 - [ ] Page/module header is `OPageHeader` (not a hand-built header bar).
+- [ ] Peer/section tabs pass **`tabs-below`** so the strip is the full-width
+      row-2 band — the bare `#header-tabs`/`#tabs` slot renders them inline
+      beside the title, where they shift as the title's width changes.
+- [ ] The header **`icon` matches the page's nav entry** verbatim
+      (`navGroups.ts` / `linksList` / `settingsItems` / SectionRail). A module
+      showing one glyph in the rail and another in its header reads as two
+      places — see [navigation-menus](references/navigation-menus.md#icon-parity).
 - [ ] Every interactive control is an O2 component if one exists in
       `web/src/lib` — no bare HTML controls or third-party primitives with an O2 equivalent.
 - [ ] A self-contained/repeated UI element with no matching component was
@@ -562,6 +589,11 @@ considering the UI done:
       from `<div>` + utility classes. Classes are for layout only.
 - [ ] Tabular data uses `OTable` with `OTableColumnDef[]` columns; server mode
       only for backend-paginated data.
+- [ ] **Every data chart goes through `PanelSchemaRenderer`** (panel schema) — no
+      `echarts.init` / `<v-chart>` / ApexCharts / D3 / hand-rolled `<canvas>`/`<svg>`
+      plot in a feature page. Low-level `panels/ChartRenderer.vue` only as the
+      annotated escape hatch for chart-click forwarding. Sparklines/progress/data
+      bars stay `OSparkline`/`OProgressBar`/`ODataBarCell` (not charts).
 - [ ] **Server mode was checked against the backend**: every `sortable: true`
       column has a real sort key in the handler (an unknown key falls back
       silently and orders by something else), and any page-relative device
@@ -582,9 +614,12 @@ considering the UI done:
       (`#toolbar-trailing`, wired to fetch), and the **column show/hide toggle**
       (`:persist-columns` + `table-id` + a `hideable` column). Non-essential
       columns hidden by default via `:column-visibility`.
-- [ ] Empty state is a single `OEmptyState` with a `preset` + **`:filtered`**
+- [ ] Every empty/zero state is a single `OEmptyState` (never a hand-rolled
+      `<div>` + centered text + button). Use a `preset` + **`:filtered`**
       (search/filter active) + `@action` resetting on `clear-filters`; `#error` if
-      fetch can fail.
+      fetch can fail. Its actions use the standard layout — an `EmptyStateActionCard`
+      (`actions` prop / `#actions` slot) for a primary CTA, or `actionLabel`, never
+      a custom button row.
 - [ ] Page is **registered in navigation** (route + one of rail item / Settings
       sub-page / flyout child) and **gated** for env/role
       (`config.isEnterprise` / `config.isCloud` / `zoConfig.*`), with the route,
@@ -615,6 +650,12 @@ considering the UI done:
 - [ ] Corner radius is `rounded-default` / `rounded-surface` / `rounded-full`
       only — no bare `rounded`, no `rounded-[..]`, no retired
       `rounded-{sm,md,lg,xl}`.
+- [ ] **No uppercase anywhere except established short forms** — no caps baked
+      into a string and no `uppercase` utility on labels/headers/chips/badges/
+      buttons/tooltips. Copy is sentence case; the only caps allowed are acronyms/
+      abbreviations (`SQL`, `API`, `URL`, `ID`, `AI`) and metric tokens (`p95`).
+      A full word (`DELETE`, `SAVE`, `NEW`) is never a short form. Make labels
+      quiet with size/weight/colour, not capitals.
 - [ ] No `<style scoped>` block added. No `style="…"` attribute added.
 - [ ] No literal colors anywhere. Colours come from `--color-*` token **utilities**
       (`bg-surface-base`, `text-text-secondary`) — not a raw `var(--color-*)` in a
@@ -622,6 +663,12 @@ considering the UI done:
       keyframes, `color-mix`, `calc`, SVG `fill`/`stroke`, `v-html`). No raw
       Tailwind palette (`bg-gray-*`) and no raw `grey-*`/`primary-*` ramp — use a
       semantic token.
+- [ ] **Text colour matches its semantic role** — `heading` (titles/emphasis),
+      `body` (main content), `secondary` (labels, captions, metadata, units),
+      `muted` ONLY for disabled/absent content (disabled control, em-dash /
+      "none" / "not measured" placeholder). Never `muted` on a live value or a
+      label; make text quieter by dropping one tier + size/weight, not opacity.
+      See [design-tokens § text colour](references/design-tokens.md).
 - [ ] **Calm Signal** — the screen's *primary signal* is coloured (state /
       category / role / progress) via the shared toolkit (`OStatStrip`/`OStatCard`,
       `OTag` chips, row rail + exception tint, relative `OTimeCell`), and the rest
@@ -662,7 +709,11 @@ considering the UI done:
       a re-telling of the code or the history of the PR that added it (no ticket
       ids, "review finding", "as discussed"). Same in specs. See
       [conventions § Comments stay short](references/conventions.md).
-- [ ] `cd web && npm run lint && npm run type-check` pass.
+- [ ] `cd web && npm run lint && npm run type-check:app` pass. **`type-check:app`,
+      not `type-check`** — the latter runs `tsconfig.vitest.json`, whose `include`
+      is only `src/**/*.spec.{ts,js}`, so it never checks a `.vue` file and a
+      green run says nothing about the component you just wrote. `type-check:app`
+      (`tsconfig.app.json`) is the one that covers `src/**/*.vue`.
 
 ## When a rule can't be satisfied
 
diff --git a/.claude/skills/ui-architect/references/core-display.md b/.claude/skills/ui-architect/references/core-display.md
index 3d69ab09c99..273043b1589 100644
--- a/.claude/skills/ui-architect/references/core-display.md
+++ b/.claude/skills/ui-architect/references/core-display.md
@@ -29,7 +29,11 @@ Reference for the O2 display and content primitives under `@/lib/core/*`. Each e
 **Key props:**
 - `variant` (default `"default"`) — one of: `default`, `primary`, `success`, `warning`, `error`, `default-outline`, `primary-outline`, `success-outline`, `warning-outline`, `error-outline`, `info-outline`, `purple-outline`, `default-soft`, `primary-soft`, `success-soft`, `warning-soft`, `error-soft`, `teal`, `teal-outline`, `teal-soft`, `orange`, `orange-outline`, `orange-soft`, `lime`, `lime-outline`, `lime-soft`, `amber`, `amber-outline`, `amber-soft`, `cyan`, `cyan-outline`, `cyan-soft`, `blue`, `blue-outline`, `blue-soft`, `purple`, `purple-soft`, `indigo`, `indigo-outline`, `indigo-soft`
 - `size` (`xs` | `sm` | `md` — default `md`)
-- `shape` (`pill` | `rounded` | `square` — default `pill`)
+- `shape` (`pill` | `rounded` | `square` — default `pill`).
+  **Strict: badges are `pill` by default and STAY pill.** Do not pass `rounded`
+  or `square` — never pick a badge shape by eye. Only set another shape when a
+  design explicitly calls for it, and for a typed `OTag` that shape lives in the
+  badge-registry group (`badgeGroups.ts`), never as a call-site prop.
 - `icon` (string — Material icon or OIcon registry name; overridden by `#icon` slot)
 - `count` (number — trailing segment; `0` still renders unless `hideZeroCount`)
 - `hideZeroCount` (boolean — suppress trailing segment when count is 0)
@@ -51,11 +55,29 @@ Reference for the O2 display and content primitives under `@/lib/core/*`. Each e
 **Import:** `@/lib/core/Badge/OTag.vue`
 **Use when:** This is THE badge for application code. Use it for any status pill, type chip, or label — either semantic (pass `type` + `value` to resolve colour/icon/dot/label from the badge registry) or manual (pass `variant`/slots like OBadge).
 **Don't use for:** Two-segment `key=value` dimension chips — use `ODimensionChip`. Don't reach past it to `OBadge`.
+
+> **Rule (strict) — styling lives in the registry, never at the call site.**
+> A semantic `<OTag>` in application code carries **only `type` + `value`** (plus
+> `label`/`count`/`#default` for genuinely dynamic *content* the registry cannot
+> enumerate — a version string, an instance name, a live count). Every VISUAL
+> decision — `variant`, `size`, `shape`, `icon`, `dot` — belongs in the group's
+> entry in `badgeGroups.ts`, **not** as a per-call prop, so one edit restyles
+> every use and the look cannot drift between call sites. The style-override props
+> listed below exist for the low-level library and one-off *manual* passthrough
+> badges (no `type`) only — do not use them to re-style a typed badge.
+>
+> A badge that must look different **in a different context** gets its own
+> registry **group** (e.g. a distinct `type`), not a call-site override. If you
+> find yourself writing `<OTag type="x" value="y" size="md" shape="pill">`, the
+> size/shape belong in group `x` (or a new group), not on the tag.
 **Key props:**
 - `type` (`BadgeGroupName | string` — registry group e.g. `"alertType"`; omit for a manual badge)
 - `value` (unknown — raw value resolved against the group)
 - `size` (`xs` | `sm` | `md` — precedence: prop → registry → `sm`)
-- `shape` (`pill` | `rounded` | `square` — precedence: prop → registry → `pill`)
+- `shape` (`pill` | `rounded` | `square` — precedence: prop → registry → `pill`).
+  Default is `pill` and stays pill; a group sets `rounded`/`square` in
+  `badgeGroups.ts` only when a design explicitly calls for it — never a call-site
+  shape override.
 - `label` (string — override resolved label)
 - `variant` (`BadgeVariant` — override resolved colour; same enum as OBadge)
 - `icon` (string — override resolved OIcon name; `""` suppresses)
@@ -233,6 +255,17 @@ Reference for the O2 display and content primitives under `@/lib/core/*`. Each e
 **Import:** `@/lib/core/EmptyState/OEmptyState.vue`
 **Use when:** The app-wide empty-state primitive for "no data / no results" contexts. Driven by a named `preset` (fills illustration + copy + actions) or by props/slots. Three sizes for the three contexts: full page/section (`hero`), inside a card/panel (`block`), inside a table/dropdown (`inline`).
 **Don't use for:** Loading/skeleton states, or inline validation errors.
+
+> **Rule (strict) — every empty state IS `OEmptyState`.** Never hand-roll a
+> "no data / no results / nothing here yet" state from a bare `<div>` with
+> centered text and a button. ANY empty/zero state — a table with no rows, a
+> panel or tab with nothing yet, a filtered list with no matches — renders
+> through `OEmptyState` (illustration + title + description). **Actions use the
+> ONE standard layout:** a rich action **card** — `EmptyStateActionCard` via the
+> `actions` prop or the `#actions` slot — for a primary next-step CTA (icon chip
+> + label + sublabel + chevron, the same card the empty dashboard uses), or
+> `actionLabel` + `@action` for a simple button. Never a hand-placed button row
+> or a custom card. So every empty state across the app reads as one system.
 **Key props:**
 - `preset` (`EmptyStatePresetName` — named scenario from the catalog; see `EmptyState/presets.ts`)
 - `size` (`hero` | `block` | `inline` — default `block`)
@@ -265,6 +298,24 @@ Reference for the O2 display and content primitives under `@/lib/core/*`. Each e
 **Don't use for:** Decorative illustrations in empty states — those come from `OEmptyState`.
 **Key props:**
 - `name` (`IconName | string`, required — registry name or `img:<path>`). There are several hundred registry names; discover them in `@/lib/core/Icon/OIcon.icons.ts` (the exported `iconRegistry` / `IconName` type). Do not guess — check the registry.
+  - **A wrong name fails SILENTLY — nothing renders, no error, no warning.** The
+    `| string` in the prop type (needed for `img:<path>`) collapses the union, so
+    a typo type-checks. Registry keys are **kebab-case**; Material Symbols'
+    snake_case names are NOT valid. The SLO module shipped `format_list_bulleted`,
+    `gpp_maybe`, `timelapse`, `hourglass_empty`, `restart_alt` and `data_usage` —
+    six blank icons across four files, one of which left a filter row where only
+    one of four options had a glyph.
+  - **So type the literal, don't rely on the prop.** When icon names live in a
+    data array, annotate it so the strings are checked against the union:
+    ```ts
+    import type { IconName } from "@/lib/core/Icon/OIcon.icons";
+    const options = computed<{ value: string; label: I18nText; icon: IconName }[]>(() => [
+      { value: "all", label: t("x.all"), icon: "format-list-bulleted" },
+    ]);
+    ```
+    Without the annotation TS infers `icon: string` and the typo survives to
+    production. To audit an existing file, grep its icon literals against
+    `iconRegistry`'s keys.
 - `size` (`xs` | `sm` | `md` | `lg` | `xl` — default `md`; xs=12px, sm=16px, md=24px, lg=32px, xl=40px)
 - `label` (string — accessible label; sets `role="img"`, otherwise the icon is `aria-hidden`)
 
diff --git a/.claude/skills/ui-architect/references/creating-components.md b/.claude/skills/ui-architect/references/creating-components.md
index 59579399c39..9ecacfb4d9b 100644
--- a/.claude/skills/ui-architect/references/creating-components.md
+++ b/.claude/skills/ui-architect/references/creating-components.md
@@ -264,4 +264,7 @@ describe("OButton", () => {
 ## 11. After building
 
 1. **Add the component to this skill's catalog** — the sibling reference files in this folder are grouped by domain: `core-controls-table.md`, `core-display.md`, `forms-inputs.md`, `forms-specialized.md`, `forms-validation.md`, `feedback-data.md`, `overlay-navigation.md`, `keyboard-shortcuts.md`. Document the new component (props, variants, usage) in the matching file so consumers can find it.
-2. **Lint + type-check** (from `web/`): `npm run lint` then `npm run type-check`. Fix everything before declaring done. Do **not** run `npm run build`.
+2. **Lint + type-check** (from `web/`): `npm run lint` then **`npm run type-check:app`**. Fix everything before declaring done. Do **not** run `npm run build`.
+   Use `type-check:app`, not `type-check`: the plain script targets
+   `tsconfig.vitest.json`, which includes only `src/**/*.spec.{ts,js}` and so
+   never type-checks a `.vue` file.
diff --git a/.claude/skills/ui-architect/references/design-tokens.md b/.claude/skills/ui-architect/references/design-tokens.md
index e070bf5b02d..a8ffceb7b2a 100644
--- a/.claude/skills/ui-architect/references/design-tokens.md
+++ b/.claude/skills/ui-architect/references/design-tokens.md
@@ -36,8 +36,27 @@ set. The legacy **`--o2-*`** set is banned (see the bottom of this file).
   `bg-surface-base`, `--color-border-default` → `border-border-default`.) This also
   covers the v4 shorthand `bg-(--color-x)` — same rule, use `bg-x`.
   > **Retired aliases** (CI-banned, `retiredTextAlias`): `text-text-primary` →
-  > `text-text-heading`, `text-text-caption` → `text-text-secondary`. The text
-  > hierarchy is heading / body / secondary / label / muted.
+  > `text-text-heading`, `text-text-caption` → `text-text-secondary`.
+- **Text colour — pick by SEMANTIC role, never by eye.** The hierarchy is
+  heading / body / secondary / label / muted, and each has exactly one job.
+  Using the wrong tier is a bug even though CI can't catch it: `muted` on a real
+  value reads as "turned off", and a label in `body` fails to recede.
+  - `text-text-heading` — titles, section headings, and the emphasized primary
+    value in a tile/row. The strongest text.
+  - `text-text-body` — main/primary reading content: the sentence, or the data
+    value that is the row's whole point.
+  - `text-text-secondary` — labels, captions, metadata, units, sub-labels, and
+    any supporting text beside the primary. **This is where "quiet" text goes —
+    NOT muted.**
+  - `text-text-label` — a form field's own label (the `OForm*` controls render
+    it for you; reach for it directly only for a bare field label).
+  - `text-text-muted` — the faintest tier, reserved for INACTIVE / disabled /
+    absent content ONLY: a disabled control, an em-dash / "not measured" / "none"
+    placeholder, a value nobody supplied. Never a label, never body text.
+    (`text-text-disabled` is the dedicated disabled-control token when a
+    component needs a distinct one.)
+  - To make text quieter, drop ONE tier on this ladder (+ size/weight) — never a
+    raw `opacity` and never a lighter palette colour.
 - **Arbitrary `[var(--color-x)]` in a class now FAILS CI** (`rawVarInTemplate`,
   zero tolerance) — including the v4 shorthand `bg-(--color-x)` and the fallback
   form `bg-[var(--color-x,#fff)]`. "The token has no registered utility" is no
diff --git a/.claude/skills/ui-architect/references/house-rules.md b/.claude/skills/ui-architect/references/house-rules.md
index ef4a1a34c9f..9943068c8a1 100644
--- a/.claude/skills/ui-architect/references/house-rules.md
+++ b/.claude/skills/ui-architect/references/house-rules.md
@@ -11,7 +11,7 @@ recurring class of bug or drift in this codebase.
 ### 1. Every page/module header is `OPageHeader`
 
 **What.** The top of any routed view or module screen (title + icon + actions,
-optionally tabs/breadcrumb/back) is rendered by `OPageHeader`, never a
+optionally peer tabs and a back button) is rendered by `OPageHeader`, never a
 hand-rolled `<div class="header">…<h1>…` or a bespoke `q-toolbar`.
 
 ```vue
@@ -36,8 +36,9 @@ import OButton from "@/lib/core/Button/OButton.vue";
 ```
 
 **Why.** `OPageHeader` encodes a single header contract used app-wide: row 1 is
-a fixed-height band (icon tile + `<h1>` + right-aligned actions); row 2 shows
-**exactly one** of peer tabs, an ancestor breadcrumb, or a plain tagline. Every
+a fixed-height band (icon tile + `<h1>` + right-aligned actions, with the
+`subtitle` tagline under the title); row 2 is the full-width peer-tab strip.
+Tabs and a tagline coexist — most module index pages carry both. Every
 hand-built header silently re-litigates title font size, icon tile geometry,
 back-button placement, and the tab underline — and drifts. Reusing the component
 keeps the title's X/Y position identical as a user navigates list → detail →
@@ -49,11 +50,25 @@ edit, which is the whole point.
   the one-row-content contract — is documented in this rule (below), so you can
   use it correctly without opening the file.
 - Props: `title`, `subtitle`, `icon` (an `IconName` from
-  `@/lib/core/Icon/OIcon.icons`), `breadcrumb` (`BreadcrumbItem[]`),
-  `breadcrumbMaxInline`, `back` (`{ label, to | onClick, dataTest }`),
-  `tabsBelow`.
+  `@/lib/core/Icon/OIcon.icons`), `back` (`{ label, to | onClick, dataTest }`),
+  `tabsBelow`, `titleDataTest` (a `data-test` on the `<h1>`, so a page needn't
+  take over the `#title` slot just to attach a test hook), and `titleOverflow`
+  (`"truncate"` default | `"visible"` — pass `"visible"` when `#title` hosts an
+  interactive control, or the `<h1>`'s `overflow:hidden` swallows its focus ring).
+  There is **no breadcrumb prop** — `OPageHeader` has no breadcrumb affordance
+  and no `BreadcrumbItem` type exists; a sub-page uses `back` instead.
 - Slots: `title-prefix`, `title`, `subtitle`, `title-trail`, `actions`, `tabs`,
   `back`.
+- **Peer tabs need `tabs-below` — the slot alone is not enough.** `#tabs`
+  (`#header-tabs` on `OPageLayout`) defaults to rendering the strip *inline
+  beside the title*, which is the wrong place for Level-2 section nav: the title
+  block is `shrink-0`, so the strip's x-position moves with the title's width
+  and shifts under the cursor as you navigate between siblings. Pass
+  `tabs-below` to get the full-width row-2 strip every module uses. The four
+  alerting pages shipped inline for exactly this reason — nothing failed, they
+  just didn't look like the rest of the app.
+- **The header `icon` must be the SAME `IconName` the page's nav entry
+  declares** — see [navigation-menus](navigation-menus.md#icon-parity).
 - Put page actions in `#actions` using O2 components. Do not add your own
   `border-b`, height, or padding around it — the header owns its own chrome.
 - **Do not** style `OPageHeader` from the outside with utility classes or a
diff --git a/.claude/skills/ui-architect/references/navigation-menus.md b/.claude/skills/ui-architect/references/navigation-menus.md
index 2e6257a1afd..3f42e8e26ae 100644
--- a/.claude/skills/ui-architect/references/navigation-menus.md
+++ b/.claude/skills/ui-architect/references/navigation-menus.md
@@ -312,3 +312,11 @@ strings in `navGroups.ts` (flyout children may also use feature namespaces like
 - [ ] Reachable from exactly **one** nav surface.
 - [ ] Gate expressed identically across route + nav entry (+ SectionRail
       `visible` / flyout `gate`).
+- [ ] <a id="icon-parity"></a>**Icon parity** — the `icon` on the page's
+      `OPageLayout`/`OPageHeader` is the SAME `IconName` string as the nav
+      entry's (`navGroups.ts`, `linksList`, `settingsItems`, or the section
+      rail). The rail is how a user learns what the module's mark means; a
+      different glyph in the header reads as a different place. SLOs drifted
+      this way — `navGroups.ts` said `target` while every SLO view said
+      `track-changes`. Grep the nav entry for the route `name` and copy its
+      `icon` verbatim; if a page needs a different mark, change BOTH.
diff --git a/.claude/skills/ui-architect/references/overlay-navigation.md b/.claude/skills/ui-architect/references/overlay-navigation.md
index d73cbc85663..b62faad8429 100644
--- a/.claude/skills/ui-architect/references/overlay-navigation.md
+++ b/.claude/skills/ui-architect/references/overlay-navigation.md
@@ -203,27 +203,67 @@ Rule of thumb: **Tooltip = hint, Dropdown = action menu, Popover = anything free
 **Key props:** `open` (boolean, `v-model:open`) · `modal` (boolean — default `false`; traps focus + blocks outside pointer events) · `side` (`"top"|"right"|"bottom"|"left"` — default `"bottom"`) · `align` (`"start"|"center"|"end"` — default `"start"`) · `sideOffset` (number — default `4`) · `hideWhenDetached` (boolean — default `true`; hide when trigger scrolls out of view) · `ariaLabel` (string) · `zIndex` (number — default `6000`) · `contentClass` (string)
 **Slots:** `trigger` (as-child), `default` (content)
 **Emits:** `update:open` (boolean)
-**Example:**
+
+**Filter-popover recipe.** A "Filters" / "Narrow results" popover is not a loose
+stack of selects — build it as **one panel you fill in and dismiss**: a title, a
+column of **labelled** fields, then a **pinned action row**. Three rules:
+
+- **Every field carries a label**, even when its placeholder names the dimension
+  (`ENGINE` label above an `All engines` placeholder). The label is the axis; the
+  placeholder is the *unset* state. Prefer the field component's own `label` prop
+  (it wires `for`/`id` + a11y); drop to a hand-rolled micro-label
+  (`text-text-label text-3xs font-semibold tracking-wide uppercase`) only when the
+  label must match a string shown elsewhere (e.g. a toolbar chip) and be cased up.
+- **A pinned action row** at the bottom, divided by `border-t border-border-default`,
+  holding **Clear all** (`variant="outline"`, `:disabled` when nothing is set so its
+  place never shifts). **No Done/Apply button when filters apply live** — each
+  `@update` already commits, so there is nothing to submit, and the popover
+  dismisses on outside-click. Add a primary Done only if the popover *stages*
+  changes and commits on close.
+- **Don't reserve/hide the reset by presence** — keep Clear all in the layout and
+  toggle `:disabled`, so opening the popover doesn't shift the fields upward.
 
 ```vue
-<OPopover align="end">
+<OPopover v-model:open="open" align="end">
   <template #trigger>
-    <OButton variant="outline" icon="filter">Filters</OButton>
+    <OButton variant="outline" icon-left="filter-list">{{ t("dbm.filters.scope") }}</OButton>
   </template>
-  <div class="flex flex-col gap-2 p-3">
-    <OSelect v-model="status" :options="statusOptions" label="Status" />
-    <OInput v-model="search" label="Contains" />
+  <div class="flex w-72 flex-col">
+    <div class="flex flex-col gap-2.5 p-3">
+      <p class="text-text-primary text-sm font-semibold">{{ t("dbm.filters.popoverTitle") }}</p>
+      <OSelect
+        v-for="f in filters"
+        :key="f.key"
+        :label="f.dimension"
+        :model-value="f.value"
+        :options="f.options"
+        size="sm"
+        clearable
+        :placeholder="f.placeholder"
+        @update:model-value="f.onChange"
+      />
+    </div>
+    <div class="border-border-default flex items-center border-t px-3 py-2.5">
+      <OButton variant="outline" size="sm" :disabled="!activeCount" @click="clear">
+        {{ t("dbm.filters.clearScope") }}
+      </OButton>
+    </div>
   </div>
 </OPopover>
 ```
 
+Reference implementation: `web/src/components/dbm/DbmScopeFilters.vue` (adds the
+set dimensions back to the toolbar as removable `ODimensionChip`s, so an
+unfiltered page spends one button and a filtered one shows its scope as chips).
+
 **Family:** ODropdown (menu sibling — shares overlay coordination).
 
 ---
 
 ### OTooltip
 **Import:** `@/lib/overlay/Tooltip/OTooltip.vue`
-**Use when:** A passive hover/focus hint describing an element — icon-button labels, truncated text, disabled-state explanations. Two modes: **wrapper** (default slot wraps the trigger) or **child** (no default slot — attaches to its parent element as its tooltip).
+**Use when:** A passive hover/focus hint describing an element — icon-button labels, truncated text, disabled-state explanations. Two modes: **wrapper** (default slot wraps the trigger) or **child** (no default slot).
+**Child mode binds to the nearest previous VISIBLE ELEMENT SIBLING, and only falls back to the parent when there is none** — it is not simply "the parent". So put a child-mode `OTooltip` **first** inside the element you want as the trigger; placed after an `<OIcon>` it silently binds to that icon, and only the icon becomes hoverable. Text nodes do not count as siblings, so `<OIcon/> {{ count }} <OTooltip/>` binds to the icon and leaves the count dead. This shipped in the alert-dependency count chips, where the tooltip fired on a 12px glyph inside a 52px chip.
 **Don't use for:** Anything the user interacts with inside the bubble (use **OPopover**); an action menu (use **ODropdown**).
 **Key props:** `content` (string — plain-text shorthand for `#content`) · `side` (`"top"|"right"|"bottom"|"left"` — default `"top"`) · `align` (`"start"|"center"|"end"` — default `"center"`) · `sideOffset` (number — default `4`) · `alignOffset` (number — default `0`) · `delay` (number ms — default `700`) · `maxWidth` (string — default `"320px"`) · `disabled` (boolean) · `open` (boolean — controlled) · `contentClass` (string) · `shortcut` (`string | string[]`) · `shortcutId` (string — prefer over `shortcut`)
 **Slots:** `default` (trigger — single focusable element; omit for child mode), `content` (rich content, overrides `content` prop)
diff --git a/CLAUDE.md b/CLAUDE.md
index 2e0b0782487..c0e9c4a2d02 100644
--- a/CLAUDE.md
+++ b/CLAUDE.md
@@ -31,12 +31,22 @@ Item order inside a file/module, top to bottom (clippy's default grouping):
 
 ## Comments
 
-- Default is no comment. Write one only for a non-obvious constraint the code
-  itself cannot express.
-- One line is the norm. Do not write multi-line comment essays, narration of
-  what the code does, or notes about the change itself ("now we correctly...").
-- `///` doc comments on public APIs: one summary sentence; add more only for
-  real caveats, never to restate the signature.
+STRICT, and enforced in review — a comment that breaks these is deleted, not
+reworded:
+
+- **One line, or none.** A comment is at most a single line. No multi-line
+  comment blocks in code (the license header is the only exception). If the
+  point needs more than one line, it does not belong in a comment.
+- **Only logical comments — explain WHY.** The single allowed reason to write a
+  comment is to state a non-obvious constraint, invariant, or gotcha the code
+  itself cannot express. If a comment is not explaining logic, do not write it;
+  no comment is better than a filler one.
+- **Banned outright** (remove on sight): styling/visual narration (layout,
+  spacing, `px`, "matches the header height"), change/modification notes ("now
+  we...", "changed to...", "was X"), and narration that restates what the code
+  plainly does.
+- `///` / JSDoc doc comments on public APIs: one summary sentence, and only when
+  the behaviour is non-obvious; never to restate the signature.
 
 ## PR conventions
 
diff --git a/scripts/translations/.translation_state.json b/scripts/translations/.translation_state.json
index 94133b8ce1f..15d37af6d19 100644
--- a/scripts/translations/.translation_state.json
+++ b/scripts/translations/.translation_state.json
@@ -6018,15 +6018,12 @@
       "state": {
         "unknown": "0e906fd86d00ebf4"
       },
-      "subtitle": "baced9b1b8cf6357",
-      "subtitlePast": "c1d9c44562703ce3",
       "summary": {
         "idleInTransaction": "ff2e0c7318ecdc5b",
         "onCpu": "41d57724c8eb5b2c",
         "sessions": "6fa3cbf451b2a1d5",
         "waiting": "6e293a8c009e0011"
       },
-      "title": "38da1505ca837328",
       "transactionOpen": "56ca82c3416e10d6",
       "wait": {
         "onCpu": "08e24f59d004b84b",
@@ -6096,11 +6093,11 @@
         "descriptionPast": "448694694219f0fa",
         "title": "2f66294abed43c76"
       },
-      "holdsTheLock": "9e135b7e930459cd",
+      "holdsTheLock": "96c8a7f71fea2845",
       "idleInTransaction": "80790b2dabe82b6a",
       "inChain": "9185ac12ea245ca5",
       "lastStatement": "b27c3cbfcbacffbf",
-      "longestWait": "c7e8963260c371b4",
+      "longestWait": "0344209e8a455cc7",
       "notCollecting": {
         "checklistTitle": "cf26b983696fd931",
         "checks": {
@@ -6139,9 +6136,9 @@
         "waitingHint": "9e6c00f40d167720"
       },
       "pills": {
-        "root": "32446a10e7e96768",
-        "waiting": "77aaf105c28c4c82",
-        "waitingBlocking": "ef05150edc8cd88c"
+        "root": "1d20872bc27f266d",
+        "waiting": "6e293a8c009e0011",
+        "waitingBlocking": "2e882e7000557d32"
       },
       "rootCount": "ce593fc61daaa6cb",
       "rowActions": {
@@ -6151,8 +6148,6 @@
       },
       "searchPlaceholder": "9d15a817c957bdb7",
       "sessionCount": "872855722557db38",
-      "subtitle": "9d56af5532ac67e4",
-      "subtitlePast": "72e80c743af6f901",
       "summary": {
         "chains": "79aaa1a603013cfd",
         "longest": "0344209e8a455cc7",
@@ -6163,7 +6158,6 @@
         "hint": "80decc27d1bb1fa9",
         "hintNoInstance": "3af0c760403de2d7"
       },
-      "title": "61d2afe2349913ce",
       "waitEvent": {
         "advisory": "398b32c9f4819823",
         "buffer": "9520b883245dd5a8",
@@ -6187,6 +6181,7 @@
       "shortfall": "883e563de1437274"
     },
     "common": {
+      "actions": "ff8059dc6752afdd",
       "lastRefreshed": "41200a71998f3e68",
       "loadFailed": "188f776fec6ce374",
       "reload": "0e91610117029a62"
@@ -6249,23 +6244,21 @@
       "moreServices": "ecadd87ff0e5d0aa",
       "openQueries": "1cc7003257a55ac4",
       "searchPlaceholder": "5b5e4ae9bc9f4827",
-      "subtitle": "ca2269cf34caf992",
       "summary": {
         "calls": "b73a5e2ca6458665",
         "databases": "61d2afe2349913ce",
         "failed": "031a8f0f659df890",
         "time": "1235846a41c69aa3"
       },
-      "title": "61d2afe2349913ce",
       "unnamedInstance": "11ca9f99e438664d",
       "unnamedInstanceHint": "d793c53a423bbdd2"
     },
     "deadlocks": {
       "chips": {
-        "cadence": "a2189667d81080f0",
-        "noQueries": "cede8c832375a5cf",
-        "oppositeOrder": "0ac624b15136c63b",
-        "partial": "930887d6546ac57f"
+        "cadence": "ad4e4b835817ca07",
+        "noQueries": "2d8555529fc0177d",
+        "oppositeOrder": "5a873ed7162026e8",
+        "partial": "ab2b74be6ba03379"
       },
       "columns": {
         "applications": "98e33b0f3104fcf1",
@@ -6291,8 +6284,8 @@
         "copyForSlackHint": "71294c89cabdc3f2",
         "copyHint": "a07ec10b0a0dd73c",
         "copySqlTooltip": "636199e234e8d7c2",
-        "deadlock": "4f6c0efa1a0f9935",
-        "eachWaits": "33f89b01a3f49d2d",
+        "deadlock": "aaf168baa09e163b",
+        "eachWaits": "180ec31d59123e9c",
         "earlier": "e10ae99074011888",
         "fixBody": "88c1adb586d85b45",
         "fixGeneric": "7828652ddd07e5b8",
@@ -6404,12 +6397,10 @@
         "copyForSlack": "e21f935f11d7e966",
         "title": "c904ede925e58ed8"
       },
-      "subtitle": "cb6dd0125d1cabd5",
       "summary": {
         "deadlocks": "cdc2395de66afa4f",
         "pairs": "dd72f4dae2323619"
-      },
-      "title": "61d2afe2349913ce"
+      }
     },
     "delta": {
       "comparison": "99d351b2e23b19f2",
@@ -6476,11 +6467,11 @@
         "sampleHint": "14ce64e949b654ce",
         "title": "68e083296db6bae0"
       },
-      "firstSeen": "7ef8346a2b878fa9",
+      "firstSeen": "80a3b2a23bf8b137",
       "firstSeenCaveat": "c72190d565eff3cc",
       "firstSeenHint": "99328bb38f9b438f",
       "invalidStream": "42bb740f4729b7d6",
-      "lastSeen": "9d2f737d748ddf26",
+      "lastSeen": "30444ca8a5f3abae",
       "lastSeenHint": "e7cbd5f9d66bd7fb",
       "latencyTitle": "7f5fb7a6e419d850",
       "liveSeries": "f756770545aec2b1",
@@ -6663,12 +6654,12 @@
       "allServices": "5b980982fa251af0",
       "clearScope": "29a390f9237e3c84",
       "dimension": {
-        "env": "ba5285161ba6eed0",
-        "insight": "e43ba08557cbb4f1",
-        "instance": "3549b0028b75d981",
-        "namespace": "df0ad6e43880f09c",
-        "service": "9df6b026a8c6c26e",
-        "system": "ed9f6f25068608ef"
+        "env": "9e471951a1b4106e",
+        "insight": "1d7ecddb3e1f0881",
+        "instance": "fa7fe67124e94375",
+        "namespace": "07b091a3fdc4e4c0",
+        "service": "d677190e0a9990e7",
+        "system": "8e75ebbdb21505d2"
       },
       "popoverTitle": "2ee6635f891bb689",
       "removeScope": "7b7141ee715851b5",
@@ -6677,6 +6668,10 @@
     "freshness": {
       "neverAggregated": "97037dc42d71e89c"
     },
+    "header": {
+      "subtitle": "edac89e67399fbc7",
+      "title": "61d2afe2349913ce"
+    },
     "insights": {
       "all-failing": {
         "body": "de128c63d8d03478",
@@ -6856,18 +6851,16 @@
       "stmtClass": {
         "all": "ab9dcc553467b834",
         "allHint": "8e5beeb9b29be0dd",
+        "overheadLabel": "387ec3bc2fec5dab",
         "query": "80d2e18c7da9f0be",
         "queryHint": "4610c57ac29e9a78"
       },
-      "subtitle": "bde88aa5104f1ad3",
-      "subtitleServer": "41d8d980fcbae490",
       "summary": {
         "calls": "b73a5e2ca6458665",
         "failed": "031a8f0f659df890",
         "queries": "f0e5d5b427726b2c",
         "time": "1235846a41c69aa3"
       },
-      "title": "6fbe590f4fe13e19",
       "trendLabel": "95ded69384a7e085",
       "truncatedText": "b1c01c2c3d2d401b"
     },
@@ -6903,12 +6896,12 @@
       }
     },
     "rowChips": {
-      "allFailed": "a883b01930629f33",
-      "busier": "4e65a51122fc3af4",
-      "climbed": "c541f40ab519f89e",
-      "loop": "f8ca436660128d95",
-      "new": "770f54e880a10153",
-      "slower": "c38639a0bd66b303"
+      "allFailed": "efbbb17bb1350c3c",
+      "busier": "722040bc714a79ae",
+      "climbed": "f775bd54b60b28b9",
+      "loop": "fdeff924ee008929",
+      "new": "a29c643668058f08",
+      "slower": "1476a550f6f3ae46"
     },
     "samples": {
       "columnHints": {
@@ -6949,9 +6942,6 @@
         "title": "72a3187c47d84035",
         "truncated": "890656bf3bd9bccf"
       },
-      "subtitle": "67869fd3af7fcf97",
-      "subtitleServer": "643ec8b14b6ff53b",
-      "title": "7e38c4258cbb11a2",
       "viewTrace": "0e2865ffff2d46c0"
     },
     "tableHealth": {
@@ -6986,8 +6976,6 @@
       "notCollectingDescription": "7ebf5d6d83793673",
       "notCollectingTitle": "7b0476ed807f91ed",
       "searchPlaceholder": "3394de3474d7cbf2",
-      "subtitle": "7b3f3ed37654fd29",
-      "title": "e25af56211da67f1",
       "tuplesEstimated": "0ede6fcd9e883244"
     }
   },
@@ -13348,6 +13336,7 @@
       "describeBurn": "bcc89db9478e8be3",
       "disabled": "75081b593d15cf6e",
       "editTargetMissing": "2a30412f3ed2c01b",
+      "emptyActionDesc": "9cb6ebd6b000c133",
       "emptyHint": "9ffb4354995a558b",
       "emptyTitle": "1f1d4fee0e7c5a9e",
       "loadFailed": "1760368c39b88f33",
diff --git a/tests/ui-testing/pages/alertsPages/alertsPage.js b/tests/ui-testing/pages/alertsPages/alertsPage.js
index 20f904327a1..fbd7b2ef48c 100644
--- a/tests/ui-testing/pages/alertsPages/alertsPage.js
+++ b/tests/ui-testing/pages/alertsPages/alertsPage.js
@@ -304,11 +304,10 @@ export class AlertsPage {
             // TODO: replace with context-specific dialog data-test attribute
             qDialogLocator: '[data-test$="-dialog"]',
 
-            // v3 list page tab locators
-            // Note: AppTabs.vue generates data-test="tab-{value}" (not alert-list-tab-*)
-            alertListTabAll: '[data-test="tab-all"]',
-            alertListTabScheduled: '[data-test="tab-scheduled"]',
-            alertListTabRealtime: '[data-test="tab-realTime"]',
+            // v3 list page tab locators — the OToggleGroup renders data-test="alert-list-tab-{value}"
+            alertListTabAll: '[data-test="alert-list-tab-all"]',
+            alertListTabScheduled: '[data-test="alert-list-tab-scheduled"]',
+            alertListTabRealtime: '[data-test="alert-list-tab-realTime"]',
             alertListFilterChip: '[data-test="alert-list-filter-chip"]',
 
             // v3 AddAlert wizard tabs (OToggleGroupItem with data-test="add-alert-tab-{key}")
@@ -1597,7 +1596,7 @@ export class AlertsPage {
         // Click Scheduled tab and count rows
 
         // Wait for tabs to be visible first (handle slow page loads)
-        const scheduledTab = this.page.locator('[data-test="tab-scheduled"]');
+        const scheduledTab = this.page.locator('[data-test="alert-list-tab-scheduled"]');
         try {
             await scheduledTab.waitFor({ state: 'visible', timeout: 15000 });
         } catch {
@@ -1617,7 +1616,7 @@ export class AlertsPage {
         const scheduledAlertsCount = String(await scheduledRows.count());
 
         // Click Real-time tab and count rows
-        await this.page.locator('[data-test="tab-realTime"]').click();
+        await this.page.locator('[data-test="alert-list-tab-realTime"]').click();
         await this.page.waitForTimeout(1000);
         const realTimeRows = this.page.locator('tbody tr[data-index]');
         const realTimeAlertsCount = String(await realTimeRows.count());
@@ -1783,11 +1782,11 @@ export class AlertsPage {
     }
 
     async verifyTabContents() {
-        await this.page.locator('[data-test="tab-scheduled"]').click();
+        await this.page.locator('[data-test="alert-list-tab-scheduled"]').click();
         await expect(this.page.locator('[data-test="o2-empty-state"]')).toBeVisible();
-        await this.page.locator('[data-test="tab-realTime"]').click();
+        await this.page.locator('[data-test="alert-list-tab-realTime"]').click();
         await expect(this.page.getByText('Showing 1 - 1 of')).toBeVisible();
-        await this.page.locator('[data-test="tab-all"]').click();
+        await this.page.locator('[data-test="alert-list-tab-all"]').click();
         await expect(this.page.getByText('Showing 1 - 1 of')).toBeVisible();
     }
 
diff --git a/web/src/components/alertLibrary/LibraryCard.spec.ts b/web/src/components/alertLibrary/LibraryCard.spec.ts
index d2760837d22..a729e0385b9 100644
--- a/web/src/components/alertLibrary/LibraryCard.spec.ts
+++ b/web/src/components/alertLibrary/LibraryCard.spec.ts
@@ -62,7 +62,7 @@ describe("LibraryCard", () => {
   });
 
   it("shows the query language as a quiet neutral chip", () => {
-    expect(mountCard().find('[data-test="alert-library-card-query-type"]').text()).toBe("PROMQL");
+    expect(mountCard().find('[data-test="alert-library-card-query-type"]').text()).toBe("PromQL");
   });
 
   it("carries NO ready label — it would describe almost every card and signal nothing", () => {
diff --git a/web/src/components/alertLibrary/LibraryCard.vue b/web/src/components/alertLibrary/LibraryCard.vue
index 3dc689c19cb..7038c2410ae 100644
--- a/web/src/components/alertLibrary/LibraryCard.vue
+++ b/web/src/components/alertLibrary/LibraryCard.vue
@@ -42,7 +42,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
 -->
 <template>
   <article
-    class="rounded-surface border-border-default bg-surface-base hover:border-border-strong focus-visible:ring-accent/40 flex h-full cursor-pointer flex-col gap-2 border p-3 outline-none focus-visible:ring-2"
+    class="rounded-default border-border-default bg-surface-base hover:border-border-strong focus-visible:ring-accent/40 flex h-full cursor-pointer flex-col gap-2 border p-3 outline-none focus-visible:ring-2"
     role="button"
     tabindex="0"
     :aria-label="t('alert_library.openDetails', { title: entry.title })"
@@ -57,7 +57,6 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       </h3>
       <OTag
         type="severity"
-        size="xs"
         :value="severityValue"
         :label="severityText"
         class="shrink-0"
@@ -75,8 +74,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
              would colour the norm. -->
         <OTag
           variant="default-soft"
-          size="xs"
-          :label="queryTypeLabel"
+          :label="queryTypeLabelText"
           data-test="alert-library-card-query-type"
         />
 
@@ -104,7 +102,6 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       <OTag
         v-if="!ready"
         variant="warning-quiet"
-        size="xs"
         class="max-w-full min-w-0 self-start"
         :title="t('alert_library.notIngestedHint', { stream: entry.stream })"
         data-test="alert-library-card-needs-data"
@@ -129,9 +126,9 @@ import { computed } from "vue";
 import OTag from "@/lib/core/Badge/OTag.vue";
 import OIcon from "@/lib/core/Icon/OIcon.vue";
 import type { AlertLibraryEntry } from "@/types/alertLibrary";
-import { raw, useI18nTyped } from "@/types/i18n";
+import { useI18nTyped } from "@/types/i18n";
 
-import { severityBadgeValue, severityLabel } from "./libraryFacets";
+import { queryTypeLabel, severityBadgeValue, severityLabel } from "./libraryFacets";
 
 const props = defineProps<{
   entry: AlertLibraryEntry;
@@ -146,5 +143,5 @@ const { t } = useI18nTyped();
 const severityValue = computed(() => severityBadgeValue(props.entry.severity));
 const severityText = computed(() => severityLabel(t, props.entry.severity));
 // A query language, not prose — one correct form worldwide.
-const queryTypeLabel = computed(() => raw(String(props.entry.query_type ?? "").toUpperCase()));
+const queryTypeLabelText = computed(() => queryTypeLabel(props.entry.query_type));
 </script>
diff --git a/web/src/components/alertLibrary/LibraryDrawer.vue b/web/src/components/alertLibrary/LibraryDrawer.vue
index c5b011d0ebb..b9fc2e6d18e 100644
--- a/web/src/components/alertLibrary/LibraryDrawer.vue
+++ b/web/src/components/alertLibrary/LibraryDrawer.vue
@@ -39,20 +39,17 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       <div class="flex flex-wrap items-center gap-1.5">
         <OTag
           type="severity"
-          size="xs"
           :value="severityValue"
           :label="severityText"
           data-test="alert-library-drawer-severity"
         />
         <OTag
           variant="default-soft"
-          size="xs"
-          :label="queryTypeLabel"
+          :label="queryTypeLabelText"
           data-test="alert-library-drawer-query-type"
         />
         <OTag
           :variant="availability.variant"
-          size="xs"
           :icon="availability.icon"
           :label="availability.label"
           data-test="alert-library-drawer-availability"
@@ -60,7 +57,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       </div>
 
       <section>
-        <h3 class="text-text-secondary text-2xs pb-1 font-semibold uppercase">
+        <h3 class="text-text-secondary pb-1 text-xs font-semibold">
           {{ t("alert_library.drawer.detects") }}
         </h3>
         <p class="text-text-body text-sm" data-test="alert-library-drawer-description">
@@ -100,9 +97,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
 
       <template v-else-if="file">
         <section>
-          <h3
-            class="text-text-secondary text-2xs flex items-center gap-1.5 pb-2 font-semibold uppercase"
-          >
+          <h3 class="text-text-secondary flex items-center gap-1.5 pb-2 text-xs font-semibold">
             <OIcon name="code" size="xs" />
             <span>{{ t("alert_library.drawer.query") }}</span>
           </h3>
@@ -130,9 +125,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         </section>
 
         <section data-test="alert-library-drawer-preview">
-          <h3
-            class="text-text-secondary text-2xs flex items-center gap-1.5 pb-2 font-semibold uppercase"
-          >
+          <h3 class="text-text-secondary flex items-center gap-1.5 pb-2 text-xs font-semibold">
             <OIcon name="query-stats" size="xs" />
             <span>{{ t("alerts.preview") }}</span>
             <template v-if="evaluation">
@@ -219,7 +212,13 @@ import { formatDistanceToNowStrict } from "date-fns";
 import { raw, useI18nTyped } from "@/types/i18n";
 import { buildPrefillFromLibrary } from "@/utils/alerts/prefill/fromLibrary";
 
-import { categoryLabel, packLabel, severityBadgeValue, severityLabel } from "./libraryFacets";
+import {
+  categoryLabel,
+  packLabel,
+  queryTypeLabel,
+  severityBadgeValue,
+  severityLabel,
+} from "./libraryFacets";
 import { readTunables, applyTunables } from "./libraryTunables";
 
 const props = defineProps<{
@@ -311,7 +310,7 @@ const drawerSubtitle = computed(() => {
 const severityValue = computed(() => severityBadgeValue(props.entry?.severity ?? ""));
 const severityText = computed(() => severityLabel(t, props.entry?.severity ?? ""));
 // A query language, not prose — one correct form worldwide.
-const queryTypeLabel = computed(() => raw(String(props.entry?.query_type ?? "").toUpperCase()));
+const queryTypeLabelText = computed(() => queryTypeLabel(props.entry?.query_type));
 
 const asRecord = (value: unknown): Record<string, unknown> =>
   typeof value === "object" && value !== null && !Array.isArray(value)
diff --git a/web/src/components/alertLibrary/LibraryRail.vue b/web/src/components/alertLibrary/LibraryRail.vue
index bbf1bfbeb8e..3d0eac46bd9 100644
--- a/web/src/components/alertLibrary/LibraryRail.vue
+++ b/web/src/components/alertLibrary/LibraryRail.vue
@@ -45,7 +45,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
          rail that scrolls as one block carries its own controls off-screen. -->
     <div class="flex shrink-0 flex-col gap-3 px-2 pt-3 pb-2">
       <section class="flex flex-col gap-1">
-        <h2 class="text-text-secondary text-2xs font-semibold uppercase">
+        <h2 class="text-text-secondary text-xs font-semibold">
           {{ t("alert_library.severity") }}
         </h2>
         <!-- Chips rather than three more tick rows: it is single-select — one
@@ -63,7 +63,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             v-for="item in severities"
             :key="item.id"
             :value="item.id"
-            size="xs"
+            size="sm"
             :data-test="`alert-library-rail-severity-${item.id}`"
           >
             {{ item.label }}
@@ -72,7 +72,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       </section>
 
       <section class="flex flex-col gap-1">
-        <h2 class="text-text-secondary text-2xs font-semibold uppercase">
+        <h2 class="text-text-secondary text-xs font-semibold">
           {{ t("alert_library.categories") }}
         </h2>
 
@@ -105,10 +105,11 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       </section>
     </div>
 
-    <div class="min-h-0 flex-1 overflow-y-auto px-2 pb-3">
+    <div class="min-h-0 flex-1 overflow-y-auto pb-3">
       <OEmptyState
         v-if="visibleItems.length === 0"
         size="inline"
+        class="px-2"
         illustration="no-results"
         variant="no-results"
         :title="t('alert_library.noCategoryMatch')"
diff --git a/web/src/components/alertLibrary/libraryFacets.ts b/web/src/components/alertLibrary/libraryFacets.ts
index 05598895d09..911232e6d1e 100644
--- a/web/src/components/alertLibrary/libraryFacets.ts
+++ b/web/src/components/alertLibrary/libraryFacets.ts
@@ -77,8 +77,29 @@ export const severityLabel = (t: TranslateFn, severity: string): I18nText =>
  * pack without a frontend release.
  */
 const PACK_LABELS: Record<string, string> = {
+  applications: "Applications",
+  "ci-cd": "CI/CD",
+  "cloud-providers": "Cloud providers",
+  "data-engineering": "Data engineering",
+  databases: "Databases",
+  infrastructure: "Infrastructure",
   k8s: "Kubernetes",
+  "message-brokers": "Message brokers",
+  "network-and-security": "Network and security",
+  observability: "Observability",
   openobserve: "OpenObserve",
+  orchestrators: "Orchestrators",
+  "proxies-and-service-mesh": "Proxies and service mesh",
+  runtimes: "Runtimes",
+  storage: "Storage",
+};
+
+const QUERY_TYPE_LABELS: Record<string, string> = { promql: "PromQL", sql: "SQL" };
+
+// Product names, not shouted acronyms — toUpperCase() rendered "PROMQL".
+export const queryTypeLabel = (queryType: string | null | undefined): I18nText => {
+  const key = String(queryType ?? "").toLowerCase();
+  return raw(QUERY_TYPE_LABELS[key] ?? key.toUpperCase());
 };
 
 export const packLabel = (id: string): I18nText =>
diff --git a/web/src/components/alerts/AddAlert.vue b/web/src/components/alerts/AddAlert.vue
index 29d1a3c1bef..b8f5443b988 100644
--- a/web/src/components/alerts/AddAlert.vue
+++ b/web/src/components/alerts/AddAlert.vue
@@ -229,7 +229,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                   data-test="add-alert-stream-name-select-dropdown"
                   :options="indexOptions"
                   :loading="isFetchingStreams"
-                  class="stream-name-select w-full! min-w-0"
+                  class="stream-name-select w-full! max-w-75 min-w-0"
                   :disabled="beingUpdated || anomalyEditMode || !formData.stream_type"
                   @update:model-value="updateStreamFields($event)"
                 />
diff --git a/web/src/components/alerts/AlertGroupsTable.vue b/web/src/components/alerts/AlertGroupsTable.vue
index 599faeddc49..60a6ddbabf4 100644
--- a/web/src/components/alerts/AlertGroupsTable.vue
+++ b/web/src/components/alerts/AlertGroupsTable.vue
@@ -77,7 +77,7 @@
     <template #cell-group="{ row }">
       <div v-if="row.labels?.length" class="flex flex-wrap items-center gap-2">
         <span v-for="label in row.labels" :key="label.name" class="inline-flex items-center gap-1">
-          <span class="text-2xs text-text-tertiary uppercase">{{ label.name }}</span>
+          <span class="text-2xs text-text-tertiary">{{ label.name }}</span>
           <span class="text-compact text-text-heading font-mono">{{ label.value }}</span>
         </span>
       </div>
diff --git a/web/src/components/alerts/AlertHistoryDrawer.vue b/web/src/components/alerts/AlertHistoryDrawer.vue
index d6dd588bdcf..12b85d6fe3d 100644
--- a/web/src/components/alerts/AlertHistoryDrawer.vue
+++ b/web/src/components/alerts/AlertHistoryDrawer.vue
@@ -359,7 +359,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             <!-- Description (only show if exists) -->
             <div v-if="alertDetails.description" class="mt-3 shrink-0">
               <div
-                class="mb-1 flex items-center gap-1.5 text-xs font-semibold tracking-wider uppercase"
+                class="mb-1 flex items-center gap-1.5 text-xs font-semibold"
                 :class="'text-text-secondary'"
               >
                 <OIcon name="info-outline" size="xs" />
diff --git a/web/src/components/alerts/AlertList.spec.ts b/web/src/components/alerts/AlertList.spec.ts
index e12f4355488..edffd47b48d 100644
--- a/web/src/components/alerts/AlertList.spec.ts
+++ b/web/src/components/alerts/AlertList.spec.ts
@@ -205,14 +205,17 @@ async function mountAlertList() {
           emits: ["update:ok", "update:cancel", "update:modelValue"],
           template: '<div class="confirm-dialog-stub" :data-open="modelValue"></div>',
         },
-        AppTabs: {
-          name: "AppTabs",
-          props: ["tabs", "activeTab"],
-          emits: ["update:active-tab"],
-          template:
-            '<div class="app-tabs-stub">' +
-            '<button v-for="tab in tabs" :key="tab.value" :class="`tab-${tab.value}`" @click="$emit(\'update:active-tab\', tab.value)">{{tab.label}}</button>' +
-            "</div>",
+        // Real reka-ui toggle groups slow every mount, and this file mounts 70 times — enough to push borderline tests past their timeouts.
+        OToggleGroup: {
+          name: "OToggleGroup",
+          props: ["modelValue"],
+          emits: ["update:modelValue"],
+          template: '<div class="toggle-group-stub"><slot /></div>',
+        },
+        OToggleGroupItem: {
+          name: "OToggleGroupItem",
+          props: ["value"],
+          template: '<button type="button"><slot /></button>',
         },
         SelectFolderDropDown: true,
       },
@@ -241,7 +244,7 @@ beforeEach(() => {
   // query.tab) run during mount(), before mountAlertList can blank the query —
   // so a leftover action/tab from an earlier test (e.g. "when action=add"
   // pushes {action:"add"}) would asynchronously re-open the add/import dialog
-  // and hide the list (and its AppTabs), breaking every later assertion.
+  // and hide the list (and its type filter), breaking every later assertion.
   router.currentRoute.value.query = {};
   router.currentRoute.value.params = {};
   router.currentRoute.value.name = "alertList";
@@ -1385,8 +1388,10 @@ describe("AlertList - ODialog/ODrawer migration", () => {
       expect(wrapper.vm.tabs).toEqual(
         expect.arrayContaining([expect.objectContaining({ value: "composite" })]),
       );
-      const tabs = wrapper.findComponent({ name: "AppTabs" });
-      await tabs.vm.$emit("update:active-tab", "composite");
+      const typeFilter = wrapper
+        .findAllComponents({ name: "OToggleGroup" })
+        .find((group: any) => group.attributes("data-test") === "alert-list-tabs");
+      await typeFilter.vm.$emit("update:modelValue", "composite");
       await flushPromises();
 
       expect(AlertService.listByFolderId).toHaveBeenLastCalledWith(
diff --git a/web/src/components/alerts/AlertList.vue b/web/src/components/alerts/AlertList.vue
index 79b5adb6436..28c40e739a9 100644
--- a/web/src/components/alerts/AlertList.vue
+++ b/web/src/components/alerts/AlertList.vue
@@ -23,23 +23,13 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       v-if="!showAddAlertDialog && !showImportAlertDialog"
       :title="t('alerts.header')"
       icon="shield-alert-outline"
+      :subtitle="t('alerts.subtitle')"
+      tabs-below
     >
       <!-- The header names the GROUP the four tabs form — "Alerts" — not the
-           page. The same string and the same icon on all four siblings; the
-           active tab is what says which page you are on. It deliberately reads
-           the same as the first tab, which is the price of naming the group
-           after its main page (PipelineSectionTabs makes the same trade).
-
-           That is what keeps the tab strip still. The title block is shrink-0
-           and sizes to its content, so a per-page title moved the strip
-           horizontally on every navigation, and peer tabs that jump under the
-           cursor are worse than no tabs. A constant title fixes it by
-           construction; the previous fix reserved a fixed 15rem box, which
-           bought the same stillness with 196px of dead space.
-
-           For the same reason these four pages carry NO subtitle: a subtitle is
-           usually wider than the title, so it would size the block and move the
-           strip again. -->
+           page; the active tab says which sibling you are on. Same title,
+           subtitle and icon on all four (PipelineSectionTabs makes the same
+           trade). -->
       <template #header-tabs>
         <AlertSectionTabs />
       </template>
@@ -70,6 +60,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
           data-test="alert-list-add-alert-btn"
           variant="primary"
           size="sm"
+          icon-left="add"
           :disabled="!destinations.length || !templates.length"
           :title="!destinations.length ? t('alerts.noDestinations') : ''"
           @click="
@@ -98,14 +89,6 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         <!-- Right: Table -->
         <div class="h-full min-w-0 flex-1">
           <div class="bg-card-glass-bg flex h-full flex-col">
-            <div class="border-border-default shrink-0 border-b px-3 py-2">
-              <AppTabs
-                :tabs="alertTabs"
-                :active-tab="activeTab"
-                size="sm"
-                @update:active-tab="onAlertTabChange"
-              />
-            </div>
             <!-- Alert List Table (shows all alert types including anomaly detection rows) -->
             <OTable
               class="min-h-0 flex-1"
@@ -153,6 +136,22 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
               <!-- Toolbar: alert-type filter + search (inline folder scope) + refresh. -->
               <template #toolbar>
                 <div class="flex w-full items-center gap-2">
+                  <OToggleGroup
+                    :model-value="activeTab"
+                    data-test="alert-list-tabs"
+                    @update:model-value="(v) => onAlertTabChange(v as string)"
+                  >
+                    <OToggleGroupItem
+                      v-for="tab in alertTabs"
+                      :key="tab.value"
+                      :value="tab.value"
+                      size="sm"
+                      :icon-left="tab.icon"
+                      :data-test="`alert-list-tab-${tab.value}`"
+                    >
+                      {{ tab.label }}
+                    </OToggleGroupItem>
+                  </OToggleGroup>
                   <div class="min-w-0 flex-1">
                     <OInput
                       v-model="dynamicQueryModel"
@@ -908,7 +907,6 @@ import OTimeCell from "@/lib/core/Table/cells/OTimeCell.vue";
 import OUserCell from "@/lib/core/Table/cells/OUserCell.vue";
 import OTag from "@/lib/core/Badge/OTag.vue";
 import OStatStrip from "@/lib/data/StatStrip/OStatStrip.vue";
-import AppTabs from "@/components/common/AppTabs.vue";
 import IacRegistryLinks from "@/components/common/IacRegistryLinks.vue";
 import AlertSectionTabs from "@/components/alerts/AlertSectionTabs.vue";
 import CompositeReferencesDrawer from "@/components/alerts/composite/CompositeReferencesDrawer.vue";
@@ -952,7 +950,6 @@ export default defineComponent({
     OUserCell,
     OTag,
     OStatStrip,
-    AppTabs,
     CompositeReferencesDrawer,
     ExportResourceDialog,
     IacRegistryLinks,
diff --git a/web/src/components/alerts/AlertSettingsHelpDrawer.vue b/web/src/components/alerts/AlertSettingsHelpDrawer.vue
index 896cd44f3cf..bb1470a3e05 100644
--- a/web/src/components/alerts/AlertSettingsHelpDrawer.vue
+++ b/web/src/components/alerts/AlertSettingsHelpDrawer.vue
@@ -459,10 +459,9 @@ defineExpose({ applyTemplate, previewTemplate });
             <div
               class="rounded-surface border-border-default border-l-warning-500 flex flex-col gap-1.5 border border-l-2 p-3"
             >
-              <span
-                class="text-text-secondary text-2xs font-semibold tracking-[0.03em] uppercase"
-                >{{ t("alerts.alertSettings.helpWhyWithoutLabel") }}</span
-              >
+              <span class="text-text-secondary text-2xs font-semibold">{{
+                t("alerts.alertSettings.helpWhyWithoutLabel")
+              }}</span>
               <p class="text-text-secondary text-compact m-0 leading-[1.45]">
                 {{ t("alerts.alertSettings.helpWhyWithoutDesc") }}
               </p>
@@ -474,10 +473,9 @@ defineExpose({ applyTemplate, previewTemplate });
             <div
               class="rounded-surface border-border-default border-l-primary-500 flex flex-col gap-1.5 border border-l-2 p-3"
             >
-              <span
-                class="text-text-secondary text-2xs font-semibold tracking-[0.03em] uppercase"
-                >{{ t("alerts.alertSettings.helpWhyWithLabel") }}</span
-              >
+              <span class="text-text-secondary text-2xs font-semibold">{{
+                t("alerts.alertSettings.helpWhyWithLabel")
+              }}</span>
               <p class="text-text-secondary text-compact m-0 leading-[1.45]">
                 {{ t("alerts.alertSettings.helpWhyWithDesc") }}
               </p>
@@ -503,10 +501,9 @@ defineExpose({ applyTemplate, previewTemplate });
             data-test="help-preview-box"
             class="rounded-surface border-border-default bg-surface-subtle text-text-body m-0 border p-3 font-mono text-xs leading-[1.6] break-words whitespace-pre-wrap"
             >{{ SAMPLE.variablesCode }}</pre>
-          <span
-            class="text-text-secondary text-2xs mx-0 mt-2.5 mb-1 block font-semibold tracking-[0.03em] uppercase"
-            >{{ t("alerts.alertSettings.helpExampleResultLabel") }}</span
-          >
+          <span class="text-text-secondary text-2xs mx-0 mt-2.5 mb-1 block font-semibold">{{
+            t("alerts.alertSettings.helpExampleResultLabel")
+          }}</span>
           <pre
             data-test="help-preview-box"
             class="rounded-surface border-border-default bg-surface-subtle text-text-body border-l-primary-500 m-0 border border-l-2 p-3 font-mono text-xs leading-[1.6] break-words whitespace-pre-wrap"
@@ -610,24 +607,23 @@ defineExpose({ applyTemplate, previewTemplate });
             {{ t("alerts.alertSettings.helpRowTemplateExampleCaption") }}
           </p>
           <div class="flex flex-col gap-1.5">
-            <span class="text-text-secondary text-2xs font-semibold tracking-[0.03em] uppercase">{{
+            <span class="text-text-secondary text-2xs font-semibold">{{
               t("alerts.alertSettings.helpRowTemplateExampleRowLabel")
             }}</span>
             <pre
               data-test="help-preview-box"
               class="rounded-surface border-border-subtle bg-surface-panel text-text-body m-0 border p-3 font-mono text-xs leading-[1.6] break-words whitespace-pre-wrap"
               >{{ SAMPLE.rowTemplateRow }}</pre>
-            <span class="text-text-secondary text-2xs font-semibold tracking-[0.03em] uppercase">{{
+            <span class="text-text-secondary text-2xs font-semibold">{{
               t("alerts.alertSettings.helpRowTemplateExampleMainLabel")
             }}</span>
             <pre
               data-test="help-preview-box"
               class="rounded-surface border-border-subtle bg-surface-panel text-text-body m-0 border p-3 font-mono text-xs leading-[1.6] break-words whitespace-pre-wrap"
               >{{ SAMPLE.rowTemplateMain }}</pre>
-            <span
-              class="text-text-secondary text-2xs mx-0 mt-2.5 mb-1 block font-semibold tracking-[0.03em] uppercase"
-              >{{ t("alerts.alertSettings.helpExampleResultLabel") }}</span
-            >
+            <span class="text-text-secondary text-2xs mx-0 mt-2.5 mb-1 block font-semibold">{{
+              t("alerts.alertSettings.helpExampleResultLabel")
+            }}</span>
             <pre
               data-test="help-preview-box"
               class="rounded-surface border-border-default bg-surface-subtle text-text-body border-l-primary-500 m-0 border border-l-2 p-3 font-mono text-xs leading-[1.6] break-words whitespace-pre-wrap"
diff --git a/web/src/components/alerts/AlertsDestinationList.vue b/web/src/components/alerts/AlertsDestinationList.vue
index 3fa530404ed..a71055b6697 100644
--- a/web/src/components/alerts/AlertsDestinationList.vue
+++ b/web/src/components/alerts/AlertsDestinationList.vue
@@ -22,37 +22,14 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       :title="t('alerts.header')"
       title-data-test="alert-destinations-list-title"
       icon="shield-alert-outline"
+      :subtitle="t('alerts.subtitle')"
+      tabs-below
     >
-      <!-- Section-level title, identical on all four alerting pages, so the
-           peer tabs anchor at the same x — see AlertList.vue for the rationale.
-           Subtitle-free for the same reason. -->
       <template #header-tabs>
         <AlertSectionTabs />
       </template>
 
       <template #actions>
-        <OToggleGroup
-          :model-value="activeTab"
-          @update:model-value="
-            (v) => {
-              activeTab = v as 'all' | 'prebuilt' | 'custom';
-            }
-          "
-          data-test="destination-list-tabs"
-        >
-          <OToggleGroupItem value="all" size="sm" data-test="destination-tab-all">
-            <template #icon-left><OIcon name="format-list-bulleted" size="sm" /></template>
-            {{ t("alert_destinations.filterAll") }}
-          </OToggleGroupItem>
-          <OToggleGroupItem value="prebuilt" size="sm" data-test="destination-tab-prebuilt">
-            <template #icon-left><OIcon name="auto-awesome" size="sm" /></template>
-            {{ t("alert_destinations.filterPrebuilt") }}
-          </OToggleGroupItem>
-          <OToggleGroupItem value="custom" size="sm" data-test="destination-tab-custom">
-            <template #icon-left><OIcon name="settings" size="sm" /></template>
-            {{ t("alert_destinations.filterCustom") }}
-          </OToggleGroupItem>
-        </OToggleGroup>
         <OButton
           variant="outline"
           size="sm"
@@ -64,6 +41,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
           data-test="alert-destination-list-add-alert-btn"
           variant="primary"
           size="sm"
+          icon-left="add"
           :disabled="!templates.length"
           @click="editDestination(null)"
           >{{ t(`alert_destinations.add`) }}</OButton
@@ -92,12 +70,36 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
           @update:selected-ids="handleSelectedIdsUpdate"
         >
           <template #toolbar>
-            <OSearchInput
-              v-model="filterQuery"
-              data-test="destination-list-search-input"
-              class="flex-1"
-              :placeholder="t('alert_destinations.search')"
-            />
+            <div class="flex w-full items-center gap-2">
+              <OToggleGroup
+                :model-value="activeTab"
+                @update:model-value="
+                  (v) => {
+                    activeTab = v as 'all' | 'prebuilt' | 'custom';
+                  }
+                "
+                data-test="destination-list-tabs"
+              >
+                <OToggleGroupItem value="all" size="sm" data-test="destination-tab-all">
+                  <template #icon-left><OIcon name="format-list-bulleted" size="sm" /></template>
+                  {{ t("alert_destinations.filterAll") }}
+                </OToggleGroupItem>
+                <OToggleGroupItem value="prebuilt" size="sm" data-test="destination-tab-prebuilt">
+                  <template #icon-left><OIcon name="auto-awesome" size="sm" /></template>
+                  {{ t("alert_destinations.filterPrebuilt") }}
+                </OToggleGroupItem>
+                <OToggleGroupItem value="custom" size="sm" data-test="destination-tab-custom">
+                  <template #icon-left><OIcon name="settings" size="sm" /></template>
+                  {{ t("alert_destinations.filterCustom") }}
+                </OToggleGroupItem>
+              </OToggleGroup>
+              <OSearchInput
+                v-model="filterQuery"
+                data-test="destination-list-search-input"
+                class="flex-1"
+                :placeholder="t('alert_destinations.search')"
+              />
+            </div>
           </template>
           <template #toolbar-trailing>
             <OButton
diff --git a/web/src/components/alerts/CreateAlertFromSourceDialog.vue b/web/src/components/alerts/CreateAlertFromSourceDialog.vue
index 947db3d72b1..e5d7e5b2c4d 100644
--- a/web/src/components/alerts/CreateAlertFromSourceDialog.vue
+++ b/web/src/components/alerts/CreateAlertFromSourceDialog.vue
@@ -112,12 +112,13 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         <OToggleGroup
           v-model="thresholdShape"
           type="single"
+          class="self-start"
           data-test="create-alert-threshold-shape"
         >
-          <OToggleGroupItem value="matching-rows" size="xs" data-test="create-alert-threshold-rows">
+          <OToggleGroupItem value="matching-rows" size="sm" data-test="create-alert-threshold-rows">
             {{ t("alerts.prefill.dialog.thresholdMatchingRows") }}
           </OToggleGroupItem>
-          <OToggleGroupItem value="count" size="xs" data-test="create-alert-threshold-count">
+          <OToggleGroupItem value="count" size="sm" data-test="create-alert-threshold-count">
             {{ t("alerts.prefill.dialog.thresholdCount") }}
           </OToggleGroupItem>
         </OToggleGroup>
diff --git a/web/src/components/alerts/DependencyImpactDialog.vue b/web/src/components/alerts/DependencyImpactDialog.vue
index 1e9b33f1351..428da39f111 100644
--- a/web/src/components/alerts/DependencyImpactDialog.vue
+++ b/web/src/components/alerts/DependencyImpactDialog.vue
@@ -84,7 +84,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             >
               <div class="mb-1.5 flex items-center gap-1.5 px-1">
                 <OIcon name="location-on" size="sm" class="text-info" />
-                <span class="text-text-secondary text-2xs font-semibold tracking-wide uppercase">
+                <span class="text-text-secondary text-2xs font-semibold">
                   {{ t("alert_dependencies.sectionDestinations") }}
                 </span>
                 <OTag type="countChip" value="neutral">{{ chain.destinations.length }}</OTag>
@@ -139,7 +139,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
           >
             <div class="mb-1.5 flex items-center gap-1.5 px-1">
               <OIcon name="shield-alert-outline" size="sm" class="text-status-positive" />
-              <span class="text-text-secondary text-2xs font-semibold tracking-wide uppercase">
+              <span class="text-text-secondary text-2xs font-semibold">
                 {{ t("alert_dependencies.sectionAlerts") }}
               </span>
               <OTag type="countChip" value="neutral">{{ chain.alerts.length }}</OTag>
diff --git a/web/src/components/alerts/DependencyUsageCell.vue b/web/src/components/alerts/DependencyUsageCell.vue
index f4f2f43968a..1b5bdb30182 100644
--- a/web/src/components/alerts/DependencyUsageCell.vue
+++ b/web/src/components/alerts/DependencyUsageCell.vue
@@ -51,9 +51,9 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         value="neutral"
         :data-test="`used-by-${key}-${b.kind}`"
       >
+        <OTooltip side="top" :content="b.label" />
         <OIcon :name="depKindIcon(b.kind)" size="xs" class="mr-0.5" :class="kindColor(b.kind)" />
         {{ b.count }}
-        <OTooltip side="top" :content="b.label" />
       </OTag>
     </template>
   </OButton>
diff --git a/web/src/components/alerts/DestinationTestResult.vue b/web/src/components/alerts/DestinationTestResult.vue
index 14c928f0d31..3e94c7581e1 100644
--- a/web/src/components/alerts/DestinationTestResult.vue
+++ b/web/src/components/alerts/DestinationTestResult.vue
@@ -102,9 +102,7 @@ limitations under the License. -->
 
           <div data-test="test-failure-details" class="pt-2">
             <div v-if="result.error" data-test="test-error-message" class="error-item mb-3">
-              <div
-                class="text-3xs text-text-secondary mb-1 font-semibold tracking-[0.03125rem] uppercase"
-              >
+              <div class="text-3xs text-text-secondary mb-1 font-semibold">
                 {{ t("alerts.error") }}
               </div>
               <div class="text-2xs text-text-body leading-[1.5] break-words">
@@ -113,9 +111,7 @@ limitations under the License. -->
             </div>
 
             <div v-if="result.statusCode" data-test="test-http-status" class="error-item mb-3">
-              <div
-                class="text-3xs text-text-secondary mb-1 font-semibold tracking-[0.03125rem] uppercase"
-              >
+              <div class="text-3xs text-text-secondary mb-1 font-semibold">
                 {{ t("alerts.httpStatus") }}
               </div>
               <div class="text-2xs text-text-body leading-[1.5] break-words">
@@ -124,9 +120,7 @@ limitations under the License. -->
             </div>
 
             <div v-if="result.responseBody" data-test="test-response-body" class="error-item mb-3">
-              <div
-                class="text-3xs text-text-secondary mb-1 font-semibold tracking-[0.03125rem] uppercase"
-              >
+              <div class="text-3xs text-text-secondary mb-1 font-semibold">
                 {{ t("alerts.responseBody") }}
               </div>
               <pre
diff --git a/web/src/components/alerts/ImportAlert.vue b/web/src/components/alerts/ImportAlert.vue
index 2f80e42e85e..a86c9297f9b 100644
--- a/web/src/components/alerts/ImportAlert.vue
+++ b/web/src/components/alerts/ImportAlert.vue
@@ -263,10 +263,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
           </div>
 
           <div class="mb-2.5 p-2.5" v-if="alertCreators.length > 0">
-            <div
-              class="text-primary mb-2.5 text-base uppercase"
-              data-test="alert-import-creation-title"
-            >
+            <div class="text-primary mb-2.5 text-base" data-test="alert-import-creation-title">
               {{ t("alerts.alertCreationTitle") }}
             </div>
             <div
diff --git a/web/src/components/alerts/ImportDestination.vue b/web/src/components/alerts/ImportDestination.vue
index 1030f238105..258cefdf809 100644
--- a/web/src/components/alerts/ImportDestination.vue
+++ b/web/src/components/alerts/ImportDestination.vue
@@ -263,7 +263,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
           <!-- Destination Creation Success Messages -->
           <div class="error-section mb-2.5 p-2.5" v-if="destinationCreators.length > 0">
             <div
-              class="text-primary mb-2.5 text-base uppercase"
+              class="text-primary mb-2.5 text-base"
               data-test="destination-import-creation-title"
             >
               {{ t("alert_destinations.import.destinationCreation") }}
diff --git a/web/src/components/alerts/ImportTemplate.vue b/web/src/components/alerts/ImportTemplate.vue
index a87758950fc..44d9e952f00 100644
--- a/web/src/components/alerts/ImportTemplate.vue
+++ b/web/src/components/alerts/ImportTemplate.vue
@@ -151,10 +151,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
           </div>
 
           <div class="error-section mb-2.5 p-2.5" v-if="tempalteCreators.length > 0">
-            <div
-              class="text-primary mb-2.5 text-base uppercase"
-              data-test="template-import-creation-title"
-            >
+            <div class="text-primary mb-2.5 text-base" data-test="template-import-creation-title">
               {{ t("alert_templates.templateCreationTitle") }}
             </div>
             <div
diff --git a/web/src/components/alerts/IncidentDetailDrawer.vue b/web/src/components/alerts/IncidentDetailDrawer.vue
index 9b08957b137..9eefd6d1112 100644
--- a/web/src/components/alerts/IncidentDetailDrawer.vue
+++ b/web/src/components/alerts/IncidentDetailDrawer.vue
@@ -24,6 +24,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       }"
       :subtitle="t('alerts.incidents.incident')"
       title-overflow="visible"
+      tabs-below
       bleed
     >
       <!-- Incident name — click the title to rename, Enter or blur saves,
@@ -118,49 +119,48 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         </template>
       </template>
 
+      <template #header-tabs>
+        <OTabs v-model="activeTab" align="left" class="flex-1" mobile-arrows :breakpoint="0">
+          <OTab
+            name="overview"
+            :label="t('alerts.insights.tabs.overview')"
+            data-test="incident-overview-tab"
+          />
+          <OTab
+            name="activity"
+            :label="t('alerts.incidents.activityTab')"
+            data-test="incident-activity-tab"
+          />
+          <OTab
+            name="incidentAnalysis"
+            :label="t('alerts.incidents.incidentAnalysis')"
+            data-test="incident-analysis-tab"
+          />
+          <OTab
+            name="serviceGraph"
+            :label="t('alerts.incidents.alertGraph')"
+            data-test="incident-alert-graph-tab"
+          />
+          <OTab name="alertTriggers" data-test="incident-alert-triggers-tab">
+            <template #default>
+              <div class="flex items-center gap-1.5">
+                <span>{{ t("alerts.incidents.alertTriggers") }}</span>
+                <OTag type="countChip" value="neutral">{{ triggers.length }}</OTag>
+              </div>
+            </template>
+          </OTab>
+
+          <OTab name="logs" :label="t('common.logs')" data-test="incident-logs-tab" />
+          <OTab name="metrics" :label="t('search.metrics')" data-test="incident-metrics-tab" />
+          <OTab name="traces" :label="t('menu.traces')" data-test="incident-traces-tab" />
+        </OTabs>
+      </template>
+
       <!-- Content -->
       <div
         v-if="!loading && incidentDetails"
         class="bg-card-glass-bg flex min-h-0 flex-1 flex-col overflow-hidden"
       >
-        <div class="px-page-edge border-border-default flex-shrink-0 border-b">
-          <OTabs v-model="activeTab" align="left" class="flex-1" mobile-arrows :breakpoint="0">
-            <OTab
-              name="overview"
-              :label="t('alerts.insights.tabs.overview')"
-              data-test="incident-overview-tab"
-            />
-            <OTab
-              name="activity"
-              :label="t('alerts.incidents.activityTab')"
-              data-test="incident-activity-tab"
-            />
-            <OTab
-              name="incidentAnalysis"
-              :label="t('alerts.incidents.incidentAnalysis')"
-              data-test="incident-analysis-tab"
-            />
-            <OTab
-              name="serviceGraph"
-              :label="t('alerts.incidents.alertGraph')"
-              data-test="incident-alert-graph-tab"
-            />
-            <OTab name="alertTriggers" data-test="incident-alert-triggers-tab">
-              <template #default>
-                <div class="flex items-center gap-1.5">
-                  <span>{{ t("alerts.incidents.alertTriggers") }}</span>
-                  <OTag type="countChip" value="neutral">{{ triggers.length }}</OTag>
-                </div>
-              </template>
-            </OTab>
-
-            <!-- Telemetry tabs always inline -->
-            <OTab name="logs" :label="t('common.logs')" data-test="incident-logs-tab" />
-            <OTab name="metrics" :label="t('search.metrics')" data-test="incident-metrics-tab" />
-            <OTab name="traces" :label="t('menu.traces')" data-test="incident-traces-tab" />
-          </OTabs>
-        </div>
-
         <!-- Tab Content Container -->
         <div class="flex flex-1 overflow-hidden">
           <!-- Left Column: Incident Details (only show on Incident Analysis tab, HIDDEN for Overview) -->
@@ -877,10 +877,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                         <div class="space-y-2">
                           <!-- Alert Name -->
                           <div class="flex flex-col gap-0.5">
-                            <span
-                              :class="'text-text-secondary'"
-                              class="text-3xs tracking-wide uppercase"
-                            >
+                            <span :class="'text-text-secondary'" class="text-3xs">
                               {{ t("alerts.incidents.alertName") }}
                             </span>
                             <span :class="'text-text-body'" class="text-sm font-medium">
@@ -893,10 +890,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                             v-if="alerts[selectedAlertIndex]?.alert_type === 'composite'"
                             class="flex flex-col gap-0.5"
                           >
-                            <span
-                              :class="'text-text-secondary'"
-                              class="text-3xs tracking-wide uppercase"
-                            >
+                            <span :class="'text-text-secondary'" class="text-3xs">
                               {{ t("alerts.alertType") }}
                             </span>
                             <OTag type="alertType" :value="'composite'" class="w-fit" />
@@ -905,10 +899,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                           <!-- Stream Type & Name -->
                           <div v-if="!isSelectedComposite" class="grid grid-cols-2 gap-2">
                             <div class="flex flex-col gap-0.5">
-                              <span
-                                :class="'text-text-secondary'"
-                                class="text-3xs tracking-wide uppercase"
-                              >
+                              <span :class="'text-text-secondary'" class="text-3xs">
                                 {{ t("alerts.streamType") }}
                               </span>
                               <OTag
@@ -918,10 +909,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                               />
                             </div>
                             <div class="flex flex-col gap-0.5">
-                              <span
-                                :class="'text-text-secondary'"
-                                class="text-3xs tracking-wide uppercase"
-                              >
+                              <span :class="'text-text-secondary'" class="text-3xs">
                                 {{ t("alerts.stream_name") }}
                               </span>
                               <span :class="'text-text-body'" class="truncate text-sm font-medium">
@@ -933,10 +921,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                           <!-- Threshold & Period -->
                           <div v-if="!isSelectedComposite" class="grid grid-cols-2 gap-2">
                             <div class="flex flex-col gap-0.5">
-                              <span
-                                :class="'text-text-secondary'"
-                                class="text-3xs tracking-wide uppercase"
-                              >
+                              <span :class="'text-text-secondary'" class="text-3xs">
                                 {{ t("alerts.messages.thresholdMarkLine") }}
                               </span>
                               <span :class="'text-text-body'" class="text-sm font-medium">
@@ -948,10 +933,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                               </span>
                             </div>
                             <div class="flex flex-col gap-0.5">
-                              <span
-                                :class="'text-text-secondary'"
-                                class="text-3xs tracking-wide uppercase"
-                              >
+                              <span :class="'text-text-secondary'" class="text-3xs">
                                 {{ t("alerts.incidents.period") }}
                               </span>
                               <span :class="'text-text-body'" class="text-sm font-medium">
@@ -967,10 +949,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                           <!-- Frequency & Silence -->
                           <div v-if="!isSelectedComposite" class="grid grid-cols-2 gap-2">
                             <div class="flex flex-col gap-0.5">
-                              <span
-                                :class="'text-text-secondary'"
-                                class="text-3xs tracking-wide uppercase"
-                              >
+                              <span :class="'text-text-secondary'" class="text-3xs">
                                 {{ t("alerts.incidents.frequency") }}
                               </span>
                               <span :class="'text-text-body'" class="text-sm font-medium">
@@ -985,10 +964,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                               </span>
                             </div>
                             <div class="flex flex-col gap-0.5">
-                              <span
-                                :class="'text-text-secondary'"
-                                class="text-3xs tracking-wide uppercase"
-                              >
+                              <span :class="'text-text-secondary'" class="text-3xs">
                                 {{ t("alerts.incidents.silence") }}
                               </span>
                               <span :class="'text-text-body'" class="text-sm font-medium">
@@ -1016,10 +992,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                               'border-border-default',
                             ]"
                           >
-                            <span
-                              :class="'text-text-secondary'"
-                              class="text-2xs font-semibold tracking-wide uppercase"
-                            >
+                            <span :class="'text-text-secondary'" class="text-2xs font-semibold">
                               {{
                                 alerts[selectedAlertIndex]?.query_condition?.type === "sql"
                                   ? t("alerts.alertDetails.sqlQuery")
diff --git a/web/src/components/alerts/IncidentRCAAnalysis.vue b/web/src/components/alerts/IncidentRCAAnalysis.vue
index 3a8a2b12bb3..ba9654fabe7 100644
--- a/web/src/components/alerts/IncidentRCAAnalysis.vue
+++ b/web/src/components/alerts/IncidentRCAAnalysis.vue
@@ -550,7 +550,6 @@ export default defineComponent({
       padding: 0.5rem 0.75rem;
       color: var(--rca-text-primary);
       font-weight: 700;
-      text-transform: uppercase;
       font-size: var(--text-3xs);
       letter-spacing: 0.08em;
       text-align: left;
diff --git a/web/src/components/alerts/TemplateList.vue b/web/src/components/alerts/TemplateList.vue
index f1ced4eaeec..5c59731eda1 100644
--- a/web/src/components/alerts/TemplateList.vue
+++ b/web/src/components/alerts/TemplateList.vue
@@ -21,38 +21,14 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       v-if="!showImportTemplate && !showTemplateEditor"
       :title="t('alerts.header')"
       icon="shield-alert-outline"
+      :subtitle="t('alerts.subtitle')"
+      tabs-below
     >
-      <!-- Section-level title, identical on all four alerting pages, so the
-           peer tabs anchor at the same x — see AlertList.vue for the rationale.
-           Subtitle-free for the same reason. -->
       <template #header-tabs>
         <AlertSectionTabs />
       </template>
 
       <template #actions>
-        <OToggleGroup
-          :model-value="activeTab"
-          @update:model-value="
-            (v: any) => {
-              activeTab = v;
-            }
-          "
-          data-test="template-list-tabs"
-          class="mr-2"
-        >
-          <OToggleGroupItem value="all" size="sm" data-test="template-tab-all">
-            <template #icon-left><OIcon name="format-list-bulleted" size="sm" /></template>
-            {{ t("alert_templates.filterAll") }}
-          </OToggleGroupItem>
-          <OToggleGroupItem value="prebuilt" size="sm" data-test="template-tab-prebuilt">
-            <template #icon-left><OIcon name="auto-awesome" size="sm" /></template>
-            {{ t("alert_templates.filterPrebuilt") }}
-          </OToggleGroupItem>
-          <OToggleGroupItem value="custom" size="sm" data-test="template-tab-custom">
-            <template #icon-left><OIcon name="settings" size="sm" /></template>
-            {{ t("alert_templates.filterCustom") }}
-          </OToggleGroupItem>
-        </OToggleGroup>
         <OButton
           variant="outline"
           size="sm-action"
@@ -64,6 +40,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
           data-test="template-list-add-btn"
           variant="primary"
           size="sm"
+          icon-left="add"
           @click="editTemplate(null)"
           >{{ t(`alert_templates.add`) }}</OButton
         >
@@ -91,12 +68,36 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
           @update:selected-ids="handleSelectedIdsUpdate"
         >
           <template #toolbar>
-            <OSearchInput
-              v-model="filterQuery"
-              class="flex-1"
-              :placeholder="t('template.search')"
-              data-test="template-list-search-input"
-            />
+            <div class="flex w-full items-center gap-2">
+              <OToggleGroup
+                :model-value="activeTab"
+                @update:model-value="
+                  (v: any) => {
+                    activeTab = v;
+                  }
+                "
+                data-test="template-list-tabs"
+              >
+                <OToggleGroupItem value="all" size="sm" data-test="template-tab-all">
+                  <template #icon-left><OIcon name="format-list-bulleted" size="sm" /></template>
+                  {{ t("alert_templates.filterAll") }}
+                </OToggleGroupItem>
+                <OToggleGroupItem value="prebuilt" size="sm" data-test="template-tab-prebuilt">
+                  <template #icon-left><OIcon name="auto-awesome" size="sm" /></template>
+                  {{ t("alert_templates.filterPrebuilt") }}
+                </OToggleGroupItem>
+                <OToggleGroupItem value="custom" size="sm" data-test="template-tab-custom">
+                  <template #icon-left><OIcon name="settings" size="sm" /></template>
+                  {{ t("alert_templates.filterCustom") }}
+                </OToggleGroupItem>
+              </OToggleGroup>
+              <OSearchInput
+                v-model="filterQuery"
+                class="flex-1"
+                :placeholder="t('template.search')"
+                data-test="template-list-search-input"
+              />
+            </div>
           </template>
           <template #toolbar-trailing>
             <OButton
diff --git a/web/src/components/alerts/template-content/TemplatePreviewPanel.vue b/web/src/components/alerts/template-content/TemplatePreviewPanel.vue
index d1beeab980b..33f77bdaf49 100644
--- a/web/src/components/alerts/template-content/TemplatePreviewPanel.vue
+++ b/web/src/components/alerts/template-content/TemplatePreviewPanel.vue
@@ -148,7 +148,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                naming the channel here, switching the Channel select looks like
                it did nothing, because the card genuinely does not change. -->
           <div
-            class="text-text-secondary text-2xs flex items-center gap-1 font-medium uppercase"
+            class="text-text-secondary text-2xs flex items-center gap-1 font-medium"
             data-test="template-preview-panel-channel-badge"
           >
             {{ activeChannelLabel }}
diff --git a/web/src/components/dbm/DbmCheckList.vue b/web/src/components/dbm/DbmCheckList.vue
index 5891f012a9d..8dbc18da8b3 100644
--- a/web/src/components/dbm/DbmCheckList.vue
+++ b/web/src/components/dbm/DbmCheckList.vue
@@ -38,27 +38,27 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
     :data-test="dataTest"
   >
     <p
-      class="border-border-subtle bg-surface-panel text-text-label text-2xs border-b px-3 py-1.5 font-semibold tracking-wide uppercase"
+      class="border-border-subtle bg-surface-panel text-text-secondary text-2xs border-b px-3 py-1.5 font-semibold"
     >
       {{ title }}
     </p>
     <div
       v-for="check in checks"
       :key="check.id"
-      class="border-border-subtle flex items-start gap-2 px-3 py-1.5 not-last:border-b"
+      class="border-border-subtle flex flex-col gap-0.5 px-3 py-1.5 not-last:border-b"
       :data-test="`${rowTestPrefix}${check.id}`"
     >
-      <span
-        class="text-3xs mt-px grid size-3.5 shrink-0 place-items-center rounded-full font-bold text-white"
-        :class="STATUS_TONES[check.status]"
-      >
-        {{ STATUS_GLYPHS[check.status] }}
-      </span>
-      <span class="min-w-0 flex-1">
-        <span class="text-text-heading block text-xs font-semibold">{{ check.title }}</span>
-        <span class="text-text-secondary text-2xs mt-px block leading-relaxed">
-          {{ check.detail }}
+      <div class="flex items-center gap-2">
+        <span
+          class="text-3xs grid size-3.5 shrink-0 place-items-center rounded-full leading-none font-bold text-white"
+          :class="STATUS_TONES[check.status]"
+        >
+          {{ STATUS_GLYPHS[check.status] }}
         </span>
+        <span class="text-text-heading min-w-0 text-xs font-semibold">{{ check.title }}</span>
+      </div>
+      <span v-if="check.detail" class="text-text-secondary text-2xs ps-5.5 leading-relaxed">
+        {{ check.detail }}
       </span>
     </div>
   </div>
diff --git a/web/src/components/dbm/DbmCoverageLine.vue b/web/src/components/dbm/DbmCoverageLine.vue
index 4876bd14cc1..8ace276d775 100644
--- a/web/src/components/dbm/DbmCoverageLine.vue
+++ b/web/src/components/dbm/DbmCoverageLine.vue
@@ -42,33 +42,63 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
     <!-- The line itself. `min-h-6.5` holds the 26px budget the space plan
          allocates it, so a healthy page spends no more than that. -->
     <div
-      class="px-page-edge flex min-h-6.5 min-w-0 items-center gap-2 py-1"
+      class="px-page-edge flex min-h-6.5 min-w-0 items-center justify-between gap-4 py-1"
       :class="toneSurface"
       data-test="dbm-coverage-line"
     >
-      <span class="size-1.5 shrink-0 rounded-full" :class="dotTone" aria-hidden="true"></span>
-
-      <span class="text-2xs min-w-0 truncate" :class="toneText" data-test="dbm-coverage-text">
-        {{ summary }}
-      </span>
-
-      <!-- The bar is the same claim as the number beside it, drawn. It appears
-           only when there IS a share to draw — and `showBar` requires a healthy
-           line, so it is always the success variant: a degraded line already
-           speaks through its sentence and wash, not through a bar. -->
-      <OProgressBar
-        v-if="showBar"
-        :value="coverage ?? 0"
-        variant="success"
-        size="xs"
-        class="w-12 shrink-0"
-        data-test="dbm-coverage-bar"
-      />
-
-      <template v-if="countedTo">
-        <span class="text-text-muted text-2xs shrink-0" aria-hidden="true">·</span>
-        <span class="text-2xs shrink-0" :class="toneText">{{ countedTo }}</span>
-      </template>
+      <div class="flex min-w-0 items-center gap-2">
+        <span class="size-1.5 shrink-0 rounded-full" :class="dotTone" aria-hidden="true"></span>
+
+        <span class="text-2xs min-w-0 truncate" :class="toneText" data-test="dbm-coverage-text">
+          {{ summary }}
+        </span>
+
+        <!-- The bar is the same claim as the number beside it, drawn. It appears
+             only when there IS a share to draw — and `showBar` requires a healthy
+             line, so it is always the success variant: a degraded line already
+             speaks through its sentence and wash, not through a bar. -->
+        <OProgressBar
+          v-if="showBar"
+          :value="coverage ?? 0"
+          variant="success"
+          size="xs"
+          class="w-12 shrink-0"
+          data-test="dbm-coverage-bar"
+        />
+      </div>
+
+      <div
+        v-if="countedTo"
+        class="text-text-muted flex shrink-0 items-center gap-1.5"
+        data-test="dbm-coverage-counted"
+      >
+        <OIcon name="access-time" size="xs" class="shrink-0" aria-hidden="true" />
+        <i18n-t
+          v-if="countedTo.behind !== null"
+          keypath="dbm.coverage.lineCountedToBehind"
+          tag="span"
+          class="text-2xs"
+          :class="toneText"
+        >
+          <template #time>
+            <span class="font-semibold tabular-nums">{{ countedTo.time }}</span>
+          </template>
+          <template #minutes>
+            <span class="font-semibold tabular-nums">{{ countedTo.behind }}</span>
+          </template>
+        </i18n-t>
+        <i18n-t
+          v-else
+          keypath="dbm.coverage.lineCountedTo"
+          tag="span"
+          class="text-2xs"
+          :class="toneText"
+        >
+          <template #time>
+            <span class="font-semibold tabular-nums">{{ countedTo.time }}</span>
+          </template>
+        </i18n-t>
+      </div>
     </div>
   </div>
 </template>
@@ -76,6 +106,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
 <script setup lang="ts">
 import { computed } from "vue";
 
+import OIcon from "@/lib/core/Icon/OIcon.vue";
 import OProgressBar from "@/lib/data/ProgressBar/OProgressBar.vue";
 import type { Freshness, QueryStatsRow } from "@/services/db_monitoring";
 import { useI18nTyped, type I18nText } from "@/types/i18n";
@@ -263,8 +294,8 @@ const dotTone = computed(() => {
 
 const showBar = computed(() => !degraded.value && coverage.value !== null);
 
-/** Wall-clock time our counting reaches, for "counted up to 16:11:53". */
-const countedTo = computed<I18nText | null>(() => {
+/** Wall-clock time the counting reaches; `behind` is set only past {@link BEHIND_NOTE_MINUTES}, where the "still coming in" phrasing would assert a freshness the lag no longer supports. */
+const countedTo = computed<{ time: string; behind: number | null } | null>(() => {
   if (degraded.value) return null;
   const through = props.freshness?.data_through ?? 0;
   if (through <= 0) return null;
@@ -274,16 +305,8 @@ const countedTo = computed<I18nText | null>(() => {
     second: "2-digit",
     hour12: false,
   });
-  // "The last half-minute is still coming in" is true only while the counting
-  // really is current. Under the stale threshold but past ordinary rollup
-  // delay, that sentence asserts a freshness the data does not support — at 25
-  // minutes behind it is simply false — so the line states the real lag
-  // instead. It stays the quiet grey trailer either way: this is a correction
-  // to a claim, not a promotion to a warning.
   const behind = behindMinutes.value ?? 0;
-  return behind > BEHIND_NOTE_MINUTES
-    ? t("dbm.coverage.lineCountedToBehind", { time, minutes: behind })
-    : t("dbm.coverage.lineCountedTo", { time });
+  return { time, behind: behind > BEHIND_NOTE_MINUTES ? behind : null };
 });
 
 /**
diff --git a/web/src/components/dbm/DbmDeadlockCycle.vue b/web/src/components/dbm/DbmDeadlockCycle.vue
index b8155dca834..6386776f8ff 100644
--- a/web/src/components/dbm/DbmDeadlockCycle.vue
+++ b/web/src/components/dbm/DbmDeadlockCycle.vue
@@ -68,9 +68,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         v-if="!event.partial"
         class="border-border-subtle bg-surface-panel flex flex-col items-center justify-center gap-1 px-1 py-2 md:border-x"
       >
-        <span
-          class="text-text-label text-3xs text-center leading-tight font-semibold tracking-wide uppercase"
-        >
+        <span class="text-text-secondary text-3xs text-center leading-tight font-semibold">
           {{ t("dbm.deadlocks.detail.eachWaits") }}
         </span>
         <svg viewBox="0 0 128 68" fill="none" class="h-17 w-32" aria-hidden="true">
@@ -123,9 +121,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             {{ raw(objectLabel) }}
           </text>
         </svg>
-        <span
-          class="text-status-error-text text-3xs text-center leading-tight font-semibold tracking-wide uppercase"
-        >
+        <span class="text-status-error-text text-3xs text-center leading-tight font-semibold">
           {{ t("dbm.deadlocks.detail.deadlock") }}
         </span>
       </div>
diff --git a/web/src/components/dbm/DbmDeadlockSide.vue b/web/src/components/dbm/DbmDeadlockSide.vue
index bc99f1d255c..17c58fc5904 100644
--- a/web/src/components/dbm/DbmDeadlockSide.vue
+++ b/web/src/components/dbm/DbmDeadlockSide.vue
@@ -32,7 +32,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
     :class="isVictim ? 'bg-status-error-bg' : 'bg-status-success-bg'"
     :data-test="dataTest"
   >
-    <div class="text-2xs flex items-center gap-1.5 font-bold tracking-wide uppercase">
+    <div class="text-2xs flex items-center gap-1.5 font-bold">
       <OIcon
         :name="isVictim ? 'error-outline' : 'check-circle'"
         class="size-3.5 shrink-0"
@@ -73,7 +73,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
     >
       {{ raw(participant.query) }}
     </code>
-    <span v-else class="text-text-label text-2xs italic">
+    <span v-else class="text-text-secondary text-2xs italic">
       {{ t("dbm.deadlocks.detail.noQueryCaptured") }}
     </span>
 
diff --git a/web/src/components/dbm/DbmDeltaCell.spec.ts b/web/src/components/dbm/DbmDeltaCell.spec.ts
index 95f657d69bd..e2bb8c9eb42 100644
--- a/web/src/components/dbm/DbmDeltaCell.spec.ts
+++ b/web/src/components/dbm/DbmDeltaCell.spec.ts
@@ -16,6 +16,7 @@
 import { mount } from "@vue/test-utils";
 import { describe, expect, it } from "vitest";
 
+import OTooltip from "@/lib/overlay/Tooltip/OTooltip.vue";
 import i18n from "@/locales";
 import type { DbmDelta } from "@/utils/dbm/insights";
 
@@ -138,7 +139,10 @@ describe("DbmDeltaCell", () => {
       const wrapper = mountWith({ state: "new", current: 500 });
       // The caveat is the point: "new to this list" is first RANK ENTRY, not a
       // newly written query, and the hover has to say so or the chip misleads.
-      expect(wrapper.attributes("title")).toContain("wasn't among the heaviest earlier");
+      // The hover rides OTooltip's content, not a native title attribute.
+      expect(String(wrapper.findComponent(OTooltip).props("content"))).toContain(
+        "wasn't among the heaviest earlier",
+      );
     });
 
     it("shows both windows' values when they are supplied", () => {
@@ -146,8 +150,9 @@ describe("DbmDeltaCell", () => {
         { state: "changed", current: 200, previous: 100, ratio: 1 },
         { currentLabel: "200ms", previousLabel: "100ms" },
       );
-      expect(wrapper.attributes("title")).toContain("100ms");
-      expect(wrapper.attributes("title")).toContain("200ms");
+      const content = String(wrapper.findComponent(OTooltip).props("content"));
+      expect(content).toContain("100ms");
+      expect(content).toContain("200ms");
     });
   });
 });
diff --git a/web/src/components/dbm/DbmDeltaCell.vue b/web/src/components/dbm/DbmDeltaCell.vue
index 4e7baa9f1fb..0c87a008270 100644
--- a/web/src/components/dbm/DbmDeltaCell.vue
+++ b/web/src/components/dbm/DbmDeltaCell.vue
@@ -37,8 +37,8 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
   <span
     class="text-3xs flex items-center justify-end gap-1 font-mono tabular-nums"
     :data-test="dataTest"
-    :title="hoverTitle"
   >
+    <OTooltip v-if="hoverTitle" :content="hoverTitle" />
     <!-- `words` and `was` say the change the way a person would — "5× more",
          "was 98ms", "no change" — because a signed percentage makes the reader
          do the arithmetic to know whether +567% matters. `percent` keeps the
@@ -77,6 +77,7 @@ import { computed } from "vue";
 import OTag from "@/lib/core/Badge/OTag.vue";
 import OIcon from "@/lib/core/Icon/OIcon.vue";
 import type { IconName } from "@/lib/core/Icon/OIcon.icons";
+import OTooltip from "@/lib/overlay/Tooltip/OTooltip.vue";
 import { raw, useI18nTyped, type I18nText } from "@/types/i18n";
 import type { DbmDelta } from "@/utils/dbm/insights";
 import { formatSignedPercent } from "@/utils/dbm/format";
diff --git a/web/src/components/dbm/DbmDisclosureLine.spec.ts b/web/src/components/dbm/DbmDisclosureLine.spec.ts
index b46767bd24a..9ba7b44bf22 100644
--- a/web/src/components/dbm/DbmDisclosureLine.spec.ts
+++ b/web/src/components/dbm/DbmDisclosureLine.spec.ts
@@ -40,7 +40,7 @@ describe("DbmDisclosureLine", () => {
       "gap-1.5",
     ]);
     expect(wrapper.findComponent(OIcon).classes()).toEqual(
-      expect.arrayContaining(["mt-px", "shrink-0"]),
+      expect.arrayContaining(["relative", "top-1", "shrink-0"]),
     );
   });
 
diff --git a/web/src/components/dbm/DbmDisclosureLine.vue b/web/src/components/dbm/DbmDisclosureLine.vue
index 510dabd4316..42668076d6c 100644
--- a/web/src/components/dbm/DbmDisclosureLine.vue
+++ b/web/src/components/dbm/DbmDisclosureLine.vue
@@ -17,9 +17,12 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
 <!--
   DbmDisclosureLine — a caveat about the numbers, stated in the reading path.
 
-  `items-start` rather than `items-center`, and the icon nudged down a hair:
+  `items-start` rather than `items-center`, with the icon nudged down to the
+  centre of the FIRST line (`mt-1` centres a 12px glyph in the 20px line box):
   these lines WRAP, and a centred glyph on a two-line caveat floats into the gap
-  between the lines instead of marking where the sentence begins.
+  between the lines instead of marking where the sentence begins. Aligned to the
+  first line, it sits on the text on a one-liner and still marks the start on a
+  wrapped one.
 
   Never a tooltip. A reader who does not see the caveat reads a lifetime counter
   as a per-window one — which is a wrong number, not a missing nicety — so it
@@ -27,7 +30,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
 -->
 <template>
   <div class="text-text-secondary text-2xs flex items-start gap-1.5" :data-test="dataTest">
-    <OIcon :name="icon" class="mt-px shrink-0" size="xs" />
+    <OIcon :name="icon" class="relative top-1 shrink-0" size="xs" />
     <span><slot /></span>
   </div>
 </template>
diff --git a/web/src/components/dbm/DbmHistoryPanel.spec.ts b/web/src/components/dbm/DbmHistoryPanel.spec.ts
index 0344c6d8d5f..9a57c770f49 100644
--- a/web/src/components/dbm/DbmHistoryPanel.spec.ts
+++ b/web/src/components/dbm/DbmHistoryPanel.spec.ts
@@ -57,19 +57,20 @@ describe("DbmHistoryPanel", () => {
   it("renders the card and its heading", () => {
     const wrapper = mountPanel();
 
-    expect(wrapper.element.tagName).toBe("SECTION");
+    // Card is LLMPanelCard (a div) so both history charts share the dashboard panels' shell.
+    expect(wrapper.element.tagName).toBe("DIV");
     expect(wrapper.classes()).toEqual(
       expect.arrayContaining([
-        "card-container",
+        "bg-card-glass-bg",
         "border-border-default",
-        "rounded-surface",
+        "rounded-default",
         "flex",
         "flex-col",
+        "overflow-hidden",
         "border",
-        "p-3",
       ]),
     );
-    expect(wrapper.get("h3").text()).toBe("Latency");
+    expect(wrapper.get(".text-text-heading").text()).toBe("Latency");
   });
 
   /**
@@ -121,7 +122,7 @@ describe("DbmHistoryPanel", () => {
    */
   it("holds one height across all three states", async () => {
     expect(mountPanel({ loading: true }).get("[data-stub='skeleton']").classes()).toContain("h-55");
-    expect(mountPanel({ hasSeries: false }).get(".flex.items-center").classes()).toContain("h-55");
+    expect(mountPanel({ hasSeries: false }).get(".justify-center").classes()).toContain("h-55");
 
     const drawn = mountPanel();
     await flushPromises();
diff --git a/web/src/components/dbm/DbmHistoryPanel.vue b/web/src/components/dbm/DbmHistoryPanel.vue
index 44e5a001302..6c9cc57d68c 100644
--- a/web/src/components/dbm/DbmHistoryPanel.vue
+++ b/web/src/components/dbm/DbmHistoryPanel.vue
@@ -35,13 +35,12 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
   renderer through its pre-fetched-results injection path.
 -->
 <template>
-  <section class="card-container border-border-default rounded-surface flex flex-col border p-3">
-    <h3 class="text-text-heading mb-1 text-sm font-medium">{{ title }}</h3>
-    <OSkeleton v-if="loading" variant="button" class="h-55 w-full" />
+  <LLMPanelCard :title="title">
+    <OSkeleton v-if="loading" variant="button" class="h-55 w-full p-3" />
     <div v-else-if="!hasSeries" class="text-text-muted flex h-55 items-center justify-center">
       {{ emptyLabel }}
     </div>
-    <div v-else class="h-55 w-full">
+    <div v-else class="h-55 w-full p-2">
       <PanelSchemaRenderer
         :panel-schema="panelSchema"
         :selected-time-obj="selectedTimeObj"
@@ -52,12 +51,13 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         :data-test="panelDataTest"
       />
     </div>
-  </section>
+  </LLMPanelCard>
 </template>
 
 <script setup lang="ts">
 import { defineAsyncComponent } from "vue";
 
+import LLMPanelCard from "@/plugins/traces/LLMPanelCard.vue";
 import OSkeleton from "@/lib/feedback/Skeleton/OSkeleton.vue";
 import type { I18nText } from "@/types/i18n";
 import type { buildInjectedHistoryData } from "@/utils/dbm/historyPanelSchema";
diff --git a/web/src/components/dbm/DbmInstanceHealthCell.vue b/web/src/components/dbm/DbmInstanceHealthCell.vue
index f23bb88f0d1..6a6a5b5ff17 100644
--- a/web/src/components/dbm/DbmInstanceHealthCell.vue
+++ b/web/src/components/dbm/DbmInstanceHealthCell.vue
@@ -64,7 +64,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
           {{ formatPercent(metrics.saturation.ratio, 0) }}
         </span>
       </div>
-      <span class="text-text-label text-3xs font-mono tabular-nums">
+      <span class="text-text-secondary text-3xs font-mono tabular-nums">
         {{ connectionsLabel }}
       </span>
     </template>
@@ -85,7 +85,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
           {{ formatCount(metrics.saturation.used) }}
         </span>
       </div>
-      <span class="text-text-label text-3xs">{{ connectionsLabel }}</span>
+      <span class="text-text-secondary text-3xs">{{ connectionsLabel }}</span>
       <OTooltip side="left" :content="noLimitHint" />
     </template>
 
@@ -110,7 +110,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
          and a database with no deadlocks are all silence, not zeroes. -->
     <div
       v-if="secondary.length"
-      class="text-text-label text-3xs flex flex-wrap items-center justify-end gap-1.5"
+      class="text-text-secondary text-3xs flex flex-wrap items-center justify-end gap-1.5"
     >
       <span
         v-for="item in secondary"
diff --git a/web/src/components/dbm/DbmLoadCell.vue b/web/src/components/dbm/DbmLoadCell.vue
index cff9bf8abda..f49eff2c23b 100644
--- a/web/src/components/dbm/DbmLoadCell.vue
+++ b/web/src/components/dbm/DbmLoadCell.vue
@@ -59,23 +59,27 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
              divide the engine's own total by a traced subtotal, a ratio of two
              different populations that routinely exceeds 100%. So it is
              withheld, and the qualifier takes its place. -->
-        <span v-if="showsShare" class="text-text-label text-3xs font-mono tabular-nums">
+        <span v-if="showsShare" class="text-text-secondary text-3xs font-mono tabular-nums">
           {{ formatPercent(share, 0) }}
         </span>
       </span>
-      <!-- What this duration IS, when it came from the database: execution
-           time on Postgres, WAIT time on MySQL/MariaDB. The column heading is
-           the generic "Database time" and this list mixes engines, so the
-           distinction can only be stated per row. -->
-      <span
-        v-if="qualifier"
-        class="text-text-label text-3xs"
-        :title="qualifierTitle"
-        data-test="dbm-overlap-qualifier"
-      >
-        {{ qualifier }}
+      <span v-if="showsShare || qualifier" class="flex items-center gap-1.5">
+        <OProgressBar
+          v-if="showsShare"
+          :value="share"
+          :variant="barVariant"
+          size="xs"
+          class="w-14"
+        />
+        <span
+          v-if="qualifier"
+          class="text-text-secondary text-3xs"
+          data-test="dbm-overlap-qualifier"
+        >
+          <OTooltip v-if="qualifierTitle" :content="qualifierTitle" />
+          {{ qualifier }}
+        </span>
       </span>
-      <OProgressBar v-if="showsShare" :value="share" :variant="barVariant" size="xs" class="w-18" />
     </div>
   </div>
 </template>
@@ -85,6 +89,7 @@ import { computed } from "vue";
 
 import OProgressBar from "@/lib/data/ProgressBar/OProgressBar.vue";
 import OSparkline from "@/lib/data/Sparkline/OSparkline.vue";
+import OTooltip from "@/lib/overlay/Tooltip/OTooltip.vue";
 import type { SparklinePoint, SparklineTone } from "@/lib/data/Sparkline/OSparkline.types";
 import { useI18nTyped } from "@/types/i18n";
 import { formatNs, formatPercent } from "@/utils/dbm/format";
@@ -142,11 +147,9 @@ const qualifier = computed(() =>
 const qualifierTitle = computed(() =>
   props.qualifierKey === null
     ? undefined
-    : String(
-        t(`dbm.detail.overlap.${props.qualifierKey}` as "dbm.detail.overlap.serverWait", {
-          engine: props.engine ?? "",
-        }),
-      ),
+    : t(`dbm.detail.overlap.${props.qualifierKey}` as "dbm.detail.overlap.serverWait", {
+        engine: props.engine ?? "",
+      }),
 );
 
 /** A single query owning this much of a database's time is worth noticing. */
diff --git a/web/src/components/dbm/DbmMetricTiles.spec.ts b/web/src/components/dbm/DbmMetricTiles.spec.ts
index 0ad42918496..2fb9801900e 100644
--- a/web/src/components/dbm/DbmMetricTiles.spec.ts
+++ b/web/src/components/dbm/DbmMetricTiles.spec.ts
@@ -99,7 +99,7 @@ describe("DbmMetricTiles", () => {
     expect(tile.text()).not.toContain("p95");
     expect(tile.text()).not.toContain("2× more");
     expect(tile.get("div").classes()).toEqual(
-      expect.arrayContaining(["text-text-label", "text-3xs", "font-semibold", "uppercase"]),
+      expect.arrayContaining(["text-text-secondary", "text-3xs", "font-semibold"]),
     );
   });
 
diff --git a/web/src/components/dbm/DbmMetricTiles.vue b/web/src/components/dbm/DbmMetricTiles.vue
index b8408ec26cc..45f98a0525d 100644
--- a/web/src/components/dbm/DbmMetricTiles.vue
+++ b/web/src/components/dbm/DbmMetricTiles.vue
@@ -29,8 +29,8 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
   it would change the page's DOM wholesale, which is the opposite of what a
   dedup is for.
 
-  Two containers, one tile. `standalone` is the client block — a `rounded-surface`
-  card with a border all round. `attached` is the server block, which sits INSIDE a
+  Two containers, one tile. `standalone` is a `rounded-surface` card with a
+  border all round. `attached` is the server block, which sits INSIDE a
   `DbmSection` under its heading, so it carries only the rule that separates it
   from that heading.
 -->
@@ -46,14 +46,18 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
            the quiet weight — the tile says what it means and what it is. Only
            the grid that HAS sub-labels pays for the row that holds them. -->
       <div v-if="withSubLabels" class="flex items-baseline gap-1">
-        <span class="text-text-label text-3xs font-semibold tracking-wide uppercase">
+        <span class="text-text-secondary text-3xs font-semibold">
           {{ item.label }}
         </span>
-        <span v-if="item.sub" class="text-text-muted text-3xs" :data-test="`${tileDataTest}-sub`">
+        <span
+          v-if="item.sub"
+          class="text-text-secondary text-3xs"
+          :data-test="`${tileDataTest}-sub`"
+        >
           {{ item.sub }}
         </span>
       </div>
-      <div v-else class="text-text-label text-3xs font-semibold tracking-wide uppercase">
+      <div v-else class="text-text-secondary text-3xs font-semibold">
         {{ item.label }}
       </div>
       <div
@@ -62,7 +66,9 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       >
         {{ item.value }}
       </div>
-      <div v-if="withSubLabels" class="text-text-secondary text-3xs">{{ item.detail }}</div>
+      <div v-if="withSubLabels" class="text-3xs" :class="item.detailTone ?? 'text-text-secondary'">
+        {{ item.detail }}
+      </div>
     </div>
   </div>
 </template>
@@ -70,6 +76,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
 <script setup lang="ts">
 import { computed } from "vue";
 
+import type { IconName } from "@/lib/core/Icon/OIcon.icons";
 import type { I18nText } from "@/types/i18n";
 
 /** One figure: what it is, what it reads, and what it is worth. */
@@ -83,6 +90,10 @@ export interface DbmMetricTile {
   detail?: I18nText;
   /** A colour class, applied only where the page's own threshold fired. */
   tone?: string;
+  /** Colour class for the caption, to tint a signed delta by direction (rise red, fall green); neutral when unset. */
+  detailTone?: string;
+  /** Corner icon (OIcon name) for the KPI-card rendering of this figure. */
+  icon?: IconName;
 }
 
 const props = withDefaults(
diff --git a/web/src/components/dbm/DbmOverlapValue.vue b/web/src/components/dbm/DbmOverlapValue.vue
index d9b8fe6e0e4..1459fafb99f 100644
--- a/web/src/components/dbm/DbmOverlapValue.vue
+++ b/web/src/components/dbm/DbmOverlapValue.vue
@@ -49,12 +49,12 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       <!-- The marker names the MEASUREMENT, and its tooltip spells the whole
            sentence out — the short form fits the column, the long form is what
            settles an argument about which number is being quoted. -->
-      <span
-        class="text-text-label text-3xs"
-        :title="qualifierTitle"
-        data-test="dbm-overlap-qualifier"
-      >
-        {{ qualifierLabel }}
+      <span class="flex items-center gap-1.5">
+        <span class="text-text-secondary text-3xs" data-test="dbm-overlap-qualifier">
+          <OTooltip v-if="qualifierTitle" :content="qualifierTitle" />
+          {{ qualifierLabel }}
+        </span>
+        <slot name="trailing" />
       </span>
     </template>
     <!-- Absent, or unqualifiable. Never `0`: that is an all-clear nobody
@@ -66,6 +66,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
 <script setup lang="ts">
 import { computed } from "vue";
 
+import OTooltip from "@/lib/overlay/Tooltip/OTooltip.vue";
 import { raw, useI18nTyped } from "@/types/i18n";
 import type { DbmOverlapSource } from "@/utils/dbm/overlapMetrics";
 
@@ -116,10 +117,8 @@ const qualifierLabel = computed(() =>
 const qualifierTitle = computed(() =>
   props.qualifierKey === null
     ? undefined
-    : String(
-        t(`dbm.detail.overlap.${props.qualifierKey}` as "dbm.detail.overlap.serverWait", {
-          engine: props.engine ?? "",
-        }),
-      ),
+    : t(`dbm.detail.overlap.${props.qualifierKey}` as "dbm.detail.overlap.serverWait", {
+        engine: props.engine ?? "",
+      }),
 );
 </script>
diff --git a/web/src/components/dbm/DbmPageChrome.spec.ts b/web/src/components/dbm/DbmPageChrome.spec.ts
index 59197da2b42..013c6c2ad4f 100644
--- a/web/src/components/dbm/DbmPageChrome.spec.ts
+++ b/web/src/components/dbm/DbmPageChrome.spec.ts
@@ -16,11 +16,9 @@
 import { mount } from "@vue/test-utils";
 import { describe, expect, it, vi } from "vitest";
 
-import type { DbmRange } from "@/composables/dbm/useDbmScope";
 import type { DbmTabCountProps } from "@/composables/dbm/useDbmTabCounts";
 import OPageLayout from "@/lib/core/PageLayout/OPageLayout.vue";
 import i18n from "@/locales";
-import { raw } from "@/types/i18n";
 
 import DbmPageChrome from "./DbmPageChrome.vue";
 import DbmSectionTabs from "./DbmSectionTabs.vue";
@@ -31,38 +29,19 @@ vi.mock("vue-router", () => ({
   useRouter: () => ({ push: vi.fn(() => Promise.resolve()) }),
 }));
 
-const range: DbmRange = {
-  type: "relative",
-  relativeTimePeriod: "6h",
-  startTime: 1_000,
-  endTime: 2_000,
-};
-
 const tabCounts: DbmTabCountProps = { queryCount: 12, databaseCount: 3 };
 
-/** Named, so the emit test can find it; the real picker needs a store and a DOM. */
-const dateTimeStub = {
-  name: "DateTime",
-  props: ["defaultType", "defaultAbsoluteTime", "defaultRelativeTime", "dataTestName"],
-  emits: ["on:date-change"],
-  template: "<div data-test='date-time-stub' />",
-};
-
 const mountChrome = (props: Record<string, unknown> = {}, slots: Record<string, string> = {}) =>
   mount(DbmPageChrome, {
     props: {
-      title: raw("Deadlocks"),
-      subtitle: raw("Query pairs that deadlocked"),
       titleDataTest: "dbm-deadlocks-title",
-      dateTimeDataTest: "dbm-deadlocks-date-time",
       tabCounts,
-      range,
       ...props,
     },
     slots,
     global: {
       plugins: [i18n],
-      stubs: { DateTime: dateTimeStub, RouterLink: true },
+      stubs: { RouterLink: true },
     },
   });
 
@@ -80,11 +59,12 @@ describe("DbmPageChrome", () => {
     expect(layout.props("bleed")).toBe(true);
   });
 
-  it("carries the page's title, subtitle and title data-test", () => {
+  // One shared "Databases" header across tabs; per-tab titles made the section read as unrelated screens. Only the title's data-test varies, for e2e selectors.
+  it("wears the one shared Databases header, with the page's own title data-test", () => {
     const layout = mountChrome().findComponent(OPageLayout);
 
-    expect(layout.props("title")).toBe("Deadlocks");
-    expect(layout.props("subtitle")).toBe("Query pairs that deadlocked");
+    expect(layout.props("title")).toBe("Databases");
+    expect(layout.props("subtitle")).toBe("Query performance and health across your databases");
     expect(layout.props("titleDataTest")).toBe("dbm-deadlocks-title");
   });
 
@@ -100,40 +80,6 @@ describe("DbmPageChrome", () => {
     });
   });
 
-  /** The picker opens on the page's window, not on its own default. */
-  it("opens the picker on the page's window", () => {
-    const picker = mountChrome().findComponent({ name: "DateTime" });
-
-    expect(picker.props("defaultType")).toBe("relative");
-    expect(picker.props("defaultRelativeTime")).toBe("6h");
-    expect(picker.props("defaultAbsoluteTime")).toEqual({ startTime: 1_000, endTime: 2_000 });
-    expect(picker.props("dataTestName")).toBe("dbm-deadlocks-date-time");
-  });
-
-  /** A pick has to reach the page — that is what reloads the table. */
-  it("reports a pick to the page", async () => {
-    const wrapper = mountChrome();
-    wrapper.findComponent({ name: "DateTime" }).vm.$emit("on:date-change", {
-      relativeTimePeriod: "1d",
-      userChangedValue: true,
-    });
-    await wrapper.vm.$nextTick();
-
-    expect(wrapper.emitted("dateChange")).toEqual([
-      [{ relativeTimePeriod: "1d", userChangedValue: true }],
-    ]);
-  });
-
-  /**
-   * Query detail is not one of these pages, but two list pages DO add a control
-   * beside the picker; the slot keeps that possible without a second header.
-   */
-  it("lets a page add its own action beside the picker", () => {
-    const wrapper = mountChrome({}, { "actions-extra": '<button data-test="extra" />' });
-
-    expect(wrapper.find('[data-test="extra"]').exists()).toBe(true);
-  });
-
   it("renders the page's body", () => {
     expect(
       mountChrome({}, { default: '<div data-test="body" />' }).find('[data-test="body"]').exists(),
diff --git a/web/src/components/dbm/DbmPageChrome.vue b/web/src/components/dbm/DbmPageChrome.vue
index 6d2380872b9..56122bd8c0f 100644
--- a/web/src/components/dbm/DbmPageChrome.vue
+++ b/web/src/components/dbm/DbmPageChrome.vue
@@ -18,9 +18,10 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
   DbmPageChrome — the header every DBM list tab wears.
 
   All seven list pages opened with the same twenty lines: an OPageLayout with the
-  database icon, tabs on a second row, a bleeding body, the shared tab strip in
-  `#header-tabs`, and the same six-prop DateTime in `#actions`. Only the title,
-  the subtitle and two data-tests differed, so those are the props.
+  database icon, tabs on a second row, a bleeding body and the shared tab strip
+  in `#header-tabs`. Title and subtitle are one constant "Databases" header for
+  the whole section — the tab strip already names the view. Only the title's
+  data-test differs per page, for the e2e selectors that key off it.
 
   Two things deliberately stay in the page. The tab strip's markup lives HERE
   rather than in `DbmShell` for the reason `dbmTabCounts.ts` documents — it
@@ -30,8 +31,8 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
 -->
 <template>
   <OPageLayout
-    :title="title"
-    :subtitle="subtitle"
+    :title="t('dbm.header.title')"
+    :subtitle="t('dbm.header.subtitle')"
     icon="database"
     :title-data-test="titleDataTest"
     tabs-below
@@ -43,42 +44,22 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       <DbmSectionTabs v-bind="tabCounts" />
     </template>
 
-    <template #actions>
-      <DateTime
-        auto-apply
-        menu-align="end"
-        :default-type="range.type"
-        :default-absolute-time="{ startTime: range.startTime, endTime: range.endTime }"
-        :default-relative-time="range.relativeTimePeriod ?? undefined"
-        :data-test-name="dateTimeDataTest"
-        class="h-8"
-        @on:date-change="emit('dateChange', $event)"
-      />
-      <slot name="actions-extra" />
-    </template>
-
     <slot />
   </OPageLayout>
 </template>
 
 <script setup lang="ts">
-import DateTime from "@/components/DateTime.vue";
 import DbmSectionTabs from "@/components/dbm/DbmSectionTabs.vue";
-import type { DbmDateChange, DbmRange } from "@/composables/dbm/useDbmScope";
 import type { DbmTabCountProps } from "@/composables/dbm/useDbmTabCounts";
 import OPageLayout from "@/lib/core/PageLayout/OPageLayout.vue";
-import type { I18nText } from "@/types/i18n";
+import { useI18nTyped } from "@/types/i18n";
 
 defineProps<{
-  title: I18nText;
-  subtitle: I18nText;
+  /** Per-tab data-test on the shared title, for the e2e selectors that key off it. */
   titleDataTest: string;
-  dateTimeDataTest: string;
   /** What the tab strip paints — the shell's fan-out with this page's own count substituted. */
   tabCounts: DbmTabCountProps;
-  /** The window the picker opens on. */
-  range: DbmRange;
 }>();
 
-const emit = defineEmits<{ dateChange: [value: DbmDateChange] }>();
+const { t } = useI18nTyped();
 </script>
diff --git a/web/src/components/dbm/DbmQueryCell.spec.ts b/web/src/components/dbm/DbmQueryCell.spec.ts
index 700db6ee930..e035871b1c1 100644
--- a/web/src/components/dbm/DbmQueryCell.spec.ts
+++ b/web/src/components/dbm/DbmQueryCell.spec.ts
@@ -17,6 +17,7 @@ import { mount } from "@vue/test-utils";
 import { describe, expect, it } from "vitest";
 
 import OTag from "@/lib/core/Badge/OTag.vue";
+import OTooltip from "@/lib/overlay/Tooltip/OTooltip.vue";
 import i18n from "@/locales";
 import { raw } from "@/types/i18n";
 
@@ -41,7 +42,10 @@ describe("DbmQueryCell", () => {
     expect(line.classes()).toEqual(
       expect.arrayContaining(["text-text-code", "min-w-0", "truncate", "font-mono", "text-xs"]),
     );
-    expect(line.attributes("title")).toBe("SELECT * FROM orders WHERE id = $1");
+    // Full statement rides OTooltip's content, not a native title attr.
+    expect(String(line.findComponent(OTooltip).props("content"))).toBe(
+      "SELECT * FROM orders WHERE id = $1",
+    );
     expect(line.text()).toBe("SELECT * FROM orders");
   });
 
diff --git a/web/src/components/dbm/DbmQueryCell.vue b/web/src/components/dbm/DbmQueryCell.vue
index 8d681222c80..aeb8b1b8e8f 100644
--- a/web/src/components/dbm/DbmQueryCell.vue
+++ b/web/src/components/dbm/DbmQueryCell.vue
@@ -32,10 +32,11 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
 -->
 <template>
   <div class="flex min-w-0 flex-col gap-px">
-    <span class="text-text-code min-w-0 truncate font-mono text-xs" :title="titleAttr">{{
-      text || raw("—")
-    }}</span>
-    <div class="text-text-label text-3xs flex min-w-0 items-center gap-1 truncate">
+    <span class="text-text-code min-w-0 truncate font-mono text-xs">
+      <OTooltip v-if="titleAttr" :content="raw(titleAttr)" />
+      {{ text || raw("—") }}
+    </span>
+    <div class="text-text-secondary text-3xs flex min-w-0 items-center gap-1 truncate">
       <OTag v-if="dbSystem" type="dbSystem" :value="dbSystem" size="xs" />
       <template v-for="item in shownItems" :key="item.key">
         <span class="opacity-45">·</span>
@@ -50,6 +51,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
 import { computed } from "vue";
 
 import OTag from "@/lib/core/Badge/OTag.vue";
+import OTooltip from "@/lib/overlay/Tooltip/OTooltip.vue";
 import { raw, type I18nText } from "@/types/i18n";
 
 /**
diff --git a/web/src/components/dbm/DbmRefreshButton.spec.ts b/web/src/components/dbm/DbmRefreshButton.spec.ts
index e9f7f4e50ac..20a3ee64219 100644
--- a/web/src/components/dbm/DbmRefreshButton.spec.ts
+++ b/web/src/components/dbm/DbmRefreshButton.spec.ts
@@ -152,17 +152,16 @@ describe("DbmRefreshButton last-refreshed", () => {
     expect(dot.classes()).not.toContain("bg-refresh-dot-critical");
   });
 
-  /** The age rides the button's own tooltip rather than costing toolbar width. */
+  // Before a load the button names the action alone; after one, the staleness reading rides the dot's tooltip.
   it("names the action alone before a load, and adds the age after one", () => {
     expect(mountButton().findAllComponents(OTooltip)[0].props("content")).toBe("Refresh");
 
     const loaded = mountButton({ lastRunAt: Date.now() });
-    // Two tooltips now — the dot's and the button's. The button's is last in
-    // tree order, since the dot is rendered before it.
+    // Two tooltips: the dot's (first in tree order) and the button's.
     const tooltips = loaded.findAllComponents(OTooltip);
     expect(tooltips).toHaveLength(2);
+    expect(String(tooltips[0].props("content"))).toContain("Last refreshed");
     expect(String(tooltips[1].props("content"))).toContain("Refresh");
-    expect(String(tooltips[1].props("content"))).toContain("Last refreshed");
   });
 
   /** The dot alone encodes nothing to a reader who has not learned the colours. */
diff --git a/web/src/components/dbm/DbmRefreshButton.vue b/web/src/components/dbm/DbmRefreshButton.vue
index c521ebdebb6..3603478fed4 100644
--- a/web/src/components/dbm/DbmRefreshButton.vue
+++ b/web/src/components/dbm/DbmRefreshButton.vue
@@ -34,20 +34,16 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
   health's bare full-width search input has none to give.
 
   So the AFFORDANCE is adopted and the geometry is kept: same OButton, with the
-  staleness dot beside it and the relative time on hover rather than always-on.
-  The reading itself is `ORefreshButton`'s, shared through
-  `useDbmLastRefreshed` — same thresholds, same copy keys — so the two controls
-  cannot drift into disagreeing about what "stale" means.
+  staleness dot and the relative age beside it. The reading is `ORefreshButton`'s,
+  shared through `useDbmLastRefreshed`, so the two controls cannot drift on what
+  "stale" means.
 -->
 <template>
   <div class="inline-flex items-center gap-1.5" :class="shrink ? 'shrink-0' : undefined">
-    <!-- The dot is the always-visible half: it costs almost no width, and it
-         is the part that answers "should I trust this number?" at a glance.
-         Rendered only once a load has succeeded — before that there is no
-         staleness to report, and a grey dot on first paint would read as a
-         verdict rather than as the absence of one. -->
+    <!-- Rendered only after a successful load: a grey dot on first paint would read
+         as a verdict rather than the absence of one. -->
     <span
-      v-if="hasRun"
+      v-if="hasRun && mode !== 'button'"
       class="size-2 shrink-0 rounded-full transition-colors duration-700"
       :class="dotClass"
       :data-test="`${dataTest}-dot`"
@@ -57,7 +53,16 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       <OTooltip side="bottom" :content="dotStatus" />
     </span>
 
+    <span
+      v-if="hasRun && mode !== 'button'"
+      class="text-text-secondary text-xs whitespace-nowrap tabular-nums select-none"
+      :data-test="`${dataTest}-age`"
+    >
+      {{ relative }}
+    </span>
+
     <OButton
+      v-if="mode !== 'status'"
       variant="outline"
       size="icon-sm"
       icon-left="refresh"
@@ -66,11 +71,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       :data-test="dataTest"
       @click="emit('refresh')"
     >
-      <!-- One tooltip, two sentences: what the button does, and when the data
-           under it was last replaced. Before the first load only the action is
-           claimed — "not yet refreshed" beside a table mid-fetch would be a
-           staleness verdict on data that has not arrived. -->
-      <OTooltip side="bottom" :content="tooltip" />
+      <OTooltip side="bottom" :content="t('dbm.common.reload')" />
     </OButton>
   </div>
 </template>
@@ -100,8 +101,10 @@ const props = withDefaults(
      * adopting the timestamp is per-page rather than a flag day.
      */
     lastRunAt?: number | null;
+    /** Which halves to render: `full` (dot + age + button), `status` (dot + age), or `button` (just the reload). */
+    mode?: "full" | "status" | "button";
   }>(),
-  { loading: false, shrink: true, lastRunAt: null },
+  { loading: false, shrink: true, lastRunAt: null, mode: "full" },
 );
 
 const emit = defineEmits<{ refresh: [] }>();
@@ -115,14 +118,4 @@ const { relative, dotClass, dotLabel, exact, hasRun } = useDbmLastRefreshed({
 
 /** The dot's own hover text — the staleness verdict plus the exact clock time. */
 const dotStatus = computed<I18nText>(() => raw(`${dotLabel.value} — ${exact.value}`));
-
-/**
- * The button's hover text. The action always; the age only once there IS one,
- * so this never asserts staleness about a table that has not loaded.
- */
-const tooltip = computed<I18nText>(() =>
-  hasRun.value
-    ? raw(`${t("dbm.common.reload")} — ${t("dbm.common.lastRefreshed", { age: relative.value })}`)
-    : t("dbm.common.reload"),
-);
 </script>
diff --git a/web/src/components/dbm/DbmRowActions.vue b/web/src/components/dbm/DbmRowActions.vue
index 94f453f69ea..6dabc1a54a3 100644
--- a/web/src/components/dbm/DbmRowActions.vue
+++ b/web/src/components/dbm/DbmRowActions.vue
@@ -23,33 +23,22 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
   ticket, look at an example trace, set an alert) a three-step detour through a
   detail page. So the four things a reader actually does next live on the row.
 
-  They are hidden until hover/focus because a column of 14 identical button
-  clusters is visual noise that competes with the data — but they are in the
-  DOM and focusable throughout, so keyboard users reach them in tab order and
-  screen readers announce them. `opacity` rather than `v-if` is what makes that
-  true; `display:none` would take them out of the tab order entirely.
+  Always visible, matching the app-standard action column on the Dashboards and
+  Alerts lists. They stay in the DOM and focusable, so keyboard users reach them
+  in tab order and screen readers announce them.
 -->
 <template>
-  <div
-    class="flex items-center justify-end gap-0.5 opacity-0 transition-opacity group-hover/row:opacity-100 focus-within:opacity-100"
-    :data-test="dataTest"
-  >
+  <div class="flex items-center justify-end gap-0.5" :data-test="dataTest">
     <OButton
       v-for="action in actions"
       :key="action.id"
-      variant="ghost-muted"
-      size="icon-xs"
+      variant="ghost"
+      size="icon-xs-sq"
       :icon-left="action.icon"
       :data-test="`${dataTest}-${action.id}`"
       @click.stop="emit('action', action.id)"
     >
-      <!-- `left`, the convention for an icon-only action in a table's trailing
-           cell — it opens INTO the row rather than over the row above, and it
-           is what the other right-edge row actions already use (the
-           open-in-new buttons on Slowest calls and Query detail). These
-           buttons carry no label, so the tooltip is the only thing naming
-           them; where it opens is the difference between reading it and
-           covering the row you are pointing at. -->
+      <!-- Icon-only actions carry no label, so the tooltip names them; `left` opens it into the row, not over the row above. -->
       <OTooltip side="left" :content="action.label" />
     </OButton>
   </div>
@@ -61,7 +50,7 @@ import type { IconName } from "@/lib/core/Icon/OIcon.icons";
 import OTooltip from "@/lib/overlay/Tooltip/OTooltip.vue";
 import type { I18nText } from "@/types/i18n";
 
-/** One inline action. `id` is what the row emits back to the page. */
+/** One row action. `id` is what the row emits back to the page. */
 export interface DbmRowAction {
   id: string;
   icon: IconName;
diff --git a/web/src/components/dbm/DbmScopeFilters.vue b/web/src/components/dbm/DbmScopeFilters.vue
index 6e6c9fc50f6..295bfb198c8 100644
--- a/web/src/components/dbm/DbmScopeFilters.vue
+++ b/web/src/components/dbm/DbmScopeFilters.vue
@@ -46,28 +46,31 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         </OButton>
       </template>
 
-      <!-- The placeholder already names the dimension ("All engines"), so a
-           label above each select would say it twice. -->
-      <div class="flex w-72 flex-col gap-1.5 p-1">
-        <p class="text-text-label text-3xs px-1 font-semibold tracking-wide uppercase">
-          {{ t("dbm.filters.popoverTitle") }}
-        </p>
-        <OSelect
-          v-for="filter in filters"
-          :key="filter.key"
-          :model-value="filter.value"
-          :options="filter.options"
-          size="sm"
-          :searchable="false"
-          clearable
-          :placeholder="filter.placeholder"
-          :data-test="`dbm-queries-filter-${filter.key}`"
-          @update:model-value="filter.onChange"
-        />
-        <div v-if="activeCount" class="flex items-center gap-1.5 px-1 pt-0.5">
+      <div class="flex w-72 flex-col">
+        <div class="flex flex-col gap-2.5 p-3">
+          <p class="text-text-heading text-sm font-semibold" data-test="dbm-queries-scope-title">
+            {{ t("dbm.filters.popoverTitle") }}
+          </p>
+          <OSelect
+            v-for="filter in filters"
+            :key="filter.key"
+            :label="filter.dimension"
+            :model-value="filter.value"
+            :options="filter.options"
+            size="md"
+            :searchable="false"
+            clearable
+            :placeholder="filter.placeholder"
+            :data-test="`dbm-queries-filter-${filter.key}`"
+            @update:model-value="filter.onChange"
+          />
+        </div>
+
+        <div class="border-border-default flex items-center border-t px-3 py-2.5">
           <OButton
-            variant="ghost-primary"
-            size="xs"
+            variant="outline"
+            size="sm"
+            :disabled="!activeCount"
             data-test="dbm-queries-scope-clear"
             @click="emit('clear')"
           >
@@ -80,15 +83,16 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
     <!-- The chips scroll rather than push: five set dimensions are wider than
          the toolbar, and a chip strip that grows must not shove the refresh and
          column controls off the right edge. -->
-    <div class="flex min-w-0 flex-1 items-center gap-1.5 overflow-x-auto">
+    <!-- `overflow-x-auto` forces overflow-y to auto, clipping the remove badge that pokes above each chip; the `py-1`/`-my-1` pair grows the clip box to fit it. -->
+    <div class="-my-1 flex min-w-0 flex-1 items-center gap-1.5 overflow-x-auto py-1">
       <!-- Active scope, inline and clearable. The chip is the app-standard
            ODimensionChip, so `service` here is the same colour it is on the
-           incident list; the remove button sits beside it rather than inside,
-           because the chip is a shared primitive with no slot for one. -->
+           incident list. The remove button sits outside the chip because
+           ODimensionChip is a shared primitive with no slot for one. -->
       <span
         v-for="filter in activeFilters"
         :key="filter.key"
-        class="inline-flex min-w-0 shrink-0 items-center gap-0.5"
+        class="group relative inline-flex min-w-0 shrink-0 items-center"
         :data-test="`dbm-queries-scope-chip-${filter.key}`"
       >
         <ODimensionChip
@@ -97,15 +101,16 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
           :value="filter.value ?? ''"
           class="min-w-0"
         />
-        <OButton
-          variant="ghost-muted"
-          size="icon-xs-circle"
-          icon-left="close"
+        <button
+          type="button"
+          :aria-label="t('dbm.filters.removeScope')"
+          class="border-border-default bg-surface-base text-text-secondary hover:bg-surface-subtle-hover hover:text-text-heading focus-visible:ring-accent ring-offset-surface-base absolute -top-1 -right-1 z-10 inline-flex size-4 cursor-pointer items-center justify-center rounded-full border opacity-0 shadow-sm transition outline-none group-hover:opacity-100 focus-visible:opacity-100 focus-visible:ring-2 focus-visible:ring-offset-1"
           :data-test="`dbm-queries-scope-chip-${filter.key}-remove`"
           @click="filter.onChange(null)"
         >
+          <OIcon name="close" size="xs" />
           <OTooltip side="bottom" :content="t('dbm.filters.removeScope')" />
-        </OButton>
+        </button>
       </span>
 
       <!-- The insight filter deliberately stays amber and stays hand-rolled: it
@@ -114,7 +119,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
            segments, sm badge metrics, rounded-default) so it sits level. -->
       <span
         v-if="insightChip"
-        class="inline-flex min-w-0 shrink-0 items-center gap-0.5"
+        class="group relative inline-flex min-w-0 shrink-0 items-center"
         data-test="dbm-queries-scope-chip-insight"
       >
         <span
@@ -127,15 +132,16 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             insightChip.label
           }}</span>
         </span>
-        <OButton
-          variant="ghost-muted"
-          size="icon-xs-circle"
-          icon-left="close"
+        <button
+          type="button"
+          :aria-label="t('dbm.filters.removeScope')"
+          class="border-border-default bg-surface-base text-text-secondary hover:bg-surface-subtle-hover hover:text-text-heading focus-visible:ring-accent ring-offset-surface-base absolute -top-1 -right-1 z-10 inline-flex size-4 cursor-pointer items-center justify-center rounded-full border opacity-0 shadow-sm transition outline-none group-hover:opacity-100 focus-visible:opacity-100 focus-visible:ring-2 focus-visible:ring-offset-1"
           data-test="dbm-queries-scope-chip-insight-remove"
           @click="emit('clearInsight')"
         >
+          <OIcon name="close" size="xs" />
           <OTooltip side="bottom" :content="t('dbm.filters.removeScope')" />
-        </OButton>
+        </button>
       </span>
 
       <!-- Clear all, INLINE beside the chips.
@@ -165,6 +171,7 @@ import { computed, ref } from "vue";
 import ODimensionChip from "@/lib/core/Badge/ODimensionChip.vue";
 import OTag from "@/lib/core/Badge/OTag.vue";
 import OButton from "@/lib/core/Button/OButton.vue";
+import OIcon from "@/lib/core/Icon/OIcon.vue";
 import OSelect from "@/lib/forms/Select/OSelect.vue";
 import OPopover from "@/lib/overlay/Popover/OPopover.vue";
 import OTooltip from "@/lib/overlay/Tooltip/OTooltip.vue";
diff --git a/web/src/components/dbm/DbmSection.spec.ts b/web/src/components/dbm/DbmSection.spec.ts
index fbf328abb83..0d18bab62f4 100644
--- a/web/src/components/dbm/DbmSection.spec.ts
+++ b/web/src/components/dbm/DbmSection.spec.ts
@@ -35,11 +35,12 @@ describe("DbmSection", () => {
     expect(wrapper.element.tagName).toBe("SECTION");
     expect(wrapper.classes()).toEqual(
       expect.arrayContaining([
-        "card-container",
+        "bg-card-glass-bg",
         "border-border-default",
-        "rounded-surface",
+        "rounded-default",
         "flex",
         "flex-col",
+        "overflow-hidden",
         "border",
       ]),
     );
@@ -49,7 +50,7 @@ describe("DbmSection", () => {
     const heading = mountSection().get("h3");
     expect(heading.text()).toBe("Query plans");
     expect(heading.classes()).toEqual(
-      expect.arrayContaining(["text-text-heading", "text-sm", "font-medium"]),
+      expect.arrayContaining(["text-text-heading", "text-compact", "font-medium"]),
     );
   });
 
@@ -60,9 +61,9 @@ describe("DbmSection", () => {
    * actually uses, and nothing else.
    */
   it.each([
-    ["baseline", "flex flex-wrap items-baseline gap-2 p-3 pb-1"],
-    ["center", "flex flex-wrap items-center gap-2 p-3 pb-1"],
-    ["between", "flex items-center justify-between gap-2 p-3 pb-1"],
+    ["baseline", "border-border-default flex flex-wrap items-baseline gap-2 border-b px-3 py-2"],
+    ["center", "border-border-default flex flex-wrap items-center gap-2 border-b px-3 py-2"],
+    ["between", "border-border-default flex items-center justify-between gap-2 border-b px-3 py-2"],
   ])("lays the header out for headerAlign=%s", (headerAlign, expected) => {
     const header = mountSection({ headerAlign }).get("h3").element.parentElement;
     expect(header?.getAttribute("class")).toBe(expected);
@@ -71,7 +72,9 @@ describe("DbmSection", () => {
   /** Baseline is the common case, so it is what an unqualified section gets. */
   it("defaults to the baseline header", () => {
     const header = mountSection().get("h3").element.parentElement;
-    expect(header?.getAttribute("class")).toBe("flex flex-wrap items-baseline gap-2 p-3 pb-1");
+    expect(header?.getAttribute("class")).toBe(
+      "border-border-default flex flex-wrap items-baseline gap-2 border-b px-3 py-2",
+    );
   });
 
   /**
diff --git a/web/src/components/dbm/DbmSection.vue b/web/src/components/dbm/DbmSection.vue
index 72d13245b70..585f3ee4f93 100644
--- a/web/src/components/dbm/DbmSection.vue
+++ b/web/src/components/dbm/DbmSection.vue
@@ -27,11 +27,17 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
   can put a table, a tile grid or a state note under the heading without this
   component knowing which. `#hint` follows the title inside the header row;
   `#actions` closes it.
+
+  The shell is the shared LLM-Insights panel chrome (glass card, `rounded-default`,
+  a header bar closed by a bottom divider) so every section on the page — these,
+  the trend charts, the summary — reads as one family of panels.
 -->
 <template>
-  <section class="card-container border-border-default rounded-surface flex flex-col border">
+  <section
+    class="bg-card-glass-bg border-border-default rounded-default flex flex-col overflow-hidden border"
+  >
     <div :class="headerClass">
-      <h3 class="text-text-heading text-sm font-medium">{{ title }}</h3>
+      <h3 class="text-compact text-text-heading font-medium">{{ title }}</h3>
       <slot name="hint" />
       <slot name="actions" />
     </div>
@@ -61,9 +67,9 @@ const props = withDefaults(
 const headerClass = computed(
   () =>
     ({
-      baseline: "flex flex-wrap items-baseline gap-2 p-3 pb-1",
-      center: "flex flex-wrap items-center gap-2 p-3 pb-1",
-      between: "flex items-center justify-between gap-2 p-3 pb-1",
+      baseline: "border-border-default flex flex-wrap items-baseline gap-2 border-b px-3 py-2",
+      center: "border-border-default flex flex-wrap items-center gap-2 border-b px-3 py-2",
+      between: "border-border-default flex items-center justify-between gap-2 border-b px-3 py-2",
     })[props.headerAlign],
 );
 </script>
diff --git a/web/src/components/dbm/DbmTerminateSql.spec.ts b/web/src/components/dbm/DbmTerminateSql.spec.ts
index 28a4423b9d7..f774beb1964 100644
--- a/web/src/components/dbm/DbmTerminateSql.spec.ts
+++ b/web/src/components/dbm/DbmTerminateSql.spec.ts
@@ -43,21 +43,24 @@ describe("DbmTerminateSql", () => {
     expect(mountWith({ pid: null }).find("[data-test='dbm-terminate-sql']").exists()).toBe(false);
   });
 
+  // Hint is the copy button's tooltip (side="left"), not the statement's own first-in-tree OTooltip.
+  const hintContent = (wrapper: ReturnType<typeof mountWith>) =>
+    wrapper
+      .findAllComponents({ name: "OTooltip" })
+      .find((t) => t.props("side") === "left")
+      ?.props("content") as string;
+
   it("names the instance in the hint, so the operator knows where to run it", () => {
     // The hint lives on OTooltip, which only renders its content on hover — so
     // the assertion reads the prop rather than the DOM.
-    const content = mountWith({ instance: "dbmlab" })
-      .findComponent({ name: "OTooltip" })
-      .props("content") as string;
+    const content = hintContent(mountWith({ instance: "dbmlab" }));
     expect(content).toContain("dbmlab");
     // It must also say plainly that O2 will not run it.
     expect(content).toContain("never run it for you");
   });
 
   it("still offers the statement when the instance is unknown", () => {
-    const content = mountWith({ instance: null })
-      .findComponent({ name: "OTooltip" })
-      .props("content") as string;
+    const content = hintContent(mountWith({ instance: null }));
     expect(content).toContain("never run it for you");
   });
 });
diff --git a/web/src/components/dbm/DbmTerminateSql.vue b/web/src/components/dbm/DbmTerminateSql.vue
index 08435b42942..04c1c09490e 100644
--- a/web/src/components/dbm/DbmTerminateSql.vue
+++ b/web/src/components/dbm/DbmTerminateSql.vue
@@ -34,9 +34,9 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
   <div v-if="statement" class="flex min-w-0 items-center gap-1.5" :data-test="dataTest">
     <code
       class="bg-surface-subtle text-text-code rounded-default text-2xs min-w-0 truncate px-1.5 py-0.5 font-mono"
-      :title="statement"
       :data-test="`${dataTest}-statement`"
     >
+      <OTooltip :content="raw(statement)" />
       {{ raw(statement) }}
     </code>
     <OButton
diff --git a/web/src/components/slos/SloAlertCondition.vue b/web/src/components/slos/SloAlertCondition.vue
index 13bfffd05e9..a85bd6f438e 100644
--- a/web/src/components/slos/SloAlertCondition.vue
+++ b/web/src/components/slos/SloAlertCondition.vue
@@ -204,7 +204,7 @@
 
 <script setup lang="ts">
 import { computed, onMounted, ref, watch } from "vue";
-import { raw, useI18nTyped } from "@/types/i18n";
+import { raw, useI18nTyped, type I18nText } from "@/types/i18n";
 import { useStore } from "vuex";
 
 import OInput from "@/lib/forms/Input/OInput.vue";
@@ -213,6 +213,7 @@ import OToggleGroup from "@/lib/core/ToggleGroup/OToggleGroup.vue";
 import OIcon from "@/lib/core/Icon/OIcon.vue";
 import OToggleGroupItem from "@/lib/core/ToggleGroup/OToggleGroupItem.vue";
 import sloService from "@/services/slos";
+import type { IconName } from "@/lib/core/Icon/OIcon.icons";
 import type { Slo } from "@/ts/interfaces/slo";
 import { formatTimeToExhaust, formatWindow } from "@/composables/useSloFormat";
 
@@ -236,9 +237,9 @@ const selectedSlo = computed(
   () => props.slo ?? slos.value.find((s) => s.id === model.value.slo_id) ?? null,
 );
 
-const kindOptions = computed(() => [
+const kindOptions = computed<{ value: string; label: I18nText; icon: IconName }[]>(() => [
   { value: "burn_rate", label: t("slos.alert.kind.burnRate"), icon: "local-fire-department" },
-  { value: "error_budget", label: t("slos.alert.kind.errorBudget"), icon: "data_usage" },
+  { value: "error_budget", label: t("slos.alert.kind.errorBudget"), icon: "data-usage" },
 ]);
 
 const operatorOptions = [
diff --git a/web/src/components/slos/SloAlertsPanel.vue b/web/src/components/slos/SloAlertsPanel.vue
index 5d3b59f9f2c..2880d40f525 100644
--- a/web/src/components/slos/SloAlertsPanel.vue
+++ b/web/src/components/slos/SloAlertsPanel.vue
@@ -59,14 +59,24 @@
       />
     </div>
 
-    <div
+    <OEmptyState
       v-else-if="!alerts.length"
-      class="text-text-secondary flex flex-col gap-1 py-6 text-center"
+      size="block"
+      illustration="alert"
+      :title="t('slos.alerts.emptyTitle')"
+      :description="t('slos.alerts.emptyHint')"
       data-test="slo-alerts-empty"
     >
-      <span class="text-sm font-medium">{{ t("slos.alerts.emptyTitle") }}</span>
-      <span class="text-compact">{{ t("slos.alerts.emptyHint") }}</span>
-    </div>
+      <template #actions>
+        <EmptyStateActionCard
+          icon="add"
+          :label="t('slos.alerts.add')"
+          :sublabel="t('slos.alerts.emptyActionDesc')"
+          data-test="slo-alerts-empty-add"
+          @click="startCreate"
+        />
+      </template>
+    </OEmptyState>
 
     <ul v-else class="flex flex-col gap-2" data-test="slo-alerts-list">
       <li
@@ -105,6 +115,8 @@ import { useI18nTyped } from "@/types/i18n";
 
 import OBanner from "@/lib/feedback/Banner/OBanner.vue";
 import OButton from "@/lib/core/Button/OButton.vue";
+import OEmptyState from "@/lib/core/EmptyState/OEmptyState.vue";
+import EmptyStateActionCard from "@/lib/core/EmptyState/EmptyStateActionCard.vue";
 import OTag from "@/lib/core/Badge/OTag.vue";
 import SloAlertForm from "@/components/slos/SloAlertForm.vue";
 import alertsService from "@/services/alerts";
diff --git a/web/src/components/slos/SloBurndownChart.vue b/web/src/components/slos/SloBurndownChart.vue
index 001547df486..7322806e3f4 100644
--- a/web/src/components/slos/SloBurndownChart.vue
+++ b/web/src/components/slos/SloBurndownChart.vue
@@ -89,7 +89,7 @@
                 <div class="flex flex-col gap-2">
                   <p class="text-xs leading-relaxed">{{ panel.explain }}</p>
                   <div class="flex flex-col gap-1">
-                    <span class="text-text-secondary text-2xs font-semibold uppercase">
+                    <span class="text-text-secondary text-2xs font-semibold">
                       {{ t("slos.chart.formulaLabel") }}
                     </span>
                     <!-- The interpolation hugs the tags: `whitespace-pre-line`
diff --git a/web/src/composables/dbm/useDbmChartTheme.spec.ts b/web/src/composables/dbm/useDbmChartTheme.spec.ts
index 55b6faeb2e1..0c86287b49b 100644
--- a/web/src/composables/dbm/useDbmChartTheme.spec.ts
+++ b/web/src/composables/dbm/useDbmChartTheme.spec.ts
@@ -32,17 +32,17 @@ describe("useDbmChartTheme", () => {
     chartColor.mockClear();
   });
 
-  /**
-   * ECharts draws on a canvas and cannot read a CSS custom property, so every
-   * token has to arrive as a resolved literal. The four are the scatter's whole
-   * palette — both callers draw the same chart and must not drift apart.
-   */
-  it("resolves the scatter's four tokens to literals", () => {
+  // ECharts draws on canvas and can't read CSS custom properties, so every token must resolve to a literal; both callers draw the same chart and must not drift apart.
+  it("resolves the scatter's tokens to literals", () => {
     expect(useDbmChartTheme().value).toEqual({
       calls: "light:--color-chart-series-1",
       errors: "light:--color-severity-error-color",
       axisLabel: "light:--color-text-secondary",
-      splitLine: "light:--color-border-default",
+      splitLine: "light:--color-chart-gridline",
+      tooltipBg: "light:--color-tooltip-bg",
+      tooltipBorder: "light:--color-tooltip-border",
+      tooltipText: "light:--color-tooltip-text",
+      crosshairBg: "light:--color-chart-crosshair-bg",
     });
   });
 
@@ -66,6 +66,6 @@ describe("useDbmChartTheme", () => {
     void theme.value;
     void theme.value;
 
-    expect(chartColor).toHaveBeenCalledTimes(4);
+    expect(chartColor).toHaveBeenCalledTimes(8);
   });
 });
diff --git a/web/src/composables/dbm/useDbmChartTheme.ts b/web/src/composables/dbm/useDbmChartTheme.ts
index d23ebdaebc2..350dbb1d5e0 100644
--- a/web/src/composables/dbm/useDbmChartTheme.ts
+++ b/web/src/composables/dbm/useDbmChartTheme.ts
@@ -45,7 +45,11 @@ export const useDbmChartTheme = (): ComputedRef<DbmChartTheme> => {
       calls: chartColor("--color-chart-series-1"),
       errors: chartColor("--color-severity-error-color"),
       axisLabel: chartColor("--color-text-secondary"),
-      splitLine: chartColor("--color-border-default"),
+      splitLine: chartColor("--color-chart-gridline"),
+      tooltipBg: chartColor("--color-tooltip-bg"),
+      tooltipBorder: chartColor("--color-tooltip-border"),
+      tooltipText: chartColor("--color-tooltip-text"),
+      crosshairBg: chartColor("--color-chart-crosshair-bg"),
     };
   });
 };
diff --git a/web/src/lib/core/Badge/badgeGroups.ts b/web/src/lib/core/Badge/badgeGroups.ts
index 731057ee5d3..c36f4733af7 100644
--- a/web/src/lib/core/Badge/badgeGroups.ts
+++ b/web/src/lib/core/Badge/badgeGroups.ts
@@ -1544,7 +1544,6 @@ export const BADGE_GROUPS = {
   // is an open vocabulary and a new engine must still render.
   dbSystem: {
     mode: "plain",
-    shape: "rounded",
     size: "xs",
     values: {
       postgresql: { variant: "blue-soft", label: raw("PostgreSQL") },
diff --git a/web/src/lib/core/PageLayout/OPageLayout.vue b/web/src/lib/core/PageLayout/OPageLayout.vue
index 642257e7806..faab7a5a589 100644
--- a/web/src/lib/core/PageLayout/OPageLayout.vue
+++ b/web/src/lib/core/PageLayout/OPageLayout.vue
@@ -122,7 +122,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
     <div v-else-if="!!slots.sidebar" class="flex min-h-0 flex-1">
       <aside
         class="border-border-default flex h-full shrink-0 flex-col overflow-hidden border-r"
-        :style="{ width: (sidebarWidth ?? 200) + 'px' }"
+        :style="{ width: sidebarWidth + 'px' }"
       >
         <slot name="sidebar" />
       </aside>
@@ -166,6 +166,8 @@ interface BackTarget {
   dataTest?: string;
 }
 
+const RAIL_WIDTH = 230;
+
 const props = withDefaults(
   defineProps<{
     // Header (from props)
@@ -202,7 +204,7 @@ const props = withDefaults(
     bleed: false,
     padY: false,
     scroll: false,
-    sidebarWidth: 200,
+    sidebarWidth: RAIL_WIDTH,
     resizable: false,
     splitterLimits: () => [0, 400] as [number, number],
     constrained: false,
diff --git a/web/src/lib/core/RefreshButton/ORefreshButton.types.ts b/web/src/lib/core/RefreshButton/ORefreshButton.types.ts
index 6e977273a52..74ad3ac54e1 100644
--- a/web/src/lib/core/RefreshButton/ORefreshButton.types.ts
+++ b/web/src/lib/core/RefreshButton/ORefreshButton.types.ts
@@ -5,6 +5,8 @@ export interface RefreshButtonProps {
   loading?: boolean;
   /** Disables the button independently of loading */
   disabled?: boolean;
+  /** Reload icon-button style. `outline` draws a border; default is borderless `ghost`. */
+  variant?: "ghost" | "outline";
 }
 
 export interface RefreshButtonEmits {
diff --git a/web/src/lib/core/RefreshButton/ORefreshButton.vue b/web/src/lib/core/RefreshButton/ORefreshButton.vue
index 9b5cdf9ed92..518e6c20b13 100644
--- a/web/src/lib/core/RefreshButton/ORefreshButton.vue
+++ b/web/src/lib/core/RefreshButton/ORefreshButton.vue
@@ -2,6 +2,7 @@
 import { ref, computed, onMounted, onBeforeUnmount, watch } from "vue";
 import { useI18nTyped } from "@/types/i18n";
 import OButton from "../Button/OButton.vue";
+import OTooltip from "../../overlay/Tooltip/OTooltip.vue";
 import type { RefreshButtonProps, RefreshButtonEmits } from "./ORefreshButton.types";
 
 const { t } = useI18nTyped();
@@ -10,6 +11,7 @@ const props = withDefaults(defineProps<RefreshButtonProps>(), {
   lastRunAt: null,
   loading: false,
   disabled: false,
+  variant: "ghost",
 });
 
 const emit = defineEmits<RefreshButtonEmits>();
@@ -84,46 +86,27 @@ function handleClick(e: MouseEvent) {
 <template>
   <div class="inline-flex items-center gap-1.5">
     <!-- staleness dot -->
-    <span
-      :class="['size-2 shrink-0 rounded-full transition-colors duration-700', dotColor]"
-      :title="dotTitle"
-    />
+    <span :class="['size-2 shrink-0 rounded-full transition-colors duration-700', dotColor]">
+      <OTooltip :content="dotTitle" />
+    </span>
     <!-- relative timestamp -->
     <span
       v-if="lastRunAt"
       class="text-text-secondary text-xs whitespace-nowrap tabular-nums select-none"
-      :title="exactTime"
     >
       {{ relativeTime || t("refreshButton.justNow") }}
+      <OTooltip :content="exactTime" />
     </span>
-    <!-- refresh icon button -->
     <OButton
-      variant="ghost"
-      size="icon"
+      :variant="variant"
+      size="icon-toolbar"
+      icon-left="refresh"
       :loading="loading"
       :disabled="disabled"
-      :title="exactTime"
       data-test="refresh-button"
-      class="size-7"
       @click="handleClick"
     >
-      <svg
-        xmlns="http://www.w3.org/2000/svg"
-        width="15"
-        height="15"
-        viewBox="0 0 24 24"
-        fill="none"
-        stroke="currentColor"
-        stroke-width="2"
-        stroke-linecap="round"
-        stroke-linejoin="round"
-        :class="{ 'animate-spin': loading }"
-      >
-        <path d="M3 12a9 9 0 0 1 9-9 9.75 9.75 0 0 1 6.74 2.74L21 8" />
-        <path d="M21 3v5h-5" />
-        <path d="M21 12a9 9 0 0 1-9 9 9.75 9.75 0 0 1-6.74-2.74L3 16" />
-        <path d="M8 16H3v5" />
-      </svg>
+      <OTooltip :content="exactTime" />
     </OButton>
   </div>
 </template>
diff --git a/web/src/lib/core/Table/OTable.types.ts b/web/src/lib/core/Table/OTable.types.ts
index f4366febc80..329b1710bc0 100644
--- a/web/src/lib/core/Table/OTable.types.ts
+++ b/web/src/lib/core/Table/OTable.types.ts
@@ -363,6 +363,8 @@ export interface OTableProps<TData = any> {
    * (e.g. a page card) so it renders flush without a double border.
    */
   frame?: boolean;
+  /** Draws the hairline divider under the toolbar row (default true); set false when a subheader below would read as a double line. */
+  toolbarBordered?: boolean;
   striped?: boolean;
   stickyHeader?: boolean;
   showHeader?: boolean;
diff --git a/web/src/lib/core/Table/OTable.vue b/web/src/lib/core/Table/OTable.vue
index 2aaa4fac831..af2ecdafa84 100644
--- a/web/src/lib/core/Table/OTable.vue
+++ b/web/src/lib/core/Table/OTable.vue
@@ -71,6 +71,7 @@ const props = withDefaults(defineProps<OTableProps<TData>>(), {
   // content, and CRUD listing tables sit flush to the edges. Row dividers (the
   // `bordered` row-bottom hairlines) stay; only the surrounding border is gone.
   frame: false,
+  toolbarBordered: true,
   striped: false,
   stickyHeader: true,
   wrap: false,
@@ -1054,7 +1055,10 @@ defineExpose({
       <!-- ── Custom toolbar slot (rendered INSIDE the frame, above the table) ── -->
       <div
         v-if="slots.toolbar || slots['toolbar-trailing']"
-        class="px-page-edge border-table-row-divider flex items-center gap-2 border-b py-2"
+        :class="[
+          'px-page-edge flex items-center gap-2 py-2',
+          props.toolbarBordered ? 'border-table-row-divider border-b' : '',
+        ]"
         data-test="o2-table-toolbar"
       >
         <slot name="toolbar" />
diff --git a/web/src/lib/core/Table/sub-components/OTableHeader.vue b/web/src/lib/core/Table/sub-components/OTableHeader.vue
index ebefc0ada5c..e01c2faacc7 100644
--- a/web/src/lib/core/Table/sub-components/OTableHeader.vue
+++ b/web/src/lib/core/Table/sub-components/OTableHeader.vue
@@ -606,7 +606,7 @@ function getStandardStickyTotalStyle(header: any): Record<string, any> {
               </span>
               <span
                 v-if="headerSubLabel(header)"
-                class="text-text-muted text-2xs w-full min-w-0 truncate leading-tight font-normal normal-case"
+                class="text-text-secondary text-2xs w-full min-w-0 truncate leading-tight font-normal normal-case"
                 :data-test="`o2-table-th-sublabel-${header.id}`"
               >
                 {{ headerSubLabel(header) }}
@@ -664,7 +664,7 @@ function getStandardStickyTotalStyle(header: any): Record<string, any> {
             </span>
             <span
               v-if="headerSubLabel(header)"
-              class="text-text-muted text-2xs w-full min-w-0 truncate leading-tight font-normal normal-case"
+              class="text-text-secondary text-2xs w-full min-w-0 truncate leading-tight font-normal normal-case"
               :data-test="`o2-table-th-sublabel-${header.id}`"
             >
               {{ headerSubLabel(header) }}
@@ -935,7 +935,7 @@ function getStandardStickyTotalStyle(header: any): Record<string, any> {
               </span>
               <span
                 v-if="headerSubLabel(header)"
-                class="text-text-muted text-2xs w-full min-w-0 truncate leading-tight font-normal normal-case"
+                class="text-text-secondary text-2xs w-full min-w-0 truncate leading-tight font-normal normal-case"
                 :data-test="`o2-table-th-sublabel-${header.id}`"
               >
                 {{ headerSubLabel(header) }}
diff --git a/web/src/lib/forms/Select/OSelect.vue b/web/src/lib/forms/Select/OSelect.vue
index 9545ad30792..6cc38968a1d 100644
--- a/web/src/lib/forms/Select/OSelect.vue
+++ b/web/src/lib/forms/Select/OSelect.vue
@@ -1869,6 +1869,10 @@ const fieldWidthClass = computed(() => {
               />
             </template>
 
+            <div v-else-if="!slots.default" class="text-select-placeholder px-3 py-2 text-sm">
+              <slot name="empty">{{ t("components.select.noOptionsFound") }}</slot>
+            </div>
+
             <slot />
           </SelectViewport>
 
diff --git a/web/src/lib/navigation/Tabs/OTabPanel.types.ts b/web/src/lib/navigation/Tabs/OTabPanel.types.ts
index d4aebd9e156..0c529a8e5f8 100644
--- a/web/src/lib/navigation/Tabs/OTabPanel.types.ts
+++ b/web/src/lib/navigation/Tabs/OTabPanel.types.ts
@@ -18,6 +18,8 @@ export interface OTabPanelProps {
   layout?: TabPanelLayout;
   /** Makes the panel root element fill 100% of parent height. Default: false */
   stretch?: boolean;
+  /** Extra classes for the panel root; use instead of bare `class=`, which Vue drops silently on this multi-root component. */
+  contentClass?: string;
 }
 
 export interface OTabPanelSlots {
diff --git a/web/src/lib/navigation/Tabs/OTabPanel.vue b/web/src/lib/navigation/Tabs/OTabPanel.vue
index 4a4b1048da7..5330c6ea1f8 100644
--- a/web/src/lib/navigation/Tabs/OTabPanel.vue
+++ b/web/src/lib/navigation/Tabs/OTabPanel.vue
@@ -40,6 +40,7 @@ const panelClasses = computed<string[]>(() => {
   const layout = layoutClasses[props.layout];
   if (layout) classes.push(layout);
   if (props.stretch) classes.push("h-full");
+  if (props.contentClass) classes.push(props.contentClass);
   return classes;
 });
 </script>
diff --git a/web/src/lib/styles/tokens/component.css b/web/src/lib/styles/tokens/component.css
index c209c17f70f..46ed38cc6e3 100644
--- a/web/src/lib/styles/tokens/component.css
+++ b/web/src/lib/styles/tokens/component.css
@@ -578,11 +578,7 @@
 
   /* Input (OInput, OTextarea) */
   --color-input-bg: var(--color-surface-base);
-  /* `border-control`, not `border-default`: the edge IS the affordance here —
-     it is the only signal that this is something you type into — so WCAG 2.1
-     SC 1.4.11 applies and wants 3:1. `border-default` reaches 1.48:1, which is
-     right for a card outline and not for a control. */
-  --color-input-border: var(--color-border-control);
+  --color-input-border: var(--color-border-default);
   --color-input-border-hover: var(--color-primary-300);
   --color-input-border-focus: var(--color-primary-300);
   --color-input-border-error: var(--color-error-500);
@@ -607,10 +603,7 @@
 
   /* Select (OSelect) */
   --color-select-bg: var(--color-surface-base);
-  /* Same reasoning as --color-input-border: an operable control's edge. */
-  --color-select-border: var(--color-border-control);
-  /* Quieted like the input's, so a disabled select does not keep drawing the
-     3:1 edge that means "you can operate this". */
+  --color-select-border: var(--color-border-default);
   --color-select-disabled-border: var(--color-border-default);
   --color-select-border-hover: var(--color-primary-300);
   --color-select-border-focus: var(--color-primary-300);
diff --git a/web/src/lib/styles/tokens/dark.css b/web/src/lib/styles/tokens/dark.css
index e21d6ccdc12..56cfe192d1b 100644
--- a/web/src/lib/styles/tokens/dark.css
+++ b/web/src/lib/styles/tokens/dark.css
@@ -178,7 +178,7 @@
 
   /* outline */
   --color-button-outline-text: var(--color-text-heading);
-  --color-button-outline-border: var(--color-border-default);
+  --color-button-outline-border: var(--color-border-control);
   --color-button-outline-hover-bg: var(--color-primary-900);
   --color-button-outline-hover-border: var(--color-border-strong);
   --color-button-outline-active-bg: var(--color-surface-accent-active);
@@ -376,11 +376,10 @@
        a distinct lighter gray; disabled-bg uses --d-well so disabled fields
        still read as a clearly different, inert surface. */
   --color-input-bg: var(--color-surface-base);
-  /* border-control, NOT border-default: this line sits after the
-     --color-border-control definition above and used to override it, so the
-     3:1 fix reached light mode only and dark inputs kept --d-border (#454545
-     on #0d1117 = 1.97:1) — the exact contrast the change existed to fix. */
-  --color-input-border: var(--color-border-control);
+  /* Matched to the outline button's border (--color-border-default) so a text
+     field and a button side by side share one border weight — product decision
+     for visual uniformity, below the WCAG 2.1 SC 1.4.11 3:1 target. */
+  --color-input-border: var(--color-border-default);
   --color-input-disabled-bg: var(--d-well);
   --color-input-disabled-text: var(--color-text-secondary);
   --color-input-placeholder: var(--color-text-placeholder);
diff --git a/web/src/locales/languages/de-DE.json b/web/src/locales/languages/de-DE.json
index 92d37768db6..0bffc018072 100644
--- a/web/src/locales/languages/de-DE.json
+++ b/web/src/locales/languages/de-DE.json
@@ -14923,6 +14923,7 @@
       "disabled": "Deaktiviert",
       "emptyTitle": "Noch keine Alarme für dieses SLO",
       "emptyHint": "Burn-Rate-Alarme warnen Sie, solange noch Budget übrig ist, um das SLO zu schützen.",
+      "emptyActionDesc": "Erhalten Sie eine Warnung, bevor das Budget aufgebraucht ist",
       "loadFailed": "Die Alarme für dieses SLO konnten nicht geladen werden.",
       "editTargetMissing": "Dieser Alarm ist diesem SLO nicht mehr zugeordnet.",
       "describeBurn": "Burn-Rate > {critical} über {long} und {short}",
@@ -15215,7 +15216,12 @@
     "common": {
       "reload": "Aktualisieren",
       "lastRefreshed": "Zuletzt aktualisiert {age}",
-      "loadFailed": "Datenbankdaten konnten nicht geladen werden. Versuchen Sie es erneut oder grenzen Sie den Zeitraum ein."
+      "loadFailed": "Datenbankdaten konnten nicht geladen werden. Versuchen Sie es erneut oder grenzen Sie den Zeitraum ein.",
+      "actions": "Aktionen"
+    },
+    "header": {
+      "title": "Datenbanken",
+      "subtitle": "Abfrageleistung und -zustand in Ihren Datenbanken"
     },
     "ai": {
       "suggestFix": "KI fragen, wie das zu beheben ist",
@@ -15242,7 +15248,7 @@
         "namespace": "Schema",
         "env": "Umgebung",
         "service": "Dienst",
-        "insight": "Erkenntnis"
+        "insight": "Einblick"
       }
     },
     "delta": {
@@ -15257,8 +15263,6 @@
       "wasValue": "war {value}"
     },
     "databases": {
-      "title": "Datenbanken",
-      "subtitle": "Abfrageleistung und -zustand pro Datenbank",
       "searchPlaceholder": "Datenbank oder Dienst suchen…",
       "unnamedInstance": "Instanz nicht benannt",
       "unnamedInstanceHint": "Die eigenen Anweisungsberichte der Datenbank beweisen, dass diese Engine läuft, aber sie nennen keinen Host. Die Zeile zeigt eine Adresse, sobald ein Feed eingerichtet ist, der eine solche liefert — Session-Sampling oder Instanzmetriken.",
@@ -15305,9 +15309,6 @@
       }
     },
     "queries": {
-      "title": "Top-Abfragen",
-      "subtitle": "Die Abfragen mit der höchsten Datenbankzeit",
-      "subtitleServer": "Die Anweisungen, die Ihre Datenbanken ausgeführt haben, innerhalb der Datenbank gezählt — am häufigsten ausgeführte zuerst",
       "searchPlaceholder": "Abfragetext, Tabelle oder Dienst suchen…",
       "trendLabel": "Wie sich die Zeit dieser Abfrage über den Zeitraum entwickelt hat",
       "otherRow": "Alle anderen Abfragen",
@@ -15316,7 +15317,8 @@
         "query": "Abfragen",
         "queryHint": "Nur echte SQL-Befehle — SELECT, INSERT, UPDATE und DELETE.",
         "all": "Abfragen + Verbindungsaufwand",
-        "allHint": "Zählt auch die Buchhaltung, die Ihr Treiber zwischen Abfragen sendet: BEGIN und COMMIT, SET und die Prüfungen, die eine gepoolte Verbindung am Leben halten."
+        "allHint": "Zählt auch die Buchhaltung, die Ihr Treiber zwischen Abfragen sendet: BEGIN und COMMIT, SET und die Prüfungen, die eine gepoolte Verbindung am Leben halten.",
+        "overheadLabel": "Verbindungsaufwand einbeziehen"
       },
       "completionBias": {
         "title": "Aufrufe sanken um {drop}%, während Fehlschläge stiegen",
@@ -15562,10 +15564,10 @@
       "noStream": "Wir konnten nicht ermitteln, welcher Trace-Stream diese Abfrage überträgt, daher können die Aufrufer und Beispiele nicht geladen werden.",
       "ambiguousStream": "Diese Organisation hat mehr als einen Trace-Stream, und wir können nicht feststellen, auf welchem diese Abfrage läuft. Wählen Sie einen Stream, um dessen Aufrufer und Beispiel-Traces zu laden.",
       "pickStream": "Trace-Stream auswählen",
-      "invalidStream": "Dieser Trace-Stream-Name wird von uns nicht erkannt, daher haben wir die Suche nicht ausgeführt.",
-      "firstSeen": "ZUM ERSTEN MAL IN DIESER LISTE UM {time}",
+      "invalidStream": "Dieser Trace-Stream-Name ist uns nicht bekannt, daher haben wir die Suche nicht ausgeführt.",
+      "firstSeen": "Erstmals erschienen in dieser Liste um {time}",
       "firstSeenHint": "Der früheste Punkt in diesem Zeitraum, an dem diese Abfrage schwer genug war, um eigenständig verfolgt zu werden.",
-      "lastSeen": "ZULETZT ERSCHIENEN UM {time}",
+      "lastSeen": "Zuletzt erschienen um {time}",
       "lastSeenHint": "Der letzte Punkt in diesem Zeitraum, an dem diese Abfrage schwer genug war, um eigenständig verfolgt zu werden. Sie läuft möglicherweise noch – dies ist das letzte Mal, dass sie in der Liste auftauchte.",
       "errorsByCode": {
         "title": "Fehler nach Ursachencode",
@@ -15732,9 +15734,6 @@
       }
     },
     "samples": {
-      "title": "Langsamste Aufrufe",
-      "subtitle": "Die langsamsten einzelnen Datenbankaufrufe in diesem Zeitraum, über alle Datenbanken und Abfragen hinweg — so wie ihre Aufrufer sie gemessen haben, nur abgeschlossene Aufrufe.",
-      "subtitleServer": "Die langsamsten Anweisungen, die Ihre Datenbanken in diesem Zeitraum abgeschlossen haben, innerhalb der Datenbank gemessen.",
       "logOffTitle": "Ihre Datenbanken protokollieren noch keine langsamen Anweisungen.",
       "logOffDescription": "Nichts hat diese Aufrufe getracet, also haben wir die Datenbanken nach ihren eigenen langsamsten Anweisungen gefragt — und sie protokollieren keine. Diese Liste stammt aus dem Anweisungsprotokoll von PostgreSQL (log_min_duration_statement), das standardmäßig deaktiviert ist — der Protokollierungsschritt der Setup-Karte aktiviert es. Die Erfassung pro Anweisung ist derzeit nur für PostgreSQL verfügbar, daher können MySQL und MariaDB diese Liste noch nicht speisen. Top-Abfragen können Ihnen weiterhin zeigen, was am häufigsten läuft, da sie stattdessen die Zähltabellen liest.",
       "searchPlaceholder": "Abfragetext, Dienst oder Datenbank suchen…",
@@ -15776,16 +15775,14 @@
       }
     },
     "rowChips": {
-      "new": "NEU IN DIESER LISTE",
-      "slower": "{ratio}× LANGSAMER ALS ÜBLICH",
-      "loop": "LÄUFT {multiplier}× PRO ANFRAGE",
-      "allFailed": "ALLE {count} FEHLGESCHLAGEN",
-      "busier": "BESCHÄFTIGTER, NICHT LANGSAMER",
-      "climbed": "NEUE SCHWERSTE ABFRAGE"
+      "new": "Neu in dieser Liste",
+      "slower": "{ratio}× langsamer als üblich",
+      "loop": "Läuft {multiplier}× pro Anfrage",
+      "allFailed": "Alle {count} fehlgeschlagen",
+      "busier": "Stärker ausgelastet, nicht langsamer",
+      "climbed": "Neue schwerste Abfrage"
     },
     "deadlocks": {
-      "title": "Datenbanken",
-      "subtitle": "Transaktionen, die sich gegenseitig blockiert haben und zurückgerollt wurden",
       "deadlockCount": "keine Deadlocks | {count} Deadlock | {count} Deadlocks",
       "pairCount": "keine Abfragepaare | {count} Abfragepaar | {count} Abfragepaare",
       "searchPlaceholder": "Abfragetext, Tabelle oder Anwendung suchen…",
@@ -15812,10 +15809,10 @@
         "mysqlPartial": "Ihre MySQL-Datenbank speichert nur den neuesten Deadlock, sofern innodb_print_all_deadlocks nicht aktiviert ist."
       },
       "chips": {
-        "oppositeOrder": "UMGEKEHRTE ZEILENREIHENFOLGE",
-        "cadence": "ALLE {seconds}s",
-        "noQueries": "ABFRAGEN NICHT ERFASST",
-        "partial": "NUR EINE SEITE"
+        "oppositeOrder": "Umgekehrte Zeilenreihenfolge",
+        "cadence": "Alle {seconds}s",
+        "noQueries": "Abfragen nicht erfasst",
+        "partial": "Nur eine Seite"
       },
       "rowActions": {
         "topQueries": "Finden Sie diese in den Top-Abfragen",
@@ -15832,7 +15829,7 @@
         "pid": "pid {pid}",
         "thread": "thread {pid}",
         "transaction": "Transaktion {id}",
-        "eachWaits": "jede wartet auf die andere",
+        "eachWaits": "Jede wartet auf die andere",
         "deadlock": "Deadlock",
         "noQueryCaptured": "Das Protokoll hat diesen Deadlock aufgezeichnet, aber nicht die Anweisung.",
         "waitingFor": "Wartete auf das Ende von {target}.",
@@ -15935,9 +15932,6 @@
       }
     },
     "blocked": {
-      "title": "Datenbanken",
-      "subtitle": "Sitzungen, die derzeit auf eine Sperre warten",
-      "subtitlePast": "Sitzungen, die in diesem Zeitraum auf eine Sperre gewartet haben",
       "waitingCount": "keine Sitzungen warten auf eine Sperre | {count} Sitzung wartet gerade auf eine Sperre | {count} Sitzungen warten gerade auf eine Sperre",
       "waitingCountPast": "keine Sitzungen haben auf eine Sperre gewartet | {count} Sitzung hat in diesem Zeitraum auf eine Sperre gewartet | {count} Sitzungen haben in diesem Zeitraum auf eine Sperre gewartet",
       "sessionCount": "keine Sitzungen | {count} Sitzung | {count} Sitzungen",
@@ -15983,14 +15977,14 @@
       },
       "deep": "{count} tief",
       "notWaiting": "wartet nicht",
-      "longestWait": "LÄNGSTE WARTEZEIT",
-      "holdsTheLock": "HÄLT DIE SPERRE, DIE ALLE WOLLEN",
+      "longestWait": "Längste Wartezeit",
+      "holdsTheLock": "Hält die Sperre, die alle wollen",
       "lastStatement": "letzte Anweisung vor {age}",
       "idleInTransaction": "wartet auf nichts · im Leerlauf in einer offenen Transaktion",
       "pills": {
-        "root": "WURZEL — HIER STARTEN",
-        "waitingBlocking": "WARTET & BLOCKIERT {count}",
-        "waiting": "WARTET"
+        "root": "Ursprung – hier beginnen",
+        "waitingBlocking": "Wartet & blockiert {count}",
+        "waiting": "Wartend"
       },
       "waitingOn": "wartet auf {pid}",
       "degraded": "Eine Sitzung hält {count} auf. Sie hat seit {age} eine offene Transaktion und in dieser Zeit keine Abfrage ausgeführt — höchstwahrscheinlich eine Anwendung, die vergessen hat, einen Commit auszuführen.",
@@ -16068,9 +16062,6 @@
       }
     },
     "activity": {
-      "title": "Aktivität",
-      "subtitle": "Was Ihre Datenbanken gerade tun, abgetastet aus ihren eigenen Sitzungstabellen.",
-      "subtitlePast": "Was Ihre Datenbanken während dieses Zeitfensters taten, abgetastet aus ihren eigenen Sitzungstabellen.",
       "searchPlaceholder": "Sitzungen, Benutzer, Anwendungen durchsuchen",
       "wait": {
         "onCpu": "Auf CPU / keine Wartezeit",
@@ -16194,8 +16185,6 @@
       "deadlockLabel": "Deadlocks"
     },
     "tableHealth": {
-      "title": "Tabellenzustand",
-      "subtitle": "Größe, Bloat und Vakuum-Status für jede Tabelle, neuester Snapshot zuerst.",
       "searchPlaceholder": "Tabellen durchsuchen",
       "never": "Nie",
       "countersCumulative": "Scan- und Vakuum-Zähler sind Gesamtwerte seit dem letzten Statistik-Reset, keine Zähler für den ausgewählten Zeitraum.",
diff --git a/web/src/locales/languages/en-US.json b/web/src/locales/languages/en-US.json
index 5cdeb2338e2..1903c08f6f0 100644
--- a/web/src/locales/languages/en-US.json
+++ b/web/src/locales/languages/en-US.json
@@ -14923,6 +14923,7 @@
       "disabled": "Disabled",
       "emptyTitle": "No alerts on this SLO yet",
       "emptyHint": "Burn-rate alerts page you while there is still budget left to protect.",
+      "emptyActionDesc": "Get warned before the budget runs out",
       "loadFailed": "Could not load this SLO's alerts.",
       "editTargetMissing": "That alert is no longer on this SLO.",
       "describeBurn": "Burn rate > {critical} over {long} and {short}",
@@ -15215,7 +15216,12 @@
     "common": {
       "reload": "Refresh",
       "lastRefreshed": "Last refreshed {age}",
-      "loadFailed": "Could not load database data. Try again, or narrow the time range."
+      "loadFailed": "Could not load database data. Try again, or narrow the time range.",
+      "actions": "Actions"
+    },
+    "header": {
+      "title": "Databases",
+      "subtitle": "Query performance and health across your databases"
     },
     "ai": {
       "suggestFix": "Ask AI how to fix this",
@@ -15237,12 +15243,12 @@
       "removeScope": "Remove this filter",
       "popoverTitle": "Narrow this down",
       "dimension": {
-        "system": "engine",
-        "instance": "database",
-        "namespace": "schema",
-        "env": "environment",
-        "service": "service",
-        "insight": "insight"
+        "system": "Engine",
+        "instance": "Database",
+        "namespace": "Schema",
+        "env": "Environment",
+        "service": "Service",
+        "insight": "Insight"
       }
     },
     "delta": {
@@ -15257,8 +15263,6 @@
       "wasValue": "was {value}"
     },
     "databases": {
-      "title": "Databases",
-      "subtitle": "Query performance and health per database",
       "searchPlaceholder": "Search database or service…",
       "unnamedInstance": "Instance not named",
       "unnamedInstanceHint": "The database's own statement reports prove this engine is running, but they don't name a host. The row will show an address once a feed that carries one — session sampling or instance metrics — is set up.",
@@ -15305,9 +15309,6 @@
       }
     },
     "queries": {
-      "title": "Top queries",
-      "subtitle": "The queries using the most database time",
-      "subtitleServer": "The statements your databases ran, counted inside the database — most frequently run first",
       "searchPlaceholder": "Search query text, table or service…",
       "trendLabel": "How this query's time has moved across the range",
       "otherRow": "All other queries",
@@ -15316,7 +15317,8 @@
         "query": "Queries",
         "queryHint": "Only real SQL — SELECT, INSERT, UPDATE and DELETE.",
         "all": "Queries + connection overhead",
-        "allHint": "Also counts the bookkeeping your driver sends between queries: BEGIN and COMMIT, SET, and the checks that keep a pooled connection alive."
+        "allHint": "Also counts the bookkeeping your driver sends between queries: BEGIN and COMMIT, SET, and the checks that keep a pooled connection alive.",
+        "overheadLabel": "Include connection overhead"
       },
       "completionBias": {
         "title": "Calls dropped {drop}% while failures rose",
@@ -15563,9 +15565,9 @@
       "ambiguousStream": "This organization has more than one trace stream, and we can't tell which one this query runs on. Pick a stream to load its callers and sample traces.",
       "pickStream": "Choose a trace stream",
       "invalidStream": "That trace stream name isn't one we recognize, so we didn't run the search.",
-      "firstSeen": "FIRST APPEARED IN THIS LIST AT {time}",
+      "firstSeen": "First appeared in this list at {time}",
       "firstSeenHint": "The earliest point in this range where this query was heavy enough to track on its own.",
-      "lastSeen": "LAST APPEARED AT {time}",
+      "lastSeen": "Last appeared at {time}",
       "lastSeenHint": "The most recent point in this range where this query was heavy enough to track on its own. It may still be running now — this is the last time it made the list.",
       "errorsByCode": {
         "title": "Failures by reason code",
@@ -15732,9 +15734,6 @@
       }
     },
     "samples": {
-      "title": "Slowest calls",
-      "subtitle": "The slowest individual database calls in this window, across every database and query — as their callers measured them, finished calls only",
-      "subtitleServer": "The slowest statements your databases completed in this window, measured inside the database",
       "logOffTitle": "Your databases are not logging slow statements yet",
       "logOffDescription": "Nothing traced these calls, so we asked the databases for their own slowest statements — and they log none. This list comes from PostgreSQL's statement log (log_min_duration_statement), which is off by default — the setup card's logging step turns it on. Per-statement capture is currently PostgreSQL-only, so MySQL and MariaDB can't feed this list yet. Top queries can still show you what runs most, because it reads the counter tables instead.",
       "searchPlaceholder": "Search query text, service or database…",
@@ -15776,16 +15775,14 @@
       }
     },
     "rowChips": {
-      "new": "NEW TO THIS LIST",
-      "slower": "{ratio}× SLOWER THAN USUAL",
-      "loop": "RUNS {multiplier}× PER REQUEST",
-      "allFailed": "ALL {count} FAILED",
-      "busier": "BUSIER, NOT SLOWER",
-      "climbed": "NEW HEAVIEST QUERY"
+      "new": "New to this list",
+      "slower": "{ratio}× slower than usual",
+      "loop": "Runs {multiplier}× per request",
+      "allFailed": "All {count} failed",
+      "busier": "Busier, not slower",
+      "climbed": "New heaviest query"
     },
     "deadlocks": {
-      "title": "Databases",
-      "subtitle": "Transactions that blocked each other and were rolled back",
       "deadlockCount": "no deadlocks | {count} deadlock | {count} deadlocks",
       "pairCount": "no query pairs | {count} query pair | {count} query pairs",
       "searchPlaceholder": "Search query text, table or application…",
@@ -15812,10 +15809,10 @@
         "mysqlPartial": "Your MySQL database keeps only the most recent deadlock unless innodb_print_all_deadlocks is on."
       },
       "chips": {
-        "oppositeOrder": "OPPOSITE ROW ORDER",
-        "cadence": "EVERY {seconds}s",
-        "noQueries": "QUERIES NOT CAPTURED",
-        "partial": "ONE SIDE ONLY"
+        "oppositeOrder": "Opposite row order",
+        "cadence": "Every {seconds}s",
+        "noQueries": "Queries not captured",
+        "partial": "One side only"
       },
       "rowActions": {
         "topQueries": "Find these in Top queries",
@@ -15832,8 +15829,8 @@
         "pid": "pid {pid}",
         "thread": "thread {pid}",
         "transaction": "transaction {id}",
-        "eachWaits": "each waits for the other",
-        "deadlock": "deadlock",
+        "eachWaits": "Each waits for the other",
+        "deadlock": "Deadlock",
         "noQueryCaptured": "The log recorded this deadlock but not the statement.",
         "waitingFor": "Was waiting for {target} to finish.",
         "waitingForRow": "Was waiting for {target} to finish — the other session had already changed the row it wanted.",
@@ -15935,9 +15932,6 @@
       }
     },
     "blocked": {
-      "title": "Databases",
-      "subtitle": "Sessions currently waiting on a lock",
-      "subtitlePast": "Sessions that were waiting on a lock during this window",
       "waitingCount": "no sessions waiting on a lock | {count} session waiting on a lock right now | {count} sessions waiting on a lock right now",
       "waitingCountPast": "no sessions waited on a lock | {count} session waited on a lock in this window | {count} sessions waited on a lock in this window",
       "sessionCount": "no sessions | {count} session | {count} sessions",
@@ -15983,14 +15977,14 @@
       },
       "deep": "{count} deep",
       "notWaiting": "not waiting",
-      "longestWait": "LONGEST WAIT",
-      "holdsTheLock": "HOLDS THE LOCK EVERYONE WANTS",
+      "longestWait": "Longest wait",
+      "holdsTheLock": "Holds the lock everyone wants",
       "lastStatement": "last statement {age} ago",
       "idleInTransaction": "not waiting for anything · idle in an open transaction",
       "pills": {
-        "root": "ROOT — START HERE",
-        "waitingBlocking": "WAITING & BLOCKING {count}",
-        "waiting": "WAITING"
+        "root": "Root — start here",
+        "waitingBlocking": "Waiting & blocking {count}",
+        "waiting": "Waiting"
       },
       "waitingOn": "waiting on {pid}",
       "degraded": "One session is holding up {count}. It has had an open transaction for {age} and hasn't run a query in that time — most likely an application that forgot to commit.",
@@ -16068,9 +16062,6 @@
       }
     },
     "activity": {
-      "title": "Activity",
-      "subtitle": "What your databases are doing right now, sampled from their own session tables.",
-      "subtitlePast": "What your databases were doing during this window, sampled from their own session tables.",
       "searchPlaceholder": "Search sessions, users, applications",
       "wait": {
         "onCpu": "On CPU / no wait",
@@ -16194,8 +16185,6 @@
       "deadlockLabel": "deadlocks"
     },
     "tableHealth": {
-      "title": "Table health",
-      "subtitle": "Size, bloat and vacuum state for every table, newest snapshot first.",
       "searchPlaceholder": "Search tables",
       "never": "Never",
       "countersCumulative": "Scan and vacuum counts are lifetime totals since the last statistics reset, not counts for the selected time range.",
diff --git a/web/src/locales/languages/es-ES.json b/web/src/locales/languages/es-ES.json
index a9c5c316054..5c7a4b9bec3 100644
--- a/web/src/locales/languages/es-ES.json
+++ b/web/src/locales/languages/es-ES.json
@@ -14923,6 +14923,7 @@
       "disabled": "Deshabilitado",
       "emptyTitle": "Este SLO aún no tiene alertas",
       "emptyHint": "Las alertas de tasa de consumo te avisan mientras todavía queda presupuesto que proteger.",
+      "emptyActionDesc": "Recibe una advertencia antes de que se agote el presupuesto",
       "loadFailed": "No se pudieron cargar las alertas de este SLO.",
       "editTargetMissing": "Esa alerta ya no está en este SLO.",
       "describeBurn": "Tasa de consumo > {critical} en {long} y {short}",
@@ -15215,7 +15216,12 @@
     "common": {
       "reload": "Actualizar",
       "lastRefreshed": "Última actualización {age}",
-      "loadFailed": "No se pudieron cargar los datos de la base de datos. Vuelva a intentarlo o reduzca el intervalo de tiempo."
+      "loadFailed": "No se pudieron cargar los datos de la base de datos. Vuelva a intentarlo o reduzca el intervalo de tiempo.",
+      "actions": "Acciones"
+    },
+    "header": {
+      "title": "Bases de datos",
+      "subtitle": "Rendimiento y salud de las consultas en tus bases de datos"
     },
     "ai": {
       "suggestFix": "Preguntar a la IA cómo solucionar esto",
@@ -15237,12 +15243,12 @@
       "removeScope": "Eliminar este filtro",
       "popoverTitle": "Reducir esto",
       "dimension": {
-        "system": "motor",
-        "instance": "base de datos",
-        "namespace": "esquema",
-        "env": "entorno",
-        "service": "servicio",
-        "insight": "información"
+        "system": "Motor",
+        "instance": "Base de datos",
+        "namespace": "Esquema",
+        "env": "Entorno",
+        "service": "Servicio",
+        "insight": "Análisis"
       }
     },
     "delta": {
@@ -15257,8 +15263,6 @@
       "wasValue": "era {value}"
     },
     "databases": {
-      "title": "Bases de datos",
-      "subtitle": "Rendimiento y salud de consultas por base de datos",
       "searchPlaceholder": "Buscar base de datos o servicio…",
       "unnamedInstance": "Instancia sin nombre",
       "unnamedInstanceHint": "Los reportes de sentencias de la propia base de datos demuestran que este motor está en ejecución, pero no nombran un host. La fila mostrará una dirección una vez que se configure una fuente que la incluya — muestreo de sesión o métricas de instancia —.",
@@ -15305,9 +15309,6 @@
       }
     },
     "queries": {
-      "title": "Consultas principales",
-      "subtitle": "Las consultas que más tiempo de base de datos usan",
-      "subtitleServer": "Las sentencias que ejecutaron sus bases de datos, contadas dentro de la base de datos — las ejecutadas con más frecuencia primero",
       "searchPlaceholder": "Buscar texto de consulta, tabla o servicio…",
       "trendLabel": "Cómo se ha movido el tiempo de esta consulta a lo largo del rango",
       "otherRow": "Todas las demás consultas",
@@ -15316,7 +15317,8 @@
         "query": "Consultas",
         "queryHint": "Solo SQL real — SELECT, INSERT, UPDATE y DELETE.",
         "all": "Consultas + sobrecarga de conexión",
-        "allHint": "También cuenta el trabajo de contabilidad que su controlador envía entre consultas: BEGIN y COMMIT, SET y las comprobaciones que mantienen viva una conexión agrupada."
+        "allHint": "También cuenta el trabajo de contabilidad que su controlador envía entre consultas: BEGIN y COMMIT, SET y las comprobaciones que mantienen viva una conexión agrupada.",
+        "overheadLabel": "Incluir la sobrecarga de conexión"
       },
       "completionBias": {
         "title": "Las llamadas cayeron un {drop}% mientras los fallos aumentaron",
@@ -15559,13 +15561,13 @@
       "noSamples": "No se cargaron ejecuciones individuales para este rango.",
       "noSeries": "No hay historial para esta consulta en el rango que seleccionaste.",
       "liveSeries": "Los puntos más recientes aún se están contando y pueden cambiar.",
-      "noStream": "No pudimos determinar qué stream de trazas transporta esta consulta, por lo que sus llamadores y ejemplos no se pueden cargar.",
-      "ambiguousStream": "Esta organización tiene más de un stream de trazas y no podemos saber en cuál se ejecuta esta consulta. Elige un stream para cargar sus llamadores y trazas de muestra.",
-      "pickStream": "Elige un stream de trazas",
-      "invalidStream": "Ese nombre de stream de trazas no es uno que reconozcamos, por lo que no ejecutamos la búsqueda.",
-      "firstSeen": "PRIMERA APARICIÓN EN ESTA LISTA A LAS {time}",
+      "noStream": "No pudimos determinar qué flujo de trazas transporta esta consulta, por lo que sus llamantes y ejemplos no se pueden cargar.",
+      "ambiguousStream": "Esta organización tiene más de un flujo de trazas y no podemos saber en cuál se ejecuta esta consulta. Elige un flujo para cargar sus llamantes y trazas de muestra.",
+      "pickStream": "Elige un flujo de trazas",
+      "invalidStream": "Ese nombre de flujo de trazas no es reconocido, por lo que no ejecutamos la búsqueda.",
+      "firstSeen": "Apareció por primera vez en esta lista en {time}",
       "firstSeenHint": "El punto más temprano en este rango donde esta consulta fue lo suficientemente pesada como para rastrearse por sí sola.",
-      "lastSeen": "ÚLTIMA APARICIÓN A LAS {time}",
+      "lastSeen": "Apareció por última vez en {time}",
       "lastSeenHint": "El punto más reciente en este rango donde esta consulta fue lo suficientemente pesada como para rastrearse por sí sola. Puede que aún se esté ejecutando ahora — esta es la última vez que entró en la lista.",
       "errorsByCode": {
         "title": "Fallos por código de motivo",
@@ -15732,9 +15734,6 @@
       }
     },
     "samples": {
-      "title": "Llamadas más lentas",
-      "subtitle": "Las llamadas individuales a base de datos más lentas en esta ventana, en todas las bases de datos y consultas — tal como las midieron quienes las llamaron, solo llamadas finalizadas",
-      "subtitleServer": "Las declaraciones más lentas que sus bases de datos completaron en esta ventana, medidas dentro de la base de datos",
       "logOffTitle": "Sus bases de datos aún no registran declaraciones lentas",
       "logOffDescription": "Nada rastreó estas llamadas, así que preguntamos a las bases de datos por sus propias declaraciones más lentas — y no registran ninguna. Esta lista proviene del registro de declaraciones de PostgreSQL (log_min_duration_statement), que está desactivado por defecto — el paso de registro de la tarjeta de configuración lo activa. La captura por declaración actualmente es solo de PostgreSQL, por lo que MySQL y MariaDB no pueden alimentar esta lista aún. Las consultas principales pueden mostrarle qué se ejecuta más, porque leen las tablas de contadores en su lugar.",
       "searchPlaceholder": "Buscar texto de consulta, servicio o base de datos…",
@@ -15776,16 +15775,14 @@
       }
     },
     "rowChips": {
-      "new": "NUEVO EN ESTA LISTA",
-      "slower": "{ratio}× MÁS LENTO DE LO HABITUAL",
-      "loop": "SE EJECUTA {multiplier}× POR SOLICITUD",
-      "allFailed": "TODAS LAS {count} FALLARON",
-      "busier": "MÁS OCUPADO, NO MÁS LENTO",
-      "climbed": "NUEVA CONSULTA MÁS PESADA"
+      "new": "Nuevo en esta lista",
+      "slower": "{ratio}× más lento de lo habitual",
+      "loop": "Se ejecuta {multiplier}× por solicitud",
+      "allFailed": "Todos los {count} fallaron",
+      "busier": "Más ocupado, no más lento",
+      "climbed": "Nueva consulta más pesada"
     },
     "deadlocks": {
-      "title": "Bases de datos",
-      "subtitle": "Transacciones que se bloquearon entre sí y se revirtieron",
       "deadlockCount": "sin interbloqueos | {count} interbloqueo | {count} interbloqueos",
       "pairCount": "sin pares de consultas | {count} par de consultas | {count} pares de consultas",
       "searchPlaceholder": "Buscar texto de consulta, tabla o aplicación…",
@@ -15812,10 +15809,10 @@
         "mysqlPartial": "Tu base de datos MySQL guarda solo el interbloqueo más reciente a menos que innodb_print_all_deadlocks esté activado."
       },
       "chips": {
-        "oppositeOrder": "ORDEN OPUESTO DE FILAS",
-        "cadence": "CADA {seconds}s",
-        "noQueries": "CONSULTAS NO CAPTURADAS",
-        "partial": "SOLO UN LADO"
+        "oppositeOrder": "Orden de filas opuesto",
+        "cadence": "Cada {seconds} s",
+        "noQueries": "Consultas no capturadas",
+        "partial": "Solo un lado"
       },
       "rowActions": {
         "topQueries": "Encuentra estos en Consultas principales.",
@@ -15832,8 +15829,8 @@
         "pid": "pid {pid}",
         "thread": "hilo {pid}",
         "transaction": "transacción {id}",
-        "eachWaits": "cada uno espera al otro",
-        "deadlock": "interbloqueo",
+        "eachWaits": "Cada uno espera al otro",
+        "deadlock": "Interbloqueo",
         "noQueryCaptured": "El registro capturó este interbloqueo pero no la sentencia.",
         "waitingFor": "Estaba esperando que {target} terminara.",
         "waitingForRow": "Estaba esperando que {target} terminara — la otra sesión ya había cambiado la fila que quería.",
@@ -15935,9 +15932,6 @@
       }
     },
     "blocked": {
-      "title": "Bases de datos",
-      "subtitle": "Sesiones que esperan actualmente un bloqueo",
-      "subtitlePast": "Sesiones que esperaban un bloqueo durante esta ventana",
       "waitingCount": "ninguna sesión esperando un bloqueo | {count} sesión esperando un bloqueo ahora mismo | {count} sesiones esperando un bloqueo ahora mismo",
       "waitingCountPast": "ninguna sesión esperó un bloqueo | {count} sesión esperó un bloqueo en esta ventana | {count} sesiones esperaron un bloqueo en esta ventana",
       "sessionCount": "ninguna sesión | {count} sesión | {count} sesiones",
@@ -15983,14 +15977,14 @@
       },
       "deep": "{count} de profundidad",
       "notWaiting": "no esperando",
-      "longestWait": "ESPERA MÁS LARGA",
-      "holdsTheLock": "TIENE EL BLOQUEO QUE TODOS QUIEREN",
+      "longestWait": "Espera más larga",
+      "holdsTheLock": "Mantiene el bloqueo que todos quieren",
       "lastStatement": "última sentencia hace {age}",
       "idleInTransaction": "no esperando nada · inactivo en una transacción abierta",
       "pills": {
-        "root": "RAÍZ — EMPIEZA AQUÍ",
-        "waitingBlocking": "ESPERANDO Y BLOQUEANDO {count}",
-        "waiting": "ESPERANDO"
+        "root": "Raíz — comienza aquí",
+        "waitingBlocking": "Esperando y bloqueando {count}",
+        "waiting": "Esperando"
       },
       "waitingOn": "esperando a {pid}",
       "degraded": "Una sesión está bloqueando {count}. Ha tenido una transacción abierta durante {age} y no ha ejecutado una consulta en ese tiempo — muy probablemente una aplicación que olvidó confirmar.",
@@ -16068,9 +16062,6 @@
       }
     },
     "activity": {
-      "title": "Actividad",
-      "subtitle": "Lo que tus bases de datos están haciendo ahora mismo, muestreado de sus propias tablas de sesiones.",
-      "subtitlePast": "Lo que tus bases de datos estaban haciendo durante esta ventana, muestreado de sus propias tablas de sesiones.",
       "searchPlaceholder": "Buscar sesiones, usuarios, aplicaciones",
       "wait": {
         "onCpu": "En CPU / sin espera",
@@ -16194,8 +16185,6 @@
       "deadlockLabel": "interbloqueos"
     },
     "tableHealth": {
-      "title": "Salud de las tablas",
-      "subtitle": "Tamaño, bloat y estado de vacuum para cada tabla, primero la instantánea más reciente.",
       "searchPlaceholder": "Buscar tablas",
       "never": "Nunca",
       "countersCumulative": "Los recuentos de escaneo y vacuum son totales de por vida desde el último restablecimiento de estadísticas, no recuentos del rango de tiempo seleccionado.",
diff --git a/web/src/locales/languages/fr-FR.json b/web/src/locales/languages/fr-FR.json
index b6ee06b0d95..f47f5df9716 100644
--- a/web/src/locales/languages/fr-FR.json
+++ b/web/src/locales/languages/fr-FR.json
@@ -14923,6 +14923,7 @@
       "disabled": "Désactivé",
       "emptyTitle": "Aucune alerte sur ce SLO pour le moment.",
       "emptyHint": "Les alertes de taux de combustion vous préviennent tant qu'il reste encore du budget à protéger.",
+      "emptyActionDesc": "Soyez averti avant l'épuisement du budget",
       "loadFailed": "Impossible de charger les alertes de ce SLO.",
       "editTargetMissing": "Cette alerte ne fait plus partie de ce SLO.",
       "describeBurn": "Taux de combustion > {critical} sur {long} et {short}",
@@ -15215,7 +15216,12 @@
     "common": {
       "reload": "Actualiser",
       "lastRefreshed": "Dernière actualisation il y a {age}",
-      "loadFailed": "Impossible de charger les données de la base. Réessayez ou réduisez la plage de temps."
+      "loadFailed": "Impossible de charger les données de la base. Réessayez ou réduisez la plage de temps.",
+      "actions": "Actions"
+    },
+    "header": {
+      "title": "Bases de données",
+      "subtitle": "Performances et santé des requêtes sur vos bases de données"
     },
     "ai": {
       "suggestFix": "Demander à l'IA comment corriger ceci",
@@ -15237,12 +15243,12 @@
       "removeScope": "Supprimer ce filtre",
       "popoverTitle": "Affiner",
       "dimension": {
-        "system": "moteur",
-        "instance": "base de données",
-        "namespace": "schéma",
-        "env": "environnement",
-        "service": "service",
-        "insight": "aperçu"
+        "system": "Moteur",
+        "instance": "Base de données",
+        "namespace": "Schéma",
+        "env": "Environnement",
+        "service": "Service",
+        "insight": "Aperçu"
       }
     },
     "delta": {
@@ -15257,8 +15263,6 @@
       "wasValue": "était {value}"
     },
     "databases": {
-      "title": "Bases de données",
-      "subtitle": "Performance et santé des requêtes par base de données",
       "searchPlaceholder": "Rechercher une base de données ou un service…",
       "unnamedInstance": "Instance non nommée",
       "unnamedInstanceHint": "Les rapports d'instructions de la base de données prouvent que ce moteur est en cours d'exécution, mais ils ne nomment pas d'hôte. La ligne affichera une adresse dès qu'un flux qui en contient — échantillonnage de session ou métriques d'instance — sera configuré.",
@@ -15305,9 +15309,6 @@
       }
     },
     "queries": {
-      "title": "Requêtes principales",
-      "subtitle": "Les requêtes qui utilisent le plus de temps de base de données",
-      "subtitleServer": "Les instructions exécutées par vos bases de données, comptées dans la base de données — les plus fréquemment exécutées en premier",
       "searchPlaceholder": "Rechercher un texte de requête, une table ou un service…",
       "trendLabel": "Comment le temps de cette requête a évolué sur la période",
       "otherRow": "Toutes les autres requêtes",
@@ -15316,7 +15317,8 @@
         "query": "Requêtes",
         "queryHint": "Uniquement du vrai SQL — SELECT, INSERT, UPDATE et DELETE.",
         "all": "Requêtes + surcharge de connexion",
-        "allHint": "Compte également les opérations de gestion que votre pilote envoie entre les requêtes : BEGIN et COMMIT, SET, et les vérifications qui maintiennent une connexion de pool en vie."
+        "allHint": "Compte également les opérations de gestion que votre pilote envoie entre les requêtes : BEGIN et COMMIT, SET, et les vérifications qui maintiennent une connexion de pool en vie.",
+        "overheadLabel": "Inclure la surcharge de connexion"
       },
       "completionBias": {
         "title": "Les appels ont chuté de {drop} % tandis que les échecs augmentaient",
@@ -15559,13 +15561,13 @@
       "noSamples": "Aucune exécution individuelle chargée pour cette plage.",
       "noSeries": "Aucun historique pour cette requête dans la plage que vous avez choisie.",
       "liveSeries": "Les points les plus récents sont encore en cours de calcul et peuvent changer.",
-      "noStream": "Nous n'avons pas pu déterminer quel flux de traces transporte cette requête, donc ses appelants et exemples ne peuvent pas être chargés.",
-      "ambiguousStream": "Cette organisation a plus d'un flux de traces, et nous ne pouvons pas déterminer sur lequel cette requête s'exécute. Choisissez un flux pour charger ses appelants et des exemples de traces.",
-      "pickStream": "Choisir un flux de traces",
-      "invalidStream": "Ce nom de flux de traces n'est pas reconnu, nous n'avons donc pas effectué la recherche.",
-      "firstSeen": "PREMIÈRE APPARITION DANS CETTE LISTE À {time}",
+      "noStream": "Nous n'avons pas pu déterminer quel flux de trace transporte cette requête, de sorte que ses appelants et exemples ne peuvent pas être chargés.",
+      "ambiguousStream": "Cette organisation possède plus d'un flux de trace, et nous ne pouvons pas déterminer sur lequel s'exécute cette requête. Choisissez un flux pour charger ses appelants et des exemples de traces.",
+      "pickStream": "Choisir un flux de trace",
+      "invalidStream": "Ce nom de flux de trace n'est pas reconnu, c'est pourquoi nous n'avons pas effectué la recherche.",
+      "firstSeen": "Première apparition dans cette liste à {time}",
       "firstSeenHint": "Le point le plus ancien de cette plage où cette requête était assez lourde pour être suivie individuellement.",
-      "lastSeen": "DERNIÈRE APPARITION À {time}",
+      "lastSeen": "Dernière apparition à {time}",
       "lastSeenHint": "Le point le plus récent de cette plage où cette requête était assez lourde pour être suivie individuellement. Elle peut encore être en cours d'exécution — c'est la dernière fois qu'elle a figuré dans la liste.",
       "errorsByCode": {
         "title": "Échecs par code de raison",
@@ -15732,9 +15734,6 @@
       }
     },
     "samples": {
-      "title": "Appels les plus lents",
-      "subtitle": "Les appels de base de données individuels les plus lents dans cette fenêtre, sur toutes les bases et toutes les requêtes — tels que mesurés par leurs appelants, uniquement les appels terminés",
-      "subtitleServer": "Les instructions les plus lentes que vos bases de données ont exécutées dans cette fenêtre, mesurées à l'intérieur de la base de données",
       "logOffTitle": "Vos bases de données ne journalisent pas encore les instructions lentes",
       "logOffDescription": "Aucune trace n'a été faite pour ces appels, nous avons donc demandé aux bases de données leurs propres instructions les plus lentes — et elles n'en journalisent aucune. Cette liste provient du journal des instructions de PostgreSQL (log_min_duration_statement), désactivé par défaut — l'étape de journalisation de la carte de configuration l'active. La capture par instruction est actuellement réservée à PostgreSQL, donc MySQL et MariaDB ne peuvent pas encore alimenter cette liste. Les requêtes principales peuvent toujours montrer ce qui s'exécute le plus, car elles lisent les tables de compteurs à la place.",
       "searchPlaceholder": "Rechercher du texte de requête, un service ou une base de données…",
@@ -15776,16 +15775,14 @@
       }
     },
     "rowChips": {
-      "new": "NOUVEAU DANS CETTE LISTE",
-      "slower": "{ratio}× PLUS LENT QUE D'HABITUDE",
-      "loop": "S'EXÉCUTE {multiplier}× PAR REQUÊTE",
-      "allFailed": "TOUS LES {count} ONT ÉCHOUÉ",
-      "busier": "PLUS OCCUPÉ, PAS PLUS LENT",
-      "climbed": "NOUVELLE REQUÊTE LA PLUS LOURDE"
+      "new": "Nouveau dans cette liste",
+      "slower": "{ratio}× plus lent que d'habitude",
+      "loop": "S'exécute {multiplier}× par requête",
+      "allFailed": "Tous les {count} ont échoué",
+      "busier": "Plus chargé, pas plus lent",
+      "climbed": "Nouvelle requête la plus lourde"
     },
     "deadlocks": {
-      "title": "Bases de données",
-      "subtitle": "Transactions qui se sont mutuellement bloquées et ont été annulées",
       "deadlockCount": "aucun interblocage | {count} interblocage | {count} interblocages",
       "pairCount": "aucune paire de requêtes | {count} paire de requêtes | {count} paires de requêtes",
       "searchPlaceholder": "Rechercher du texte de requête, une table ou une application…",
@@ -15812,10 +15809,10 @@
         "mysqlPartial": "Votre base de données MySQL ne conserve que le blocage le plus récent, sauf si innodb_print_all_deadlocks est activé."
       },
       "chips": {
-        "oppositeOrder": "ORDRE DE LIGNES OPPOSÉ",
-        "cadence": "TOUTES LES {seconds}s",
-        "noQueries": "REQUÊTES NON CAPTURÉES",
-        "partial": "UN SEUL CÔTÉ"
+        "oppositeOrder": "Ordre des lignes inverse",
+        "cadence": "Toutes les {seconds}s",
+        "noQueries": "Requêtes non capturées",
+        "partial": "Un côté uniquement"
       },
       "rowActions": {
         "topQueries": "Trouvez-les dans les requêtes principales",
@@ -15832,8 +15829,8 @@
         "pid": "pid {pid}",
         "thread": "thread {pid}",
         "transaction": "transaction {id}",
-        "eachWaits": "chacun attend l'autre",
-        "deadlock": "blocage",
+        "eachWaits": "Chacun attend l'autre",
+        "deadlock": "Interblocage",
         "noQueryCaptured": "Le journal a enregistré ce blocage mais pas l'instruction.",
         "waitingFor": "Attendait que {target} se termine.",
         "waitingForRow": "Attendait que {target} se termine — l'autre session avait déjà modifié la ligne voulue.",
@@ -15935,9 +15932,6 @@
       }
     },
     "blocked": {
-      "title": "Bases de données",
-      "subtitle": "Sessions en attente d'un verrou actuellement",
-      "subtitlePast": "Sessions qui étaient en attente d'un verrou pendant cette fenêtre",
       "waitingCount": "aucune session en attente d'un verrou | {count} session en attente d'un verrou actuellement | {count} sessions en attente d'un verrou actuellement",
       "waitingCountPast": "aucune session n'a attendu un verrou | {count} session a attendu un verrou dans cette fenêtre | {count} sessions ont attendu un verrou dans cette fenêtre",
       "sessionCount": "aucune session | {count} session | {count} sessions",
@@ -15983,14 +15977,14 @@
       },
       "deep": "{count} de profondeur",
       "notWaiting": "pas en attente",
-      "longestWait": "ATTENTE LA PLUS LONGUE",
-      "holdsTheLock": "DÉTIENT LE VERROU QUE TOUT LE MONDE VEUT",
+      "longestWait": "Attente la plus longue",
+      "holdsTheLock": "Détient le verrou que tout le monde veut",
       "lastStatement": "dernière instruction il y a {age}",
       "idleInTransaction": "n'attend rien · inactif dans une transaction ouverte",
       "pills": {
-        "root": "RACINE — COMMENCEZ ICI",
-        "waitingBlocking": "EN ATTENTE & BLOQUANT {count}",
-        "waiting": "EN ATTENTE"
+        "root": "Racine — commencez ici",
+        "waitingBlocking": "En attente et bloquant {count}",
+        "waiting": "En attente"
       },
       "waitingOn": "en attente de {pid}",
       "degraded": "Une session bloque {count}. Elle a une transaction ouverte depuis {age} et n'a pas exécuté de requête pendant ce temps — probablement une application qui a oublié de valider.",
@@ -16068,9 +16062,6 @@
       }
     },
     "activity": {
-      "title": "Activité",
-      "subtitle": "Ce que font vos bases de données en ce moment, échantillonné à partir de leurs propres tables de sessions.",
-      "subtitlePast": "Ce que faisaient vos bases de données pendant cette fenêtre, échantillonné à partir de leurs propres tables de sessions.",
       "searchPlaceholder": "Rechercher sessions, utilisateurs, applications",
       "wait": {
         "onCpu": "Sur CPU / sans attente",
@@ -16194,8 +16185,6 @@
       "deadlockLabel": "interblocages"
     },
     "tableHealth": {
-      "title": "Santé des tables",
-      "subtitle": "Taille, bloat et état du vacuum pour chaque table, instantané le plus récent en premier.",
       "searchPlaceholder": "Rechercher des tables",
       "never": "Jamais",
       "countersCumulative": "Les compteurs de scan et de vacuum sont des totaux cumulés depuis la dernière réinitialisation des statistiques, pas des compteurs pour la période sélectionnée.",
diff --git a/web/src/locales/languages/it-IT.json b/web/src/locales/languages/it-IT.json
index b3af717cad2..cec804b90aa 100644
--- a/web/src/locales/languages/it-IT.json
+++ b/web/src/locales/languages/it-IT.json
@@ -14923,6 +14923,7 @@
       "disabled": "Disabilitato",
       "emptyTitle": "Ancora nessun avviso su questo SLO",
       "emptyHint": "Gli avvisi di tasso di consumo ti avvisano finché c'è ancora budget rimasto da proteggere.",
+      "emptyActionDesc": "Ricevi un avviso prima che il budget si esaurisca",
       "loadFailed": "Impossibile caricare gli avvisi di questo SLO.",
       "editTargetMissing": "Questo avviso non è più su questo SLO.",
       "describeBurn": "Tasso di consumo > {critical} su {long} e {short}",
@@ -15215,7 +15216,12 @@
     "common": {
       "reload": "Aggiorna",
       "lastRefreshed": "Ultimo aggiornamento {age}",
-      "loadFailed": "Impossibile caricare i dati del database. Riprova oppure restringi l'intervallo di tempo."
+      "loadFailed": "Impossibile caricare i dati del database. Riprova oppure restringi l'intervallo di tempo.",
+      "actions": "Azioni"
+    },
+    "header": {
+      "title": "Database",
+      "subtitle": "Prestazioni e salute delle query sui tuoi database"
     },
     "ai": {
       "suggestFix": "Chiedi all'AI come risolvere",
@@ -15237,12 +15243,12 @@
       "removeScope": "Rimuovi questo filtro",
       "popoverTitle": "Restringi",
       "dimension": {
-        "system": "motore",
-        "instance": "database",
-        "namespace": "schema",
-        "env": "ambiente",
-        "service": "servizio",
-        "insight": "approfondimento"
+        "system": "Motore",
+        "instance": "Database",
+        "namespace": "Schema",
+        "env": "Ambiente",
+        "service": "Servizio",
+        "insight": "Insight"
       }
     },
     "delta": {
@@ -15257,8 +15263,6 @@
       "wasValue": "era {value}"
     },
     "databases": {
-      "title": "Database",
-      "subtitle": "Prestazioni e salute delle query per database",
       "searchPlaceholder": "Cerca database o servizio…",
       "unnamedInstance": "Istanza senza nome",
       "unnamedInstanceHint": "I report delle istruzioni del database dimostrano che questo motore è in esecuzione, ma non indicano un host. La riga mostrerà un indirizzo quando sarà configurato un feed che lo trasporta — campionamento della sessione o metriche dell'istanza.",
@@ -15305,9 +15309,6 @@
       }
     },
     "queries": {
-      "title": "Query principali",
-      "subtitle": "Le query che usano più tempo database",
-      "subtitleServer": "Le istruzioni eseguite dai tuoi database, contate all'interno del database — le più frequenti prima",
       "searchPlaceholder": "Cerca testo query, tabella o servizio…",
       "trendLabel": "Come il tempo di questa query è cambiato nell'intervallo",
       "otherRow": "Tutte le altre query",
@@ -15316,7 +15317,8 @@
         "query": "Query",
         "queryHint": "Solo SQL reale — SELECT, INSERT, UPDATE e DELETE.",
         "all": "Query + overhead di connessione",
-        "allHint": "Conta anche la contabilità che il tuo driver invia tra le query: BEGIN e COMMIT, SET, e i controlli che mantengono viva una connessione in pool."
+        "allHint": "Conta anche la contabilità che il tuo driver invia tra le query: BEGIN e COMMIT, SET, e i controlli che mantengono viva una connessione in pool.",
+        "overheadLabel": "Includi sovraccarico di connessione"
       },
       "completionBias": {
         "title": "Chiamate diminuite del {drop}% mentre i fallimenti sono aumentati",
@@ -15559,13 +15561,13 @@
       "noSamples": "Nessuna esecuzione individuale caricata per questo intervallo.",
       "noSeries": "Nessuna cronologia per questa query nell'intervallo scelto.",
       "liveSeries": "I punti più recenti sono ancora in fase di conteggio e potrebbero cambiare.",
-      "noStream": "Non siamo riusciti a determinare quale stream di tracce trasporta questa query, quindi i suoi chiamanti e gli esempi non possono essere caricati.",
-      "ambiguousStream": "Questa organizzazione ha più di uno stream di tracce e non possiamo dire su quale venga eseguita questa query. Scegli uno stream per caricare i suoi chiamanti e le tracce di esempio.",
-      "pickStream": "Scegli uno stream di tracce",
-      "invalidStream": "Il nome di quello stream di tracce non è riconosciuto, quindi non abbiamo eseguito la ricerca.",
-      "firstSeen": "PRIMA APPARIZIONE IN QUESTA LISTA ALLE {time}",
+      "noStream": "Non siamo riusciti a determinare quale flusso di trace trasporta questa query, quindi i suoi chiamanti ed esempi non possono essere caricati.",
+      "ambiguousStream": "Questa organizzazione ha più di un flusso di trace e non possiamo dire su quale venga eseguita questa query. Scegli un flusso per caricare i suoi chiamanti e trace di esempio.",
+      "pickStream": "Scegli un flusso di trace",
+      "invalidStream": "Quel nome di flusso di trace non è uno che riconosciamo, quindi non abbiamo eseguito la ricerca.",
+      "firstSeen": "È apparso per la prima volta in questo elenco alle {time}",
       "firstSeenHint": "Il punto più precoce in questo intervallo in cui questa query era abbastanza pesante da essere tracciata da sola.",
-      "lastSeen": "ULTIMA APPARIZIONE ALLE {time}",
+      "lastSeen": "Ultima apparizione alle {time}",
       "lastSeenHint": "Il punto più recente in questo intervallo in cui questa query era abbastanza pesante da essere tracciata da sola. Potrebbe essere ancora in esecuzione ora — questa è l'ultima volta che è apparsa nella lista.",
       "errorsByCode": {
         "title": "Errori per codice motivo",
@@ -15732,9 +15734,6 @@
       }
     },
     "samples": {
-      "title": "Chiamate più lente",
-      "subtitle": "Le singole chiamate di database più lente in questa finestra, su ogni database e query — come misurate dai chiamanti, solo chiamate completate.",
-      "subtitleServer": "Le istruzioni più lente completate dai tuoi database in questa finestra, misurate all'interno del database.",
       "logOffTitle": "I tuoi database non registrano ancora istruzioni lente.",
       "logOffDescription": "Nessuna traccia ha catturato queste chiamate, quindi abbiamo chiesto ai database le loro istruzioni più lente — e non ne registrano nessuna. Questa lista proviene dal log delle istruzioni di PostgreSQL (log_min_duration_statement), che è disattivato per impostazione predefinita — il passaggio di logging della scheda di configurazione lo attiva. La cattura per istruzione è attualmente solo per PostgreSQL, quindi MySQL e MariaDB non possono ancora alimentare questa lista. Le query principali possono comunque mostrarti cosa viene eseguito di più, perché legge invece le tabelle dei contatori.",
       "searchPlaceholder": "Cerca testo query, servizio o database…",
@@ -15776,16 +15775,14 @@
       }
     },
     "rowChips": {
-      "new": "NUOVO IN QUESTO ELENCO",
-      "slower": "{ratio}× PIÙ LENTO DEL SOLITO",
-      "loop": "ESEGUE {multiplier}× PER RICHIESTA",
-      "allFailed": "TUTTE LE {count} FALLITE",
-      "busier": "PIÙ CARICO, NON PIÙ LENTO",
-      "climbed": "NUOVA QUERY PIÙ PESANTE"
+      "new": "Nuovo in questo elenco",
+      "slower": "{ratio}× più lento del solito",
+      "loop": "Esegue {multiplier}× per richiesta",
+      "allFailed": "Tutti i {count} falliti",
+      "busier": "Più occupato, non più lento",
+      "climbed": "Nuova query più pesante"
     },
     "deadlocks": {
-      "title": "Database",
-      "subtitle": "Transazioni che si sono bloccate a vicenda e sono state sottoposte a rollback",
       "deadlockCount": "nessun deadlock | {count} deadlock | {count} deadlock",
       "pairCount": "nessuna coppia di query | {count} coppia di query | {count} coppie di query",
       "searchPlaceholder": "Cerca testo query, tabella o applicazione…",
@@ -15812,10 +15809,10 @@
         "mysqlPartial": "Il tuo database MySQL conserva solo il deadlock più recente a meno che innodb_print_all_deadlocks non sia attivo."
       },
       "chips": {
-        "oppositeOrder": "ORDINE DI RIGHE OPPOSTO",
-        "cadence": "OGNI {seconds}s",
-        "noQueries": "QUERY NON ACQUISITE",
-        "partial": "SOLO UN LATO"
+        "oppositeOrder": "Ordine di righe invertito",
+        "cadence": "Ogni {seconds}s",
+        "noQueries": "Query non catturate",
+        "partial": "Solo un lato"
       },
       "rowActions": {
         "topQueries": "Trova queste nelle query principali",
@@ -15832,8 +15829,8 @@
         "pid": "pid {pid}",
         "thread": "thread {pid}",
         "transaction": "transazione {id}",
-        "eachWaits": "ognuna aspetta l'altra",
-        "deadlock": "deadlock",
+        "eachWaits": "Ciascuno aspetta l'altro",
+        "deadlock": "Deadlock",
         "noQueryCaptured": "Il log ha registrato questo deadlock ma non l'istruzione.",
         "waitingFor": "Stava aspettando che {target} finisse.",
         "waitingForRow": "Stava aspettando che {target} finisse — l'altra sessione aveva già modificato la riga che voleva.",
@@ -15935,9 +15932,6 @@
       }
     },
     "blocked": {
-      "title": "Database",
-      "subtitle": "Sessioni attualmente in attesa di un lock",
-      "subtitlePast": "Sessioni che erano in attesa di un lock durante questa finestra",
       "waitingCount": "nessuna sessione in attesa di un lock | {count} sessione in attesa di un lock in questo momento | {count} sessioni in attesa di un lock in questo momento",
       "waitingCountPast": "nessuna sessione ha atteso un lock | {count} sessione ha atteso un lock in questa finestra | {count} sessioni hanno atteso un lock in questa finestra",
       "sessionCount": "nessuna sessione | {count} sessione | {count} sessioni",
@@ -15983,14 +15977,14 @@
       },
       "deep": "{count} di profondità",
       "notWaiting": "non in attesa",
-      "longestWait": "ATTESA PIÙ LUNGA",
-      "holdsTheLock": "DETIENE IL LOCK CHE TUTTI VOGLIONO",
+      "longestWait": "Attesa più lunga",
+      "holdsTheLock": "Detiene il blocco che tutti vogliono",
       "lastStatement": "ultima istruzione {age} fa",
       "idleInTransaction": "non in attesa di nulla · inattiva in una transazione aperta",
       "pills": {
-        "root": "RADICE — INIZIA DA QUI",
-        "waitingBlocking": "IN ATTESA & BLOCCANTE {count}",
-        "waiting": "IN ATTESA"
+        "root": "Radice — inizia qui",
+        "waitingBlocking": "In attesa e blocco {count}",
+        "waiting": "In attesa"
       },
       "waitingOn": "in attesa di {pid}",
       "degraded": "Una sessione sta bloccando {count}. Ha una transazione aperta da {age} e non ha eseguito alcuna query in quel periodo — molto probabilmente un'applicazione che ha dimenticato di fare commit.",
@@ -16068,9 +16062,6 @@
       }
     },
     "activity": {
-      "title": "Attività",
-      "subtitle": "Cosa stanno facendo i tuoi database in questo momento, campionato dalle loro tabelle delle sessioni.",
-      "subtitlePast": "Cosa stavano facendo i tuoi database durante questa finestra, campionato dalle loro tabelle delle sessioni.",
       "searchPlaceholder": "Cerca sessioni, utenti, applicazioni",
       "wait": {
         "onCpu": "Su CPU / senza attesa",
@@ -16194,8 +16185,6 @@
       "deadlockLabel": "deadlock"
     },
     "tableHealth": {
-      "title": "Salute delle tabelle",
-      "subtitle": "Dimensioni, bloat e stato del vacuum per ogni tabella, prima l'istantanea più recente.",
       "searchPlaceholder": "Cerca tabelle",
       "never": "Mai",
       "countersCumulative": "I conteggi di scan e vacuum sono totali cumulativi dall'ultimo ripristino delle statistiche, non conteggi per l'intervallo di tempo selezionato.",
diff --git a/web/src/locales/languages/ja-JP.json b/web/src/locales/languages/ja-JP.json
index a0726e55745..d4bfc284417 100644
--- a/web/src/locales/languages/ja-JP.json
+++ b/web/src/locales/languages/ja-JP.json
@@ -14923,6 +14923,7 @@
       "disabled": "無効",
       "emptyTitle": "このSLOにはまだアラートがありません",
       "emptyHint": "バーンレートアラートは、まだ予算が残っているうちにあなたに通知します。",
+      "emptyActionDesc": "予算が尽きる前に警告を受け取る",
       "loadFailed": "このSLOのアラートを読み込めませんでした。",
       "editTargetMissing": "そのアラートはこのSLOにはもうありません。",
       "describeBurn": "バーンレート > {critical}（{long} と {short} にわたって）",
@@ -15215,7 +15216,12 @@
     "common": {
       "reload": "更新",
       "lastRefreshed": "最終更新 {age}",
-      "loadFailed": "データベースデータを読み込めませんでした。再試行するか、時間範囲を絞り込んでください。"
+      "loadFailed": "データベースデータを読み込めませんでした。再試行するか、時間範囲を絞り込んでください。",
+      "actions": "アクション"
+    },
+    "header": {
+      "title": "データベース",
+      "subtitle": "データベース全体のクエリパフォーマンスと健全性"
     },
     "ai": {
       "suggestFix": "AIに修正方法を尋ねる",
@@ -15257,8 +15263,6 @@
       "wasValue": "以前は {value}"
     },
     "databases": {
-      "title": "データベース",
-      "subtitle": "データベースごとのクエリパフォーマンスと健全性",
       "searchPlaceholder": "データベースまたはサービスを検索…",
       "unnamedInstance": "インスタンス名がありません",
       "unnamedInstanceHint": "データベース自身のステートメントレポートはこのエンジンが稼働していることを証明しますが、ホスト名は示されません。ホスト情報を含むフィード — セッションサンプリングやインスタンスメトリクス — が設定されると、行にアドレスが表示されます。",
@@ -15305,9 +15309,6 @@
       }
     },
     "queries": {
-      "title": "上位クエリ",
-      "subtitle": "最もデータベース時間を使用しているクエリ",
-      "subtitleServer": "データベースが実行したステートメントで、データベース内部でカウントされます — 最も頻繁に実行された順に表示されます",
       "searchPlaceholder": "クエリテキスト、テーブル、またはサービスを検索…",
       "trendLabel": "このクエリの時間が範囲全体でどのように変化したか",
       "otherRow": "その他すべてのクエリ",
@@ -15316,7 +15317,8 @@
         "query": "クエリ",
         "queryHint": "実際のSQLのみ — SELECT、INSERT、UPDATE、DELETE。",
         "all": "クエリ + 接続オーバーヘッド",
-        "allHint": "また、ドライバーがクエリ間で送信するブックキーピング（BEGIN、COMMIT、SET、プールされた接続を維持するためのチェック）もカウントします。"
+        "allHint": "また、ドライバーがクエリ間で送信するブックキーピング（BEGIN、COMMIT、SET、プールされた接続を維持するためのチェック）もカウントします。",
+        "overheadLabel": "接続オーバーヘッドを含める"
       },
       "completionBias": {
         "title": "呼び出しが {drop}% 減少し、失敗が増加しました",
@@ -15565,7 +15567,7 @@
       "invalidStream": "そのトレースストリーム名は認識できませんでした。そのため検索を実行しませんでした。",
       "firstSeen": "このリストに最初に表示されたのは {time} です",
       "firstSeenHint": "この期間内で、このクエリが単独で追跡するほど重かった最も早い時点。",
-      "lastSeen": "このリストに最後に表示されたのは {time} です",
+      "lastSeen": "最後に表示されたのは {time} です",
       "lastSeenHint": "この期間内で、このクエリが単独で追跡するほど重かった最新の時点。現在も実行中かもしれません。これが最後にリストに載った時点です。",
       "errorsByCode": {
         "title": "理由コード別の失敗数",
@@ -15732,9 +15734,6 @@
       }
     },
     "samples": {
-      "title": "最も遅いコール",
-      "subtitle": "このウィンドウ内で、すべてのデータベースとクエリにわたる最も遅い個々のデータベースコール — 呼び出し元が測定したもので、完了したコールのみ",
-      "subtitleServer": "このウィンドウでデータベースが完了した最も遅いステートメント（データベース内で測定）",
       "logOffTitle": "データベースはまだ遅いステートメントをログに記録していません",
       "logOffDescription": "これらのコールをトレースしたものはなかったため、データベースに独自の最も遅いステートメントを問い合わせましたが、何もログに記録されていません。このリストはPostgreSQLのステートメントログ（log_min_duration_statement）からのもので、デフォルトではオフになっています — セットアップカードのロギング手順でオンになります。ステートメントごとのキャプチャは現在PostgreSQLのみであるため、MySQLとMariaDBはこのリストにまだ対応していません。上位クエリは代わりにカウンターテーブルを読み取るため、何が最も多く実行されているかを引き続き表示できます。",
       "searchPlaceholder": "クエリテキスト、サービス、またはデータベースを検索…",
@@ -15776,16 +15775,14 @@
       }
     },
     "rowChips": {
-      "new": "このリストに新登場",
-      "slower": "{ratio}× 通常より遅い",
-      "loop": "リクエストごとに{multiplier}×実行",
-      "allFailed": "すべて{count}件失敗",
-      "busier": "ビジーだが低速ではない",
-      "climbed": "新しい最重量クエリ"
+      "new": "このリストに新しい",
+      "slower": "通常より {ratio} 倍遅い",
+      "loop": "リクエストごとに {multiplier} 回実行",
+      "allFailed": "{count} 件すべて失敗",
+      "busier": "遅いのではなく、忙しい",
+      "climbed": "新しい最も重いクエリ"
     },
     "deadlocks": {
-      "title": "データベース",
-      "subtitle": "互いにブロックし合い、ロールバックされたトランザクション",
       "deadlockCount": "デッドロックなし | {count}件のデッドロック | {count}件のデッドロック",
       "pairCount": "クエリペアなし | {count}件のクエリペア | {count}件のクエリペア",
       "searchPlaceholder": "クエリテキスト、テーブル、またはアプリケーションを検索…",
@@ -15813,7 +15810,7 @@
       },
       "chips": {
         "oppositeOrder": "逆の行順序",
-        "cadence": "毎{seconds}秒",
+        "cadence": "{seconds}秒ごと",
         "noQueries": "キャプチャされていないクエリ",
         "partial": "片側のみ"
       },
@@ -15832,7 +15829,7 @@
         "pid": "pid {pid}",
         "thread": "スレッド {pid}",
         "transaction": "トランザクション {id}",
-        "eachWaits": "互いに相手を待っている",
+        "eachWaits": "互いに待ち合っている",
         "deadlock": "デッドロック",
         "noQueryCaptured": "ログにはこのデッドロックが記録されていますが、ステートメントは記録されていません。",
         "waitingFor": "{target} の完了を待っていました。",
@@ -15935,9 +15932,6 @@
       }
     },
     "blocked": {
-      "title": "データベース",
-      "subtitle": "現在ロックを待機しているセッション",
-      "subtitlePast": "この期間中にロックを待機していたセッション",
       "waitingCount": "ロックを待機しているセッションなし | {count} セッションが現在ロックを待機しています | {count} セッションが現在ロックを待機しています",
       "waitingCountPast": "ロックを待機していたセッションなし | {count} セッションがこの期間中にロックを待機していました | {count} セッションがこの期間中にロックを待機していました",
       "sessionCount": "セッションなし | {count} セッション | {count} セッション",
@@ -15983,7 +15977,7 @@
       },
       "deep": "深さ{count}",
       "notWaiting": "待機していません",
-      "longestWait": "最長待ち時間",
+      "longestWait": "最長待機",
       "holdsTheLock": "全員が求めるロックを保持",
       "lastStatement": "最後のステートメントは{age}前",
       "idleInTransaction": "何も待機していません · オープントランザクションでアイドル中",
@@ -16068,9 +16062,6 @@
       }
     },
     "activity": {
-      "title": "アクティビティ",
-      "subtitle": "データベースが現在何をしているかを、各データベースのセッションテーブルからサンプリングしています。",
-      "subtitlePast": "この期間中にデータベースが何をしていたかを、各データベースのセッションテーブルからサンプリングしています。",
       "searchPlaceholder": "セッション、ユーザー、アプリケーションを検索",
       "wait": {
         "onCpu": "CPU実行中 / 待機なし",
@@ -16194,8 +16185,6 @@
       "deadlockLabel": "デッドロック"
     },
     "tableHealth": {
-      "title": "テーブルの健全性",
-      "subtitle": "各テーブルのサイズ、肥大化、バキューム状態。最新のスナップショットが最初に表示されます。",
       "searchPlaceholder": "テーブルを検索",
       "never": "なし",
       "countersCumulative": "スキャン数とバキューム数は、最後の統計リセット以降の累計であり、選択した期間内の回数ではありません。",
diff --git a/web/src/locales/languages/ko-KR.json b/web/src/locales/languages/ko-KR.json
index 07a93d03bd0..32b17441b49 100644
--- a/web/src/locales/languages/ko-KR.json
+++ b/web/src/locales/languages/ko-KR.json
@@ -14923,6 +14923,7 @@
       "disabled": "비활성화됨",
       "emptyTitle": "이 SLO에 아직 알림이 없습니다",
       "emptyHint": "소진율 알림은 보호할 예산이 아직 남아 있을 때 알려줍니다.",
+      "emptyActionDesc": "예산이 소진되기 전에 경고를 받으세요",
       "loadFailed": "이 SLO의 알림을 불러올 수 없습니다.",
       "editTargetMissing": "그 알림은 더 이상 이 SLO에 없습니다.",
       "describeBurn": "소진율 > {critical} ({long} 및 {short} 동안)",
@@ -15215,7 +15216,12 @@
     "common": {
       "reload": "새로고침",
       "lastRefreshed": "마지막 새로고침: {age}",
-      "loadFailed": "데이터베이스 데이터를 불러오지 못했습니다. 다시 시도하거나 시간 범위를 좁히세요."
+      "loadFailed": "데이터베이스 데이터를 불러오지 못했습니다. 다시 시도하거나 시간 범위를 좁히세요.",
+      "actions": "작업"
+    },
+    "header": {
+      "title": "데이터베이스",
+      "subtitle": "데이터베이스 전반의 쿼리 성능 및 상태"
     },
     "ai": {
       "suggestFix": "AI에게 수정 방법 묻기",
@@ -15257,8 +15263,6 @@
       "wasValue": "이전 값: {value}"
     },
     "databases": {
-      "title": "데이터베이스",
-      "subtitle": "데이터베이스별 쿼리 성능 및 상태",
       "searchPlaceholder": "데이터베이스 또는 서비스 검색…",
       "unnamedInstance": "인스턴스 이름 없음",
       "unnamedInstanceHint": "데이터베이스 자체의 명령문 보고서는 이 엔진이 실행 중임을 증명하지만 호스트 이름을 명명하지 않습니다. 호스트 정보를 전달하는 피드(세션 샘플링 또는 인스턴스 메트릭)가 설정되면 행에 주소가 표시됩니다.",
@@ -15305,9 +15309,6 @@
       }
     },
     "queries": {
-      "title": "상위 쿼리",
-      "subtitle": "데이터베이스 시간을 가장 많이 사용하는 쿼리",
-      "subtitleServer": "데이터베이스 내부에서 집계된, 데이터베이스가 실행한 명령문 — 가장 자주 실행된 순서",
       "searchPlaceholder": "쿼리 텍스트, 테이블 또는 서비스 검색…",
       "trendLabel": "이 쿼리의 시간이 범위에 걸쳐 어떻게 변화했는지",
       "otherRow": "기타 모든 쿼리",
@@ -15316,7 +15317,8 @@
         "query": "쿼리",
         "queryHint": "실제 SQL만 — SELECT, INSERT, UPDATE 및 DELETE.",
         "all": "쿼리 + 연결 오버헤드",
-        "allHint": "또한 드라이버가 쿼리 사이에 보내는 부가 작업(BEGIN 및 COMMIT, SET, 풀 연결을 유지하는 검사)도 집계합니다."
+        "allHint": "또한 드라이버가 쿼리 사이에 보내는 부가 작업(BEGIN 및 COMMIT, SET, 풀 연결을 유지하는 검사)도 집계합니다.",
+        "overheadLabel": "연결 오버헤드 포함"
       },
       "completionBias": {
         "title": "호출이 {drop}% 감소한 반면 실패는 증가했습니다",
@@ -15565,7 +15567,7 @@
       "invalidStream": "인식할 수 없는 트레이스 스트림 이름이므로 검색을 실행하지 않았습니다.",
       "firstSeen": "이 목록에 처음 나타난 시각: {time}",
       "firstSeenHint": "이 범위에서 이 쿼리가 자체적으로 추적할 수 있을 만큼 무거웠던 가장 이른 지점입니다.",
-      "lastSeen": "마지막으로 나타난 시각: {time}",
+      "lastSeen": "이 목록에 마지막으로 나타난 시각: {time}",
       "lastSeenHint": "이 범위에서 이 쿼리가 자체적으로 추적할 수 있을 만큼 무거웠던 가장 최근 지점입니다. 지금도 실행 중일 수 있습니다 — 목록에 마지막으로 오른 시각입니다.",
       "errorsByCode": {
         "title": "사유 코드별 실패",
@@ -15732,9 +15734,6 @@
       }
     },
     "samples": {
-      "title": "가장 느린 호출",
-      "subtitle": "이 창에서 모든 데이터베이스와 쿼리 중 가장 느린 개별 데이터베이스 호출 — 호출자가 측정한 대로, 완료된 호출만",
-      "subtitleServer": "이 창에서 데이터베이스가 완료한 가장 느린 문장, 데이터베이스 내부에서 측정됨",
       "logOffTitle": "데이터베이스가 아직 느린 문장을 기록하지 않고 있습니다",
       "logOffDescription": "이 호출들을 추적한 것이 없어 데이터베이스에 자체 가장 느린 문장을 요청했지만 로그가 없습니다. 이 목록은 PostgreSQL의 문장 로그(log_min_duration_statement)에서 가져오며 기본적으로 꺼져 있습니다 — 설정 카드의 로깅 단계에서 켭니다. 문장별 캡처는 현재 PostgreSQL 전용이므로 MySQL과 MariaDB는 아직 이 목록을 제공할 수 없습니다. 상위 쿼리는 카운터 테이블을 읽으므로 가장 많이 실행되는 것을 계속 보여줄 수 있습니다.",
       "searchPlaceholder": "쿼리 텍스트, 서비스 또는 데이터베이스 검색…",
@@ -15777,15 +15776,13 @@
     },
     "rowChips": {
       "new": "이 목록에 새로 추가됨",
-      "slower": "평소보다 {ratio}배 느림",
-      "loop": "요청당 {multiplier}회 실행",
-      "allFailed": "모두 {count}개 실패",
-      "busier": "더 바쁨, 느리지 않음",
-      "climbed": "새로운 최대 부하 쿼리"
+      "slower": "평소보다 {ratio}× 느림",
+      "loop": "요청당 {multiplier}× 실행",
+      "allFailed": "{count}개 모두 실패",
+      "busier": "더 바쁘지만 느리지는 않음",
+      "climbed": "새로운 최중량 쿼리"
     },
     "deadlocks": {
-      "title": "데이터베이스",
-      "subtitle": "서로 차단되어 롤백된 트랜잭션",
       "deadlockCount": "교착 상태 없음 | {count}개 교착 상태 | {count}개 교착 상태",
       "pairCount": "쿼리 쌍 없음 | {count}개 쿼리 쌍 | {count}개 쿼리 쌍",
       "searchPlaceholder": "쿼리 텍스트, 테이블 또는 애플리케이션 검색…",
@@ -15813,9 +15810,9 @@
       },
       "chips": {
         "oppositeOrder": "반대 행 순서",
-        "cadence": "매 {seconds}초마다",
+        "cadence": "{seconds}초마다",
         "noQueries": "캡처되지 않은 쿼리",
-        "partial": "한쪽만"
+        "partial": "한쪽에만 있음"
       },
       "rowActions": {
         "topQueries": "Top 쿼리에서 찾기",
@@ -15832,7 +15829,7 @@
         "pid": "pid {pid}",
         "thread": "스레드 {pid}",
         "transaction": "트랜잭션 {id}",
-        "eachWaits": "서로 상대를 기다림",
+        "eachWaits": "서로를 기다림",
         "deadlock": "교착 상태",
         "noQueryCaptured": "로그에 이 교착 상태가 기록되었지만 문장은 기록되지 않았습니다.",
         "waitingFor": "{target}이(가) 끝나기를 기다리고 있었습니다.",
@@ -15935,9 +15932,6 @@
       }
     },
     "blocked": {
-      "title": "데이터베이스",
-      "subtitle": "현재 잠금을 기다리는 세션",
-      "subtitlePast": "이 시간 범위 동안 잠금을 기다린 세션",
       "waitingCount": "잠금을 기다리는 세션 없음 | 지금 잠금을 기다리는 세션 {count}개 | 지금 잠금을 기다리는 세션 {count}개",
       "waitingCountPast": "잠금을 기다린 세션 없음 | 이 시간 범위에 잠금을 기다린 세션 {count}개 | 이 시간 범위에 잠금을 기다린 세션 {count}개",
       "sessionCount": "세션 없음 | 세션 {count}개 | 세션 {count}개",
@@ -15983,8 +15977,8 @@
       },
       "deep": "{count}단계 깊이",
       "notWaiting": "대기 중 아님",
-      "longestWait": "최장 대기",
-      "holdsTheLock": "모두가 원하는 잠금 보유",
+      "longestWait": "가장 긴 대기",
+      "holdsTheLock": "모두가 원하는 잠금을 보유 중",
       "lastStatement": "마지막 쿼리 {age} 전",
       "idleInTransaction": "아무것도 기다리지 않음 · 열린 트랜잭션에서 유휴 상태",
       "pills": {
@@ -16068,9 +16062,6 @@
       }
     },
     "activity": {
-      "title": "활동",
-      "subtitle": "지금 데이터베이스가 무엇을 하고 있는지, 자체 세션 테이블에서 샘플링한 내용입니다.",
-      "subtitlePast": "이 기간 동안 데이터베이스가 무엇을 했는지, 자체 세션 테이블에서 샘플링한 내용입니다.",
       "searchPlaceholder": "세션, 사용자, 애플리케이션 검색",
       "wait": {
         "onCpu": "CPU 사용 중 / 대기 없음",
@@ -16194,8 +16185,6 @@
       "deadlockLabel": "교착 상태"
     },
     "tableHealth": {
-      "title": "테이블 상태",
-      "subtitle": "각 테이블의 크기, 블로트, 진공 상태를 최신 스냅샷부터 표시합니다.",
       "searchPlaceholder": "테이블 검색",
       "never": "없음",
       "countersCumulative": "스캔 및 진공 횟수는 마지막 통계 초기화 이후의 누적 총계이며 선택한 기간 동안의 횟수가 아닙니다.",
diff --git a/web/src/locales/languages/nl-NL.json b/web/src/locales/languages/nl-NL.json
index 76b5322a9c7..0e3d1a6e73e 100644
--- a/web/src/locales/languages/nl-NL.json
+++ b/web/src/locales/languages/nl-NL.json
@@ -14923,6 +14923,7 @@
       "disabled": "Uitgeschakeld",
       "emptyTitle": "Nog geen meldingen voor deze SLO",
       "emptyHint": "Burn-rate-meldingen waarschuwen u terwijl er nog budget over is om te beschermen.",
+      "emptyActionDesc": "Word gewaarschuwd voordat het budget op is",
       "loadFailed": "Kon de meldingen van deze SLO niet laden.",
       "editTargetMissing": "Die melding staat niet meer op deze SLO.",
       "describeBurn": "Verbrandingssnelheid > {critical} over {long} en {short}",
@@ -15215,7 +15216,12 @@
     "common": {
       "reload": "Vernieuwen",
       "lastRefreshed": "Laatst vernieuwd {age}",
-      "loadFailed": "Kan databasegegevens niet laden. Probeer het opnieuw of verklein het tijdsbereik."
+      "loadFailed": "Kan databasegegevens niet laden. Probeer het opnieuw of verklein het tijdsbereik.",
+      "actions": "Acties"
+    },
+    "header": {
+      "title": "Databases",
+      "subtitle": "Queryprestaties en status van uw databases"
     },
     "ai": {
       "suggestFix": "Vraag AI hoe dit op te lossen",
@@ -15237,12 +15243,12 @@
       "removeScope": "Verwijder dit filter",
       "popoverTitle": "Beperk dit",
       "dimension": {
-        "system": "engine",
-        "instance": "database",
-        "namespace": "schema",
-        "env": "omgeving",
-        "service": "dienst",
-        "insight": "inzicht"
+        "system": "Engine",
+        "instance": "Database",
+        "namespace": "Schema",
+        "env": "Omgeving",
+        "service": "Service",
+        "insight": "Inzicht"
       }
     },
     "delta": {
@@ -15257,8 +15263,6 @@
       "wasValue": "was {value}"
     },
     "databases": {
-      "title": "Databases",
-      "subtitle": "Queryprestaties en gezondheid per database",
       "searchPlaceholder": "Zoek database of service…",
       "unnamedInstance": "Instantie niet benoemd",
       "unnamedInstanceHint": "De eigen statement-rapporten van de database bewijzen dat deze engine draait, maar ze geven geen hostnaam. De rij toont een adres zodra een feed die er een bevat — sessiesampling of instantiemetrieken — is ingesteld.",
@@ -15305,9 +15309,6 @@
       }
     },
     "queries": {
-      "title": "Top-query's",
-      "subtitle": "De query's die de meeste databasetijd gebruiken",
-      "subtitleServer": "De statements die je databases uitvoerden, geteld in de database — de meest uitgevoerde eerst.",
       "searchPlaceholder": "Zoek querytekst, tabel of service…",
       "trendLabel": "Hoe de tijd van deze query is veranderd over het bereik",
       "otherRow": "Alle andere query's",
@@ -15316,7 +15317,8 @@
         "query": "Query's",
         "queryHint": "Alleen echte SQL — SELECT, INSERT, UPDATE en DELETE.",
         "all": "Query's + verbindingsoverhead",
-        "allHint": "Telt ook de administratie die je driver tussen query's verstuurt: BEGIN en COMMIT, SET, en de controles die een gepoolde verbinding in leven houden."
+        "allHint": "Telt ook de administratie die je driver tussen query's verstuurt: BEGIN en COMMIT, SET, en de controles die een gepoolde verbinding in leven houden.",
+        "overheadLabel": "Verbindingsoverhead meenemen"
       },
       "completionBias": {
         "title": "Aanroepen daalden met {drop}% terwijl fouten stegen.",
@@ -15559,13 +15561,13 @@
       "noSamples": "Geen individuele uitvoeringen geladen voor dit bereik.",
       "noSeries": "Geen geschiedenis voor deze query in het bereik dat je hebt gekozen.",
       "liveSeries": "De meest recente punten worden nog geteld en kunnen veranderen.",
-      "noStream": "We konden niet achterhalen welke trace-stream deze query draagt, dus de aanroepers en voorbeelden kunnen niet worden geladen.",
-      "ambiguousStream": "Deze organisatie heeft meer dan één trace-stream, en we kunnen niet zien op welke deze query draait. Kies een stream om de aanroepers en voorbeeldtraces te laden.",
-      "pickStream": "Kies een trace-stream",
-      "invalidStream": "Die trace-streamnaam herkennen we niet, dus we hebben de zoekopdracht niet uitgevoerd.",
-      "firstSeen": "VOOR HET EERST VERSCHENEN IN DEZE LIJST OM {time}",
+      "noStream": "We konden niet bepalen welke trace stream deze query draagt, dus de aanroepers en voorbeelden kunnen niet worden geladen.",
+      "ambiguousStream": "Deze organisatie heeft meer dan één trace stream, en we kunnen niet bepalen op welke deze query draait. Kies een stream om de aanroepers en voorbeeld-traces te laden.",
+      "pickStream": "Kies een trace stream",
+      "invalidStream": "Die naam van de trace stream herkennen we niet, dus we hebben de zoekopdracht niet uitgevoerd.",
+      "firstSeen": "Voor het eerst verschenen in deze lijst om {time}",
       "firstSeenHint": "Het vroegste punt in dit bereik waarop deze query zwaar genoeg was om apart te worden gevolgd.",
-      "lastSeen": "LAATST VERSCHENEN OM {time}",
+      "lastSeen": "Laatst verschenen om {time}",
       "lastSeenHint": "Het meest recente punt in dit bereik waarop deze query zwaar genoeg was om apart te worden gevolgd. Het kan nu nog steeds actief zijn — dit is de laatste keer dat het op de lijst stond.",
       "errorsByCode": {
         "title": "Fouten per redencode",
@@ -15732,9 +15734,6 @@
       }
     },
     "samples": {
-      "title": "Traagste aanroepen",
-      "subtitle": "De traagste individuele database-aanroepen in dit venster, over elke database en query — zoals hun aanroepers ze hebben gemeten, alleen voltooide aanroepen",
-      "subtitleServer": "De traagste statements die je databases in dit venster hebben voltooid, gemeten binnen de database",
       "logOffTitle": "Je databases loggen nog geen trage statements",
       "logOffDescription": "Niets heeft deze aanroepen getraceerd, dus we hebben de databases gevraagd naar hun eigen traagste statements — en ze loggen er geen. Deze lijst komt uit PostgreSQL's statementlog (log_min_duration_statement), dat standaard is uitgeschakeld — de logstap van de setupkaart schakelt dit in. Per-statement-capturering is momenteel alleen voor PostgreSQL, dus MySQL en MariaDB kunnen deze lijst nog niet voeden. Top queries kunnen nog steeds tonen wat het meest draait, omdat die in plaats daarvan de tellertabellen leest.",
       "searchPlaceholder": "Zoek querytekst, service of database…",
@@ -15776,16 +15775,14 @@
       }
     },
     "rowChips": {
-      "new": "NIEUW IN DEZE LIJST",
-      "slower": "{ratio}× LANGZAMER DAN GEBRUIKELIJK",
-      "loop": "DRAAIT {multiplier}× PER AANVRAAG",
-      "allFailed": "ALLE {count} MISLUKT",
-      "busier": "DRUKKER, NIET LANGZAMER",
-      "climbed": "NIEUWE ZWAARSTE QUERY"
+      "new": "Nieuw in deze lijst",
+      "slower": "{ratio}× langzamer dan normaal",
+      "loop": "Draait {multiplier}× per verzoek",
+      "allFailed": "Alle {count} zijn mislukt",
+      "busier": "Drukker, niet langzamer",
+      "climbed": "Nieuwe zwaarste query"
     },
     "deadlocks": {
-      "title": "Databases",
-      "subtitle": "Transacties die elkaar blokkeerden en zijn teruggedraaid",
       "deadlockCount": "geen deadlocks | {count} deadlock | {count} deadlocks",
       "pairCount": "geen queryparen | {count} querypaar | {count} queryparen",
       "searchPlaceholder": "Zoek querytekst, tabel of applicatie…",
@@ -15812,10 +15809,10 @@
         "mysqlPartial": "Je MySQL-database bewaart alleen de meest recente deadlock, tenzij innodb_print_all_deadlocks aan staat."
       },
       "chips": {
-        "oppositeOrder": "TEGENGESTELDE RIJVOLGORDE",
-        "cadence": "ELKE {seconds}s",
-        "noQueries": "QUERIES NIET VASTGELEGD",
-        "partial": "MAAR ÉÉN KANT"
+        "oppositeOrder": "Tegengestelde rijvolgorde",
+        "cadence": "Elke {seconds}s",
+        "noQueries": "Query's niet vastgelegd",
+        "partial": "Slechts één kant"
       },
       "rowActions": {
         "topQueries": "Vind deze in Top-query's",
@@ -15832,8 +15829,8 @@
         "pid": "pid {pid}",
         "thread": "thread {pid}",
         "transaction": "transactie {id}",
-        "eachWaits": "elk wacht op de ander",
-        "deadlock": "deadlock",
+        "eachWaits": "Elke wacht op de andere",
+        "deadlock": "Deadlock",
         "noQueryCaptured": "Het logboek heeft deze deadlock vastgelegd, maar niet de statement.",
         "waitingFor": "Wachtte tot {target} klaar was.",
         "waitingForRow": "Wachtte tot {target} klaar was — de andere sessie had de rij die het wilde al gewijzigd.",
@@ -15935,9 +15932,6 @@
       }
     },
     "blocked": {
-      "title": "Databases",
-      "subtitle": "Sessies die momenteel wachten op een lock",
-      "subtitlePast": "Sessies die tijdens dit tijdsvenster op een lock wachtten",
       "waitingCount": "geen sessies wachten op een lock | {count} sessie wacht nu op een lock | {count} sessies wachten nu op een lock",
       "waitingCountPast": "geen sessies wachtten op een lock | {count} sessie wachtte op een lock in dit tijdsvenster | {count} sessies wachtten op een lock in dit tijdsvenster",
       "sessionCount": "geen sessies | {count} sessie | {count} sessies",
@@ -15983,14 +15977,14 @@
       },
       "deep": "{count} diep",
       "notWaiting": "niet aan het wachten",
-      "longestWait": "LANGSTE WACHTTIJD",
-      "holdsTheLock": "HOUDT DE LOCK VAST DIE IEDEREEN WIL",
+      "longestWait": "Langste wachttijd",
+      "holdsTheLock": "Houdt de lock die iedereen wil",
       "lastStatement": "laatste statement {age} geleden",
       "idleInTransaction": "niet aan het wachten op iets · inactief in een open transactie",
       "pills": {
-        "root": "ROOT — BEGIN HIER",
-        "waitingBlocking": "WACHTEND & BLOKKEREND {count}",
-        "waiting": "WACHTEND"
+        "root": "Root — begin hier",
+        "waitingBlocking": "Wachten en blokkeren {count}",
+        "waiting": "Wachten"
       },
       "waitingOn": "wachtend op {pid}",
       "degraded": "Eén sessie houdt {count} tegen. Deze heeft al {age} een open transactie en heeft in die tijd geen query uitgevoerd — hoogstwaarschijnlijk een applicatie die is vergeten te committen.",
@@ -16068,9 +16062,6 @@
       }
     },
     "activity": {
-      "title": "Activiteit",
-      "subtitle": "Wat je databases op dit moment doen, gesampled uit hun eigen sessietabellen.",
-      "subtitlePast": "Wat je databases tijdens dit venster deden, gesampled uit hun eigen sessietabellen.",
       "searchPlaceholder": "Zoek sessies, gebruikers, applicaties",
       "wait": {
         "onCpu": "Op CPU / geen wachttijd",
@@ -16194,8 +16185,6 @@
       "deadlockLabel": "deadlocks"
     },
     "tableHealth": {
-      "title": "Tabelgezondheid",
-      "subtitle": "Grootte, bloat en vacuumstatus voor elke tabel, nieuwste snapshot eerst.",
       "searchPlaceholder": "Zoek tabellen",
       "never": "Nooit",
       "countersCumulative": "Scan- en vacuumtellingen zijn totalen over de levensduur sinds de laatste reset van statistieken, geen tellingen voor het geselecteerde tijdsbereik.",
diff --git a/web/src/locales/languages/pl-PL.json b/web/src/locales/languages/pl-PL.json
index d749da4b0db..783ba8a20af 100644
--- a/web/src/locales/languages/pl-PL.json
+++ b/web/src/locales/languages/pl-PL.json
@@ -14923,6 +14923,7 @@
       "disabled": "Wyłączone",
       "emptyTitle": "Brak jeszcze alertów na tym SLO.",
       "emptyHint": "Alerty wskaźnika wypalania powiadamiają Cię, dopóki pozostał budżet do ochrony.",
+      "emptyActionDesc": "Otrzymuj ostrzeżenie zanim budżet się wyczerpie",
       "loadFailed": "Nie udało się wczytać alertów tego SLO.",
       "editTargetMissing": "Ten alert nie jest już przypisany do tego SLO.",
       "describeBurn": "Wskaźnik wypalania > {critical} w ciągu {long} i {short}",
@@ -15215,7 +15216,12 @@
     "common": {
       "reload": "Odśwież",
       "lastRefreshed": "Ostatnio odświeżono {age}",
-      "loadFailed": "Nie udało się załadować danych z bazy danych. Spróbuj ponownie lub zawęź zakres czasu."
+      "loadFailed": "Nie udało się załadować danych z bazy danych. Spróbuj ponownie lub zawęź zakres czasu.",
+      "actions": "Akcje"
+    },
+    "header": {
+      "title": "Bazy danych",
+      "subtitle": "Wydajność i kondycja zapytań w Twoich bazach danych"
     },
     "ai": {
       "suggestFix": "Zapytaj AI, jak to naprawić",
@@ -15237,12 +15243,12 @@
       "removeScope": "Usuń ten filtr",
       "popoverTitle": "Zawęź to",
       "dimension": {
-        "system": "silnik",
-        "instance": "baza danych",
-        "namespace": "schemat",
-        "env": "środowisko",
-        "service": "usługa",
-        "insight": "spostrzeżenie"
+        "system": "Silnik",
+        "instance": "Baza danych",
+        "namespace": "Schemat",
+        "env": "Środowisko",
+        "service": "Usługa",
+        "insight": "Wgląd"
       }
     },
     "delta": {
@@ -15257,8 +15263,6 @@
       "wasValue": "było {value}"
     },
     "databases": {
-      "title": "Bazy danych",
-      "subtitle": "Wydajność zapytań i kondycja w podziale na bazy danych",
       "searchPlaceholder": "Szukaj bazy danych lub usługi…",
       "unnamedInstance": "Instancja bez nazwy",
       "unnamedInstanceHint": "Raporty o instrukcjach samej bazy danych potwierdzają, że ten silnik działa, ale nie podają nazwy hosta. Wiersz pokaże adres, gdy zostanie skonfigurowane źródło danych, które go przenosi — próbkowanie sesji lub metryki instancji.",
@@ -15305,9 +15309,6 @@
       }
     },
     "queries": {
-      "title": "Czołowe zapytania",
-      "subtitle": "Zapytania zużywające najwięcej czasu bazy danych",
-      "subtitleServer": "Instrukcje wykonane przez Twoje bazy danych, zliczane wewnątrz bazy — od najczęściej wykonywanych",
       "searchPlaceholder": "Szukaj tekstu zapytania, tabeli lub usługi…",
       "trendLabel": "Jak zmieniał się czas tego zapytania w wybranym zakresie",
       "otherRow": "Wszystkie pozostałe zapytania",
@@ -15316,7 +15317,8 @@
         "query": "Zapytania",
         "queryHint": "Tylko prawdziwy SQL — SELECT, INSERT, UPDATE i DELETE.",
         "all": "Zapytania + narzut połączeń",
-        "allHint": "Liczy również narzut, który sterownik wysyła między zapytaniami: BEGIN i COMMIT, SET oraz testy, które utrzymują połączenie z puli przy życiu."
+        "allHint": "Liczy również narzut, który sterownik wysyła między zapytaniami: BEGIN i COMMIT, SET oraz testy, które utrzymują połączenie z puli przy życiu.",
+        "overheadLabel": "Uwzględnij narzut połączenia"
       },
       "completionBias": {
         "title": "Wywołania spadły o {drop}%, a liczba nieudanych wzrosła",
@@ -15559,13 +15561,13 @@
       "noSamples": "Nie załadowano żadnych pojedynczych wykonań dla tego zakresu.",
       "noSeries": "Brak historii dla tego zapytania w wybranym zakresie.",
       "liveSeries": "Najnowsze punkty są nadal zliczane i mogą się zmienić.",
-      "noStream": "Nie udało nam się ustalić, który strumień śladów przenosi to zapytanie, więc jego wywołujący i przykłady nie mogą zostać załadowane.",
-      "ambiguousStream": "Ta organizacja ma więcej niż jeden strumień śladów i nie możemy określić, na którym działa to zapytanie. Wybierz strumień, aby załadować jego wywołujących i przykładowe ślady.",
-      "pickStream": "Wybierz strumień śladów",
-      "invalidStream": "Ta nazwa strumienia śladów nie jest przez nas rozpoznawana, więc nie uruchomiliśmy wyszukiwania.",
-      "firstSeen": "PO RAZ PIERWSZY POJAWIŁO SIĘ NA TEJ LIŚCIE O {time}",
+      "noStream": "Nie mogliśmy ustalić, który strumień śledzenia przenosi to zapytanie, więc nie można załadować jego wywołujących i przykładów.",
+      "ambiguousStream": "Ta organizacja ma więcej niż jeden strumień śledzenia i nie możemy określić, na którym działa to zapytanie. Wybierz strumień, aby załadować jego wywołujących i przykładowe ślady.",
+      "pickStream": "Wybierz strumień śledzenia",
+      "invalidStream": "Ta nazwa strumienia śledzenia nie jest nam znana, więc nie uruchomiliśmy wyszukiwania.",
+      "firstSeen": "Po raz pierwszy pojawiło się na tej liście o {time}",
       "firstSeenHint": "Najwcześniejszy punkt w tym zakresie, w którym to zapytanie było na tyle ciężkie, aby śledzić je osobno.",
-      "lastSeen": "OSTATNIO POJAWIŁO SIĘ O {time}",
+      "lastSeen": "Ostatnio pojawiło się o {time}",
       "lastSeenHint": "Najnowszy punkt w tym zakresie, w którym to zapytanie było na tyle ciężkie, aby śledzić je osobno. Może nadal działać — to ostatni raz, gdy znalazło się na liście.",
       "errorsByCode": {
         "title": "Błędy według kodu przyczyny",
@@ -15732,9 +15734,6 @@
       }
     },
     "samples": {
-      "title": "Najwolniejsze wywołania",
-      "subtitle": "Najwolniejsze pojedyncze wywołania baz danych w tym oknie, we wszystkich bazach danych i zapytaniach — mierzone przez ich wywołujących, tylko zakończone wywołania",
-      "subtitleServer": "Najwolniejsze instrukcje, które Twoje bazy danych ukończyły w tym oknie, mierzone wewnątrz bazy danych.",
       "logOffTitle": "Twoje bazy danych nie rejestrują jeszcze wolnych instrukcji.",
       "logOffDescription": "Nic nie prześledziło tych wywołań, więc zapytaliśmy bazy danych o ich własne najwolniejsze instrukcje — a one nie rejestrują żadnych. Ta lista pochodzi z dziennika instrukcji PostgreSQL (log_min_duration_statement), który jest domyślnie wyłączony — krok logowania w karcie konfiguracji włącza go. Przechwytywanie pojedynczych instrukcji jest obecnie dostępne tylko dla PostgreSQL, więc MySQL i MariaDB nie mogą jeszcze zasilać tej listy. Najczęstsze zapytania nadal pokazują, co działa najczęściej, ponieważ odczytują tabele liczników.",
       "searchPlaceholder": "Szukaj tekstu zapytania, usługi lub bazy danych…",
@@ -15776,16 +15775,14 @@
       }
     },
     "rowChips": {
-      "new": "NOWE NA TEJ LIŚCIE",
-      "slower": "{ratio}× WOLNIEJ NIŻ ZWYKLE",
-      "loop": "URUCHOMIENIA {multiplier}× NA ŻĄDANIE",
-      "allFailed": "WSZYSTKIE {count} NIEUDANE",
-      "busier": "BARDZIEJ OBCIĄŻONE, NIE WOLNIEJSZE",
-      "climbed": "NOWE NAJCIĘŻSZE ZAPYTANIE"
+      "new": "Nowość na tej liście",
+      "slower": "{ratio}× wolniej niż zwykle",
+      "loop": "Uruchamiane {multiplier}× na żądanie",
+      "allFailed": "Wszystkie {count} nie powiodły się",
+      "busier": "Bardziej obciążone, nie wolniejsze",
+      "climbed": "Nowe najcięższe zapytanie"
     },
     "deadlocks": {
-      "title": "Bazy danych",
-      "subtitle": "Transakcje, które wzajemnie się blokowały i zostały wycofane",
       "deadlockCount": "brak zakleszczeń | {count} zakleszczenie | {count} zakleszczeń",
       "pairCount": "brak par zapytań | {count} para zapytań | {count} par zapytań",
       "searchPlaceholder": "Szukaj tekstu zapytania, tabeli lub aplikacji…",
@@ -15812,10 +15809,10 @@
         "mysqlPartial": "Twoja baza danych MySQL przechowuje tylko najnowsze zakleszczenie, chyba że włączono innodb_print_all_deadlocks."
       },
       "chips": {
-        "oppositeOrder": "PRZECIWNA KOLEJNOŚĆ WIERSZY",
-        "cadence": "CO {seconds}s",
-        "noQueries": "ZAPYTANIA NIEPRZECHWYCONE",
-        "partial": "TYLKO JEDNA STRONA"
+        "oppositeOrder": "Odwrotna kolejność wierszy",
+        "cadence": "Co {seconds}s",
+        "noQueries": "Zapytania nie przechwycone",
+        "partial": "Tylko jedna strona"
       },
       "rowActions": {
         "topQueries": "Znajdź je w Top queries",
@@ -15832,8 +15829,8 @@
         "pid": "pid {pid}",
         "thread": "wątek {pid}",
         "transaction": "transakcja {id}",
-        "eachWaits": "każde z nich czeka na drugie",
-        "deadlock": "zakleszczenie",
+        "eachWaits": "Każdy czeka na drugi",
+        "deadlock": "Zakleszczenie",
         "noQueryCaptured": "Dziennik zarejestrował to zakleszczenie, ale nie zapytanie.",
         "waitingFor": "Czekało na zakończenie {target}.",
         "waitingForRow": "Czekało na zakończenie {target} — druga sesja zmieniła już wiersz, którego chciało.",
@@ -15935,9 +15932,6 @@
       }
     },
     "blocked": {
-      "title": "Bazy danych",
-      "subtitle": "Sesje aktualnie oczekujące na blokadę",
-      "subtitlePast": "Sesje, które czekały na blokadę w tym oknie",
       "waitingCount": "brak sesji oczekujących na blokadę | {count} sesja oczekuje teraz na blokadę | {count} sesji oczekuje teraz na blokadę",
       "waitingCountPast": "brak sesji, które czekały na blokadę | {count} sesja czekała na blokadę w tym oknie | {count} sesji czekało na blokadę w tym oknie",
       "sessionCount": "brak sesji | {count} sesja | {count} sesji",
@@ -15983,14 +15977,14 @@
       },
       "deep": "głębokość: {count}",
       "notWaiting": "nie oczekuje",
-      "longestWait": "NAJDŁUŻSZE OCZEKIWANIE",
-      "holdsTheLock": "TRZYMA BLOKADĘ, KTÓREJ WSZYSCY CHCĄ",
+      "longestWait": "Najdłuższe oczekiwanie",
+      "holdsTheLock": "Posiada blokadę, której wszyscy chcą",
       "lastStatement": "ostatnie polecenie {age} temu",
       "idleInTransaction": "nie oczekuje na nic · bezczynna w otwartej transakcji",
       "pills": {
-        "root": "KORZEŃ — ZACZNIJ TUTAJ",
-        "waitingBlocking": "OCZEKUJE I BLOKUJE {count}",
-        "waiting": "OCZEKUJE"
+        "root": "Źródło — zacznij tutaj",
+        "waitingBlocking": "Oczekujące i blokujące {count}",
+        "waiting": "Oczekiwanie"
       },
       "waitingOn": "oczekuje na {pid}",
       "degraded": "Jedna sesja blokuje {count}. Ma otwartą transakcję od {age} i w tym czasie nie wykonała żadnego zapytania — najprawdopodobniej aplikacja zapomniała o commicie.",
@@ -16068,9 +16062,6 @@
       }
     },
     "activity": {
-      "title": "Aktywność",
-      "subtitle": "Co Twoje bazy danych robią teraz, próbkowane z ich tabel sesji.",
-      "subtitlePast": "Co Twoje bazy danych robiły w tym oknie, próbkowane z ich tabel sesji.",
       "searchPlaceholder": "Szukaj sesji, użytkowników, aplikacji",
       "wait": {
         "onCpu": "Na CPU / bez oczekiwania",
@@ -16194,8 +16185,6 @@
       "deadlockLabel": "Zakleszczenia"
     },
     "tableHealth": {
-      "title": "Kondycja tabel",
-      "subtitle": "Rozmiar, rozrost i stan vacuum dla każdej tabeli, najnowsza migawka pierwsza.",
       "searchPlaceholder": "Szukaj tabel",
       "never": "Nigdy",
       "countersCumulative": "Liczby skanowań i próżni to sumy całkowite od ostatniego resetu statystyk, a nie liczby dla wybranego zakresu czasu.",
diff --git a/web/src/locales/languages/pt-PT.json b/web/src/locales/languages/pt-PT.json
index a4c8bf0b414..8e13094fa30 100644
--- a/web/src/locales/languages/pt-PT.json
+++ b/web/src/locales/languages/pt-PT.json
@@ -14923,6 +14923,7 @@
       "disabled": "Desativado",
       "emptyTitle": "Ainda não há alertas neste SLO",
       "emptyHint": "Os alertas de taxa de consumo avisam você enquanto ainda há orçamento restante para proteger.",
+      "emptyActionDesc": "Seja avisado antes que o orçamento acabe",
       "loadFailed": "Não foi possível carregar os alertas deste SLO.",
       "editTargetMissing": "Esse alerta não está mais neste SLO.",
       "describeBurn": "Taxa de consumo > {critical} em {long} e {short}",
@@ -15215,7 +15216,12 @@
     "common": {
       "reload": "Atualizar",
       "lastRefreshed": "Última atualização {age}",
-      "loadFailed": "Não foi possível carregar os dados do banco de dados. Tente novamente ou reduza o intervalo de tempo."
+      "loadFailed": "Não foi possível carregar os dados do banco de dados. Tente novamente ou reduza o intervalo de tempo.",
+      "actions": "Ações"
+    },
+    "header": {
+      "title": "Bancos de dados",
+      "subtitle": "Desempenho e saúde das consultas em seus bancos de dados"
     },
     "ai": {
       "suggestFix": "Pergunte à IA como corrigir isso",
@@ -15237,12 +15243,12 @@
       "removeScope": "Remover este filtro",
       "popoverTitle": "Restringir isso",
       "dimension": {
-        "system": "mecanismo",
-        "instance": "banco de dados",
-        "namespace": "esquema",
-        "env": "ambiente",
-        "service": "serviço",
-        "insight": "insight"
+        "system": "Mecanismo",
+        "instance": "Banco de dados",
+        "namespace": "Esquema",
+        "env": "Ambiente",
+        "service": "Serviço",
+        "insight": "Insight"
       }
     },
     "delta": {
@@ -15257,8 +15263,6 @@
       "wasValue": "era {value}"
     },
     "databases": {
-      "title": "Bancos de dados",
-      "subtitle": "Desempenho e saúde de consultas por banco de dados",
       "searchPlaceholder": "Pesquisar banco de dados ou serviço…",
       "unnamedInstance": "Instância sem nome",
       "unnamedInstanceHint": "Os relatórios de instruções do próprio banco provam que este mecanismo está em execução, mas não identificam um host. A linha mostrará um endereço quando um feed que o contenha — amostragem de sessão ou métricas de instância — for configurado.",
@@ -15305,9 +15309,6 @@
       }
     },
     "queries": {
-      "title": "Principais consultas",
-      "subtitle": "As consultas que mais usam tempo de banco de dados",
-      "subtitleServer": "As instruções que seus bancos de dados executaram, contadas dentro do banco — as executadas com mais frequência primeiro",
       "searchPlaceholder": "Pesquisar texto da consulta, tabela ou serviço…",
       "trendLabel": "Como o tempo desta consulta variou ao longo do intervalo",
       "otherRow": "Todas as outras consultas",
@@ -15316,7 +15317,8 @@
         "query": "Consultas",
         "queryHint": "Apenas SQL real — SELECT, INSERT, UPDATE e DELETE.",
         "all": "Consultas + sobrecarga de conexão",
-        "allHint": "Também conta o trabalho de registro que seu driver envia entre consultas: BEGIN e COMMIT, SET e as verificações que mantêm uma conexão em pool ativa."
+        "allHint": "Também conta o trabalho de registro que seu driver envia entre consultas: BEGIN e COMMIT, SET e as verificações que mantêm uma conexão em pool ativa.",
+        "overheadLabel": "Incluir sobrecarga de conexão"
       },
       "completionBias": {
         "title": "Chamadas caíram {drop}% enquanto as falhas aumentaram",
@@ -15559,13 +15561,13 @@
       "noSamples": "Nenhuma execução individual carregada para este intervalo.",
       "noSeries": "Nenhum histórico para esta consulta no intervalo selecionado.",
       "liveSeries": "Os pontos mais recentes ainda estão sendo contados e podem mudar.",
-      "noStream": "Não conseguimos descobrir qual stream de trace contém esta consulta, então seus chamadores e exemplos não podem ser carregados.",
-      "ambiguousStream": "Esta organização tem mais de um stream de trace, e não conseguimos identificar em qual deles esta consulta é executada. Escolha um stream para carregar seus chamadores e traces de exemplo.",
-      "pickStream": "Escolha um stream de trace",
-      "invalidStream": "Esse nome de stream de trace não é reconhecido, então não executamos a busca.",
-      "firstSeen": "PRIMEIRA APARIÇÃO NESTA LISTA EM {time}",
+      "noStream": "Não foi possível determinar qual fluxo de rastreamento carrega esta consulta, então seus chamadores e exemplos não podem ser carregados.",
+      "ambiguousStream": "Esta organização tem mais de um fluxo de rastreamento e não podemos dizer em qual esta consulta é executada. Escolha um fluxo para carregar seus chamadores e rastreamentos de exemplo.",
+      "pickStream": "Escolha um fluxo de rastreamento",
+      "invalidStream": "Esse nome de fluxo de rastreamento não é reconhecido, então não executamos a busca.",
+      "firstSeen": "Apareceu pela primeira vez nesta lista às {time}",
       "firstSeenHint": "O ponto mais antigo neste intervalo em que esta consulta foi pesada o suficiente para ser rastreada por conta própria.",
-      "lastSeen": "ÚLTIMA APARIÇÃO EM {time}",
+      "lastSeen": "Apareceu pela última vez às {time}",
       "lastSeenHint": "O ponto mais recente neste intervalo em que esta consulta foi pesada o suficiente para ser rastreada por conta própria. Ela ainda pode estar em execução agora — esta é a última vez que entrou na lista.",
       "errorsByCode": {
         "title": "Falhas por código de motivo",
@@ -15732,9 +15734,6 @@
       }
     },
     "samples": {
-      "title": "Chamadas mais lentas",
-      "subtitle": "As chamadas individuais mais lentas ao banco de dados nesta janela, em todos os bancos de dados e consultas — conforme medidas pelos chamadores, apenas chamadas concluídas",
-      "subtitleServer": "As instruções mais lentas que seus bancos de dados concluíram nesta janela, medidas dentro do banco de dados",
       "logOffTitle": "Seus bancos de dados ainda não estão registrando instruções lentas",
       "logOffDescription": "Nada rastreou essas chamadas, então pedimos aos bancos de dados suas próprias instruções mais lentas — e eles não registram nenhuma. Esta lista vem do log de instruções do PostgreSQL (log_min_duration_statement), que está desligado por padrão — a etapa de registro do cartão de configuração o ativa. A captura por instrução é atualmente exclusiva do PostgreSQL, então MySQL e MariaDB ainda não podem alimentar esta lista. As principais consultas ainda podem mostrar o que é executado com mais frequência, pois leem as tabelas de contadores em vez disso.",
       "searchPlaceholder": "Buscar texto de consulta, serviço ou banco de dados…",
@@ -15776,16 +15775,14 @@
       }
     },
     "rowChips": {
-      "new": "NOVO NESTA LISTA",
-      "slower": "{ratio}× MAIS LENTO QUE O NORMAL",
-      "loop": "EXECUTA {multiplier}× POR REQUISIÇÃO",
-      "allFailed": "TODAS AS {count} FALHARAM",
-      "busier": "MAIS OCUPADO, NÃO MAIS LENTO",
-      "climbed": "NOVA CONSULTA MAIS PESADA"
+      "new": "Novo nesta lista",
+      "slower": "{ratio}× mais lento que o normal",
+      "loop": "Executa {multiplier}× por requisição",
+      "allFailed": "Todas as {count} falharam",
+      "busier": "Mais ocupado, não mais lento",
+      "climbed": "Nova consulta mais pesada"
     },
     "deadlocks": {
-      "title": "Bancos de dados",
-      "subtitle": "Transações que se bloquearam mutuamente e foram revertidas",
       "deadlockCount": "nenhum deadlock | {count} deadlock | {count} deadlocks",
       "pairCount": "nenhum par de consultas | {count} par de consultas | {count} pares de consultas",
       "searchPlaceholder": "Buscar texto de consulta, tabela ou aplicativo…",
@@ -15812,10 +15809,10 @@
         "mysqlPartial": "Seu banco de dados MySQL mantém apenas o deadlock mais recente, a menos que innodb_print_all_deadlocks esteja ativo."
       },
       "chips": {
-        "oppositeOrder": "ORDEM DE LINHAS OPOSTA",
-        "cadence": "A CADA {seconds}s",
-        "noQueries": "CONSULTAS NÃO CAPTURADAS",
-        "partial": "APENAS UM LADO"
+        "oppositeOrder": "Ordem inversa de linhas",
+        "cadence": "A cada {seconds}s",
+        "noQueries": "Consultas não capturadas",
+        "partial": "Apenas um lado"
       },
       "rowActions": {
         "topQueries": "Encontre essas nas Principais consultas",
@@ -15832,8 +15829,8 @@
         "pid": "pid {pid}",
         "thread": "thread {pid}",
         "transaction": "transação {id}",
-        "eachWaits": "cada um espera pelo outro",
-        "deadlock": "deadlock",
+        "eachWaits": "Cada um espera pelo outro",
+        "deadlock": "Deadlock",
         "noQueryCaptured": "O log registrou este deadlock, mas não a instrução.",
         "waitingFor": "Estava esperando {target} terminar.",
         "waitingForRow": "Estava esperando {target} terminar — a outra sessão já havia alterado a linha que queria.",
@@ -15935,9 +15932,6 @@
       }
     },
     "blocked": {
-      "title": "Bancos de dados",
-      "subtitle": "Sessões atualmente aguardando um lock",
-      "subtitlePast": "Sessões que aguardavam um lock durante esta janela",
       "waitingCount": "nenhuma sessão aguardando um lock | {count} sessão aguardando um lock agora | {count} sessões aguardando um lock agora",
       "waitingCountPast": "nenhuma sessão aguardou um lock | {count} sessão aguardou um lock nesta janela | {count} sessões aguardaram um lock nesta janela",
       "sessionCount": "nenhuma sessão | {count} sessão | {count} sessões",
@@ -15983,14 +15977,14 @@
       },
       "deep": "{count} de profundidade",
       "notWaiting": "não aguardando",
-      "longestWait": "ESPERA MAIS LONGA",
-      "holdsTheLock": "SEGURA O LOCK QUE TODOS QUEREM",
+      "longestWait": "Maior espera",
+      "holdsTheLock": "Mantém o bloqueio que todos querem",
       "lastStatement": "último comando há {age}",
       "idleInTransaction": "não aguardando nada · ocioso em uma transação aberta",
       "pills": {
-        "root": "RAIZ — COMECE AQUI",
-        "waitingBlocking": "AGUARDANDO & BLOQUEANDO {count}",
-        "waiting": "AGUARDANDO"
+        "root": "Raiz — comece aqui",
+        "waitingBlocking": "Aguardando e bloqueando {count}",
+        "waiting": "Aguardando"
       },
       "waitingOn": "aguardando {pid}",
       "degraded": "Uma sessão está bloqueando {count}. Ela está com uma transação aberta há {age} e não executou uma consulta nesse tempo — provavelmente um aplicativo que esqueceu de fazer commit.",
@@ -16068,9 +16062,6 @@
       }
     },
     "activity": {
-      "title": "Atividade",
-      "subtitle": "O que seus bancos de dados estão fazendo agora, amostrado de suas próprias tabelas de sessão.",
-      "subtitlePast": "O que seus bancos de dados estavam fazendo durante esta janela, amostrado de suas próprias tabelas de sessão.",
       "searchPlaceholder": "Pesquisar sessões, usuários, aplicativos",
       "wait": {
         "onCpu": "Em CPU / sem espera",
@@ -16194,8 +16185,6 @@
       "deadlockLabel": "deadlocks"
     },
     "tableHealth": {
-      "title": "Saúde das tabelas",
-      "subtitle": "Tamanho, bloat e estado de vacuum para cada tabela, instantâneo mais recente primeiro.",
       "searchPlaceholder": "Pesquisar tabelas",
       "never": "Nunca",
       "countersCumulative": "As contagens de scan e vacuum são totais acumulados desde a última redefinição de estatísticas, não contagens para o intervalo de tempo selecionado.",
diff --git a/web/src/locales/languages/ru-RU.json b/web/src/locales/languages/ru-RU.json
index 628f9d78fb9..570476b5571 100644
--- a/web/src/locales/languages/ru-RU.json
+++ b/web/src/locales/languages/ru-RU.json
@@ -14923,6 +14923,7 @@
       "disabled": "Отключено",
       "emptyTitle": "На этом SLO пока нет оповещений.",
       "emptyHint": "Оповещения о скорости выгорания отправляют вам сигнал, пока ещё есть бюджет для защиты.",
+      "emptyActionDesc": "Получите предупреждение до того, как бюджет закончится",
       "loadFailed": "Не удалось загрузить оповещения этого SLO.",
       "editTargetMissing": "Это оповещение больше не относится к этому SLO.",
       "describeBurn": "Скорость выгорания > {critical} за {long} и {short}",
@@ -15215,7 +15216,12 @@
     "common": {
       "reload": "Обновить",
       "lastRefreshed": "Обновлено {age}",
-      "loadFailed": "Не удалось загрузить данные базы данных. Попробуйте снова или сузьте временной диапазон."
+      "loadFailed": "Не удалось загрузить данные базы данных. Попробуйте снова или сузьте временной диапазон.",
+      "actions": "Действия"
+    },
+    "header": {
+      "title": "Базы данных",
+      "subtitle": "Производительность и состояние запросов в ваших базах данных"
     },
     "ai": {
       "suggestFix": "Спросите ИИ, как это исправить",
@@ -15237,12 +15243,12 @@
       "removeScope": "Удалить этот фильтр",
       "popoverTitle": "Сузить",
       "dimension": {
-        "system": "движок",
-        "instance": "база данных",
-        "namespace": "схема",
-        "env": "окружение",
-        "service": "сервис",
-        "insight": "инсайт"
+        "system": "Движок",
+        "instance": "База данных",
+        "namespace": "Схема",
+        "env": "Окружение",
+        "service": "Сервис",
+        "insight": "Инсайт"
       }
     },
     "delta": {
@@ -15257,8 +15263,6 @@
       "wasValue": "было {value}"
     },
     "databases": {
-      "title": "Базы данных",
-      "subtitle": "Производительность и состояние запросов по базам данных",
       "searchPlaceholder": "Поиск базы данных или сервиса…",
       "unnamedInstance": "Экземпляр не назван",
       "unnamedInstanceHint": "Собственные отчёты базы данных подтверждают, что этот движок работает, но они не указывают хост. Строка покажет адрес, когда будет настроен источник данных, который передаёт его — выборки сессий или метрик экземпляра.",
@@ -15305,9 +15309,6 @@
       }
     },
     "queries": {
-      "title": "Топ запросов",
-      "subtitle": "Запросы, использующие больше всего времени базы данных",
-      "subtitleServer": "Операторы, которые выполняли ваши базы данных, подсчитанные внутри базы данных — сначала наиболее часто выполняемые",
       "searchPlaceholder": "Поиск по тексту запроса, таблице или сервису…",
       "trendLabel": "Как изменялось время этого запроса в течение периода",
       "otherRow": "Все остальные запросы",
@@ -15316,7 +15317,8 @@
         "query": "Запросы",
         "queryHint": "Только настоящий SQL — SELECT, INSERT, UPDATE и DELETE.",
         "all": "Запросы + накладные расходы на соединение",
-        "allHint": "Также учитывает служебные сообщения, которые ваш драйвер отправляет между запросами: BEGIN и COMMIT, SET и проверки, поддерживающие активное соединение в пуле."
+        "allHint": "Также учитывает служебные сообщения, которые ваш драйвер отправляет между запросами: BEGIN и COMMIT, SET и проверки, поддерживающие активное соединение в пуле.",
+        "overheadLabel": "Учитывать накладные расходы на соединение"
       },
       "completionBias": {
         "title": "Количество вызовов упало на {drop}%, при этом сбои выросли",
@@ -15559,13 +15561,13 @@
       "noSamples": "Для этого диапазона не загружено ни одного отдельного выполнения.",
       "noSeries": "Нет истории для этого запроса в выбранном вами диапазоне.",
       "liveSeries": "Самые последние точки все еще подсчитываются и могут измениться.",
-      "noStream": "Не удалось определить, какой поток трейсов содержит этот запрос, поэтому его вызывающие стороны и примеры не могут быть загружены.",
-      "ambiguousStream": "В этой организации более одного потока трейсов, и мы не можем определить, в каком из них выполняется этот запрос. Выберите поток, чтобы загрузить его вызывающие стороны и примеры трейсов.",
-      "pickStream": "Выберите поток трейсов",
-      "invalidStream": "Это имя потока трейсов нам не знакомо, поэтому мы не выполнили поиск.",
-      "firstSeen": "ВПЕРВЫЕ ПОЯВИЛСЯ В ЭТОМ СПИСКЕ В {time}",
+      "noStream": "Мы не смогли определить, какой поток трассировок переносит этот запрос, поэтому его вызывающие и примеры не могут быть загружены.",
+      "ambiguousStream": "В этой организации более одного потока трассировок, и мы не можем определить, на каком из них работает этот запрос. Выберите поток, чтобы загрузить его вызывающие и примеры трассировок.",
+      "pickStream": "Выберите поток трассировок",
+      "invalidStream": "Имя этого потока трассировок нам не знакомо, поэтому мы не выполнили поиск.",
+      "firstSeen": "Впервые появился в этом списке в {time}",
       "firstSeenHint": "Самая ранняя точка в этом диапазоне, где этот запрос был достаточно тяжелым, чтобы отслеживаться отдельно.",
-      "lastSeen": "ПОСЛЕДНИЙ РАЗ ПОЯВИЛСЯ В {time}",
+      "lastSeen": "Последний раз появился в {time}",
       "lastSeenHint": "Самая последняя точка в этом диапазоне, где этот запрос был достаточно тяжелым, чтобы отслеживаться отдельно. Возможно, он все еще выполняется — это последний раз, когда он попал в список.",
       "errorsByCode": {
         "title": "Сбои по коду причины",
@@ -15732,9 +15734,6 @@
       }
     },
     "samples": {
-      "title": "Самые медленные вызовы",
-      "subtitle": "Самые медленные отдельные вызовы к базе данных за этот период, по всем базам данных и запросам — в том виде, в котором их измерили вызывающие приложения; только завершённые вызовы",
-      "subtitleServer": "Самые медленные запросы, выполненные вашими базами данных за этот период, измеренные внутри базы данных",
       "logOffTitle": "Ваши базы данных пока не записывают медленные запросы в журнал",
       "logOffDescription": "Эти вызовы не были отслежены, поэтому мы запросили у баз данных их собственные самые медленные запросы — но они ничего не регистрируют. Этот список формируется из журнала запросов PostgreSQL (log_min_duration_statement), который по умолчанию выключен — шаг настройки в карточке настройки включает его. Захват отдельных запросов сейчас доступен только в PostgreSQL, поэтому MySQL и MariaDB пока не могут наполнять этот список. Список топ-запросов по-прежнему показывает, что выполняется чаще всего, поскольку он читает таблицы счётчиков.",
       "searchPlaceholder": "Поиск по тексту запроса, сервису или базе данных…",
@@ -15776,16 +15775,14 @@
       }
     },
     "rowChips": {
-      "new": "НОВОЕ В ЭТОМ СПИСКЕ",
-      "slower": "{ratio}× МЕДЛЕННЕЕ ОБЫЧНОГО",
-      "loop": "ЗАПУСКОВ {multiplier}× НА ЗАПРОС",
-      "allFailed": "ВСЕ {count} ЗАВЕРШИЛИСЬ ОШИБКОЙ",
-      "busier": "НАГРУЖЕННЕЕ, НО НЕ МЕДЛЕННЕЕ",
-      "climbed": "НОВЫЙ САМЫЙ ТЯЖЁЛЫЙ ЗАПРОС"
+      "new": "Новый в этом списке",
+      "slower": "в {ratio} раз медленнее обычного",
+      "loop": "Выполняется {multiplier} раз на запрос",
+      "allFailed": "Все {count} завершились ошибкой",
+      "busier": "Нагруженнее, но не медленнее",
+      "climbed": "Новый самый тяжелый запрос"
     },
     "deadlocks": {
-      "title": "Базы данных",
-      "subtitle": "Транзакции, которые блокировали друг друга и были откачены",
       "deadlockCount": "нет взаимоблокировок | {count} взаимоблокировка | {count} взаимоблокировок",
       "pairCount": "нет пар запросов | {count} пара запросов | {count} пар запросов",
       "searchPlaceholder": "Поиск по тексту запроса, таблице или приложению…",
@@ -15812,10 +15809,10 @@
         "mysqlPartial": "Ваша база данных MySQL хранит только самую последнюю взаимоблокировку, если не включён параметр innodb_print_all_deadlocks."
       },
       "chips": {
-        "oppositeOrder": "ПРОТИВОПОЛОЖНЫЙ ПОРЯДОК СТРОК",
-        "cadence": "КАЖДЫЕ {seconds} С",
-        "noQueries": "ЗАПРОСЫ НЕ ЗАХВАЧЕНЫ",
-        "partial": "ТОЛЬКО ОДНА СТОРОНА"
+        "oppositeOrder": "Обратный порядок строк",
+        "cadence": "Каждые {seconds} с",
+        "noQueries": "Запросы не зафиксированы",
+        "partial": "Только одна сторона"
       },
       "rowActions": {
         "topQueries": "Найдите их в Top queries",
@@ -15832,8 +15829,8 @@
         "pid": "pid {pid}",
         "thread": "поток {pid}",
         "transaction": "транзакция {id}",
-        "eachWaits": "каждый ждёт другого",
-        "deadlock": "взаимоблокировка",
+        "eachWaits": "Каждый ждет другого",
+        "deadlock": "Взаимная блокировка",
         "noQueryCaptured": "Журнал записал эту взаимоблокировку, но не запрос.",
         "waitingFor": "Ожидал завершения {target}.",
         "waitingForRow": "Ожидал завершения {target} — другая сессия уже изменила нужную ему строку.",
@@ -15935,9 +15932,6 @@
       }
     },
     "blocked": {
-      "title": "Базы данных",
-      "subtitle": "Сеансы, ожидающие блокировку в данный момент",
-      "subtitlePast": "Сеансы, ожидавшие блокировку в течение этого окна",
       "waitingCount": "нет сеансов, ожидающих блокировку | {count} сеанс ожидает блокировку прямо сейчас | {count} сеансов ожидают блокировку прямо сейчас",
       "waitingCountPast": "нет сеансов, ожидавших блокировку | {count} сеанс ожидал блокировку в этом окне | {count} сеансов ожидали блокировку в этом окне",
       "sessionCount": "нет сеансов | {count} сеанс | {count} сеансов",
@@ -15983,14 +15977,14 @@
       },
       "deep": "{count} уровней",
       "notWaiting": "не ожидает",
-      "longestWait": "САМОЕ ДОЛГОЕ ОЖИДАНИЕ",
-      "holdsTheLock": "ДЕРЖИТ БЛОКИРОВКУ, КОТОРУЮ ХОТЯТ ВСЕ",
+      "longestWait": "Самое долгое ожидание",
+      "holdsTheLock": "Удерживает блокировку, которую ждут все",
       "lastStatement": "последний запрос {age} назад",
       "idleInTransaction": "ничего не ожидает · простаивает в открытой транзакции",
       "pills": {
-        "root": "КОРЕНЬ — НАЧНИТЕ ЗДЕСЬ",
-        "waitingBlocking": "ОЖИДАНИЕ И БЛОКИРОВКА {count}",
-        "waiting": "ОЖИДАНИЕ"
+        "root": "Корень — начните здесь",
+        "waitingBlocking": "Ожидание и блокировка: {count}",
+        "waiting": "Ожидание"
       },
       "waitingOn": "ожидает {pid}",
       "degraded": "Один сеанс блокирует {count} сеансов. У него открытая транзакция в течение {age}, и за это время он не выполнил ни одного запроса — скорее всего, приложение забыло зафиксировать транзакцию.",
@@ -16068,9 +16062,6 @@
       }
     },
     "activity": {
-      "title": "Активность",
-      "subtitle": "Что ваши базы данных делают прямо сейчас, по данным из их собственных таблиц сессий.",
-      "subtitlePast": "Что ваши базы данных делали за этот период, по данным из их собственных таблиц сессий.",
       "searchPlaceholder": "Поиск сессий, пользователей, приложений",
       "wait": {
         "onCpu": "На CPU / без ожидания",
@@ -16194,8 +16185,6 @@
       "deadlockLabel": "взаимоблокировки"
     },
     "tableHealth": {
-      "title": "Состояние таблиц",
-      "subtitle": "Размер, раздувание и состояние vacuum для каждой таблицы, сначала новый снимок.",
       "searchPlaceholder": "Поиск таблиц",
       "never": "Никогда",
       "countersCumulative": "Количество сканирований и vacuum — это общие значения за все время с последнего сброса статистики, а не количество за выбранный диапазон времени.",
diff --git a/web/src/locales/languages/tr-TR.json b/web/src/locales/languages/tr-TR.json
index 88ad12541f6..e8ab42edf3c 100644
--- a/web/src/locales/languages/tr-TR.json
+++ b/web/src/locales/languages/tr-TR.json
@@ -14923,6 +14923,7 @@
       "disabled": "Devre Dışı",
       "emptyTitle": "Bu SLO'da henüz uyarı yok",
       "emptyHint": "Tükenme oranı uyarıları, bütçe henüz tükenmemişken sizi uyarır.",
+      "emptyActionDesc": "Bütçe tükenmeden önce uyarı alın",
       "loadFailed": "Bu SLO'nun uyarıları yüklenemedi.",
       "editTargetMissing": "Bu uyarı artık bu SLO'da değil.",
       "describeBurn": "Tükenme oranı > {critical}, {long} ve {short} boyunca",
@@ -15215,7 +15216,12 @@
     "common": {
       "reload": "Yenile",
       "lastRefreshed": "Son yenileme {age}",
-      "loadFailed": "Veritabanı verileri yüklenemedi. Tekrar deneyin veya zaman aralığını daraltın."
+      "loadFailed": "Veritabanı verileri yüklenemedi. Tekrar deneyin veya zaman aralığını daraltın.",
+      "actions": "Eylemler"
+    },
+    "header": {
+      "title": "Veritabanları",
+      "subtitle": "Veritabanlarınızdaki sorgu performansı ve sağlığı"
     },
     "ai": {
       "suggestFix": "Bunu nasıl düzelteceğini AI'ya sor",
@@ -15237,12 +15243,12 @@
       "removeScope": "Bu filtreyi kaldır",
       "popoverTitle": "Bunu daralt",
       "dimension": {
-        "system": "motor",
-        "instance": "veritabanı",
-        "namespace": "şema",
-        "env": "ortam",
-        "service": "hizmet",
-        "insight": "içgörü"
+        "system": "Motor",
+        "instance": "Veritabanı",
+        "namespace": "Şema",
+        "env": "Ortam",
+        "service": "Servis",
+        "insight": "İçgörü"
       }
     },
     "delta": {
@@ -15257,8 +15263,6 @@
       "wasValue": "{value} idi"
     },
     "databases": {
-      "title": "Veritabanları",
-      "subtitle": "Veritabanı başına sorgu performansı ve sağlığı",
       "searchPlaceholder": "Veritabanı veya hizmet ara…",
       "unnamedInstance": "Örnek adlandırılmamış",
       "unnamedInstanceHint": "Veritabanının kendi ifade raporları bu motorun çalıştığını kanıtlıyor, ancak bir ana bilgisayar adı vermiyorlar. Satır, bir adres taşıyan bir akış — oturum örnekleme veya örnek ölçümleri — kurulduğunda bir adres gösterecek.",
@@ -15305,9 +15309,6 @@
       }
     },
     "queries": {
-      "title": "En yoğun sorgular",
-      "subtitle": "En fazla veritabanı süresi kullanan sorgular",
-      "subtitleServer": "Veritabanlarınızın çalıştırdığı ifadeler, veritabanı içinde sayılır — en sık çalıştırılanlar ilk sırada",
       "searchPlaceholder": "Sorgu metni, tablo veya hizmet ara…",
       "trendLabel": "Bu sorgunun süresinin aralık boyunca nasıl değiştiği",
       "otherRow": "Diğer tüm sorgular",
@@ -15316,7 +15317,8 @@
         "query": "Sorgular",
         "queryHint": "Yalnızca gerçek SQL — SELECT, INSERT, UPDATE ve DELETE.",
         "all": "Sorgular + bağlantı ek yükü",
-        "allHint": "Ayrıca sürücünüzün sorgular arasında gönderdiği kayıt işlemlerini sayar: BEGIN ve COMMIT, SET ve havuzlanmış bağlantıyı canlı tutan kontroller."
+        "allHint": "Ayrıca sürücünüzün sorgular arasında gönderdiği kayıt işlemlerini sayar: BEGIN ve COMMIT, SET ve havuzlanmış bağlantıyı canlı tutan kontroller.",
+        "overheadLabel": "Bağlantı yükünü dahil et"
       },
       "completionBias": {
         "title": "Çağrılar {drop}% düşerken hatalar arttı",
@@ -15562,10 +15564,10 @@
       "noStream": "Bu sorguyu hangi iz akışının taşıdığını belirleyemedik, bu nedenle çağıranları ve örnekleri yüklenemiyor.",
       "ambiguousStream": "Bu kuruluşun birden fazla iz akışı var ve bu sorgunun hangisinde çalıştığını söyleyemiyoruz. Çağıranları ve örnek izleri yüklemek için bir akış seçin.",
       "pickStream": "Bir iz akışı seçin",
-      "invalidStream": "Bu iz akışı adı tanımadığımız bir ad, bu nedenle aramayı çalıştırmadık.",
-      "firstSeen": "BU LİSTEDE İLK GÖRÜNME: {time}",
+      "invalidStream": "Bu iz akışı adı tanıdığımız bir ad değil, bu yüzden aramayı çalıştırmadık.",
+      "firstSeen": "Bu listede ilk kez {time} görüldü",
       "firstSeenHint": "Bu sorgunun kendi başına izlenecek kadar ağır olduğu bu aralıktaki en erken nokta.",
-      "lastSeen": "SON GÖRÜNME: {time}",
+      "lastSeen": "Son görülme: {time}",
       "lastSeenHint": "Bu sorgunun kendi başına izlenecek kadar ağır olduğu bu aralıktaki en son nokta. Hâlâ çalışıyor olabilir — listeye girdiği son zaman budur.",
       "errorsByCode": {
         "title": "Sebep koduna göre hatalar",
@@ -15732,9 +15734,6 @@
       }
     },
     "samples": {
-      "title": "En yavaş çağrılar",
-      "subtitle": "Bu penceredeki en yavaş bireysel veritabanı çağrıları, her veritabanı ve sorgu genelinde — çağıranların ölçtüğü şekliyle, yalnızca tamamlanan çağrılar",
-      "subtitleServer": "Bu pencerede veritabanlarınızın tamamladığı en yavaş ifadeler, veritabanı içinde ölçülmüştür",
       "logOffTitle": "Veritabanlarınız henüz yavaş ifadeleri günlüğe kaydetmiyor",
       "logOffDescription": "Hiçbir şey bu çağrıları izlemedi, bu yüzden veritabanlarına kendi en yavaş ifadelerini sorduk — ve hiçbirini günlüğe kaydetmiyorlar. Bu liste PostgreSQL'in ifade günlüğünden (log_min_duration_statement) gelir; varsayılan olarak kapalıdır — kurulum kartının günlüğe kaydetme adımı onu açar. İfade başına yakalama şu anda yalnızca PostgreSQL'e özgüdür, bu nedenle MySQL ve MariaDB henüz bu listeyi besleyemez. En çok çalışan sorgular size neyin en çok çalıştığını gösterebilir, çünkü bunun yerine sayaç tablolarını okur.",
       "searchPlaceholder": "Sorgu metni, hizmet veya veritabanı ara…",
@@ -15776,16 +15775,14 @@
       }
     },
     "rowChips": {
-      "new": "BU LİSTEDE YENİ",
-      "slower": "{ratio}× NORMALDEN DAHA YAVAŞ",
-      "loop": "İSTEK BAŞINA {multiplier}× ÇALIŞIR",
-      "allFailed": "TÜM {count} BAŞARISIZ",
-      "busier": "DAHA YOĞUN, DAHA YAVAŞ DEĞİL",
-      "climbed": "YENİ EN AĞIR SORGU"
+      "new": "Bu listede yeni",
+      "slower": "Normalden {ratio}× daha yavaş",
+      "loop": "İstek başına {multiplier}× çalışır",
+      "allFailed": "Tüm {count} başarısız oldu",
+      "busier": "Daha yoğun, daha yavaş değil",
+      "climbed": "Yeni en ağır sorgu"
     },
     "deadlocks": {
-      "title": "Veritabanları",
-      "subtitle": "Birbirini engelleyen ve geri alınan işlemler",
       "deadlockCount": "kilitlenme yok | {count} kilitlenme | {count} kilitlenme",
       "pairCount": "sorgu çifti yok | {count} sorgu çifti | {count} sorgu çifti",
       "searchPlaceholder": "Sorgu metni, tablo veya uygulama ara…",
@@ -15812,10 +15809,10 @@
         "mysqlPartial": "MySQL veritabanınız, innodb_print_all_deadlocks açık olmadığı sürece yalnızca en son deadlock'u tutar."
       },
       "chips": {
-        "oppositeOrder": "TERS SATIR SIRASI",
-        "cadence": "HER {seconds}s",
-        "noQueries": "YAKALANMAYAN SORGULAR",
-        "partial": "YALNIZCA TEK TARAF"
+        "oppositeOrder": "Ters satır sırası",
+        "cadence": "Her {seconds}s",
+        "noQueries": "Yakalanmayan sorgular",
+        "partial": "Yalnızca tek taraf"
       },
       "rowActions": {
         "topQueries": "Bunları En İyi Sorgular'da bulun",
@@ -15832,8 +15829,8 @@
         "pid": "pid {pid}",
         "thread": "thread {pid}",
         "transaction": "transaction {id}",
-        "eachWaits": "her biri diğerini bekliyor",
-        "deadlock": "deadlock",
+        "eachWaits": "Her biri diğerini bekliyor",
+        "deadlock": "Kilitlenme",
         "noQueryCaptured": "Günlük bu deadlock'u kaydetti ancak ifadeyi kaydetmedi.",
         "waitingFor": "{target} tamamlanmasını bekliyordu.",
         "waitingForRow": "{target} tamamlanmasını bekliyordu — diğer oturum istediği satırı zaten değiştirmişti.",
@@ -15935,9 +15932,6 @@
       }
     },
     "blocked": {
-      "title": "Veritabanları",
-      "subtitle": "Şu anda bir kilit üzerinde bekleyen oturumlar",
-      "subtitlePast": "Bu zaman aralığında bir kilit üzerinde bekleyen oturumlar",
       "waitingCount": "kilidi bekleyen oturum yok | şu anda kilidi bekleyen {count} oturum | şu anda kilidi bekleyen {count} oturum",
       "waitingCountPast": "kilidi bekleyen oturum olmadı | bu zaman aralığında kilidi bekleyen {count} oturum | bu zaman aralığında kilidi bekleyen {count} oturum",
       "sessionCount": "oturum yok | {count} oturum | {count} oturum",
@@ -15983,14 +15977,14 @@
       },
       "deep": "{count} derin",
       "notWaiting": "beklemiyor",
-      "longestWait": "EN UZUN BEKLEME",
-      "holdsTheLock": "HERKESİN İSTEDİĞİ KİLİDİ TUTUYOR",
+      "longestWait": "En uzun bekleme",
+      "holdsTheLock": "Herkesin istediği kilidi tutuyor",
       "lastStatement": "son ifade {age} önce",
       "idleInTransaction": "hiçbir şey beklemiyor · açık bir işlemde boşta",
       "pills": {
-        "root": "KÖK — BURADAN BAŞLAYIN",
-        "waitingBlocking": "BEKLİYOR VE ENGELLİYOR {count}",
-        "waiting": "BEKLİYOR"
+        "root": "Kök — buradan başlayın",
+        "waitingBlocking": "Bekleyen ve engelleyen {count}",
+        "waiting": "Bekliyor"
       },
       "waitingOn": "{pid} bekleniyor",
       "degraded": "Bir oturum {count} oturumu engelliyor. {age} süredir açık bir işlemi var ve bu süre içinde sorgu çalıştırmadı — büyük olasılıkla commit etmeyi unutan bir uygulama.",
@@ -16068,9 +16062,6 @@
       }
     },
     "activity": {
-      "title": "Etkinlik",
-      "subtitle": "Veritabanlarınızın şu anda ne yaptığı, kendi oturum tablolarından alınan örneklerle gösterilir.",
-      "subtitlePast": "Veritabanlarınızın bu zaman aralığında ne yaptığı, kendi oturum tablolarından alınan örneklerle gösterilir.",
       "searchPlaceholder": "Oturumları, kullanıcıları, uygulamaları ara",
       "wait": {
         "onCpu": "CPU'da / bekleme yok",
@@ -16194,8 +16185,6 @@
       "deadlockLabel": "Kilitlenmeler"
     },
     "tableHealth": {
-      "title": "Tablo sağlığı",
-      "subtitle": "Her tablo için boyut, şişkinlik ve vakum durumu, en yeni anlık görüntü önce.",
       "searchPlaceholder": "Tabloları ara",
       "never": "Asla",
       "countersCumulative": "Tarama ve vakum sayıları, son istatistik sıfırlamasından bu yana ömür boyu toplamlardır, seçilen zaman aralığı için sayılar değildir.",
diff --git a/web/src/locales/languages/vi-VN.json b/web/src/locales/languages/vi-VN.json
index c14ca261250..eb39d9ae08a 100644
--- a/web/src/locales/languages/vi-VN.json
+++ b/web/src/locales/languages/vi-VN.json
@@ -14923,6 +14923,7 @@
       "disabled": "Đã tắt",
       "emptyTitle": "Chưa có cảnh báo nào trên SLO này",
       "emptyHint": "Cảnh báo tỷ lệ đốt sẽ nhắn tin cho bạn khi vẫn còn ngân sách để bảo vệ.",
+      "emptyActionDesc": "Nhận cảnh báo trước khi ngân sách hết",
       "loadFailed": "Không thể tải cảnh báo của SLO này.",
       "editTargetMissing": "Cảnh báo đó không còn trên SLO này.",
       "describeBurn": "Tỷ lệ đốt > {critical} trong {long} và {short}",
@@ -15215,7 +15216,12 @@
     "common": {
       "reload": "Làm mới",
       "lastRefreshed": "Làm mới lần cuối {age}",
-      "loadFailed": "Không thể tải dữ liệu cơ sở dữ liệu. Hãy thử lại hoặc thu hẹp phạm vi thời gian."
+      "loadFailed": "Không thể tải dữ liệu cơ sở dữ liệu. Hãy thử lại hoặc thu hẹp phạm vi thời gian.",
+      "actions": "Hành động"
+    },
+    "header": {
+      "title": "Cơ sở dữ liệu",
+      "subtitle": "Hiệu suất và tình trạng truy vấn trên các cơ sở dữ liệu của bạn"
     },
     "ai": {
       "suggestFix": "Hỏi AI cách khắc phục điều này",
@@ -15237,12 +15243,12 @@
       "removeScope": "Xóa bộ lọc này",
       "popoverTitle": "Thu hẹp phạm vi này",
       "dimension": {
-        "system": "engine",
-        "instance": "cơ sở dữ liệu",
-        "namespace": "lược đồ",
-        "env": "môi trường",
-        "service": "dịch vụ",
-        "insight": "thông tin chi tiết"
+        "system": "Công cụ",
+        "instance": "Cơ sở dữ liệu",
+        "namespace": "Lược đồ",
+        "env": "Môi trường",
+        "service": "Dịch vụ",
+        "insight": "Thông tin chuyên sâu"
       }
     },
     "delta": {
@@ -15257,8 +15263,6 @@
       "wasValue": "đã là {value}"
     },
     "databases": {
-      "title": "Cơ sở dữ liệu",
-      "subtitle": "Hiệu suất và tình trạng truy vấn theo từng cơ sở dữ liệu",
       "searchPlaceholder": "Tìm kiếm cơ sở dữ liệu hoặc dịch vụ…",
       "unnamedInstance": "Phiên bản chưa đặt tên",
       "unnamedInstanceHint": "Báo cáo câu lệnh của cơ sở dữ liệu chứng minh công cụ này đang chạy, nhưng chúng không đặt tên máy chủ. Hàng sẽ hiển thị địa chỉ khi một nguồn cấp dữ liệu mang địa chỉ đó — lấy mẫu phiên hoặc số liệu phiên bản — được thiết lập.",
@@ -15305,9 +15309,6 @@
       }
     },
     "queries": {
-      "title": "Truy vấn hàng đầu",
-      "subtitle": "Các truy vấn sử dụng nhiều thời gian cơ sở dữ liệu nhất",
-      "subtitleServer": "Các câu lệnh mà cơ sở dữ liệu của bạn đã chạy, được đếm bên trong cơ sở dữ liệu — thường chạy nhiều nhất trước tiên",
       "searchPlaceholder": "Tìm kiếm văn bản truy vấn, bảng hoặc dịch vụ…",
       "trendLabel": "Thời gian của truy vấn này đã thay đổi như thế nào trong phạm vi",
       "otherRow": "Tất cả truy vấn khác",
@@ -15316,7 +15317,8 @@
         "query": "Truy vấn",
         "queryHint": "Chỉ SQL thực sự — SELECT, INSERT, UPDATE và DELETE.",
         "all": "Truy vấn + chi phí kết nối",
-        "allHint": "Cũng tính các hoạt động ghi sổ mà trình điều khiển của bạn gửi giữa các truy vấn: BEGIN và COMMIT, SET, và các kiểm tra giữ cho kết nối nhóm luôn hoạt động."
+        "allHint": "Cũng tính các hoạt động ghi sổ mà trình điều khiển của bạn gửi giữa các truy vấn: BEGIN và COMMIT, SET, và các kiểm tra giữ cho kết nối nhóm luôn hoạt động.",
+        "overheadLabel": "Bao gồm chi phí kết nối"
       },
       "completionBias": {
         "title": "Lời gọi giảm {drop}% trong khi số lần thất bại tăng",
@@ -15562,10 +15564,10 @@
       "noStream": "Chúng tôi không thể xác định luồng trace nào chứa truy vấn này, vì vậy không thể tải caller và ví dụ của nó.",
       "ambiguousStream": "Tổ chức này có nhiều hơn một luồng trace, và chúng tôi không thể biết truy vấn này chạy trên luồng nào. Hãy chọn một luồng để tải caller và trace mẫu.",
       "pickStream": "Chọn một luồng trace",
-      "invalidStream": "Tên luồng trace đó không được chúng tôi nhận diện, nên chúng tôi đã không chạy tìm kiếm.",
-      "firstSeen": "LẦN ĐẦU XUẤT HIỆN TRONG DANH SÁCH NÀY LÚC {time}",
+      "invalidStream": "Tên luồng trace đó không phải là tên chúng tôi nhận ra, vì vậy chúng tôi đã không chạy tìm kiếm.",
+      "firstSeen": "Xuất hiện lần đầu trong danh sách này lúc {time}",
       "firstSeenHint": "Điểm sớm nhất trong khoảng thời gian này mà truy vấn này đủ nặng để theo dõi riêng.",
-      "lastSeen": "LẦN CUỐI XUẤT HIỆN LÚC {time}",
+      "lastSeen": "Xuất hiện lần cuối lúc {time}",
       "lastSeenHint": "Điểm gần đây nhất trong khoảng thời gian này mà truy vấn này đủ nặng để theo dõi riêng. Nó có thể vẫn đang chạy — đây là lần cuối cùng nó lọt vào danh sách.",
       "errorsByCode": {
         "title": "Lỗi theo mã lý do",
@@ -15732,9 +15734,6 @@
       }
     },
     "samples": {
-      "title": "Cuộc gọi chậm nhất",
-      "subtitle": "Các cuộc gọi cơ sở dữ liệu chậm nhất trong cửa sổ này, trên mọi cơ sở dữ liệu và truy vấn — theo cách người gọi đo lường, chỉ các cuộc gọi đã hoàn thành",
-      "subtitleServer": "Các câu lệnh chậm nhất mà cơ sở dữ liệu của bạn hoàn thành trong cửa sổ này, được đo bên trong cơ sở dữ liệu",
       "logOffTitle": "Cơ sở dữ liệu của bạn chưa ghi nhật ký các câu lệnh chậm",
       "logOffDescription": "Không có gì theo dõi các cuộc gọi này, vì vậy chúng tôi đã hỏi cơ sở dữ liệu về các câu lệnh chậm nhất của chúng — và chúng không ghi nhật ký nào. Danh sách này đến từ nhật ký câu lệnh của PostgreSQL (log_min_duration_statement), mặc định bị tắt — bước ghi nhật ký trong thẻ thiết lập sẽ bật nó. Việc thu thập theo từng câu lệnh hiện chỉ dành cho PostgreSQL, vì vậy MySQL và MariaDB chưa thể cung cấp cho danh sách này. Các truy vấn hàng đầu vẫn có thể cho bạn thấy điều gì chạy nhiều nhất, vì nó đọc bảng đếm thay thế.",
       "searchPlaceholder": "Tìm kiếm văn bản truy vấn, dịch vụ hoặc cơ sở dữ liệu…",
@@ -15776,16 +15775,14 @@
       }
     },
     "rowChips": {
-      "new": "MỚI TRONG DANH SÁCH NÀY",
-      "slower": "{ratio}× CHẬM HƠN THƯỜNG LỆ",
-      "loop": "CHẠY {multiplier}× MỖI YÊU CẦU",
-      "allFailed": "TẤT CẢ {count} THẤT BẠI",
-      "busier": "BẬN HƠN, KHÔNG CHẬM HƠN",
-      "climbed": "TRUY VẤN NẶNG NHẤT MỚI"
+      "new": "Mới xuất hiện trong danh sách này",
+      "slower": "Chậm hơn thông thường {ratio}×",
+      "loop": "Chạy {multiplier}× mỗi yêu cầu",
+      "allFailed": "Tất cả {count} đều thất bại",
+      "busier": "Bận rộn hơn, không chậm hơn",
+      "climbed": "Truy vấn nặng nhất mới"
     },
     "deadlocks": {
-      "title": "Cơ sở dữ liệu",
-      "subtitle": "Các giao dịch chặn lẫn nhau và đã bị hoàn tác",
       "deadlockCount": "không có deadlock | {count} deadlock | {count} deadlock",
       "pairCount": "không có cặp truy vấn | {count} cặp truy vấn | {count} cặp truy vấn",
       "searchPlaceholder": "Tìm kiếm văn bản truy vấn, bảng hoặc ứng dụng…",
@@ -15812,10 +15809,10 @@
         "mysqlPartial": "Cơ sở dữ liệu MySQL của bạn chỉ giữ deadlock gần nhất trừ khi bật innodb_print_all_deadlocks."
       },
       "chips": {
-        "oppositeOrder": "THỨ TỰ HÀNG NGƯỢC LẠI",
-        "cadence": "MỖI {seconds}s",
-        "noQueries": "CÁC TRUY VẤN KHÔNG ĐƯỢC GHI LẠI",
-        "partial": "CHỈ MỘT PHÍA"
+        "oppositeOrder": "Thứ tự hàng đảo ngược",
+        "cadence": "Mỗi {seconds}s",
+        "noQueries": "Các truy vấn không được thu thập",
+        "partial": "Chỉ một phía"
       },
       "rowActions": {
         "topQueries": "Tìm các truy vấn này trong Top queries",
@@ -15832,8 +15829,8 @@
         "pid": "pid {pid}",
         "thread": "thread {pid}",
         "transaction": "giao dịch {id}",
-        "eachWaits": "mỗi bên chờ đợi bên kia",
-        "deadlock": "deadlock",
+        "eachWaits": "Mỗi bên chờ đợi bên kia",
+        "deadlock": "Bế tắc",
         "noQueryCaptured": "Nhật ký đã ghi lại deadlock này nhưng không ghi lại câu lệnh.",
         "waitingFor": "Đang chờ {target} hoàn thành.",
         "waitingForRow": "Đang chờ {target} hoàn thành — phiên khác đã thay đổi hàng mà nó muốn.",
@@ -15935,9 +15932,6 @@
       }
     },
     "blocked": {
-      "title": "Cơ sở dữ liệu",
-      "subtitle": "Phiên hiện đang chờ khóa",
-      "subtitlePast": "Các phiên đã chờ khóa trong khoảng thời gian này",
       "waitingCount": "không có phiên nào đang chờ khóa | {count} phiên đang chờ khóa ngay bây giờ | {count} phiên đang chờ khóa ngay bây giờ",
       "waitingCountPast": "không có phiên nào đã chờ khóa | {count} phiên đã chờ khóa trong khoảng thời gian này | {count} phiên đã chờ khóa trong khoảng thời gian này",
       "sessionCount": "không có phiên | {count} phiên | {count} phiên",
@@ -15983,14 +15977,14 @@
       },
       "deep": "sâu {count}",
       "notWaiting": "không chờ",
-      "longestWait": "THỜI GIAN CHỜ LÂU NHẤT",
-      "holdsTheLock": "GIỮ KHÓA MÀ MỌI NGƯỜI ĐỀU MUỐN",
+      "longestWait": "Thời gian chờ lâu nhất",
+      "holdsTheLock": "Giữ khóa mà mọi người đều muốn",
       "lastStatement": "câu lệnh cuối {age} trước",
       "idleInTransaction": "không chờ gì · rảnh trong một giao dịch mở",
       "pills": {
-        "root": "GỐC — BẮT ĐẦU TỪ ĐÂY",
-        "waitingBlocking": "ĐANG CHỜ & CHẶN {count}",
-        "waiting": "ĐANG CHỜ"
+        "root": "Gốc — bắt đầu từ đây",
+        "waitingBlocking": "Đang chờ & chặn {count}",
+        "waiting": "Đang chờ"
       },
       "waitingOn": "đang chờ {pid}",
       "degraded": "Một phiên đang chặn {count}. Nó đã có một giao dịch mở trong {age} và không chạy truy vấn nào trong thời gian đó — rất có thể là một ứng dụng quên cam kết.",
@@ -16068,9 +16062,6 @@
       }
     },
     "activity": {
-      "title": "Hoạt động",
-      "subtitle": "Những gì cơ sở dữ liệu của bạn đang làm ngay bây giờ, được lấy mẫu từ bảng phiên của chính chúng.",
-      "subtitlePast": "Những gì cơ sở dữ liệu của bạn đã làm trong khoảng thời gian này, được lấy mẫu từ bảng phiên của chính chúng.",
       "searchPlaceholder": "Tìm kiếm phiên, người dùng, ứng dụng",
       "wait": {
         "onCpu": "Trên CPU / không chờ",
@@ -16194,8 +16185,6 @@
       "deadlockLabel": "deadlock"
     },
     "tableHealth": {
-      "title": "Tình trạng bảng",
-      "subtitle": "Kích thước, độ phình và trạng thái vacuum cho từng bảng, ảnh chụp mới nhất trước tiên.",
       "searchPlaceholder": "Tìm kiếm bảng",
       "never": "Không bao giờ",
       "countersCumulative": "Số lần quét và vacuum là tổng tích lũy kể từ lần đặt lại số liệu thống kê gần nhất, không phải số đếm trong khoảng thời gian đã chọn.",
diff --git a/web/src/locales/languages/zh-CN.json b/web/src/locales/languages/zh-CN.json
index 7dbb835ff5b..606012efd95 100644
--- a/web/src/locales/languages/zh-CN.json
+++ b/web/src/locales/languages/zh-CN.json
@@ -14923,6 +14923,7 @@
       "disabled": "已禁用",
       "emptyTitle": "此 SLO 上还没有告警",
       "emptyHint": "燃烧速率告警会在仍有剩余预算时提醒您。",
+      "emptyActionDesc": "在预算用完前收到提醒",
       "loadFailed": "无法加载此 SLO 的告警。",
       "editTargetMissing": "该告警已不在此 SLO 上。",
       "describeBurn": "燃烧速率 > {critical} 在 {long} 和 {short} 期间",
@@ -15215,7 +15216,12 @@
     "common": {
       "reload": "刷新",
       "lastRefreshed": "上次刷新于 {age}",
-      "loadFailed": "无法加载数据库数据。请重试，或缩小时间范围。"
+      "loadFailed": "无法加载数据库数据。请重试，或缩小时间范围。",
+      "actions": "操作"
+    },
+    "header": {
+      "title": "数据库",
+      "subtitle": "数据库的查询性能与运行状况"
     },
     "ai": {
       "suggestFix": "向 AI 询问如何修复",
@@ -15257,8 +15263,6 @@
       "wasValue": "原为 {value}"
     },
     "databases": {
-      "title": "数据库",
-      "subtitle": "每个数据库的查询性能和健康状况",
       "searchPlaceholder": "搜索数据库或服务…",
       "unnamedInstance": "实例未命名",
       "unnamedInstanceHint": "数据库自身的语句报告证明该引擎正在运行，但未指明主机。一旦设置了包含主机信息的源——如会话采样或实例指标——该行将显示地址。",
@@ -15305,9 +15309,6 @@
       }
     },
     "queries": {
-      "title": "热门查询",
-      "subtitle": "使用数据库时间最多的查询",
-      "subtitleServer": "您的数据库执行的语句，在数据库内部统计——按运行频率最高的排前面",
       "searchPlaceholder": "搜索查询文本、表或服务…",
       "trendLabel": "此查询耗时在此范围内的变化",
       "otherRow": "所有其他查询",
@@ -15316,7 +15317,8 @@
         "query": "查询",
         "queryHint": "仅限真实 SQL——SELECT、INSERT、UPDATE 和 DELETE。",
         "all": "查询 + 连接开销",
-        "allHint": "同时计入驱动程序在查询之间发送的簿记信息：BEGIN 和 COMMIT、SET，以及保持连接池存活的检查。"
+        "allHint": "同时计入驱动程序在查询之间发送的簿记信息：BEGIN 和 COMMIT、SET，以及保持连接池存活的检查。",
+        "overheadLabel": "包含连接开销"
       },
       "completionBias": {
         "title": "调用量下降 {drop}%，而失败率上升",
@@ -15559,11 +15561,11 @@
       "noSamples": "此范围未加载单个执行记录。",
       "noSeries": "在您选择的时间范围内没有此查询的历史记录。",
       "liveSeries": "最近的点仍在统计中，可能会发生变化。",
-      "noStream": "我们无法确定哪个追踪数据流承载了这条查询，因此无法加载其调用方和示例。",
-      "ambiguousStream": "此组织有多个追踪数据流，我们无法确定此查询运行在哪个数据流上。请选择一个数据流以加载其调用方和示例追踪。",
-      "pickStream": "选择追踪数据流",
-      "invalidStream": "我们无法识别该追踪数据流名称，因此未执行搜索。",
-      "firstSeen": "首次出现在此列表中于 {time}",
+      "noStream": "我们无法确定哪个跟踪流承载此查询，因此无法加载其调用方和示例。",
+      "ambiguousStream": "此组织有多个跟踪流，我们无法确定此查询运行在哪个上。选择一个流以加载其调用方和示例跟踪。",
+      "pickStream": "选择跟踪流",
+      "invalidStream": "该跟踪流名称无法识别，因此我们未运行搜索。",
+      "firstSeen": "在 {time} 首次出现在此列表中",
       "firstSeenHint": "此范围内此查询负载足够高以至于需要单独跟踪的最早点。",
       "lastSeen": "最后出现于 {time}",
       "lastSeenHint": "此范围内此查询负载足够高以至于需要单独跟踪的最近点。它可能仍在运行——这是它最后一次出现在列表中。",
@@ -15732,9 +15734,6 @@
       }
     },
     "samples": {
-      "title": "最慢的调用",
-      "subtitle": "该时间窗口内最慢的各个数据库调用，跨所有数据库和查询——由调用方测量，仅统计已完成的调用",
-      "subtitleServer": "您的数据库在此时间窗口内完成的最慢的语句，在数据库内部测量",
       "logOffTitle": "您的数据库尚未记录慢语句",
       "logOffDescription": "目前没有追踪到这些调用，因此我们向数据库询问其自身最慢的语句——但它们没有记录任何内容。此列表来自 PostgreSQL 的语句日志（log_min_duration_statement），该日志默认关闭——设置卡中的日志记录步骤会将其打开。目前仅 PostgreSQL 支持按语句捕获，因此 MySQL 和 MariaDB 还无法提供此列表。热门查询仍然可以显示哪些内容运行最多，因为它改为读取计数器表。",
       "searchPlaceholder": "搜索查询文本、服务或数据库…",
@@ -15776,16 +15775,14 @@
       }
     },
     "rowChips": {
-      "new": "此列表中的新条目",
+      "new": "新加入此列表",
       "slower": "比平时慢 {ratio}×",
-      "loop": "每个请求运行 {multiplier}× 次",
-      "allFailed": "所有 {count} 次均失败",
-      "busier": "更繁忙，而非更慢",
-      "climbed": "新的最高负载查询"
+      "loop": "每次请求运行 {multiplier}×",
+      "allFailed": "全部 {count} 个失败",
+      "busier": "更繁忙，并非更慢",
+      "climbed": "新的最重查询"
     },
     "deadlocks": {
-      "title": "数据库",
-      "subtitle": "相互阻塞并被回滚的事务",
       "deadlockCount": "无死锁 | {count} 次死锁 | {count} 次死锁",
       "pairCount": "无查询对 | {count} 个查询对 | {count} 个查询对",
       "searchPlaceholder": "搜索查询文本、表或应用程序…",
@@ -15812,7 +15809,7 @@
         "mysqlPartial": "除非启用了 innodb_print_all_deadlocks，否则您的 MySQL 数据库仅保留最近的死锁。"
       },
       "chips": {
-        "oppositeOrder": "相反的行顺序",
+        "oppositeOrder": "相反行序",
         "cadence": "每 {seconds} 秒",
         "noQueries": "未捕获的查询",
         "partial": "仅单侧"
@@ -15935,9 +15932,6 @@
       }
     },
     "blocked": {
-      "title": "数据库",
-      "subtitle": "当前正在等待锁的会话",
-      "subtitlePast": "在此窗口期内等待过锁的会话",
       "waitingCount": "没有会话在等待锁 | {count} 个会话当前正在等待锁 | {count} 个会话当前正在等待锁",
       "waitingCountPast": "没有会话等待过锁 | {count} 个会话在此窗口期内等待过锁 | {count} 个会话在此窗口期内等待过锁",
       "sessionCount": "没有会话 | {count} 个会话 | {count} 个会话",
@@ -15988,9 +15982,9 @@
       "lastStatement": "最后一条语句在 {age} 前",
       "idleInTransaction": "未等待任何内容 · 在打开的事务中空闲",
       "pills": {
-        "root": "根 — 从此处开始",
-        "waitingBlocking": "等待并阻塞 {count}",
-        "waiting": "等待中"
+        "root": "根因 — 从这里开始",
+        "waitingBlocking": "等待与阻塞 {count}",
+        "waiting": "等待"
       },
       "waitingOn": "等待 {pid}",
       "degraded": "一个会话阻塞了 {count}。它的事务保持打开状态已有 {age}，且在此期间未运行任何查询——很可能是应用程序忘记提交。",
@@ -16068,9 +16062,6 @@
       }
     },
     "activity": {
-      "title": "活动",
-      "subtitle": "你的数据库当前正在做什么，从它们自己的会话表中采样。",
-      "subtitlePast": "你的数据库在此时间窗口内正在做什么，从它们自己的会话表中采样。",
       "searchPlaceholder": "搜索会话、用户、应用程序",
       "wait": {
         "onCpu": "在 CPU 上 / 无等待",
@@ -16194,8 +16185,6 @@
       "deadlockLabel": "死锁"
     },
     "tableHealth": {
-      "title": "表健康",
-      "subtitle": "每个表的大小、膨胀和 vacuum 状态，最新快照优先。",
       "searchPlaceholder": "搜索表",
       "never": "从未",
       "countersCumulative": "扫描和 vacuum 计数是自上次统计重置以来的累计总数，而不是所选时间范围内的计数。",
diff --git a/web/src/locales/languages/zh-TW.json b/web/src/locales/languages/zh-TW.json
index cbcd42a6516..414898d379d 100644
--- a/web/src/locales/languages/zh-TW.json
+++ b/web/src/locales/languages/zh-TW.json
@@ -14918,6 +14918,7 @@
       "disabled": "已停用",
       "emptyTitle": "此 SLO 上尚無警示",
       "emptyHint": "燃燒率警示會在仍有剩餘預算可保護時通知你。",
+      "emptyActionDesc": "在預算耗盡前收到警告",
       "loadFailed": "無法載入此 SLO 的警示。",
       "editTargetMissing": "該警示已不在這個 SLO 上。",
       "describeBurn": "燃燒率 > {critical} 在 {long} 和 {short} 期間",
@@ -15210,7 +15211,12 @@
     "common": {
       "reload": "重新整理",
       "lastRefreshed": "上次重新整理 {age}",
-      "loadFailed": "無法載入資料庫資料。請重試，或縮小時間範圍。"
+      "loadFailed": "無法載入資料庫資料。請重試，或縮小時間範圍。",
+      "actions": "操作"
+    },
+    "header": {
+      "title": "資料庫",
+      "subtitle": "跨資料庫的查詢效能與健康狀態"
     },
     "ai": {
       "suggestFix": "詢問 AI 如何修正",
@@ -15252,8 +15258,6 @@
       "wasValue": "原本 {value}"
     },
     "databases": {
-      "title": "資料庫",
-      "subtitle": "每個資料庫的查詢效能與健康狀態",
       "searchPlaceholder": "搜尋資料庫或服務…",
       "unnamedInstance": "執行個體未命名",
       "unnamedInstanceHint": "資料庫自身的陳述式報告證明此引擎正在執行，但未指明主機。一旦設定了帶有主機的來源 — 工作階段取樣或執行個體指標 — 該列便會顯示位址。",
@@ -15300,9 +15304,6 @@
       }
     },
     "queries": {
-      "title": "熱門查詢",
-      "subtitle": "使用最多資料庫時間的查詢",
-      "subtitleServer": "您的資料庫所執行的陳述式，在資料庫內部統計 — 最常執行的排在前面",
       "searchPlaceholder": "搜尋查詢文字、資料表或服務…",
       "trendLabel": "此查詢時間在範圍內的變化",
       "otherRow": "所有其他查詢",
@@ -15311,7 +15312,8 @@
         "query": "查詢",
         "queryHint": "僅限實際 SQL — SELECT、INSERT、UPDATE 和 DELETE。",
         "all": "查詢 + 連線負擔",
-        "allHint": "也計算驅動程式在查詢之間傳送的維護作業：BEGIN 和 COMMIT、SET，以及維持連線集區存活的檢查。"
+        "allHint": "也計算驅動程式在查詢之間傳送的維護作業：BEGIN 和 COMMIT、SET，以及維持連線集區存活的檢查。",
+        "overheadLabel": "包含連線開銷"
       },
       "completionBias": {
         "title": "呼叫量下降 {drop}%，失敗卻增加",
@@ -15558,7 +15560,7 @@
       "ambiguousStream": "此組織有多個追蹤串流，我們無法判斷此查詢在哪一個上執行。請選擇一個串流以載入其呼叫者和範例追蹤。",
       "pickStream": "選擇追蹤串流",
       "invalidStream": "該追蹤串流名稱不是我們認得的，因此我們未執行搜尋。",
-      "firstSeen": "在此清單中首次出現於 {time}",
+      "firstSeen": "首次出現於此清單的時間為 {time}",
       "firstSeenHint": "此範圍內該查詢嚴重到足以自行追蹤的最早時間點。",
       "lastSeen": "最後出現於 {time}",
       "lastSeenHint": "此範圍內該查詢嚴重到足以自行追蹤的最近時間點。它現在可能仍在執行中 — 這是它最後一次進入清單的時間。",
@@ -15727,9 +15729,6 @@
       }
     },
     "samples": {
-      "title": "最慢的呼叫",
-      "subtitle": "此時間範圍內最慢的個別資料庫呼叫，涵蓋所有資料庫與查詢——以呼叫端測量結果為準，僅限已完成的呼叫。",
-      "subtitleServer": "您的資料庫在此時間範圍內完成的最慢陳述式（在資料庫內部測量）",
       "logOffTitle": "您的資料庫尚未記錄慢速陳述式。",
       "logOffDescription": "沒有任何追蹤涵蓋這些呼叫，所以我們向資料庫查詢它們自己的最慢陳述式——但它們沒有記錄任何項目。此清單來自 PostgreSQL 的陳述式日誌 (log_min_duration_statement)，該日誌預設為關閉——設定卡中的記錄步驟會將其開啟。目前僅 PostgreSQL 支援逐陳述式擷取，因此 MySQL 和 MariaDB 尚無法提供此清單的資料。Top queries 仍可向您顯示哪些查詢最常執行，因為它改為讀取計數器資料表。",
       "searchPlaceholder": "搜尋查詢文字、服務或資料庫…",
@@ -15771,16 +15770,14 @@
       }
     },
     "rowChips": {
-      "new": "新上榜",
-      "slower": "比平常慢 {ratio}×",
-      "loop": "每個要求執行 {multiplier}×",
+      "new": "此清單的新增項目",
+      "slower": "{ratio}× 比平常慢",
+      "loop": "每次請求執行 {multiplier}×",
       "allFailed": "全部 {count} 個失敗",
-      "busier": "更繁忙，並非更慢",
-      "climbed": "新的最繁重查詢"
+      "busier": "更忙碌，而非更慢",
+      "climbed": "新的最重查詢"
     },
     "deadlocks": {
-      "title": "資料庫",
-      "subtitle": "互相封鎖並被回滾的交易",
       "deadlockCount": "沒有死結 | {count} 個死結 | {count} 個死結",
       "pairCount": "沒有查詢配對 | {count} 個查詢配對 | {count} 個查詢配對",
       "searchPlaceholder": "搜尋查詢文字、資料表或應用程式…",
@@ -15807,10 +15804,10 @@
         "mysqlPartial": "除非啟用 innodb_print_all_deadlocks，否則您的 MySQL 資料庫只會保留最近一次的死結。"
       },
       "chips": {
-        "oppositeOrder": "相反的資料列順序",
+        "oppositeOrder": "相反的列順序",
         "cadence": "每 {seconds} 秒",
         "noQueries": "未擷取的查詢",
-        "partial": "僅單一側"
+        "partial": "僅單邊"
       },
       "rowActions": {
         "topQueries": "在 Top queries 中尋找這些",
@@ -15930,9 +15927,6 @@
       }
     },
     "blocked": {
-      "title": "資料庫",
-      "subtitle": "目前正在等待鎖的會話",
-      "subtitlePast": "在此時間範圍內等待鎖的會話",
       "waitingCount": "沒有會話正在等待鎖 | {count} 個會話正在等待鎖 | {count} 個會話正在等待鎖",
       "waitingCountPast": "沒有會話等待鎖 | {count} 個會話在此時間範圍內等待鎖 | {count} 個會話在此時間範圍內等待鎖",
       "sessionCount": "沒有會話 | {count} 個會話 | {count} 個會話",
@@ -15979,12 +15973,12 @@
       "deep": "深度 {count}",
       "notWaiting": "未等待",
       "longestWait": "最長等待",
-      "holdsTheLock": "持有所有人都想要的鎖",
+      "holdsTheLock": "持有大家都要的鎖",
       "lastStatement": "最後一條語句在 {age} 前",
       "idleInTransaction": "未等待任何事物 · 在開啟的交易中閒置",
       "pills": {
-        "root": "根節點 — 從這裡開始",
-        "waitingBlocking": "等待中並阻塞 {count}",
+        "root": "根因 — 從這裡開始",
+        "waitingBlocking": "等待與封鎖 {count}",
         "waiting": "等待中"
       },
       "waitingOn": "等待 {pid}",
@@ -16063,9 +16057,6 @@
       }
     },
     "activity": {
-      "title": "活動",
-      "subtitle": "您的資料庫目前正在做什麼，取樣自它們自己的工作階段表。",
-      "subtitlePast": "您的資料庫在此時間區間內做了什麼，取樣自它們自己的工作階段表。",
       "searchPlaceholder": "搜尋工作階段、使用者、應用程式",
       "wait": {
         "onCpu": "在 CPU 上 / 無等待",
@@ -16189,8 +16180,6 @@
       "deadlockLabel": "死結"
     },
     "tableHealth": {
-      "title": "資料表健康狀態",
-      "subtitle": "每個資料表的大小、膨脹程度和 vacuum 狀態，以最新快照優先顯示。",
       "searchPlaceholder": "搜尋資料表",
       "never": "從未",
       "countersCumulative": "掃描和 vacuum 計數是自上次統計重設以來的生命週期總計，而非所選時間範圍內的計數。",
diff --git a/web/src/utils/dbm/historyChart.spec.ts b/web/src/utils/dbm/historyChart.spec.ts
index 42c80dad59f..3eb2a41ac26 100644
--- a/web/src/utils/dbm/historyChart.spec.ts
+++ b/web/src/utils/dbm/historyChart.spec.ts
@@ -26,6 +26,10 @@ const theme = {
   errors: "#5",
   axisLabel: "#7",
   splitLine: "#8",
+  tooltipBg: "#9",
+  tooltipBorder: "#a",
+  tooltipText: "#b",
+  crosshairBg: "#c",
 };
 
 const formatNs = (value: number | null | undefined) => (value == null ? "—" : `${value}ns`);
@@ -40,18 +44,19 @@ describe("buildSamplesOption", () => {
   ];
 
   it("separates errored samples into their own series", () => {
-    const option = buildSamplesOption(samples, theme, formatNs, String, {
+    const option = buildSamplesOption(samples, theme, formatNs, {
       ok: "ok",
       error: "error",
     });
     const series = seriesOf(option);
-    expect(series.find((s) => s.name === "ok")?.data).toEqual([[at(0), 100]]);
-    expect(series.find((s) => s.name === "error")?.data).toEqual([[at(1), 900]]);
+    // Points are plotted as epoch-MS (a `time` axis wants ms; samples are micros).
+    expect(series.find((s) => s.name === "ok")?.data).toEqual([[Math.floor(at(0) / 1000), 100]]);
+    expect(series.find((s) => s.name === "error")?.data).toEqual([[Math.floor(at(1) / 1000), 900]]);
   });
 
   /** Spread across time AND duration — the distribution, not only the tail. */
   it("plots on a time x-axis so the spread over the window is visible", () => {
-    const option = buildSamplesOption(samples, theme, formatNs, String, {
+    const option = buildSamplesOption(samples, theme, formatNs, {
       ok: "ok",
       error: "error",
     });
@@ -65,7 +70,7 @@ describe("buildSamplesOption", () => {
    * `{ value }` object would break the lookup silently.
    */
   it("emits bare [timestamp, duration] pairs so the trace pivot can match them", () => {
-    const option = buildSamplesOption(samples, theme, formatNs, String, {
+    const option = buildSamplesOption(samples, theme, formatNs, {
       ok: "ok",
       error: "error",
     });
diff --git a/web/src/utils/dbm/historyChart.ts b/web/src/utils/dbm/historyChart.ts
index 0dd87e739fc..b0104c8d1c3 100644
--- a/web/src/utils/dbm/historyChart.ts
+++ b/web/src/utils/dbm/historyChart.ts
@@ -39,6 +39,10 @@ export interface DbmChartTheme {
   errors: string;
   axisLabel: string;
   splitLine: string;
+  tooltipBg: string;
+  tooltipBorder: string;
+  tooltipText: string;
+  crosshairBg: string;
 }
 
 /**
@@ -54,32 +58,70 @@ export const buildSamplesOption = (
   samples: { timestamp: number; durationNs: number; isError: boolean }[],
   theme: DbmChartTheme,
   formatNs: (value: number | null | undefined) => string,
-  formatTime: (micros: number) => string,
   names: { ok: string; error: string },
 ): Record<string, unknown> => {
+  // Sample timestamps are microseconds; a `time` axis wants epoch milliseconds.
   const toPoint = (sample: { timestamp: number; durationNs: number }) => [
-    sample.timestamp,
+    Math.floor(sample.timestamp / 1000),
     sample.durationNs,
   ];
 
+  const formatDateTime = (ms: number): string => {
+    const date = new Date(ms);
+    const pad = (value: number) => String(value).padStart(2, "0");
+    return `${date.getFullYear()}-${pad(date.getMonth() + 1)}-${pad(date.getDate())} ${pad(date.getHours())}:${pad(date.getMinutes())}:${pad(date.getSeconds())}`;
+  };
+
   return {
-    grid: { left: 8, right: 8, top: 24, bottom: 8, containLabel: true },
-    legend: { top: 0, textStyle: { color: theme.axisLabel }, icon: "circle" },
+    grid: { left: 8, right: 8, top: 16, bottom: 28, containLabel: true },
+    legend: {
+      bottom: 0,
+      left: 0,
+      icon: "roundRect",
+      itemWidth: 24,
+      itemHeight: 14,
+      textStyle: { color: theme.axisLabel },
+    },
     tooltip: {
-      trigger: "item",
-      formatter: (params: { value: [number, number] }) =>
-        `${formatTime(params.value[0])}<br/>${formatNs(params.value[1])}`,
+      trigger: "axis",
+      axisPointer: {
+        type: "cross",
+        label: {
+          fontSize: 12,
+          backgroundColor: theme.crosshairBg,
+          formatter: (params: { value: number; axisDimension: string }) =>
+            params.axisDimension === "y" ? formatNs(params.value) : formatDateTime(params.value),
+        },
+      },
+      appendToBody: true,
+      className: "o2-echarts-tooltip",
+      backgroundColor: theme.tooltipBg,
+      borderColor: theme.tooltipBorder,
+      borderWidth: 1,
+      padding: [8, 12],
+      textStyle: { color: theme.tooltipText, fontSize: 12 },
+      extraCssText:
+        // eslint-disable-next-line local/no-hardcoded-px -- ECharts serialises this into its own container — no CSS cascade resolves rem
+        "border-radius: 8px !important; box-shadow: var(--shadow-md) !important;",
+      formatter: (params: { value: [number, number]; marker?: string }[]) => {
+        const points = Array.isArray(params) ? params : [params];
+        if (!points.length) return "";
+        const rows = points
+          .map((point) => `${point.marker ?? ""}${formatNs(point.value[1])}`)
+          .join("<br/>");
+        return `${formatDateTime(points[0].value[0])}<br/>${rows}`;
+      },
     },
     xAxis: {
       type: "time",
-      axisLabel: { hideOverlap: true, color: theme.axisLabel, formatter: formatTime },
-      splitLine: { show: false },
+      axisLabel: { hideOverlap: true, color: theme.axisLabel },
+      splitLine: { show: true, lineStyle: { type: "dashed", width: 1, color: theme.splitLine } },
     },
     yAxis: {
       type: "value",
       splitNumber: 3,
       axisLabel: { color: theme.axisLabel, formatter: (v: number) => formatNs(v) },
-      splitLine: { lineStyle: { color: theme.splitLine } },
+      splitLine: { lineStyle: { type: "dashed", width: 1, color: theme.splitLine } },
     },
     series: [
       {
diff --git a/web/src/views/AlertLibrary/AlertLibrary.vue b/web/src/views/AlertLibrary/AlertLibrary.vue
index 8bfb62635e1..99d508cb676 100644
--- a/web/src/views/AlertLibrary/AlertLibrary.vue
+++ b/web/src/views/AlertLibrary/AlertLibrary.vue
@@ -33,10 +33,9 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       :title="t('alerts.header')"
       icon="shield-alert-outline"
       title-data-test="alert-library-title"
+      :subtitle="t('alerts.subtitle')"
+      tabs-below
     >
-      <!-- Section-level title, identical on all four alerting pages, so the
-           peer tabs anchor at the same x — see AlertList.vue for the rationale.
-           Subtitle-free for the same reason. -->
       <template #header-tabs>
         <AlertSectionTabs />
       </template>
@@ -167,7 +166,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
               <template v-else>
                 <section v-for="group in groupedEntries" :key="group.id" class="pt-3">
                   <h2
-                    class="text-text-secondary text-2xs flex items-center gap-2 pb-2 font-semibold uppercase"
+                    class="text-text-secondary flex items-center gap-2 pb-2 text-xs font-semibold"
                     :data-test="`alert-library-group-${group.id}`"
                   >
                     <span v-if="group.packLabel" class="opacity-70"
diff --git a/web/src/views/DatabaseMonitoring/ActivityPage.vue b/web/src/views/DatabaseMonitoring/ActivityPage.vue
index da6009d5026..ba666608abd 100644
--- a/web/src/views/DatabaseMonitoring/ActivityPage.vue
+++ b/web/src/views/DatabaseMonitoring/ActivityPage.vue
@@ -39,22 +39,16 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
   one, which is why they occupy two columns and never one.
 -->
 <template>
-  <DbmPageChrome
-    :title="t('dbm.activity.title')"
-    :subtitle="t(isLiveWindow ? 'dbm.activity.subtitle' : 'dbm.activity.subtitlePast')"
-    title-data-test="dbm-activity-title"
-    date-time-data-test="dbm-activity-date-time"
-    :tab-counts="tabCounts"
-    :range="range"
-    @date-change="onDateChange"
-  >
+  <DbmPageChrome title-data-test="dbm-activity-title" :tab-counts="tabCounts">
     <div class="flex min-h-0 flex-1 flex-col">
       <OTable
+        :enable-column-resize="true"
         :data="rows"
         :columns="columns"
         row-key="rowKey"
         :loading="loading"
         :frame="false"
+        :toolbar-bordered="false"
         :error="error"
         sorting="client"
         :show-global-filter="false"
@@ -81,12 +75,30 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         </template>
 
         <template #toolbar-trailing>
-          <DbmRefreshButton
-            :loading="loading"
-            :last-run-at="lastRunAt"
-            data-test="dbm-activity-refresh"
-            @refresh="onRefresh"
-          />
+          <div class="flex items-center gap-1.5">
+            <DbmRefreshButton
+              mode="status"
+              :loading="loading"
+              :last-run-at="lastRunAt"
+              data-test="dbm-activity-refresh"
+            />
+            <DateTime
+              auto-apply
+              menu-align="end"
+              :default-type="range.type"
+              :default-absolute-time="{ startTime: range.startTime, endTime: range.endTime }"
+              :default-relative-time="range.relativeTimePeriod ?? undefined"
+              data-test-name="dbm-activity-date-time"
+              class="h-8"
+              @on:date-change="onDateChange"
+            />
+            <DbmRefreshButton
+              mode="button"
+              :loading="loading"
+              data-test="dbm-activity-refresh"
+              @refresh="onRefresh"
+            />
+          </div>
         </template>
 
         <template #subheader>
@@ -109,7 +121,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
           >
             <span
               v-if="waitStrip.shown.length"
-              class="text-text-label text-3xs shrink-0 font-semibold tracking-wide uppercase"
+              class="text-text-secondary text-3xs shrink-0 font-semibold"
             >
               {{ t("dbm.activity.columns.waitEvent") }}
             </span>
@@ -127,7 +139,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
               <span class="text-text-secondary text-2xs font-mono tabular-nums">
                 {{ formatCount(bucket.sessions) }}
               </span>
-              <span v-if="bucket.share !== null" class="text-text-label text-3xs font-mono">
+              <span v-if="bucket.share !== null" class="text-text-secondary text-3xs font-mono">
                 {{ formatPercent(bucket.share, 0) }}
               </span>
             </span>
@@ -149,7 +161,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                  missed, and the full three sentences ride in the tooltip so
                  they cost the table no rows. -->
             <span
-              class="text-text-label text-2xs flex shrink-0 items-center gap-1"
+              class="text-text-secondary text-2xs flex shrink-0 items-center gap-1"
               data-test="dbm-activity-disclosure"
             >
               <OIcon name="info-outline" class="size-3 shrink-0" />
@@ -320,6 +332,7 @@ import { useRoute, useRouter } from "vue-router";
 import DbmLockEmptyState, { type DbmLockCheck } from "@/components/dbm/DbmLockEmptyState.vue";
 import DbmPageChrome from "@/components/dbm/DbmPageChrome.vue";
 import DbmQueryCell from "@/components/dbm/DbmQueryCell.vue";
+import DateTime from "@/components/DateTime.vue";
 import DbmRefreshButton from "@/components/dbm/DbmRefreshButton.vue";
 import DbmScopeFilters from "@/components/dbm/DbmScopeFilters.vue";
 import DbmSubheaderBand from "@/components/dbm/DbmSubheaderBand.vue";
diff --git a/web/src/views/DatabaseMonitoring/BlockedQueriesPage.vue b/web/src/views/DatabaseMonitoring/BlockedQueriesPage.vue
index da2ebfa7060..3711a988284 100644
--- a/web/src/views/DatabaseMonitoring/BlockedQueriesPage.vue
+++ b/web/src/views/DatabaseMonitoring/BlockedQueriesPage.vue
@@ -36,22 +36,16 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
   reasoning. The destructive action is copyable SQL, never an execution.
 -->
 <template>
-  <DbmPageChrome
-    :title="t('dbm.blocked.title')"
-    :subtitle="t(isLiveWindow ? 'dbm.blocked.subtitle' : 'dbm.blocked.subtitlePast')"
-    title-data-test="dbm-blocked-title"
-    date-time-data-test="dbm-blocked-date-time"
-    :tab-counts="tabCounts"
-    :range="range"
-    @date-change="onDateChange"
-  >
+  <DbmPageChrome title-data-test="dbm-blocked-title" :tab-counts="tabCounts">
     <div class="flex min-h-0 flex-1 flex-col">
       <OTable
+        :enable-column-resize="true"
         :data="tableRows"
         :columns="columns"
         row-key="rowKey"
         :loading="loading"
         :frame="false"
+        :toolbar-bordered="false"
         :error="error"
         sorting="client"
         :show-global-filter="false"
@@ -92,12 +86,30 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         </template>
 
         <template #toolbar-trailing>
-          <DbmRefreshButton
-            :loading="loading"
-            :last-run-at="lastRunAt"
-            data-test="dbm-blocked-refresh"
-            @refresh="onRefresh"
-          />
+          <div class="flex items-center gap-1.5">
+            <DbmRefreshButton
+              mode="status"
+              :loading="loading"
+              :last-run-at="lastRunAt"
+              data-test="dbm-blocked-refresh"
+            />
+            <DateTime
+              auto-apply
+              menu-align="end"
+              :default-type="range.type"
+              :default-absolute-time="{ startTime: range.startTime, endTime: range.endTime }"
+              :default-relative-time="range.relativeTimePeriod ?? undefined"
+              data-test-name="dbm-blocked-date-time"
+              class="h-8"
+              @on:date-change="onDateChange"
+            />
+            <DbmRefreshButton
+              mode="button"
+              :loading="loading"
+              data-test="dbm-blocked-refresh"
+              @refresh="onRefresh"
+            />
+          </div>
         </template>
 
         <template #subheader>
@@ -157,7 +169,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             <div class="flex min-w-0 flex-1 flex-col gap-px">
               <div v-if="row.pill" class="flex min-w-0 items-center gap-1.5">
                 <span
-                  class="text-3xs shrink-0 rounded-full px-1.5 py-px font-bold tracking-wide uppercase"
+                  class="text-3xs shrink-0 rounded-full px-1.5 py-px font-bold"
                   :class="row.pill.tone"
                   :data-test="`dbm-blocked-pill-${row.kind}`"
                 >
@@ -168,14 +180,12 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                 </span>
               </div>
 
-              <span
-                class="text-text-code min-w-0 truncate font-mono text-xs"
-                :title="row.query ?? ''"
-              >
+              <span class="text-text-code min-w-0 truncate font-mono text-xs">
+                <OTooltip v-if="row.query" :content="raw(row.query)" />
                 {{ raw(row.query ?? "—") }}
               </span>
 
-              <div class="text-text-label text-3xs flex min-w-0 items-center gap-1 truncate">
+              <div class="text-text-secondary text-3xs flex min-w-0 items-center gap-1 truncate">
                 <OTag type="dbSystem" :value="row.db_system" size="xs" />
                 <template v-if="row.application">
                   <span class="opacity-45">·</span>
@@ -191,7 +201,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                 </template>
                 <span
                   v-if="row.longestWait"
-                  class="bg-status-error-bg text-status-error-text rounded-default text-3xs ml-0.5 px-1 py-px font-semibold tracking-wide uppercase"
+                  class="bg-status-error-bg text-status-error-text rounded-default text-3xs ml-0.5 px-1 py-px font-semibold"
                 >
                   {{ t("dbm.blocked.longestWait") }}
                 </span>
@@ -234,9 +244,9 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             <span v-else class="text-text-muted text-xs">{{ raw("—") }}</span>
             <span
               v-if="row.waitEventLabel && row.waitEventRaw"
-              class="text-text-label text-3xs font-mono"
-              :title="row.waitEventRaw"
+              class="text-text-secondary text-3xs font-mono"
             >
+              <OTooltip :content="raw(row.waitEventRaw)" />
               {{ raw(row.waitEventRaw) }}
             </span>
           </div>
@@ -263,10 +273,8 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         </template>
 
         <template #cell-application="{ row }">
-          <span
-            class="text-text-body block truncate font-mono text-xs"
-            :title="row.application ?? ''"
-          >
+          <span class="text-text-body block truncate font-mono text-xs">
+            <OTooltip v-if="row.application" :content="raw(row.application)" />
             {{ raw(row.application ?? "—") }}
           </span>
         </template>
@@ -279,7 +287,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             >
               {{ row.blockingCount }}
             </span>
-            <span class="text-text-label text-3xs">
+            <span class="text-text-secondary text-3xs">
               {{ t("dbm.blocked.sessionCount", { count: row.blockingCount }, row.blockingCount) }}
             </span>
           </div>
@@ -301,7 +309,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
           </div>
           <div v-else class="flex flex-col items-end leading-tight">
             <span class="text-text-muted">{{ raw("—") }}</span>
-            <span class="text-text-label text-3xs">{{ t("dbm.blocked.notWaiting") }}</span>
+            <span class="text-text-secondary text-3xs">{{ t("dbm.blocked.notWaiting") }}</span>
           </div>
         </template>
 
@@ -413,6 +421,7 @@ import { useStore } from "vuex";
 import DbmLockCoverageLine from "@/components/dbm/DbmLockCoverageLine.vue";
 import DbmLockEmptyState, { type DbmLockCheck } from "@/components/dbm/DbmLockEmptyState.vue";
 import DbmPageChrome from "@/components/dbm/DbmPageChrome.vue";
+import DateTime from "@/components/DateTime.vue";
 import DbmRefreshButton from "@/components/dbm/DbmRefreshButton.vue";
 import DbmRowActions, { type DbmRowAction } from "@/components/dbm/DbmRowActions.vue";
 import DbmScopeFilters from "@/components/dbm/DbmScopeFilters.vue";
@@ -721,10 +730,11 @@ const columns = computed<OTableColumnDef<BlockedRow>[]>(() =>
         },
         {
           id: "actions",
-          header: raw(""),
-          size: 200,
+          header: t("dbm.common.actions"),
+          isAction: true,
+          size: 92,
           enableSorting: false,
-          meta: { align: "right" },
+          meta: { align: "center", cellClass: "actions-column", actionCount: 2 },
         },
       ]
     : [
@@ -764,10 +774,11 @@ const columns = computed<OTableColumnDef<BlockedRow>[]>(() =>
         },
         {
           id: "actions",
-          header: raw(""),
-          size: 112,
+          header: t("dbm.common.actions"),
+          isAction: true,
+          size: 92,
           enableSorting: false,
-          meta: { align: "right" },
+          meta: { align: "center", cellClass: "actions-column", actionCount: 2 },
         },
       ],
 );
diff --git a/web/src/views/DatabaseMonitoring/DatabasesPage.vue b/web/src/views/DatabaseMonitoring/DatabasesPage.vue
index fb0a8b41b9a..061d142e26b 100644
--- a/web/src/views/DatabaseMonitoring/DatabasesPage.vue
+++ b/web/src/views/DatabaseMonitoring/DatabasesPage.vue
@@ -51,15 +51,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
   from the parent they belong to.
 -->
 <template>
-  <DbmPageChrome
-    :title="t('dbm.databases.title')"
-    :subtitle="t('dbm.databases.subtitle')"
-    title-data-test="dbm-databases-title"
-    date-time-data-test="dbm-databases-date-time"
-    :tab-counts="tabCounts"
-    :range="range"
-    @date-change="onDateChange"
-  >
+  <DbmPageChrome title-data-test="dbm-databases-title" :tab-counts="tabCounts">
     <div class="flex min-h-0 flex-1 flex-col">
       <OTable
         :data="treeRows"
@@ -76,6 +68,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         :persist-columns="true"
         table-id="dbm-databases"
         :enable-column-resize="true"
+        :toolbar-bordered="false"
         :get-row-style="rowStyle"
         tree
         :get-row-warning="hasShortfall"
@@ -102,12 +95,30 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         </template>
 
         <template #toolbar-trailing>
-          <DbmRefreshButton
-            :loading="loading"
-            :last-run-at="lastRunAt"
-            data-test="dbm-databases-refresh"
-            @refresh="onRefresh"
-          />
+          <div class="flex items-center gap-1.5">
+            <DbmRefreshButton
+              mode="status"
+              :loading="loading"
+              :last-run-at="lastRunAt"
+              data-test="dbm-databases-refresh"
+            />
+            <DateTime
+              auto-apply
+              menu-align="end"
+              :default-type="range.type"
+              :default-absolute-time="{ startTime: range.startTime, endTime: range.endTime }"
+              :default-relative-time="range.relativeTimePeriod ?? undefined"
+              data-test-name="dbm-databases-date-time"
+              class="h-8"
+              @on:date-change="onDateChange"
+            />
+            <DbmRefreshButton
+              mode="button"
+              :loading="loading"
+              data-test="dbm-databases-refresh"
+              @refresh="onRefresh"
+            />
+          </div>
         </template>
 
         <template #subheader>
@@ -150,12 +161,12 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                 {{ t("dbm.breakdown.noService") }}
               </span>
               <template v-else>
-                <OIcon name="database" size="xs" class="text-text-label shrink-0" />
+                <OIcon name="database" size="xs" class="text-text-secondary shrink-0" />
                 <span class="text-text-heading text-2xs min-w-0 truncate font-semibold">
                   {{ row.name ? raw(row.name) : t("dbm.breakdown.noSchema") }}
                 </span>
               </template>
-              <span class="text-text-label text-3xs shrink-0">
+              <span class="text-text-secondary text-3xs shrink-0">
                 {{ t("dbm.breakdown.queryCount", { count: row.queryCount }, row.queryCount) }}
               </span>
             </template>
@@ -171,13 +182,13 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                  exists but named no host. Saying so beats a blank name line. -->
             <span
               v-else
-              class="text-text-label text-compact truncate italic"
+              class="text-text-secondary text-compact truncate italic"
               data-test="dbm-databases-unnamed-instance"
             >
               {{ t("dbm.databases.unnamedInstance") }}
               <OTooltip side="bottom" :content="t('dbm.databases.unnamedInstanceHint')" />
             </span>
-            <div class="text-text-label text-3xs flex min-w-0 items-center gap-1 truncate">
+            <div class="text-text-secondary text-3xs flex min-w-0 items-center gap-1 truncate">
               <OTag type="dbSystem" :value="row.db_system" size="xs" />
               <template v-if="row.db_namespace">
                 <span class="opacity-45">·</span>
@@ -188,7 +199,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                    the client-vantage list cannot show by construction. -->
               <template v-if="row.trafficless">
                 <span class="opacity-45">·</span>
-                <span class="text-text-label italic" data-test="dbm-databases-no-traffic">
+                <span class="text-text-secondary italic" data-test="dbm-databases-no-traffic">
                   {{ t("dbm.instanceMetrics.noTraffic") }}
                   <OTooltip side="bottom" :content="t('dbm.instanceMetrics.noTrafficHint')" />
                 </span>
@@ -243,7 +254,9 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             :class="
               !isBreakdownRow(row) && row.critical
                 ? 'text-status-error-text font-semibold'
-                : 'text-text-muted'
+                : noQueryFigures(row)
+                  ? 'text-text-muted'
+                  : 'text-text-secondary'
             "
           >
             <template v-if="noQueryFigures(row)">{{ raw("—") }}</template>
@@ -327,7 +340,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                   : formatPercent(attentionOf(row).ratio, 0)
               }}
             </span>
-            <span class="text-text-label text-3xs">{{ attentionLabel(row) }}</span>
+            <span class="text-text-secondary text-3xs">{{ attentionLabel(row) }}</span>
           </div>
         </template>
 
@@ -352,7 +365,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
               <span class="text-text-heading text-compact font-mono font-medium tabular-nums">
                 {{ formatNs(row.total_time_ns) }}
               </span>
-              <span class="text-text-label text-3xs font-mono tabular-nums">
+              <span class="text-text-secondary text-3xs font-mono tabular-nums">
                 {{ formatPercent(row.share, 0) }}
               </span>
             </span>
@@ -361,11 +374,10 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                  vantage named reads as time the ENGINE spent, which on
                  MySQL/MariaDB would be wait time. This is span time, and it
                  includes network and pool wait the server never sees (T7). -->
-            <span
-              class="text-text-label text-3xs"
-              :title="t('dbm.detail.overlap.clientObserved', { engine: engineOf(row) ?? '' })"
-              data-test="dbm-databases-load-qualifier"
-            >
+            <span class="text-text-secondary text-3xs" data-test="dbm-databases-load-qualifier">
+              <OTooltip
+                :content="t('dbm.detail.overlap.clientObserved', { engine: engineOf(row) ?? '' })"
+              />
               {{ t("dbm.list.overlap.clientObserved") }}
             </span>
           </span>
@@ -428,6 +440,7 @@ import DbmCoverageLine from "@/components/dbm/DbmCoverageLine.vue";
 import DbmEmptyState, { type DbmEmptyCauseId } from "@/components/dbm/DbmEmptyState.vue";
 import DbmInstanceHealthCell from "@/components/dbm/DbmInstanceHealthCell.vue";
 import DbmPageChrome from "@/components/dbm/DbmPageChrome.vue";
+import DateTime from "@/components/DateTime.vue";
 import DbmRefreshButton from "@/components/dbm/DbmRefreshButton.vue";
 import DbmRowActions, { type DbmRowAction } from "@/components/dbm/DbmRowActions.vue";
 import DbmRowChips, { type DbmRowChip } from "@/components/dbm/DbmRowChips.vue";
@@ -1517,10 +1530,11 @@ const allColumns = computed<OTableColumnDef<TableRow>[]>(() => [
   },
   {
     id: "actions",
-    header: raw(""),
-    size: 72,
+    header: t("dbm.common.actions"),
+    isAction: true,
+    size: 92,
     sortable: false,
-    meta: { align: "right" },
+    meta: { align: "center", cellClass: "actions-column", actionCount: 2 },
   },
 ]);
 
diff --git a/web/src/views/DatabaseMonitoring/DeadlocksPage.vue b/web/src/views/DatabaseMonitoring/DeadlocksPage.vue
index d08ec59c34d..9c4493ce805 100644
--- a/web/src/views/DatabaseMonitoring/DeadlocksPage.vue
+++ b/web/src/views/DatabaseMonitoring/DeadlocksPage.vue
@@ -40,22 +40,16 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       list is the HEALTHY normal and is rendered as reassurance.
 -->
 <template>
-  <DbmPageChrome
-    :title="t('dbm.deadlocks.title')"
-    :subtitle="t('dbm.deadlocks.subtitle')"
-    title-data-test="dbm-deadlocks-title"
-    date-time-data-test="dbm-deadlocks-date-time"
-    :tab-counts="tabCounts"
-    :range="range"
-    @date-change="onDateChange"
-  >
+  <DbmPageChrome title-data-test="dbm-deadlocks-title" :tab-counts="tabCounts">
     <div class="flex min-h-0 flex-1 flex-col">
       <OTable
+        :enable-column-resize="true"
         :data="tableRows"
         :columns="columns"
         row-key="rowKey"
         :loading="loading"
         :frame="false"
+        :toolbar-bordered="false"
         :error="error"
         sorting="client"
         expansion="multiple"
@@ -95,12 +89,30 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         </template>
 
         <template #toolbar-trailing>
-          <DbmRefreshButton
-            :loading="loading"
-            :last-run-at="lastRunAt"
-            data-test="dbm-deadlocks-refresh"
-            @refresh="onRefresh"
-          />
+          <div class="flex items-center gap-1.5">
+            <DbmRefreshButton
+              mode="status"
+              :loading="loading"
+              :last-run-at="lastRunAt"
+              data-test="dbm-deadlocks-refresh"
+            />
+            <DateTime
+              auto-apply
+              menu-align="end"
+              :default-type="range.type"
+              :default-absolute-time="{ startTime: range.startTime, endTime: range.endTime }"
+              :default-relative-time="range.relativeTimePeriod ?? undefined"
+              data-test-name="dbm-deadlocks-date-time"
+              class="h-8"
+              @on:date-change="onDateChange"
+            />
+            <DbmRefreshButton
+              mode="button"
+              :loading="loading"
+              data-test="dbm-deadlocks-refresh"
+              @refresh="onRefresh"
+            />
+          </div>
         </template>
 
         <template #subheader>
@@ -160,17 +172,18 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
              with the ⇄ between them rather than sitting in two columns. -->
         <template #cell-pair="{ row }">
           <div class="flex min-w-0 flex-col gap-px">
-            <div class="text-text-code min-w-0 truncate font-mono text-xs" :title="row.pairTitle">
+            <div class="text-text-code min-w-0 truncate font-mono text-xs">
+              <OTooltip v-if="row.pairTitle" :content="raw(row.pairTitle)" />
               <template v-if="row.queries[0]">
                 {{ raw(row.queries[0]) }}
-                <span v-if="row.queries[1]" class="text-text-label px-1">{{ raw("⇄") }}</span>
+                <span v-if="row.queries[1]" class="text-text-secondary px-1">{{ raw("⇄") }}</span>
                 {{ raw(row.queries[1] ?? "") }}
               </template>
               <span v-else class="text-text-secondary italic">
                 {{ t("dbm.deadlocks.detail.noQueryCaptured") }}
               </span>
             </div>
-            <div class="text-text-label text-3xs flex min-w-0 items-center gap-1 truncate">
+            <div class="text-text-secondary text-3xs flex min-w-0 items-center gap-1 truncate">
               <OTag type="dbSystem" :value="row.db_system" size="xs" />
               <template v-if="row.db_instance">
                 <span class="opacity-45">·</span>
@@ -179,7 +192,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
               <span
                 v-for="chip in row.chips"
                 :key="chip.id"
-                class="rounded-default text-3xs ml-0.5 px-1 py-px font-semibold tracking-wide uppercase"
+                class="rounded-default text-3xs ml-0.5 px-1 py-px font-semibold"
                 :class="chip.tone"
               >
                 {{ chip.label }}
@@ -207,7 +220,9 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             <span class="text-text-heading text-compact font-mono font-medium tabular-nums">
               {{ row.objects.length || raw("—") }}
             </span>
-            <span class="text-text-label text-3xs truncate">{{ raw(row.objects[0] ?? "") }}</span>
+            <span class="text-text-secondary text-3xs truncate">{{
+              raw(row.objects[0] ?? "")
+            }}</span>
           </div>
         </template>
 
@@ -216,7 +231,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             <span class="text-text-body font-mono text-xs tabular-nums">
               {{ formatClock(row.lastSeen) }}
             </span>
-            <span class="text-text-label text-3xs">{{ formatAge(row.lastSeen) }}</span>
+            <span class="text-text-secondary text-3xs">{{ formatAge(row.lastSeen) }}</span>
           </div>
         </template>
 
@@ -227,7 +242,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             <div class="flex flex-col items-end leading-tight">
               <span class="text-text-heading text-compact font-mono font-semibold tabular-nums">
                 {{ row.count }}
-                <span class="text-text-label text-3xs font-normal">
+                <span class="text-text-secondary text-3xs font-normal">
                   {{ formatPercent(row.share, 0) }}
                 </span>
               </span>
@@ -313,18 +328,18 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                 data-test="dbm-deadlocks-occurrences"
               >
                 <div class="mb-1.5 flex items-center gap-2">
-                  <h4 class="text-text-label text-2xs font-semibold tracking-wide uppercase">
+                  <h4 class="text-text-secondary text-2xs font-semibold">
                     {{ t("dbm.deadlocks.detail.occurrencesTitle", { count: row.count }) }}
                   </h4>
                   <div class="flex-1"></div>
-                  <span class="text-text-label text-3xs">
+                  <span class="text-text-secondary text-3xs">
                     {{ t("dbm.deadlocks.detail.occurrencesHint") }}
                   </span>
                 </div>
 
                 <div class="flex flex-col gap-1">
                   <div v-for="lane in row.lanes" :key="lane.pid" class="flex items-center gap-2">
-                    <span class="text-text-label text-3xs w-16 shrink-0 text-right font-mono">
+                    <span class="text-text-secondary text-3xs w-16 shrink-0 text-right font-mono">
                       {{ t("dbm.deadlocks.detail.lostLabel", { pid: lane.pid }) }}
                     </span>
                     <div
@@ -355,7 +370,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                   </div>
                 </div>
 
-                <p class="text-text-label text-3xs mt-1.5">
+                <p class="text-text-secondary text-3xs mt-1.5">
                   {{ row.victimSummary }}
                 </p>
               </div>
@@ -405,6 +420,7 @@ import { useStore } from "vuex";
 import DbmDeadlockCycle from "@/components/dbm/DbmDeadlockCycle.vue";
 import DbmLockCoverageLine from "@/components/dbm/DbmLockCoverageLine.vue";
 import DbmPageChrome from "@/components/dbm/DbmPageChrome.vue";
+import DateTime from "@/components/DateTime.vue";
 import DbmRefreshButton from "@/components/dbm/DbmRefreshButton.vue";
 import DbmRowActions, { type DbmRowAction } from "@/components/dbm/DbmRowActions.vue";
 import DbmScopeFilters from "@/components/dbm/DbmScopeFilters.vue";
@@ -747,10 +763,11 @@ const columns = computed<OTableColumnDef<DeadlockRow>[]>(() => [
   },
   {
     id: "actions",
-    header: raw(""),
-    size: 96,
+    header: t("dbm.common.actions"),
+    isAction: true,
+    size: 108,
     enableSorting: false,
-    meta: { align: "right" },
+    meta: { align: "center", cellClass: "actions-column", actionCount: 3 },
   },
 ]);
 
diff --git a/web/src/views/DatabaseMonitoring/QueriesPage.vue b/web/src/views/DatabaseMonitoring/QueriesPage.vue
index 18333a68c0b..7548fd1a98a 100644
--- a/web/src/views/DatabaseMonitoring/QueriesPage.vue
+++ b/web/src/views/DatabaseMonitoring/QueriesPage.vue
@@ -48,15 +48,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       sort and didn't would be a lie the UI tells by omission.
 -->
 <template>
-  <DbmPageChrome
-    :title="t('dbm.queries.title')"
-    :subtitle="t(serverListShown ? 'dbm.queries.subtitleServer' : 'dbm.queries.subtitle')"
-    title-data-test="dbm-queries-title"
-    date-time-data-test="dbm-queries-date-time"
-    :tab-counts="tabCounts"
-    :range="range"
-    @date-change="onDateChange"
-  >
+  <DbmPageChrome title-data-test="dbm-queries-title" :tab-counts="tabCounts">
     <div class="flex min-h-0 flex-1 flex-col">
       <!-- Blocked queries emit no span until they finish, so a lock storm makes
            QPS FALL — and a falling line reads as recovery at the worst possible
@@ -88,7 +80,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
            toolbar: both tables below own a toolbar and only one of them is
            mounted at a time, so a control in either would vanish with it.
            One row, above both, serving whichever table is showing. -->
-      <div class="px-page-edge flex shrink-0 items-center gap-2 pb-1.5">
+      <div class="px-page-edge flex shrink-0 items-center gap-2 py-1.5">
         <div class="w-64 shrink-0">
           <OSearchInput
             :model-value="search"
@@ -106,24 +98,21 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
           @clear="clearScope"
           @clear-insight="activeInsightId = null"
         />
-        <OToggleGroup
-          :model-value="stmtClass"
-          class="shrink-0"
+        <!-- Off = real SQL only; on also includes the driver's connection bookkeeping. -->
+        <span
+          class="border-border-default rounded-default inline-flex shrink-0 items-center border px-2.5 py-2"
           data-test="dbm-queries-stmt-class"
-          @update:model-value="onStmtClassChange"
         >
-          <!-- The second option names what it ADDS rather than restating the
-               whole: two halves that both read as "everything" ("Queries" /
-               "All statements") make the distinction invisible. -->
-          <OToggleGroupItem value="query" size="sm">
-            {{ t("dbm.queries.stmtClass.query") }}
-            <OTooltip side="bottom" :content="t('dbm.queries.stmtClass.queryHint')" />
-          </OToggleGroupItem>
-          <OToggleGroupItem value="all" size="sm">
-            {{ t("dbm.queries.stmtClass.all") }}
-            <OTooltip side="bottom" :content="t('dbm.queries.stmtClass.allHint')" />
-          </OToggleGroupItem>
-        </OToggleGroup>
+          <OCheckbox
+            :model-value="stmtClass"
+            true-value="all"
+            false-value="query"
+            size="sm"
+            :label="t('dbm.queries.stmtClass.overheadLabel')"
+            @update:model-value="onStmtClassChange"
+          />
+          <OTooltip side="bottom" :content="t('dbm.queries.stmtClass.allHint')" />
+        </span>
 
         <!-- What every Δ and every insight is measured AGAINST (W5). Each rule
              line names the choice, so a number can always be traced to the
@@ -145,9 +134,25 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         </OToggleGroup>
 
         <DbmRefreshButton
+          mode="status"
           :loading="loading"
           :last-run-at="lastRunAt"
           data-test="dbm-queries-refresh"
+        />
+        <DateTime
+          auto-apply
+          menu-align="end"
+          :default-type="range.type"
+          :default-absolute-time="{ startTime: range.startTime, endTime: range.endTime }"
+          :default-relative-time="range.relativeTimePeriod ?? undefined"
+          data-test-name="dbm-queries-date-time"
+          class="h-8"
+          @on:date-change="onDateChange"
+        />
+        <DbmRefreshButton
+          mode="button"
+          :loading="loading"
+          data-test="dbm-queries-refresh"
           @refresh="onRefresh"
         />
       </div>
@@ -165,6 +170,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         row-key="rowKey"
         :loading="loading"
         :frame="false"
+        :toolbar-bordered="false"
         :error="error"
         sorting="server"
         :sort-by="sortBy"
@@ -228,7 +234,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             <span class="text-text-body text-xs font-medium">
               {{ t("dbm.queries.foldRowText", { count: row.foldCount ?? 0 }) }}
             </span>
-            <span class="text-text-label text-3xs truncate">
+            <span class="text-text-secondary text-3xs truncate">
               {{
                 tailExpanded
                   ? t("dbm.queries.foldRowDetailOpen", { share: formatPercent(row.share, 0) })
@@ -243,13 +249,13 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                 row.isOther ? 'text-text-secondary italic' : 'text-text-code',
                 row.isTail ? 'pl-4' : '',
               ]"
-              :title="row.queryText"
             >
+              <OTooltip v-if="row.queryText" :content="raw(row.queryText)" />
               {{ row.queryPreview }}
             </span>
             <div
               v-if="!row.isOther"
-              class="text-text-label text-3xs flex min-w-0 items-center gap-1 truncate"
+              class="text-text-secondary text-3xs flex min-w-0 items-center gap-1 truncate"
               :class="row.isTail ? 'pl-4' : ''"
             >
               <OTag type="dbSystem" :value="row.db_system" size="xs" />
@@ -264,7 +270,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             <!-- Every remainder states its own call count; only the first
                  explains what a remainder is, because three copies of the same
                  sentence is what made this block unreadable. -->
-            <span v-else class="text-text-label text-3xs">
+            <span v-else class="text-text-secondary text-3xs">
               {{
                 row.otherRowExplained
                   ? t("dbm.queries.otherRowDetail", { calls: formatCount(row.calls) })
@@ -285,25 +291,22 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
              server figure it would qualify a number drawn from a different
              population. -->
         <template #cell-calls="{ row }">
-          <div class="flex flex-col items-end leading-tight">
-            <!-- The fold's own numbers are muted: it is a control, and a bold
-                 call count on it competes with the queries either side for the
-                 same "how big is this?" read. The share it carries is stated in
-                 words on the row itself. -->
-            <DbmOverlapValue
-              :value="row.overlapCalls.value === null ? null : formatCount(row.overlapCalls.value)"
-              :source="row.overlapCalls.source"
-              :qualifier-key="row.overlapCalls.qualifierKey"
-              :engine="row.db_system"
-              data-test="dbm-queries-calls"
-            />
-            <DbmDeltaCell
-              v-if="!row.isOther && !row.isFold && row.overlapCalls.source === 'client'"
-              :delta="row.callsDelta"
-              variant="words"
-              data-test="dbm-queries-calls-delta"
-            />
-          </div>
+          <DbmOverlapValue
+            :value="row.overlapCalls.value === null ? null : formatCount(row.overlapCalls.value)"
+            :source="row.overlapCalls.source"
+            :qualifier-key="row.overlapCalls.qualifierKey"
+            :engine="row.db_system"
+            data-test="dbm-queries-calls"
+          >
+            <template #trailing>
+              <DbmDeltaCell
+                v-if="!row.isOther && !row.isFold && row.overlapCalls.source === 'client'"
+                :delta="row.callsDelta"
+                variant="words"
+                data-test="dbm-queries-calls-delta"
+              />
+            </template>
+          </DbmOverlapValue>
         </template>
 
         <!-- The slow tail, and what it was before. p95 is NOT the typical
@@ -448,15 +451,17 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
           <h2 class="text-text-heading text-sm font-semibold">
             {{ t("dbm.queries.serverList.title") }}
           </h2>
-          <p class="text-text-label text-xs">
+          <p class="text-text-secondary text-xs">
             {{ t("dbm.queries.serverList.subtitle") }}
           </p>
         </div>
         <OTable
+          :enable-column-resize="true"
           :data="filteredServerRows"
           :columns="serverColumns"
           row-key="fingerprint"
           :frame="false"
+          :toolbar-bordered="false"
           sorting="client"
           :show-global-filter="false"
           table-id="dbm-server-queries"
@@ -536,7 +541,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             />
           </template>
           <template #bottom>
-            <div v-if="serverTruncated" class="text-text-label px-page-edge py-1.5 text-xs">
+            <div v-if="serverTruncated" class="text-text-secondary px-page-edge py-1.5 text-xs">
               {{ t("dbm.queries.serverList.truncated", { count: serverRows.length }) }}
             </div>
           </template>
@@ -564,6 +569,7 @@ import DbmLoadCell from "@/components/dbm/DbmLoadCell.vue";
 import DbmOverlapValue from "@/components/dbm/DbmOverlapValue.vue";
 import DbmPageChrome from "@/components/dbm/DbmPageChrome.vue";
 import DbmQueryCell from "@/components/dbm/DbmQueryCell.vue";
+import DateTime from "@/components/DateTime.vue";
 import DbmRefreshButton from "@/components/dbm/DbmRefreshButton.vue";
 import DbmRowActions, { type DbmRowAction } from "@/components/dbm/DbmRowActions.vue";
 import DbmRowChips, { type DbmRowChip } from "@/components/dbm/DbmRowChips.vue";
@@ -579,6 +585,7 @@ import OTable from "@/lib/core/Table/OTable.vue";
 import type { OTableColumnDef } from "@/lib/core/Table/OTable.types";
 import OToggleGroup from "@/lib/core/ToggleGroup/OToggleGroup.vue";
 import OToggleGroupItem from "@/lib/core/ToggleGroup/OToggleGroupItem.vue";
+import OCheckbox from "@/lib/forms/Checkbox/OCheckbox.vue";
 import OStatStrip from "@/lib/data/StatStrip/OStatStrip.vue";
 import type { StatItem } from "@/lib/data/StatStrip/OStatStrip.types";
 import OBanner from "@/lib/feedback/Banner/OBanner.vue";
@@ -1874,7 +1881,7 @@ const errorLabel = (row: QueryRow): I18nText =>
     : raw(formatCount(row.errors));
 
 const errorClass = (row: QueryRow) => {
-  if ((row.errors ?? 0) <= 0) return "text-text-muted";
+  if ((row.errors ?? 0) <= 0) return "text-text-secondary";
   return row.critical ? "text-status-error-text font-bold" : "text-status-error-text";
 };
 
@@ -2069,10 +2076,10 @@ const columns = computed<OTableColumnDef<QueryRow>[]>(() => [
   },
   {
     id: "actions",
-    header: raw(""),
-    size: 104,
+    header: t("dbm.common.actions"),
+    isAction: true,
     sortable: false,
-    meta: { align: "right" },
+    meta: { align: "center", cellClass: "actions-column", actionCount: 5 },
   },
   {
     id: "p99_ns",
diff --git a/web/src/views/DatabaseMonitoring/QueryDetailPage.vue b/web/src/views/DatabaseMonitoring/QueryDetailPage.vue
index 231d769983e..4b63c86b8fe 100644
--- a/web/src/views/DatabaseMonitoring/QueryDetailPage.vue
+++ b/web/src/views/DatabaseMonitoring/QueryDetailPage.vue
@@ -42,6 +42,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
     icon="storage"
     title-data-test="dbm-detail-title"
     scroll
+    bleed
   >
     <!-- With no query to scope, refresh, copy and the window picker all act on
          nothing — copy in particular composes its summary from a row that is
@@ -112,20 +113,34 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       />
 
       <!-- Identity: the statement, then the dimensions that locate it. -->
-      <section v-if="hasQuery" class="flex flex-col gap-2" data-test="dbm-detail-identity">
+      <section
+        v-if="hasQuery"
+        class="px-page-edge flex flex-col gap-2"
+        data-test="dbm-detail-identity"
+      >
         <div class="flex flex-wrap items-center gap-1.5">
-          <OTag v-if="row?.db_system" type="dbSystem" :value="row.db_system" />
-          <OTag v-for="chip in identityChips" :key="chip.key" :label="chip.label" size="xs" />
-          <span v-if="firstSeenLabel" class="text-text-secondary text-xs">
-            {{ firstSeenLabel }}
+          <OTag v-if="row?.db_system" type="dbSystem" :value="row.db_system" size="md" />
+          <OTag v-for="chip in identityChips" :key="chip.key" :label="chip.label" size="md" />
+          <span v-if="firstSeenTime" class="text-text-secondary text-xs">
+            <i18n-t keypath="dbm.detail.firstSeen" tag="span">
+              <template #time>
+                <span class="text-text-heading font-semibold tabular-nums">{{
+                  firstSeenTime
+                }}</span>
+              </template>
+            </i18n-t>
             <OTooltip side="bottom" :content="t('dbm.detail.firstSeenHint')" />
           </span>
           <span
-            v-if="lastSeenLabel"
+            v-if="lastSeenTime"
             class="text-text-secondary text-xs"
             data-test="dbm-detail-last-seen"
           >
-            {{ lastSeenLabel }}
+            <i18n-t keypath="dbm.detail.lastSeen" tag="span">
+              <template #time>
+                <span class="text-text-heading font-semibold tabular-nums">{{ lastSeenTime }}</span>
+              </template>
+            </i18n-t>
             <OTooltip side="bottom" :content="t('dbm.detail.lastSeenHint')" />
           </span>
           <div class="flex-1"></div>
@@ -154,15 +169,13 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
            is, why it is slow, who ran it. Same primitives as the L2 strip in
            DbmSectionTabs, one level down — OTabs/OTab with a hint tooltip, and
            the Callers tab locked with the same lock+tooltip treatment when the
-           vantage that fills it reported nothing.
-
-           No horizontal padding: a tab strip's first label self-aligns to the
-           page-edge grid, so the labels line up with the statement above. -->
+           vantage that fills it reported nothing. -->
       <OTabs
         v-if="hasQuery"
         :model-value="activeTab"
         align="left"
-        class="shrink-0"
+        bordered
+        class="px-page-edge shrink-0"
         data-test="dbm-detail-tabs"
         @change="onTabChange"
       >
@@ -196,7 +209,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
              column: they are the two vantages on the same two measures, and
              the `order-*` classes that hoist the populated one on a zero-trace
              fleet only work while they share a flex parent. -->
-        <OTabPanel name="overview" layout="flex-col" class="gap-4">
+        <OTabPanel name="overview" layout="flex-col" content-class="gap-4 px-page-edge">
           <!-- The headline numbers, before the charts: minute 0 of an incident is
            "how bad and how much of the database is it", and that is figures,
            not a graph.
@@ -220,20 +233,55 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
            in a second branch: one copy of each section, one source of truth for
            its states, and the trace-led order restored the moment traces
            return. -->
+          <!-- The provenance caption stays CONDITIONAL: it describes the
+               trace-derived tiles, so it renders nothing over the two
+               server-sourced figures rather than misattributing them. The order-2
+               swap hoists the server block on a zero-trace fleet. -->
           <div
-            v-if="traceVantage"
-            class="text-text-muted text-xs"
-            data-test="dbm-detail-stats-provenance"
-          >
-            {{ t("dbm.detail.serverMetrics.clientSubtitle") }}
-          </div>
-          <DbmMetricTiles
-            :items="visibleHeadlineStats"
-            with-sub-labels
+            class="flex flex-col gap-2"
             :class="traceVantage ? '' : 'order-2'"
-            tile-data-test="dbm-detail-stat"
             data-test="dbm-detail-stats"
-          />
+          >
+            <div
+              v-if="traceVantage"
+              class="text-text-secondary text-xs"
+              data-test="dbm-detail-stats-provenance"
+            >
+              {{ t("dbm.detail.serverMetrics.clientSubtitle") }}
+            </div>
+            <KpiCardRow>
+              <KpiCard
+                v-for="tile in summaryCards"
+                :key="tile.id"
+                :icon="tile.icon"
+                :data-test="`dbm-detail-stat-${tile.id}`"
+              >
+                <template #label>
+                  {{ tile.label
+                  }}<span v-if="tile.sub" class="ml-1 font-normal opacity-70">{{ tile.sub }}</span>
+                </template>
+                <template #value>
+                  <OSkeleton v-if="loading" type="text" class="h-6 w-16" />
+                  <template v-else>
+                    <span
+                      class="text-2xl leading-none font-bold tabular-nums"
+                      :class="tile.tone || 'text-text-heading'"
+                      >{{ tile.num }}</span
+                    >
+                    <span v-if="tile.unit" class="text-compact text-text-secondary font-semibold">{{
+                      tile.unit
+                    }}</span>
+                  </template>
+                </template>
+                <template v-if="loading || tile.detail" #trend>
+                  <OSkeleton v-if="loading" type="text" class="h-3 w-20" />
+                  <span v-else :class="tile.detailTone ?? 'text-text-secondary'">{{
+                    tile.detail
+                  }}</span>
+                </template>
+              </KpiCard>
+            </KpiCardRow>
+          </div>
 
           <!-- Plan drift, promoted. "It got slow because the plan changed" is the
            most actionable finding on this page, and the section that computes it
@@ -283,13 +331,14 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             :class="traceVantage ? '' : 'order-1'"
             data-test="dbm-detail-server-metrics"
           >
-            <span class="text-text-muted text-xs" data-test="dbm-detail-server-metrics-off">
+            <span class="text-text-secondary text-xs" data-test="dbm-detail-server-metrics-off">
               {{ t("dbm.detail.serverMetrics.off") }}
               <OTooltip side="top" :content="t('dbm.detail.serverMetrics.offHint')" />
             </span>
             <OButton
               variant="outline"
-              size="sm"
+              size="xs"
+              icon-right="open-in-new"
               class="shrink-0"
               data-test="dbm-detail-server-metrics-setup"
               @click="openDbmSetup"
@@ -466,6 +515,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
            coverage to be stale about. -->
           <DbmCoverageLine
             v-if="traceVantage && (clientRowFound || !serverAnswering)"
+            class="-mx-page-edge"
             :freshness="freshness"
             :hits="row ? [row] : []"
             :top-n-subset="topNSubset"
@@ -500,7 +550,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
            Hidden entirely rather than shown as two empty axes labelled "No
            history for this query" — there is no history because nothing traced
            it, and an empty chart reads as a gap in data we should have. -->
-          <div v-if="traceVantage" class="grid grid-cols-1 gap-3 lg:grid-cols-2">
+          <div v-if="traceVantage" class="grid grid-cols-1 gap-4 lg:grid-cols-2">
             <DbmHistoryPanel
               :title="t('dbm.detail.latencyTitle')"
               :empty-label="t('dbm.detail.noSeries')"
@@ -570,12 +620,13 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
              needs the way back, not a page that quietly hid the exit. -->
             <div
               v-if="!whereRows.length"
-              class="text-text-muted p-6 pt-2 text-sm"
+              class="text-text-secondary p-6 pt-2 text-sm"
               data-test="dbm-detail-where-empty"
             >
               {{ t("dbm.detail.whereItRuns.emptyFiltered") }}
             </div>
             <OTable
+              :enable-column-resize="true"
               v-else
               :data="whereRows"
               :columns="whereColumns"
@@ -597,7 +648,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                     v-if="whereRow.isChild"
                     name="database"
                     size="xs"
-                    class="text-text-label shrink-0"
+                    class="text-text-secondary shrink-0"
                   />
                   <span
                     class="min-w-0 truncate"
@@ -616,14 +667,15 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                           : t("dbm.detail.whereItRuns.noInstance")
                     }}
                   </span>
-                  <OTag
+                  <!-- Tooltip is a SIBLING of the tag: OTag's default slot overrides the `label` prop, so a tooltip inside would blank the "focused" text. -->
+                  <span
                     v-if="isWhereRowActive(whereRow, whereScope)"
-                    :label="t('dbm.detail.whereItRuns.focused')"
-                    size="xs"
+                    class="inline-flex shrink-0"
                     :data-test="`dbm-detail-where-focused-${whereRow.rowKey}`"
                   >
+                    <OTag :label="t('dbm.detail.whereItRuns.focused')" size="sm" />
                     <OTooltip side="top" :content="t('dbm.detail.whereItRuns.focusHint')" />
-                  </OTag>
+                  </span>
                 </div>
               </template>
               <template #cell-load="{ row: whereRow }">
@@ -655,7 +707,9 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                   :class="
                     isCriticalErrorRate(whereRow.errors, whereRow.calls)
                       ? 'text-status-error-text font-semibold'
-                      : 'text-text-muted'
+                      : whereRow.errorRate === null
+                        ? 'text-text-muted'
+                        : 'text-text-secondary'
                   "
                 >
                   <template v-if="whereRow.errorRate === null">{{ raw("—") }}</template>
@@ -723,7 +777,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
              three tables. It is SERVER-vantage, so it is populated on a fleet
              with no traced traffic at all — exactly where the Overview panel
              collapses to two tiles and the Callers tab locks. -->
-        <OTabPanel name="plans" layout="flex-col" class="gap-4">
+        <OTabPanel name="plans" layout="flex-col" content-class="gap-4 px-page-edge">
           <!-- Query plans. Provenance is PER ROW now (W-E3): generic NULL-bound
            estimates keep the gap tag — the statement EXPLAINed with every bind
            set to NULL, never executed, no latency ever shown beside it.
@@ -910,7 +964,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
              together because they share one dependency: the resolved trace
              stream. The picker below is literally the control for the two
              tables under it, so it lives on their tab and nowhere else. -->
-        <OTabPanel name="callers" layout="flex-col" class="gap-4">
+        <OTabPanel name="callers" layout="flex-col" content-class="gap-4 px-page-edge">
           <!-- The two panels below read RAW traces, so they need to know which
            trace stream this fingerprint lives on — and the queries endpoint does
            not carry it. With several streams in the org there is no way to tell,
@@ -955,6 +1009,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
               <span class="text-text-secondary text-xs">{{ t("dbm.detail.endpointsHint") }}</span>
             </template>
             <OTable
+              :enable-column-resize="true"
               :data="endpoints"
               :columns="endpointColumns"
               row-key="rowKey"
@@ -1033,6 +1088,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             </div>
 
             <OTable
+              :enable-column-resize="true"
               :data="samples"
               :columns="sampleColumns"
               row-key="rowKey"
@@ -1123,6 +1179,9 @@ import DbmCoverageLine from "@/components/dbm/DbmCoverageLine.vue";
 import DbmHistoryPanel from "@/components/dbm/DbmHistoryPanel.vue";
 import DbmQueryText from "@/components/dbm/DbmQueryText.vue";
 import DbmMetricTiles, { type DbmMetricTile } from "@/components/dbm/DbmMetricTiles.vue";
+import KpiCard from "@/components/common/KpiCard.vue";
+import KpiCardRow from "@/components/common/KpiCardRow.vue";
+import OSkeleton from "@/lib/feedback/Skeleton/OSkeleton.vue";
 import DbmRefreshButton from "@/components/dbm/DbmRefreshButton.vue";
 import DbmSection from "@/components/dbm/DbmSection.vue";
 import DbmServiceList from "@/components/dbm/DbmServiceList.vue";
@@ -1787,10 +1846,9 @@ const identityChips = computed(() => {
  * the cut by a traffic shift would otherwise be labelled new, and mislabeling
  * it is the fastest way this page loses a DBA's trust.
  */
-const firstSeenLabel = computed(() => {
+const firstSeenTime = computed<string | null>(() => {
   const first = history.value?.points.find((point) => point.plottable);
-  if (!first) return null;
-  return t("dbm.detail.firstSeen", { time: formatClock(first.timestamp) });
+  return first ? formatClock(first.timestamp) : null;
 });
 
 /**
@@ -1799,12 +1857,12 @@ const firstSeenLabel = computed(() => {
  * most recent tracked activity). Suppressed when it would restate first-seen's
  * timestamp: one tracked window means the two chips would print one fact twice.
  */
-const lastSeenLabel = computed(() => {
+const lastSeenTime = computed<string | null>(() => {
   const points = history.value?.points ?? [];
   const first = points.find((point) => point.plottable);
   const last = [...points].reverse().find((point) => point.plottable);
   if (!first || !last || last.timestamp === first.timestamp) return null;
-  return t("dbm.detail.lastSeen", { time: formatClock(last.timestamp) });
+  return formatClock(last.timestamp);
 });
 
 const hasSeries = computed(() => (history.value?.points.length ?? 0) > 0);
@@ -1906,7 +1964,7 @@ const volumeInjectedData = computed(() => {
 const chartTheme = useDbmChartTheme();
 
 const samplesOption = computed(() =>
-  buildSamplesOption(samples.value, chartTheme.value, formatNs, formatClock, {
+  buildSamplesOption(samples.value, chartTheme.value, formatNs, {
     ok: t("dbm.detail.sampleOk"),
     error: t("dbm.detail.sampleError"),
   }),
@@ -2029,7 +2087,16 @@ const visibleHeadlineStats = computed(() =>
     : headlineStats.value.filter((tile) => tile.id === "load" || tile.id === "calls"),
 );
 
-const headlineStats = computed(() => {
+// Split the FORMATTED value into leading number and trailing unit, so "none" and "—" pass through as a number with no unit.
+const summaryCards = computed(() =>
+  visibleHeadlineStats.value.map((tile) => {
+    const formatted = String(tile.value ?? "");
+    const match = formatted.match(/^(-?[\d.,]+)(.*)$/);
+    return { ...tile, num: match ? match[1] : formatted, unit: match ? match[2].trim() : "" };
+  }),
+);
+
+const headlineStats = computed<DbmMetricTile[]>(() => {
   const current = row.value;
   const share = scopeTotalNs.value > 0 ? (current?.total_time_ns ?? 0) / scopeTotalNs.value : 0;
   // ABSENT stays absent. With no client row these tiles have no measurement to
@@ -2055,6 +2122,13 @@ const headlineStats = computed(() => {
     return raw(formatSignedPercent(delta.ratio));
   };
 
+  // Colour a signed delta by direction — a rise in a cost metric reddens, a fall greens; below the deadband it stays neutral. Same rule as the delta column.
+  const deltaTone = (delta: ReturnType<typeof deltaFor>): string | undefined => {
+    if (delta.state !== "changed" || delta.ratio === undefined) return undefined;
+    if (Math.abs(delta.ratio) < DELTA_PHRASING.deadband) return undefined;
+    return delta.ratio > 0 ? "text-status-error-text" : "text-status-success-text";
+  };
+
   // On a seeded first paint the VALUES are the clicked row's and correct, but
   // the share and the deltas are this page's own scope arithmetic and have not
   // been computed yet — their caption lines stay blank until the fetch lands,
@@ -2088,6 +2162,7 @@ const headlineStats = computed(() => {
   return [
     {
       id: "load",
+      icon: "query-stats",
       label: t("dbm.detail.stats.load"),
       sub: undefined,
       value: raw(formatNs(databaseTime.value.value ?? undefined)),
@@ -2106,6 +2181,7 @@ const headlineStats = computed(() => {
     },
     {
       id: "calls",
+      icon: "bar-chart",
       label: t("dbm.detail.stats.calls"),
       sub: undefined,
       value: raw(formatCount(callCount.value.value ?? undefined)),
@@ -2120,10 +2196,13 @@ const headlineStats = computed(() => {
                 : raw(""),
             )
           : overlapDetail(callCount.value.qualifierKey),
+      // Only the client delta is signed; the server overlap caption is not.
+      detailTone: callCount.value.source === "client" ? deltaTone(callsChange) : undefined,
       tone: "",
     },
     {
       id: "p50",
+      icon: "access-time",
       label: t("dbm.detail.stats.p50"),
       sub: raw("p50"),
       value: raw(formatNs(current?.p50_ns)),
@@ -2135,16 +2214,19 @@ const headlineStats = computed(() => {
     },
     {
       id: "p95",
+      icon: "hourglass-empty",
       label: t("dbm.detail.stats.p95"),
       sub: raw("p95"),
       value: raw(formatNs(current?.p95_ns)),
       detail: captionFor(current?.p95_ns, () =>
         rowStatsReady.value ? changeWords(latencyChange) : raw(""),
       ),
+      detailTone: deltaTone(latencyChange),
       tone: "",
     },
     {
       id: "max",
+      icon: "trending-up",
       label: t("dbm.detail.stats.max"),
       sub: raw("max"),
       value: raw(formatNs(current?.max_ns)),
@@ -2154,6 +2236,7 @@ const headlineStats = computed(() => {
     },
     {
       id: "errors",
+      icon: "error-outline",
       label: t("dbm.detail.stats.errors"),
       sub: undefined,
       // No row, no error count: "none" would be an all-clear nobody measured.
@@ -2175,7 +2258,7 @@ const headlineStats = computed(() => {
       // would make one failure in a million read as loudly as a total outage.
       tone: isCriticalErrorRate(errorsForRate, callsForRate)
         ? "text-status-error-text"
-        : "text-text-label",
+        : "text-text-secondary",
     },
   ];
 });
@@ -2234,7 +2317,13 @@ const endpointColumns = computed<OTableColumnDef<EndpointCallerRow>[]>(() => [
       headerTooltip: t("dbm.queries.columnHints.p95"),
     },
   },
-  { id: "actions", header: raw(""), size: 60, isAction: true },
+  {
+    id: "actions",
+    header: t("dbm.common.actions"),
+    isAction: true,
+    size: 84,
+    meta: { align: "center", cellClass: "actions-column", actionCount: 1 },
+  },
 ]);
 
 // ─── Where it runs ───────────────────────────────────────────────────────────
@@ -2344,7 +2433,13 @@ const sampleColumns = computed<OTableColumnDef<SampleRow>[]>(() => [
     meta: { align: "right" },
   },
   { id: "status", header: t("dbm.detail.columns.status"), size: 120, sortable: false },
-  { id: "actions", header: raw(""), size: 60, isAction: true },
+  {
+    id: "actions",
+    header: t("dbm.common.actions"),
+    isAction: true,
+    size: 84,
+    meta: { align: "center", cellClass: "actions-column", actionCount: 1 },
+  },
 ]);
 
 // ─── Loading ─────────────────────────────────────────────────────────────────
@@ -2869,10 +2964,11 @@ const loadQueryInsights = async (token: number = requestSeq.current()) => {
  * setup page instead of describing an env var and stopping there.
  */
 const openDbmSetup = () => {
-  router.push({
+  const href = router.resolve({
     name: DBM_SETUP_ROUTE,
     query: { org_identifier: store.state.selectedOrganization.identifier },
-  });
+  }).href;
+  window.open(href, "_blank", "noopener");
 };
 
 /**
@@ -2967,8 +3063,9 @@ const openEndpointTraces = (endpoint: EndpointCallerRow) => {
 const onSampleClick = (params: unknown) => {
   const value = (params as { value?: [number, number] })?.value;
   if (!value) return;
+  // The scatter plots epoch-ms (samples carry micros), so match the datum's ms back to the sample's micros; duration disambiguates.
   const sample = samples.value.find(
-    (entry) => entry.timestamp === value[0] && entry.durationNs === value[1],
+    (entry) => Math.floor(entry.timestamp / 1000) === value[0] && entry.durationNs === value[1],
   );
   if (sample) openSampleTrace(sample);
 };
diff --git a/web/src/views/DatabaseMonitoring/SamplesPage.vue b/web/src/views/DatabaseMonitoring/SamplesPage.vue
index 8b6dea63348..3e1736a0a05 100644
--- a/web/src/views/DatabaseMonitoring/SamplesPage.vue
+++ b/web/src/views/DatabaseMonitoring/SamplesPage.vue
@@ -41,15 +41,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
   "everything is slow" — which demand different responses — look different.
 -->
 <template>
-  <DbmPageChrome
-    :title="t('dbm.samples.title')"
-    :subtitle="t(serverListShown ? 'dbm.samples.subtitleServer' : 'dbm.samples.subtitle')"
-    title-data-test="dbm-samples-title"
-    date-time-data-test="dbm-samples-date-time"
-    :tab-counts="tabCounts"
-    :range="range"
-    @date-change="onDateChange"
-  >
+  <DbmPageChrome title-data-test="dbm-samples-title" :tab-counts="tabCounts">
     <div class="flex min-h-0 flex-1 flex-col">
       <!-- Scope lives ABOVE both tables, not inside either one's toolbar. It
            used to sit in the client table's `#toolbar`, so it unmounted with
@@ -62,7 +54,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
            toolbar: both tables below own a toolbar and only one of them is
            mounted at a time, so a control in either would vanish with it.
            One row, above both, serving whichever table is showing. -->
-      <div class="px-page-edge flex shrink-0 items-center gap-2 pb-1.5">
+      <div class="px-page-edge flex shrink-0 items-center gap-2 py-1.5">
         <div class="w-64 shrink-0">
           <OSearchInput
             :model-value="search"
@@ -75,9 +67,25 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         </div>
         <DbmScopeFilters class="min-w-0 flex-1" :filters="dimensionFilters" @clear="clearScope" />
         <DbmRefreshButton
+          mode="status"
           :loading="loading"
           :last-run-at="lastRunAt"
           data-test="dbm-samples-refresh"
+        />
+        <DateTime
+          auto-apply
+          menu-align="end"
+          :default-type="range.type"
+          :default-absolute-time="{ startTime: range.startTime, endTime: range.endTime }"
+          :default-relative-time="range.relativeTimePeriod ?? undefined"
+          data-test-name="dbm-samples-date-time"
+          class="h-8"
+          @on:date-change="onDateChange"
+        />
+        <DbmRefreshButton
+          mode="button"
+          :loading="loading"
+          data-test="dbm-samples-refresh"
           @refresh="onRefresh"
         />
       </div>
@@ -89,11 +97,13 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
            why the usual list is empty. -->
       <OTable
         v-if="!serverListShown"
+        :enable-column-resize="true"
         :data="rows"
         :columns="columns"
         row-key="rowKey"
         :loading="loading"
         :frame="false"
+        :toolbar-bordered="false"
         :error="error"
         sorting="client"
         :show-global-filter="false"
@@ -191,8 +201,8 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
               {{ t("dbm.samples.partial") }}
             </span>
             <div class="flex-1"></div>
-            <span class="text-text-label flex shrink-0 items-center gap-1">
-              <OIcon name="info-outline" class="size-3 shrink-0" />
+            <span class="text-text-secondary flex shrink-0 items-center gap-1">
+              <OIcon name="info-outline" class="shrink-0" size="sm" />
               {{ t("dbm.samples.disclosureShort") }}
               <OTooltip side="top" :content="t('dbm.samples.disclosureDetail')" />
             </span>
@@ -249,15 +259,17 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
           <h2 class="text-text-heading text-sm font-semibold">
             {{ t("dbm.samples.serverList.title") }}
           </h2>
-          <p class="text-text-label text-xs">
+          <p class="text-text-secondary text-xs">
             {{ t("dbm.samples.serverList.subtitle") }}
           </p>
         </div>
         <OTable
+          :enable-column-resize="true"
           :data="filteredServerRows"
           :columns="serverColumns"
           row-key="rowKey"
           :frame="false"
+          :toolbar-bordered="false"
           sorting="client"
           :show-global-filter="false"
           table-id="dbm-server-samples"
@@ -295,7 +307,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             <span class="text-text-body block truncate text-xs">{{ raw(row.db_user || "—") }}</span>
           </template>
           <template #bottom>
-            <div v-if="serverTruncated" class="text-text-label px-page-edge py-1.5 text-xs">
+            <div v-if="serverTruncated" class="text-text-secondary px-page-edge py-1.5 text-xs">
               {{ t("dbm.samples.serverList.truncated", { count: serverRows.length }) }}
             </div>
           </template>
@@ -318,6 +330,7 @@ import { useStore } from "vuex";
 import DbmEmptyState, { type DbmEmptyCauseId } from "@/components/dbm/DbmEmptyState.vue";
 import DbmPageChrome from "@/components/dbm/DbmPageChrome.vue";
 import DbmQueryCell from "@/components/dbm/DbmQueryCell.vue";
+import DateTime from "@/components/DateTime.vue";
 import DbmRefreshButton from "@/components/dbm/DbmRefreshButton.vue";
 import OSearchInput from "@/lib/forms/SearchInput/OSearchInput.vue";
 import DbmScopeFilters, { type DbmScopeFilter } from "@/components/dbm/DbmScopeFilters.vue";
@@ -562,7 +575,13 @@ const columns = computed<OTableColumnDef<DbmSampleRow>[]>(() => [
     size: 120,
     sortable: true,
   },
-  { id: "actions", header: raw(""), size: 60, isAction: true },
+  {
+    id: "actions",
+    header: t("dbm.common.actions"),
+    isAction: true,
+    size: 84,
+    meta: { align: "center", cellClass: "actions-column", actionCount: 1 },
+  },
 ]);
 
 // ─── The database-reported fallback list ─────────────────────────────────────
@@ -693,7 +712,7 @@ const scatterSamples = computed(() =>
 );
 
 const samplesOption = computed(() =>
-  buildSamplesOption(scatterSamples.value, chartTheme.value, formatNs, formatWhen, {
+  buildSamplesOption(scatterSamples.value, chartTheme.value, formatNs, {
     ok: t("dbm.samples.scatterOk"),
     error: t("dbm.samples.failed"),
   }),
@@ -705,8 +724,9 @@ const scatterData = computed(() => ({ options: samplesOption.value }));
 const onScatterClick = (params: unknown) => {
   const value = (params as { value?: [number, number] })?.value;
   if (!value) return;
+  // Scatter plots epoch-ms but rows carry micros, so match ms back to micros; duration disambiguates.
   const sample = rows.value.find(
-    (row) => row.timestamp === value[0] && row.durationNs === value[1],
+    (row) => Math.floor(row.timestamp / 1000) === value[0] && row.durationNs === value[1],
   );
   if (sample) openSampleTrace(sample);
 };
diff --git a/web/src/views/DatabaseMonitoring/TableHealthPage.vue b/web/src/views/DatabaseMonitoring/TableHealthPage.vue
index af75e137747..1e0254257fa 100644
--- a/web/src/views/DatabaseMonitoring/TableHealthPage.vue
+++ b/web/src/views/DatabaseMonitoring/TableHealthPage.vue
@@ -42,15 +42,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
   deliberately not gated on `ai_enabled`/`isEnterprise`: DBM is all-OSS.
 -->
 <template>
-  <DbmPageChrome
-    :title="t('dbm.tableHealth.title')"
-    :subtitle="t('dbm.tableHealth.subtitle')"
-    title-data-test="dbm-table-health-title"
-    date-time-data-test="dbm-table-health-date-time"
-    :tab-counts="tabCounts"
-    :range="range"
-    @date-change="onDateChange"
-  >
+  <DbmPageChrome title-data-test="dbm-table-health-title" :tab-counts="tabCounts">
     <div class="flex min-h-0 flex-1 flex-col">
       <!-- W11 · Recommendations. Deterministic checks, each showing the
            arithmetic that fired it. The rule line is one hover away rather
@@ -142,12 +134,14 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       </section>
 
       <OTable
+        :enable-column-resize="true"
         :data="rows"
         :columns="columns"
         row-key="rowKey"
         :loading="loading"
         :error="error"
         :frame="false"
+        :toolbar-bordered="false"
         sorting="client"
         :show-global-filter="false"
         table-id="dbm-table-health"
@@ -222,12 +216,30 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         </template>
 
         <template #toolbar-trailing>
-          <DbmRefreshButton
-            :loading="loading"
-            :last-run-at="lastRunAt"
-            data-test="dbm-table-health-refresh"
-            @refresh="onRefresh"
-          />
+          <div class="flex items-center gap-1.5">
+            <DbmRefreshButton
+              mode="status"
+              :loading="loading"
+              :last-run-at="lastRunAt"
+              data-test="dbm-table-health-refresh"
+            />
+            <DateTime
+              auto-apply
+              menu-align="end"
+              :default-type="range.type"
+              :default-absolute-time="{ startTime: range.startTime, endTime: range.endTime }"
+              :default-relative-time="range.relativeTimePeriod ?? undefined"
+              data-test-name="dbm-table-health-date-time"
+              class="h-8"
+              @on:date-change="onDateChange"
+            />
+            <DbmRefreshButton
+              mode="button"
+              :loading="loading"
+              data-test="dbm-table-health-refresh"
+              @refresh="onRefresh"
+            />
+          </div>
         </template>
 
         <!-- The disclosures live here, always visible, never behind a hover.
@@ -304,6 +316,7 @@ import { useRoute, useRouter } from "vue-router";
 import DbmDisclosureLine from "@/components/dbm/DbmDisclosureLine.vue";
 import DbmLockEmptyState, { type DbmLockCheck } from "@/components/dbm/DbmLockEmptyState.vue";
 import DbmPageChrome from "@/components/dbm/DbmPageChrome.vue";
+import DateTime from "@/components/DateTime.vue";
 import DbmRefreshButton from "@/components/dbm/DbmRefreshButton.vue";
 import DbmScopeFilters from "@/components/dbm/DbmScopeFilters.vue";
 import OEmptyState from "@/lib/core/EmptyState/OEmptyState.vue";
diff --git a/web/src/views/DatabaseMonitoring/dbmZeroTraceQueryDetail.spec.ts b/web/src/views/DatabaseMonitoring/dbmZeroTraceQueryDetail.spec.ts
index 069a5a7e863..2e340947d15 100644
--- a/web/src/views/DatabaseMonitoring/dbmZeroTraceQueryDetail.spec.ts
+++ b/web/src/views/DatabaseMonitoring/dbmZeroTraceQueryDetail.spec.ts
@@ -164,7 +164,7 @@ describe("Rule B — the database server is the source of truth on overlap", ()
   it("sources the two overlap tiles through the shared resolver", () => {
     expect(detail).toContain("resolveDatabaseTime");
     expect(detail).toContain("resolveCalls");
-    const stats = detail.split("const headlineStats = computed(")[1]?.split("\n});")[0] ?? "";
+    const stats = detail.split("const headlineStats = computed")[1]?.split("\n});")[0] ?? "";
     expect(stats).toContain("databaseTime.value.value");
     expect(stats).toContain("callCount.value.value");
   });
@@ -176,7 +176,7 @@ describe("Rule B — the database server is the source of truth on overlap", ()
    * The qualifier must reach the tile.
    */
   it("keeps the engine qualifier on every server-sourced tile", () => {
-    const stats = detail.split("const headlineStats = computed(")[1]?.split("\n});")[0] ?? "";
+    const stats = detail.split("const headlineStats = computed")[1]?.split("\n});")[0] ?? "";
     expect(stats).toContain("overlapDetail");
     expect(stats).toContain("dbm.detail.overlap.");
     expect(stats).toContain("engine: engineName");
@@ -189,7 +189,7 @@ describe("Rule B — the database server is the source of truth on overlap", ()
    * total by a traced subtotal.
    */
   it("only lets the client-scope captions qualify a client-sourced value", () => {
-    const stats = detail.split("const headlineStats = computed(")[1]?.split("\n});")[0] ?? "";
+    const stats = detail.split("const headlineStats = computed")[1]?.split("\n});")[0] ?? "";
     expect(stats).toContain('databaseTime.value.source === "client"');
     expect(stats).toContain('callCount.value.source === "client"');
   });
@@ -226,7 +226,7 @@ describe("absent measurements never render as zero", () => {
    * executions.
    */
   it("does not coerce absent calls or errors to zero", () => {
-    const stats = detail.split("const headlineStats = computed(")[1]?.split("\n});")[0] ?? "";
+    const stats = detail.split("const headlineStats = computed")[1]?.split("\n});")[0] ?? "";
     expect(stats, "headlineStats must exist").not.toBe("");
     expect(stats).toContain("const calls = current?.calls;");
     expect(stats).toContain("const errors = current?.errors;");
@@ -240,7 +240,7 @@ describe("absent measurements never render as zero", () => {
    * absent count cannot print as "none".
    */
   it("keeps the rate arithmetic separate from what is displayed", () => {
-    const stats = detail.split("const headlineStats = computed(")[1]?.split("\n});")[0] ?? "";
+    const stats = detail.split("const headlineStats = computed")[1]?.split("\n});")[0] ?? "";
     expect(stats).toContain("callsForRate");
     expect(stats).toContain("errorsForRate");
     expect(stats).toContain("errors === undefined");
@@ -253,7 +253,7 @@ describe("absent measurements never render as zero", () => {
    * query the trace vantage never saw.
    */
   it("suppresses the caption when the value it describes is absent", () => {
-    const stats = detail.split("const headlineStats = computed(")[1]?.split("\n});")[0] ?? "";
+    const stats = detail.split("const headlineStats = computed")[1]?.split("\n});")[0] ?? "";
     expect(stats).toContain("const captionFor =");
     // Every tile whose caption makes a claim routes through the guard.
     for (const value of [
diff --git a/web/src/views/slos/AddSlo.vue b/web/src/views/slos/AddSlo.vue
index 1279f1c150c..c42d80b73eb 100644
--- a/web/src/views/slos/AddSlo.vue
+++ b/web/src/views/slos/AddSlo.vue
@@ -27,7 +27,7 @@
 <template>
   <OPageLayout
     :title="isEdit ? t('slos.editTitle') : t('slos.newTitle')"
-    icon="track-changes"
+    icon="target"
     :back="{ to: backTarget, label: t('slos.title') }"
     scroll
     pad-y
@@ -57,7 +57,7 @@
     <OBanner
       v-if="isEdit && definitionChanged"
       variant="warning"
-      icon="restart_alt"
+      icon="restart-alt"
       class="mb-3"
       data-test="slos-addslo-regen-warning"
     >
@@ -412,39 +412,39 @@
           <!-- OToggleGroup renders OToggleGroupItem children; it has no
                  `options` prop. Passing one rendered an empty bar, which left
                  S-3's rolling window and S-4's slice interval unreachable. -->
-          <OToggleGroup
-            v-model="form.window_secs"
-            :label="t('slos.field.window')"
-            class="mt-3"
-            data-test="slos-addslo-window"
-          >
-            <OToggleGroupItem
-              v-for="opt in windowOptions"
-              :key="opt.value"
-              :value="opt.value"
-              size="sm"
-              :data-test="`slos-addslo-window-${opt.value}`"
+          <div class="mt-3 flex flex-wrap items-center gap-x-6 gap-y-3">
+            <OToggleGroup
+              v-model="form.window_secs"
+              :label="t('slos.field.window')"
+              data-test="slos-addslo-window"
             >
-              {{ opt.label }}
-            </OToggleGroupItem>
-          </OToggleGroup>
-          <OToggleGroup
-            v-model="form.slice_interval_secs"
-            :label="t('slos.field.sliceInterval')"
-            class="mt-3"
-            data-test="slos-addslo-slice"
-          >
-            <OToggleGroupItem
-              v-for="opt in sliceOptions"
-              :key="opt.value"
-              :value="opt.value"
-              :disabled="opt.disable"
-              size="sm"
-              :data-test="`slos-addslo-slice-${opt.value}`"
+              <OToggleGroupItem
+                v-for="opt in windowOptions"
+                :key="opt.value"
+                :value="opt.value"
+                size="sm"
+                :data-test="`slos-addslo-window-${opt.value}`"
+              >
+                {{ opt.label }}
+              </OToggleGroupItem>
+            </OToggleGroup>
+            <OToggleGroup
+              v-model="form.slice_interval_secs"
+              :label="t('slos.field.sliceInterval')"
+              data-test="slos-addslo-slice"
             >
-              {{ opt.label }}
-            </OToggleGroupItem>
-          </OToggleGroup>
+              <OToggleGroupItem
+                v-for="opt in sliceOptions"
+                :key="opt.value"
+                :value="opt.value"
+                :disabled="opt.disable"
+                size="sm"
+                :data-test="`slos-addslo-slice-${opt.value}`"
+              >
+                {{ opt.label }}
+              </OToggleGroupItem>
+            </OToggleGroup>
+          </div>
           <p v-if="isGrouped" class="text-compact text-text-secondary mt-1">
             {{ t("slos.groupedSliceNote") }}
           </p>
@@ -572,7 +572,7 @@
 
 <script setup lang="ts">
 import { computed, onMounted, reactive, ref, watch } from "vue";
-import { raw, useI18nTyped } from "@/types/i18n";
+import { raw, useI18nTyped, type I18nText } from "@/types/i18n";
 import { useRoute, useRouter } from "vue-router";
 import { useStore } from "vuex";
 
@@ -597,6 +597,7 @@ import { toast } from "@/lib/feedback/Toast/useToast";
 import sloService from "@/services/slos";
 import { formatTarget, formatWindow, sliTypeLabel } from "@/composables/useSloFormat";
 import { smallestLegalSlice } from "@/utils/slos/alertSource";
+import type { IconName } from "@/lib/core/Icon/OIcon.icons";
 import type { SloEligibleAlert } from "@/ts/interfaces/slo";
 
 const { t } = useI18nTyped();
@@ -640,7 +641,9 @@ const form = reactive<any>({
 const groupByList = ref<string[]>([]);
 const isGrouped = computed(() => groupByList.value.length > 0);
 
-const sliTypeOptions = computed(() => [
+const sliTypeOptions = computed<
+  { value: string; label: I18nText; icon: IconName; description: I18nText }[]
+>(() => [
   {
     value: "count",
     label: t("slos.type.count"),
@@ -650,13 +653,13 @@ const sliTypeOptions = computed(() => [
   {
     value: "time_slice",
     label: t("slos.type.timeSlice"),
-    icon: "timelapse",
+    icon: "timeline",
     description: t("slos.type.timeSliceDescription"),
   },
   {
     value: "alert",
     label: t("slos.type.alert"),
-    icon: "gpp_maybe",
+    icon: "shield-alert-outline",
     description: t("slos.type.alertDescription"),
   },
 ]);
diff --git a/web/src/views/slos/SloDetail.vue b/web/src/views/slos/SloDetail.vue
index 450ea7ebb77..9886d87db88 100644
--- a/web/src/views/slos/SloDetail.vue
+++ b/web/src/views/slos/SloDetail.vue
@@ -28,7 +28,7 @@
 <template>
   <OPageLayout
     :title="notFound ? t('slos.notFound') : raw(slo?.name || sloId)"
-    icon="track-changes"
+    icon="target"
     :subtitle="notFound ? raw('') : subtitle"
     :back="{ to: backTarget, label: t('slos.title') }"
     title-data-test="slos-slodetail-title"
@@ -113,7 +113,7 @@
     <!-- No `mt-*`: OContent's bottom inset above already separates the strip
          from the tabs, and no horizontal padding either — a tab strip's first
          label self-aligns to the page-edge grid. -->
-    <OTabs v-if="!notFound" v-model="tab" class="shrink-0" data-test="slos-slodetail-tabs">
+    <OTabs v-if="!notFound" v-model="tab" bordered class="shrink-0" data-test="slos-slodetail-tabs">
       <OTab
         name="trend"
         :label="t('slos.tab.trend')"
@@ -440,7 +440,7 @@ const frozenBanner = computed(() => {
     return {
       // Not the snowflake: a window that is still filling is not frozen, and
       // this banner now shows while the SLO is publishing figures.
-      icon: "hourglass_empty",
+      icon: "hourglass-empty",
       title: t("slos.frozen.measuringSinceTitle", { since: measuringSince.value.since }),
       body: t("slos.frozen.measuringSinceBody", measuringSince.value),
     };
diff --git a/web/src/views/slos/SloList.vue b/web/src/views/slos/SloList.vue
index 583c77490c2..648f1187ad5 100644
--- a/web/src/views/slos/SloList.vue
+++ b/web/src/views/slos/SloList.vue
@@ -27,7 +27,7 @@
 <template>
   <OPageLayout
     :title="t('slos.title')"
-    icon="track-changes"
+    icon="target"
     :subtitle="t('slos.subtitle')"
     title-data-test="slos-slolist-title"
     bleed
@@ -294,7 +294,7 @@
 
       <template #empty>
         <OEmptyState
-          icon="track-changes"
+          icon="target"
           :title="t('slos.empty.title')"
           :description="t('slos.empty.description')"
         >
@@ -404,7 +404,7 @@
 
 <script setup lang="ts">
 import { computed, onMounted, ref } from "vue";
-import { raw, useI18nTyped } from "@/types/i18n";
+import { raw, useI18nTyped, type I18nText } from "@/types/i18n";
 import { useRoute, useRouter } from "vue-router";
 import { useStore } from "vuex";
 
@@ -424,6 +424,7 @@ import OStatStrip from "@/lib/data/StatStrip/OStatStrip.vue";
 import OTable from "@/lib/core/Table/OTable.vue";
 import OTag from "@/lib/core/Badge/OTag.vue";
 import type { BadgeVariant } from "@/lib/core/Badge/OBadge.types";
+import type { IconName } from "@/lib/core/Icon/OIcon.icons";
 import OToggleGroup from "@/lib/core/ToggleGroup/OToggleGroup.vue";
 import OIcon from "@/lib/core/Icon/OIcon.vue";
 import OToggleGroupItem from "@/lib/core/ToggleGroup/OToggleGroupItem.vue";
@@ -546,11 +547,11 @@ function folderName(folderId: string): string {
   return folders.find((f: any) => f.folderId === folderId)?.name || folderId;
 }
 
-const typeOptions = computed(() => [
-  { value: "all", label: t("slos.type.all"), icon: "format_list_bulleted" },
+const typeOptions = computed<{ value: string; label: I18nText; icon: IconName }[]>(() => [
+  { value: "all", label: t("slos.type.all"), icon: "format-list-bulleted" },
   { value: "count", label: t("slos.type.count"), icon: "functions" },
-  { value: "time_slice", label: t("slos.type.timeSlice"), icon: "timelapse" },
-  { value: "alert", label: t("slos.type.alert"), icon: "gpp_maybe" },
+  { value: "time_slice", label: t("slos.type.timeSlice"), icon: "timeline" },
+  { value: "alert", label: t("slos.type.alert"), icon: "shield-alert-outline" },
 ]);
 
 const columns = computed<OTableColumnDef<SloListItem>[]>(() => [
