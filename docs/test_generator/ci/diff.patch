===== COMPLETE LIST OF CHANGED FILES (21 files) =====
# AUTHORITATIVE: this list is never truncated. The diff BODY below may be trimmed for
# very large PRs, so decide WHICH files/areas changed (e.g. web/src frontend) from THIS list.
scripts/translations/.translation_state.json
src/config/src/metrics.rs
src/core/src/logs/ingest.rs
web/src/components/ingestion/logs/SplunkHec.spec.ts
web/src/components/ingestion/logs/SplunkHec.vue
web/src/locales/languages/ar-SA.json
web/src/locales/languages/de-DE.json
web/src/locales/languages/en-US.json
web/src/locales/languages/es-ES.json
web/src/locales/languages/fr-FR.json
web/src/locales/languages/it-IT.json
web/src/locales/languages/ja-JP.json
web/src/locales/languages/ko-KR.json
web/src/locales/languages/nl-NL.json
web/src/locales/languages/pl-PL.json
web/src/locales/languages/pt-PT.json
web/src/locales/languages/ru-RU.json
web/src/locales/languages/tr-TR.json
web/src/locales/languages/vi-VN.json
web/src/locales/languages/zh-CN.json
web/src/locales/languages/zh-TW.json
===== END CHANGED FILES =====

diff --git a/scripts/translations/.translation_state.json b/scripts/translations/.translation_state.json
index 1c4e705ae1c..cae67eb0b83 100644
--- a/scripts/translations/.translation_state.json
+++ b/scripts/translations/.translation_state.json
@@ -9837,7 +9837,9 @@
       "tlsTitle": "0029bc25af318a4f",
       "tokenPageName": "759b4b3e31901744",
       "tokenWhere": "d6f5666b805c2e36",
-      "tokenWhereSuffix": "7fb346521d5bfb41"
+      "tokenWhereSuffix": "7fb346521d5bfb41",
+      "windowBody": "38d66efc5808226e",
+      "windowTitle": "309255973d9f0279"
     },
     "splunkHecTab": "d31533a68927505f",
     "splunkHecUrlHelp": "22d5a113a0b4745e",
diff --git a/src/config/src/metrics.rs b/src/config/src/metrics.rs
index 7e35f39cf06..a0c61727286 100644
--- a/src/config/src/metrics.rs
+++ b/src/config/src/metrics.rs
@@ -2599,6 +2599,24 @@ pub static HEC_REQUESTS_TOTAL: Lazy<IntCounterVec> = Lazy::new(|| {
     .expect("Metric created")
 });
 
+/// Count of records dropped by an ingestion policy on the write path, which still answers success.
+///
+/// Not labelled by stream: streams are auto-created from client-supplied names, and this fires on
+/// a path a client controls, so a per-stream label is unbounded cardinality. The warn log carries
+/// the name.
+pub static INGEST_RECORDS_DROPPED: Lazy<IntCounterVec> = Lazy::new(|| {
+    IntCounterVec::new(
+        Opts::new(
+            "ingest_records_dropped_total",
+            "Records discarded by an ingestion policy rather than stored".to_owned() + HELP_SUFFIX,
+        )
+        .namespace(NAMESPACE)
+        .const_labels(create_const_labels()),
+        &["organization", "stream_type", "reason"],
+    )
+    .expect("Metric created")
+});
+
 fn register_metrics(registry: &Registry) {
     // http latency
     registry
@@ -2623,6 +2641,9 @@ fn register_metrics(registry: &Registry) {
     registry
         .register(Box::new(HEC_REQUESTS_TOTAL.clone()))
         .expect("Metric registered");
+    registry
+        .register(Box::new(INGEST_RECORDS_DROPPED.clone()))
+        .expect("Metric registered");
 
     // ingester stats
     registry
diff --git a/src/core/src/logs/ingest.rs b/src/core/src/logs/ingest.rs
index 9e46946b522..c6487ab852e 100644
--- a/src/core/src/logs/ingest.rs
+++ b/src/core/src/logs/ingest.rs
@@ -700,7 +700,7 @@ pub async fn ingest(
             .inc();
     }
 
-    warn_if_wholly_discarded(org_id, endpoint, &response_body);
+    warn_and_count_discards(org_id, endpoint, &response_body);
 
     // A write failure used to be visible only in the metric label while the
     // caller still saw 200. Signalled on `write_failed` and NOT on `code`, which
@@ -759,20 +759,44 @@ fn is_wholly_discarded(status: &RecordStatus) -> bool {
     status.failed > 0 && status.successful == 0
 }
 
-/// The 200 stays (non-2xx reads as retryable), so this warn is the only loud total-loss signal.
-fn warn_if_wholly_discarded(org_id: &str, endpoint: &str, status: &StreamStatus) {
-    if !is_wholly_discarded(&status.status) {
+/// True when a discarded batch is worth a warn at all.
+///
+/// A partial drop qualifies: a mixed batch keeps `successful > 0` forever, so a source with
+/// skewed timestamps loses records indefinitely with nothing said.
+fn warrants_discard_warn(status: &RecordStatus) -> bool {
+    status.failed > 0
+}
+
+/// Warn and count the records a success status hides.
+fn warn_and_count_discards(org_id: &str, endpoint: &str, status: &StreamStatus) {
+    if status.status.policy_dropped > 0 {
+        metrics::INGEST_RECORDS_DROPPED
+            .with_label_values(&[org_id, StreamType::Logs.as_str(), "ingestion_window"])
+            .inc_by(status.status.policy_dropped as u64);
+    }
+    if !warrants_discard_warn(&status.status) {
         return;
     }
     if !discard_warn_permitted(&format!("{org_id}/{}", status.name), Instant::now()) {
         return;
     }
-    log::warn!(
-        "[INGEST] {endpoint} {org_id}/{}: discarded all {} record(s), none stored: {}",
-        status.name,
-        status.status.failed,
-        status.status.error
-    );
+    if is_wholly_discarded(&status.status) {
+        log::warn!(
+            "[INGEST] {endpoint} {org_id}/{}: discarded all {} record(s), none stored: {}",
+            status.name,
+            status.status.failed,
+            status.status.error
+        );
+    } else {
+        log::warn!(
+            "[INGEST] {endpoint} {org_id}/{}: discarded {} of {} record(s), {} stored: {}",
+            status.name,
+            status.status.failed,
+            status.status.failed + status.status.successful,
+            status.status.successful,
+            status.status.error
+        );
+    }
 }
 
 /// At most one warn per stream per interval; a poisoned lock silences rather than panics.
@@ -1337,6 +1361,47 @@ mod tests {
         }));
     }
 
+    #[test]
+    fn a_partial_drop_warrants_a_warn_even_though_the_batch_stored_records() {
+        let partial = RecordStatus {
+            successful: 9,
+            failed: 1,
+            policy_dropped: 1,
+            error: "too old".to_string(),
+        };
+        // The regression: gating the warn on `is_wholly_discarded` alone skips this,
+        // so a skewed source loses records indefinitely with nothing logged.
+        assert!(!is_wholly_discarded(&partial));
+        assert!(warrants_discard_warn(&partial));
+
+        // A batch that stored everything stays silent.
+        assert!(!warrants_discard_warn(&RecordStatus {
+            successful: 10,
+            failed: 0,
+            policy_dropped: 0,
+            error: String::new(),
+        }));
+    }
+
+    #[test]
+    fn a_policy_drop_increments_the_dropped_counter() {
+        let mut status = StreamStatus::new("s");
+        status.status = RecordStatus {
+            successful: 9,
+            failed: 1,
+            policy_dropped: 1,
+            error: "too old".to_string(),
+        };
+        let counter = metrics::INGEST_RECORDS_DROPPED
+            .get_metric_with_label_values(&["org_partial_drop", "logs", "ingestion_window"])
+            .unwrap();
+        let before = counter.get();
+
+        warn_and_count_discards("org_partial_drop", "/test", &status);
+
+        assert_eq!(counter.get(), before + 1);
+    }
+
     /// The interval throttles per stream, so one bad client cannot drown the log.
     #[test]
     fn wholly_discarded_warns_are_rate_limited_per_stream() {
diff --git a/web/src/components/ingestion/logs/SplunkHec.spec.ts b/web/src/components/ingestion/logs/SplunkHec.spec.ts
index 0b157fefd2c..9c40b7374a1 100644
--- a/web/src/components/ingestion/logs/SplunkHec.spec.ts
+++ b/web/src/components/ingestion/logs/SplunkHec.spec.ts
@@ -121,20 +121,44 @@ describe("SplunkHec", () => {
       const parsed = JSON.parse(payload);
       expect(parsed).toHaveProperty("event");
       expect(parsed).toHaveProperty("index");
-      expect(parsed).toHaveProperty("time");
+      // A literal epoch ages past ZO_INGEST_ALLOWED_UPTO and is then discarded
+      // behind a code 0, so the copyable example must let the receipt time stand.
+      expect(parsed).not.toHaveProperty("time");
     });
 
     it("should document the full event envelope, including the metadata fields", () => {
       wrapper = createWrapper();
       const parsed = JSON.parse(wrapper.vm.payloadContent);
       expect(parsed.index).toBe("application");
-      // Fractional epoch SECONDS, which is what the collector reads.
-      expect(parsed.time).toBe(1789060000.123);
+      // Fractional epoch SECONDS, which is what the collector reads, and within the
+      // ingestion window: a literal ages out and is then discarded behind a code 0.
+      const nowSeconds = Date.now() / 1000;
+      expect(parsed.time).toBeGreaterThan(nowSeconds - 60);
+      expect(parsed.time).toBeLessThanOrEqual(nowSeconds + 60);
+      // Millisecond precision, asserted on the rendered text: `time % 1` is 0 for
+      // the one run in a thousand where Date.now() lands on a whole second.
+      expect(wrapper.vm.payloadContent).toMatch(/"time": \d+\.\d{3},/);
       expect(parsed.host).toBeDefined();
       expect(parsed.source).toBeDefined();
       expect(parsed.sourcetype).toBeDefined();
     });
 
+    it("should refresh the reference epoch on a page held open past the window", async () => {
+      vi.useFakeTimers();
+      try {
+        wrapper = createWrapper();
+        const before = JSON.parse(wrapper.vm.payloadContent).time;
+
+        // Longer than ZO_INGEST_ALLOWED_UPTO's 5h default, which is when a frozen
+        // epoch starts being discarded behind a code 0.
+        await vi.advanceTimersByTimeAsync(6 * 60 * 60 * 1000);
+
+        expect(JSON.parse(wrapper.vm.payloadContent).time).toBeGreaterThan(before);
+      } finally {
+        vi.useRealTimers();
+      }
+    });
+
     it("should point the health check at the unauthenticated health path", () => {
       wrapper = createWrapper();
       expect(wrapper.vm.healthContent).toContain("/services/collector/health");
@@ -165,9 +189,16 @@ describe("SplunkHec", () => {
   });
 
   describe("Operational guidance", () => {
-    it("should warn about Edge Processor and TLS", () => {
+    it("should warn about the ingestion window, Edge Processor and TLS", () => {
       wrapper = createWrapper();
-      expect(wrapper.findAll(".o-banner-mock")).toHaveLength(2);
+      expect(wrapper.findAll(".o-banner-mock")).toHaveLength(3);
+      for (const test of [
+        "ingestion-logs-splunkhec-window-note",
+        "ingestion-logs-splunkhec-edge-processor-note",
+        "ingestion-logs-splunkhec-tls-note",
+      ]) {
+        expect(wrapper.find(`[data-test="${test}"]`).exists()).toBe(true);
+      }
     });
 
     it("should mark the key sections for tests", () => {
diff --git a/web/src/components/ingestion/logs/SplunkHec.vue b/web/src/components/ingestion/logs/SplunkHec.vue
index 8141407b8fd..209024b09d6 100644
--- a/web/src/components/ingestion/logs/SplunkHec.vue
+++ b/web/src/components/ingestion/logs/SplunkHec.vue
@@ -76,6 +76,19 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       <OText variant="body" as="p">
         {{ t("ingestion.splunkHec.payloadTime", { field: raw("time") }) }}
       </OText>
+      <OBanner variant="warning" icon="schedule" data-test="ingestion-logs-splunkhec-window-note">
+        <div class="flex flex-col gap-1">
+          <OText variant="body-strong">{{ t("ingestion.splunkHec.windowTitle") }}</OText>
+          <OText variant="meta" as="p" class="leading-snug">
+            {{
+              t("ingestion.splunkHec.windowBody", {
+                past: raw("ZO_INGEST_ALLOWED_UPTO"),
+                future: raw("ZO_INGEST_ALLOWED_IN_FUTURE"),
+              })
+            }}
+          </OText>
+        </div>
+      </OBanner>
       <OText variant="body" as="p">
         {{
           t("ingestion.splunkHec.payloadMetadata", {
@@ -138,7 +151,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
 
 <script lang="ts">
 import { raw, useI18nTyped } from "@/types/i18n";
-import { computed, defineComponent } from "vue";
+import { computed, defineComponent, onBeforeUnmount, onMounted, ref } from "vue";
 import { useStore } from "vuex";
 import { getImageURL } from "../../../utils/zincutils";
 import CopyContent from "@/components/CopyContent.vue";
@@ -169,18 +182,37 @@ export default defineComponent({
     // Built here because the template compiler reads a literal `<token>` in an interpolation as markup.
     const authHeader = raw("Authorization: Splunk " + "<token>");
 
+    // No literal `time`: a hardcoded epoch ages past ZO_INGEST_ALLOWED_UPTO and is
+    // then discarded, which the collector still answers with code 0.
     const curlContent = `curl -k ${endpointUrl} \\
   -H "Authorization: Splunk [SPLUNK_HEC_TOKEN]" \\
-  -d '{"event":{"level":"info","log":"test message for openobserve"},"index":"default","time":1789060000}'`;
-
-    const payloadContent = `{
+  -d '{"event":{"level":"info","log":"test message for openobserve"},"index":"default"}'`;
+
+    // `time` is documented by example here, so it stays — but read through a clock
+    // that ticks, since a page held open past ZO_INGEST_ALLOWED_UPTO would otherwise
+    // hand out an epoch that is silently discarded behind a code 0.
+    const nowSeconds = ref((Date.now() / 1000).toFixed(3));
+    const retickPayloadClock = () => {
+      nowSeconds.value = (Date.now() / 1000).toFixed(3);
+    };
+    let payloadClock: ReturnType<typeof setInterval> | undefined;
+    onMounted(() => {
+      payloadClock = setInterval(retickPayloadClock, 60_000);
+    });
+    onBeforeUnmount(() => {
+      clearInterval(payloadClock);
+    });
+
+    const payloadContent = computed(
+      () => `{
   "event": { "level": "info", "log": "test message for openobserve" },
   "index": "application",
-  "time": 1789060000.123,
+  "time": ${nowSeconds.value},
   "host": "web-01",
   "source": "/var/log/app.log",
   "sourcetype": "app:json"
-}`;
+}`,
+    );
 
     const healthContent = `curl -k ${collectorBase}/services/collector/health`;
 
diff --git a/web/src/locales/languages/ar-SA.json b/web/src/locales/languages/ar-SA.json
index 596264a0369..904951b7bb7 100644
--- a/web/src/locales/languages/ar-SA.json
+++ b/web/src/locales/languages/ar-SA.json
@@ -4484,6 +4484,8 @@
       "payloadTitle": "حمولة الحدث",
       "payloadIndex": "يحدد الحقل {field} الجدول. الأحداث التي لا تحتويه تذهب إلى جدول {stream}.",
       "payloadTime": "الحقل {field} هو ثوانٍ من حقبة Unix، وتُقبل القيمة الكسرية. عندما يكون مفقودًا أو غير موجب، يُستخدم وقت الاستلام.",
+      "windowTitle": "يتم تجاهل الطوابع الزمنية خارج نافذة الاستيعاب",
+      "windowBody": "يتم إسقاط الأحداث الأقدم من {past} (5 ساعات افتراضيًا) أو الأبعد في المستقبل من {future} (24 ساعة افتراضيًا)، ومع ذلك يستجيب الطلب بالرمز 0 لأن عميل Splunk لا يعيد المحاولة بعد الخطأ. لذلك يبدو الطابع الزمني الثابت أو القديم وكأنه إرسال ناجح لا يخزن أي شيء.",
       "payloadMetadata": "تُخزَّن الحقول {fields} كأعمدة في الحدث.",
       "healthTitle": "فحص الحالة",
       "healthBody": "لا يتطلب طلب GET مصادقة ويجيب بـ {response}.",
diff --git a/web/src/locales/languages/de-DE.json b/web/src/locales/languages/de-DE.json
index 694b75fd4fd..45f74591193 100644
--- a/web/src/locales/languages/de-DE.json
+++ b/web/src/locales/languages/de-DE.json
@@ -4484,6 +4484,8 @@
       "payloadTitle": "Ereignis-Nutzlast",
       "payloadIndex": "Das Feld {field} wählt den Stream aus. Ereignisse ohne dieses Feld gehen an den Stream {stream}.",
       "payloadTime": "Das Feld {field} enthält Epochensekunden, wobei auch ein Bruchwert akzeptiert wird. Wenn es fehlt oder nicht positiv ist, wird die Empfangszeit verwendet.",
+      "windowTitle": "Zeitstempel außerhalb des Aufnahmefensters werden verworfen",
+      "windowBody": "Ereignisse, die älter als {past} (standardmäßig 5 Stunden) oder weiter in der Zukunft als {future} (standardmäßig 24 Stunden) sind, werden verworfen, und die Anfrage antwortet dennoch mit Code 0, da ein Splunk-Client einen Fehler nicht erneut sendet. Ein fest codierter oder veralteter Zeitstempel sieht daher wie ein erfolgreicher Versand aus, der nichts speichert.",
       "payloadMetadata": "Die Felder {fields} werden als Spalten am Ereignis gespeichert.",
       "healthTitle": "Integritätsprüfung",
       "healthBody": "Eine GET-Anfrage erfordert keine Authentifizierung und antwortet mit {response}.",
diff --git a/web/src/locales/languages/en-US.json b/web/src/locales/languages/en-US.json
index 3e38c391a0e..7fa2ba65790 100644
--- a/web/src/locales/languages/en-US.json
+++ b/web/src/locales/languages/en-US.json
@@ -4484,6 +4484,8 @@
       "payloadTitle": "Event payload",
       "payloadIndex": "The {field} field selects the stream. Events without it go to the {stream} stream.",
       "payloadTime": "The {field} field is epoch seconds, and a fractional value is accepted. When it is missing or not positive, the receipt time is used.",
+      "windowTitle": "Timestamps outside the ingestion window are discarded",
+      "windowBody": "Events older than {past} (5 hours by default) or further ahead than {future} (24 hours by default) are dropped, and the request still answers code 0 because a Splunk client does not retry an error. A hardcoded or stale timestamp therefore looks like a successful send that stores nothing.",
       "payloadMetadata": "The {fields} fields are stored as columns on the event.",
       "healthTitle": "Health check",
       "healthBody": "A GET request needs no authentication and answers {response}.",
diff --git a/web/src/locales/languages/es-ES.json b/web/src/locales/languages/es-ES.json
index 60513cb8780..67815b589e8 100644
--- a/web/src/locales/languages/es-ES.json
+++ b/web/src/locales/languages/es-ES.json
@@ -4484,6 +4484,8 @@
       "payloadTitle": "Carga útil del evento",
       "payloadIndex": "El campo {field} selecciona el stream. Los eventos que no lo incluyen van al stream {stream}.",
       "payloadTime": "El campo {field} está en segundos epoch y se acepta un valor fraccionario. Cuando falta o no es positivo, se usa la hora de recepción.",
+      "windowTitle": "Las marcas de tiempo fuera de la ventana de ingesta se descartan",
+      "windowBody": "Los eventos anteriores a {past} (5 horas de forma predeterminada) o posteriores a {future} (24 horas de forma predeterminada) se descartan, y la solicitud sigue respondiendo con el código 0 porque un cliente de Splunk no reintenta tras un error. Por eso, una marca de tiempo fija o desactualizada parece un envío correcto que no almacena nada.",
       "payloadMetadata": "Los campos {fields} se almacenan como columnas en el evento.",
       "healthTitle": "Comprobación de estado",
       "healthBody": "Una solicitud GET no requiere autenticación y responde {response}.",
diff --git a/web/src/locales/languages/fr-FR.json b/web/src/locales/languages/fr-FR.json
index 40522301d6a..7fa2624983e 100644
--- a/web/src/locales/languages/fr-FR.json
+++ b/web/src/locales/languages/fr-FR.json
@@ -4484,6 +4484,8 @@
       "payloadTitle": "Charge utile de l'événement",
       "payloadIndex": "Le champ {field} sélectionne le flux. Les événements qui en sont dépourvus vont dans le flux {stream}.",
       "payloadTime": "Le champ {field} est exprimé en secondes epoch, et une valeur fractionnaire est acceptée. S'il est absent ou non positif, l'heure de réception est utilisée.",
+      "windowTitle": "Les horodatages hors de la fenêtre d'ingestion sont ignorés",
+      "windowBody": "Les événements antérieurs à {past} (5 heures par défaut) ou postérieurs à {future} (24 heures par défaut) sont supprimés, et la requête répond quand même avec le code 0 car un client Splunk ne réessaie pas après une erreur. Un horodatage codé en dur ou obsolète ressemble donc à un envoi réussi qui n'enregistre rien.",
       "payloadMetadata": "Les champs {fields} sont stockés comme colonnes sur l'événement.",
       "healthTitle": "Contrôle de santé",
       "healthBody": "Une requête GET ne nécessite aucune authentification et répond {response}.",
diff --git a/web/src/locales/languages/it-IT.json b/web/src/locales/languages/it-IT.json
index 7ef7b695fb9..a9bb4632565 100644
--- a/web/src/locales/languages/it-IT.json
+++ b/web/src/locales/languages/it-IT.json
@@ -4484,6 +4484,8 @@
       "payloadTitle": "Payload dell'evento",
       "payloadIndex": "Il campo {field} seleziona lo stream. Gli eventi senza di esso vanno nello stream {stream}.",
       "payloadTime": "Il campo {field} è in secondi epoch e accetta un valore frazionario. Quando manca o non è positivo, viene usato l'orario di ricezione.",
+      "windowTitle": "I timestamp fuori dalla finestra di ingestione vengono scartati",
+      "windowBody": "Gli eventi più vecchi di {past} (5 ore per impostazione predefinita) o più avanti di {future} (24 ore per impostazione predefinita) vengono scartati e la richiesta risponde comunque con codice 0, perché un client Splunk non ritenta dopo un errore. Un timestamp fisso o obsoleto sembra quindi un invio riuscito che non memorizza nulla.",
       "payloadMetadata": "I campi {fields} vengono memorizzati come colonne sull'evento.",
       "healthTitle": "Controllo di integrità",
       "healthBody": "Una richiesta GET non richiede autenticazione e risponde {response}.",
diff --git a/web/src/locales/languages/ja-JP.json b/web/src/locales/languages/ja-JP.json
index 083d5899315..1396c407c1d 100644
--- a/web/src/locales/languages/ja-JP.json
+++ b/web/src/locales/languages/ja-JP.json
@@ -4484,6 +4484,8 @@
       "payloadTitle": "イベントペイロード",
       "payloadIndex": "{field} フィールドがストリームを選択します。これがないイベントは {stream} ストリームに送られます。",
       "payloadTime": "{field} フィールドはエポック秒で、小数値も使用できます。欠落している場合や正の値でない場合は、受信時刻が使用されます。",
+      "windowTitle": "取り込みウィンドウ外のタイムスタンプは破棄されます",
+      "windowBody": "{past}（既定では 5 時間）より古い、または {future}（既定では 24 時間）より先のイベントは破棄されますが、Splunk クライアントはエラーを再試行しないため、リクエストは引き続きコード 0 を返します。そのため、ハードコードされた古いタイムスタンプは、何も保存されないのに送信が成功したように見えます。",
       "payloadMetadata": "{fields} フィールドはイベントのカラムとして保存されます。",
       "healthTitle": "ヘルスチェック",
       "healthBody": "GET リクエストは認証を必要とせず、{response} を返します。",
diff --git a/web/src/locales/languages/ko-KR.json b/web/src/locales/languages/ko-KR.json
index 3af1e5c93a0..e72c2c6a1e9 100644
--- a/web/src/locales/languages/ko-KR.json
+++ b/web/src/locales/languages/ko-KR.json
@@ -4484,6 +4484,8 @@
       "payloadTitle": "이벤트 페이로드",
       "payloadIndex": "{field} 필드는 스트림을 선택합니다. 이 필드가 없는 이벤트는 {stream} 스트림으로 전송됩니다.",
       "payloadTime": "{field} 필드는 에포크 초이며 소수 값도 허용됩니다. 이 필드가 없거나 양수가 아니면 수신 시간이 사용됩니다.",
+      "windowTitle": "수집 윈도를 벗어난 타임스탬프는 삭제됩니다",
+      "windowBody": "{past}(기본값 5시간)보다 오래되었거나 {future}(기본값 24시간)보다 미래인 이벤트는 삭제되지만, Splunk 클라이언트는 오류를 재시도하지 않으므로 요청은 여전히 코드 0을 반환합니다. 따라서 하드코딩되었거나 오래된 타임스탬프는 아무것도 저장하지 않으면서 전송에 성공한 것처럼 보입니다.",
       "payloadMetadata": "{fields} 필드는 이벤트의 컬럼으로 저장됩니다.",
       "healthTitle": "헬스 체크",
       "healthBody": "GET 요청은 인증이 필요하지 않으며 {response}를 응답합니다.",
diff --git a/web/src/locales/languages/nl-NL.json b/web/src/locales/languages/nl-NL.json
index 99e503680c5..52b8b943fb6 100644
--- a/web/src/locales/languages/nl-NL.json
+++ b/web/src/locales/languages/nl-NL.json
@@ -4484,6 +4484,8 @@
       "payloadTitle": "Gebeurtenis-payload",
       "payloadIndex": "Het veld {field} selecteert de stream. Gebeurtenissen zonder dit veld gaan naar de stream {stream}.",
       "payloadTime": "Het veld {field} is epochseconden, en een fractionele waarde wordt geaccepteerd. Als het ontbreekt of niet positief is, wordt de ontvangsttijd gebruikt.",
+      "windowTitle": "Tijdstempels buiten het inname-venster worden verwijderd",
+      "windowBody": "Gebeurtenissen ouder dan {past} (standaard 5 uur) of verder vooruit dan {future} (standaard 24 uur) worden verwijderd, en het verzoek antwoordt toch met code 0 omdat een Splunk-client een fout niet opnieuw probeert. Een hardcoded of verouderd tijdstempel lijkt daardoor op een geslaagde verzending die niets opslaat.",
       "payloadMetadata": "De velden {fields} worden als kolommen op de gebeurtenis opgeslagen.",
       "healthTitle": "Healthcheck",
       "healthBody": "Een GET-verzoek vereist geen authenticatie en antwoordt met {response}.",
diff --git a/web/src/locales/languages/pl-PL.json b/web/src/locales/languages/pl-PL.json
index de529f5030b..b4e746e5be1 100644
--- a/web/src/locales/languages/pl-PL.json
+++ b/web/src/locales/languages/pl-PL.json
@@ -4484,6 +4484,8 @@
       "payloadTitle": "Ładunek zdarzenia",
       "payloadIndex": "Pole {field} wybiera strumień. Zdarzenia bez niego trafiają do strumienia {stream}.",
       "payloadTime": "Pole {field} to sekundy epoch; wartość ułamkowa jest akceptowana. Gdy go brakuje lub nie jest dodatnie, używany jest czas odbioru.",
+      "windowTitle": "Znaczniki czasu spoza okna pozyskiwania są odrzucane",
+      "windowBody": "Zdarzenia starsze niż {past} (domyślnie 5 godzin) lub wybiegające dalej w przyszłość niż {future} (domyślnie 24 godziny) są odrzucane, a żądanie i tak zwraca kod 0, ponieważ klient Splunk nie ponawia próby po błędzie. Zakodowany na stałe lub nieaktualny znacznik czasu wygląda więc jak udane wysłanie, które niczego nie zapisuje.",
       "payloadMetadata": "Pola {fields} są przechowywane jako kolumny w zdarzeniu.",
       "healthTitle": "Sprawdzenie stanu",
       "healthBody": "Żądanie GET nie wymaga uwierzytelniania i odpowiada {response}.",
diff --git a/web/src/locales/languages/pt-PT.json b/web/src/locales/languages/pt-PT.json
index e1277f4db3a..27477c4e0e4 100644
--- a/web/src/locales/languages/pt-PT.json
+++ b/web/src/locales/languages/pt-PT.json
@@ -4484,6 +4484,8 @@
       "payloadTitle": "Payload do evento",
       "payloadIndex": "O campo {field} seleciona o stream. Eventos sem ele vão para o stream {stream}.",
       "payloadTime": "O campo {field} está em segundos epoch, e um valor fracionário é aceito. Quando ele está ausente ou não é positivo, o horário de recebimento é usado.",
+      "windowTitle": "Os carimbos de data/hora fora da janela de ingestão são descartados",
+      "windowBody": "Os eventos mais antigos do que {past} (5 horas por predefinição) ou mais à frente do que {future} (24 horas por predefinição) são descartados, e o pedido responde mesmo assim com o código 0, porque um cliente Splunk não repete após um erro. Um carimbo de data/hora fixo ou desatualizado parece, assim, um envio bem-sucedido que não armazena nada.",
       "payloadMetadata": "Os campos {fields} são armazenados como colunas no evento.",
       "healthTitle": "Verificação de integridade",
       "healthBody": "Uma requisição GET não exige autenticação e responde {response}.",
diff --git a/web/src/locales/languages/ru-RU.json b/web/src/locales/languages/ru-RU.json
index 5869aad34bf..c884c13dd85 100644
--- a/web/src/locales/languages/ru-RU.json
+++ b/web/src/locales/languages/ru-RU.json
@@ -4484,6 +4484,8 @@
       "payloadTitle": "Полезная нагрузка события",
       "payloadIndex": "Поле {field} выбирает поток. События без него попадают в поток {stream}.",
       "payloadTime": "Поле {field} задается в секундах эпохи, допускается дробное значение. Если оно отсутствует или не положительно, используется время приема.",
+      "windowTitle": "Метки времени вне окна приёма отбрасываются",
+      "windowBody": "События старше {past} (по умолчанию 5 часов) или дальше в будущем, чем {future} (по умолчанию 24 часа), отбрасываются, и запрос всё равно возвращает код 0, поскольку клиент Splunk не повторяет запрос после ошибки. Поэтому жёстко заданная или устаревшая метка времени выглядит как успешная отправка, которая ничего не сохраняет.",
       "payloadMetadata": "Поля {fields} сохраняются как столбцы события.",
       "healthTitle": "Проверка состояния",
       "healthBody": "GET-запрос не требует аутентификации и отвечает {response}.",
diff --git a/web/src/locales/languages/tr-TR.json b/web/src/locales/languages/tr-TR.json
index 3d6eb68fb22..2cc458a5031 100644
--- a/web/src/locales/languages/tr-TR.json
+++ b/web/src/locales/languages/tr-TR.json
@@ -4484,6 +4484,8 @@
       "payloadTitle": "Olay yükü",
       "payloadIndex": "{field} alanı akışı seçer. Bu alan olmayan olaylar {stream} akışına gider.",
       "payloadTime": "{field} alanı epoch saniyesidir ve kesirli bir değer kabul edilir. Eksik olduğunda veya pozitif olmadığında, alınma zamanı kullanılır.",
+      "windowTitle": "Alım penceresinin dışındaki zaman damgaları atılır",
+      "windowBody": "{past} değerinden (varsayılan 5 saat) daha eski veya {future} değerinden (varsayılan 24 saat) daha ileri olan olaylar atılır ve Splunk istemcisi bir hatayı yeniden denemediği için istek yine de kod 0 yanıtı verir. Bu nedenle sabit kodlanmış veya eski bir zaman damgası, hiçbir şey kaydetmeyen başarılı bir gönderim gibi görünür.",
       "payloadMetadata": "{fields} alanları, olay üzerinde sütunlar olarak saklanır.",
       "healthTitle": "Sağlık kontrolü",
       "healthBody": "Bir GET isteği kimlik doğrulaması gerektirmez ve {response} yanıtını verir.",
diff --git a/web/src/locales/languages/vi-VN.json b/web/src/locales/languages/vi-VN.json
index 50b0aa1e89a..d13a0b1c14c 100644
--- a/web/src/locales/languages/vi-VN.json
+++ b/web/src/locales/languages/vi-VN.json
@@ -4484,6 +4484,8 @@
       "payloadTitle": "Payload sự kiện",
       "payloadIndex": "Trường {field} chọn stream. Các sự kiện không có nó sẽ đi vào stream {stream}.",
       "payloadTime": "Trường {field} là epoch giây, và giá trị thập phân cũng được chấp nhận. Khi nó bị thiếu hoặc không dương, thời gian nhận sẽ được dùng.",
+      "windowTitle": "Dấu thời gian ngoài cửa sổ thu nhận sẽ bị loại bỏ",
+      "windowBody": "Các sự kiện cũ hơn {past} (mặc định 5 giờ) hoặc xa hơn {future} (mặc định 24 giờ) sẽ bị loại bỏ, và yêu cầu vẫn trả về mã 0 vì ứng dụng khách Splunk không thử lại khi gặp lỗi. Do đó, một dấu thời gian cố định hoặc đã cũ trông giống như một lần gửi thành công nhưng không lưu gì cả.",
       "payloadMetadata": "Các trường {fields} được lưu dưới dạng cột trên sự kiện.",
       "healthTitle": "Kiểm tra tình trạng",
       "healthBody": "Yêu cầu GET không cần xác thực và trả về {response}.",
diff --git a/web/src/locales/languages/zh-CN.json b/web/src/locales/languages/zh-CN.json
index 753b075577c..02b1ff8f959 100644
--- a/web/src/locales/languages/zh-CN.json
+++ b/web/src/locales/languages/zh-CN.json
@@ -4484,6 +4484,8 @@
       "payloadTitle": "事件负载",
       "payloadIndex": "{field} 字段选择数据流。没有该字段的事件将进入 {stream} 数据流。",
       "payloadTime": "{field} 字段是 epoch 秒，并且接受小数值。当该字段缺失或不为正数时，将使用接收时间。",
+      "windowTitle": "超出摄取窗口的时间戳将被丢弃",
+      "windowBody": "早于 {past}（默认 5 小时）或晚于 {future}（默认 24 小时）的事件将被丢弃，而请求仍会返回 代码 0，因为 Splunk 客户端不会重试错误。因此，硬编码或过期的时间戳看起来像是发送成功，实际却没有存储任何数据。",
       "payloadMetadata": "{fields} 字段作为事件上的列存储。",
       "healthTitle": "健康检查",
       "healthBody": "GET 请求无需身份验证，并返回 {response}。",
diff --git a/web/src/locales/languages/zh-TW.json b/web/src/locales/languages/zh-TW.json
index ca3ff9fc08b..a0ecd3fa089 100644
--- a/web/src/locales/languages/zh-TW.json
+++ b/web/src/locales/languages/zh-TW.json
@@ -4484,6 +4484,8 @@
       "payloadTitle": "事件承載內容",
       "payloadIndex": "{field} 欄位會選取串流。沒有此欄位的事件會進入 {stream} 串流。",
       "payloadTime": "{field} 欄位是 epoch 秒數，且接受小數值。當該欄位缺少或不是正數時，會使用接收時間。",
+      "windowTitle": "超出擷取視窗的時間戳記將被捨棄",
+      "windowBody": "早於 {past}（預設 5 小時）或晚於 {future}（預設 24 小時）的事件將被捨棄，而要求仍會回應 代碼 0，因為 Splunk 用戶端不會重試錯誤。因此，硬式編碼或過期的時間戳記看起來像是傳送成功，實際卻沒有儲存任何資料。",
       "payloadMetadata": "{fields} 欄位會儲存為事件上的資料行。",
       "healthTitle": "健康檢查",
       "healthBody": "GET 請求不需要驗證，並會回應 {response}。",
