===== COMPLETE LIST OF CHANGED FILES (32 files) =====
# AUTHORITATIVE: this list is never truncated. The diff BODY below may be trimmed for
# very large PRs, so decide WHICH files/areas changed (e.g. web/src frontend) from THIS list.
scripts/translations/.translation_state.json
tests/ui-testing/playwright-tests/Alerts/alerts-anomaly-detection.spec.js
web/src/components/alerts/AlertList.vue
web/src/components/anomaly_detection/AnomalyDataPreview.spec.ts
web/src/components/anomaly_detection/AnomalyDataPreview.vue
web/src/components/anomaly_detection/steps/AnomalyDetectionConfig.spec.ts
web/src/components/anomaly_detection/steps/AnomalyDetectionConfig.vue
web/src/components/common/IacRegistryLinks.spec.ts
web/src/components/common/IacRegistryLinks.vue
web/src/lib/core/Button/OButton.spec.ts
web/src/lib/core/Button/OButton.vue
web/src/locales/anomalySensitivityParity.spec.ts
web/src/locales/languages/ar-SA.json
web/src/locales/languages/de-DE.json
web/src/locales/languages/en-US.json
web/src/locales/languages/es-ES.json
web/src/locales/languages/fr-FR.json
web/src/locales/languages/it-IT.json
web/src/locales/languages/ja-JP.json
web/src/locales/languages/ko-KR.json
web/src/locales/languages/nl-NL.json
web/src/locales/languages/pl-PL.json
web/src/locales/languages/pt-PT.json
web/src/locales/languages/ru-RU.json
web/src/locales/languages/tr-TR.json
web/src/locales/languages/vi-VN.json
web/src/locales/languages/zh-CN.json
web/src/locales/languages/zh-TW.json
web/src/utils/alerts/anomalySqlBuilder.spec.ts
web/src/utils/alerts/anomalySqlBuilder.ts
web/src/utils/alerts/anomalySummaryGenerator.spec.ts
web/src/views/slos/SloList.vue
===== END CHANGED FILES =====

diff --git a/scripts/translations/.translation_state.json b/scripts/translations/.translation_state.json
index 6608c253f40..0f35721d4ad 100644
--- a/scripts/translations/.translation_state.json
+++ b/scripts/translations/.translation_state.json
@@ -2666,6 +2666,7 @@
       "checkEveryTooltip": "1d060aeac4423111",
       "destinationRequired": "10d0b2a6b1b39964",
       "detectionCharts": "bff6c2f71314f27d",
+      "detectionFunctionTooltip": "5446cf66c0f39c24",
       "detectionResolution": "12d1318bffed4742",
       "detectionResolutionTooltip": "d5da8afe60842fa5",
       "deviationChart": "eff37edc81c0f1e4",
@@ -2689,7 +2690,8 @@
       "noFieldsFound": "06b05d1636030e56",
       "notifications": "788011833a5a0f22",
       "notificationsTooltip": "6be62e6d3cce3112",
-      "percentile": "bb13af3d0ba72ee2",
+      "percentile": "1709305c9fa966d1",
+      "previewCaption": "8c330d6626e8bc99",
       "queryMode": "f93202944f8f5b93",
       "retrainEvery": "f0207dc1b4d4c620",
       "retrainEveryTooltip": "58f3d7ff0d271b4a",
@@ -2706,7 +2708,8 @@
       "sensitivityBalanced": "5386ea5db81c8579",
       "sensitivityBudgetTooltip": "268d2ec2a9cf3927",
       "sensitivityConservative": "75e1783e4e517e54",
-      "sensitivityHintPercentile": "af18ccd63ba2748a",
+      "sensitivityHintPercentile": "99ff83f5381c312c",
+      "sensitivityNotDataPercentile": "983f3edcdb12e25a",
       "sensitivityRange": "8fdc44006812bb32",
       "sensitivityTooltip": "c7e2ec81613d9cf6",
       "seriesAnomaly": "79622828d2d8d988",
@@ -2735,7 +2738,7 @@
       "summaryRetrainEveryDays": "fe194d44b92571b4",
       "summarySchedule": "9ca338ab03aabdd4",
       "summaryThreshold": "1e4fada9d2ab1f32",
-      "summaryThresholdPercentile": "72ffbcff0f2533ee",
+      "summaryThresholdPercentile": "20ff1b9e29e00400",
       "summaryTraining": "e54a950976befa4b",
       "summaryTrainingDays": "a2246fbc2542d1dd",
       "timestampAliasBanned": "f5eabfdd326c0964",
diff --git a/tests/ui-testing/playwright-tests/Alerts/alerts-anomaly-detection.spec.js b/tests/ui-testing/playwright-tests/Alerts/alerts-anomaly-detection.spec.js
index 1ab59645491..b065d0ab4c6 100644
--- a/tests/ui-testing/playwright-tests/Alerts/alerts-anomaly-detection.spec.js
+++ b/tests/ui-testing/playwright-tests/Alerts/alerts-anomaly-detection.spec.js
@@ -221,7 +221,11 @@ test.describe('Anomaly Detection', () => {
 
       const hint = pm.anomalyDetectionPage.getSensitivityHintLocator();
       await expect(hint).toBeVisible();
-      await expect(hint).toContainText('p97');
+      await expect(hint).toContainText('97%');
+
+      // The number is interpolated, not baked into the string: a stale hint would keep 97.
+      await pm.anomalyDetectionPage.selectSensitivityTier(99);
+      await expect(hint).toContainText('99%');
     });
 
     // The hint deliberately states no flag rate: the bar is the p-mark of the model's own
diff --git a/web/src/components/alerts/AlertList.vue b/web/src/components/alerts/AlertList.vue
index c1a87fb13a0..987d7fedc15 100644
--- a/web/src/components/alerts/AlertList.vue
+++ b/web/src/components/alerts/AlertList.vue
@@ -62,7 +62,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
       </template>
 
       <template #actions-overflow>
-        <IacRegistryLinks data-test="alert-list-iac-registries" />
+        <IacRegistryLinks data-test="alert-list-iac-registries" :compact="isCompactToolbar" />
         <!-- Import button -->
         <OButton
           :class="isCompactToolbar ? 'min-w-0! px-2! py-0!' : ''"
diff --git a/web/src/components/anomaly_detection/AnomalyDataPreview.spec.ts b/web/src/components/anomaly_detection/AnomalyDataPreview.spec.ts
index 33a662b5342..e8def4e7f6a 100644
--- a/web/src/components/anomaly_detection/AnomalyDataPreview.spec.ts
+++ b/web/src/components/anomaly_detection/AnomalyDataPreview.spec.ts
@@ -565,5 +565,26 @@ describe("AnomalyDataPreview", () => {
       // multiplied by a bucket count, which nothing in the system computes.
       expect((wrapper.vm as any).previewPanelSchema?.config?.mark_line).toEqual([]);
     });
+
+    // A chart with no bar reads as unfinished unless it says what it is showing.
+    it("captions the rendered chart as the training series", async () => {
+      wrapper = mountPreview();
+      await flushPromises();
+
+      const caption = wrapper.find('[data-test="anomaly-data-preview-caption"]');
+      expect(caption.exists()).toBe(true);
+      // The caption must send the reader somewhere real for anomalies, since none
+      // are drawn here — "Detection results" is the tab that actually shows them.
+      expect(caption.text()).toContain("watch");
+      expect(caption.text()).toContain("Detection results");
+    });
+
+    it("hides the caption while the empty state is showing", async () => {
+      wrapper = mountPreview({ stream_name: "" });
+      await flushPromises();
+
+      expect(wrapper.find('[data-test="anomaly-data-preview-empty"]').exists()).toBe(true);
+      expect(wrapper.find('[data-test="anomaly-data-preview-caption"]').exists()).toBe(false);
+    });
   });
 });
diff --git a/web/src/components/anomaly_detection/AnomalyDataPreview.vue b/web/src/components/anomaly_detection/AnomalyDataPreview.vue
index dadbbb594f5..db172bbc283 100644
--- a/web/src/components/anomaly_detection/AnomalyDataPreview.vue
+++ b/web/src/components/anomaly_detection/AnomalyDataPreview.vue
@@ -15,9 +15,9 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
 -->
 
 <template>
-  <div class="h-full px-3 py-2">
+  <div class="flex h-full flex-col px-3 py-2">
     <!-- The echarts canvas collapses to a sliver unless it resolves to an explicit box -->
-    <div class="relative h-full w-full">
+    <div class="relative min-h-0 w-full flex-1">
       <div
         v-if="!previewActive"
         class="absolute inset-0 flex flex-col items-center justify-center gap-2"
@@ -38,6 +38,14 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
         data-test="anomaly-data-preview-chart"
       />
     </div>
+    <!-- No model exists yet, and the score bar lives in score space, not this chart's value space -->
+    <p
+      v-if="previewActive"
+      class="text-text-secondary shrink-0 pt-1.5 text-xs"
+      data-test="anomaly-data-preview-caption"
+    >
+      {{ t("alerts.anomaly.previewCaption") }}
+    </p>
   </div>
 </template>
 
diff --git a/web/src/components/anomaly_detection/steps/AnomalyDetectionConfig.spec.ts b/web/src/components/anomaly_detection/steps/AnomalyDetectionConfig.spec.ts
index 52283938593..b3ae05c7453 100644
--- a/web/src/components/anomaly_detection/steps/AnomalyDetectionConfig.spec.ts
+++ b/web/src/components/anomaly_detection/steps/AnomalyDetectionConfig.spec.ts
@@ -535,7 +535,7 @@ describe("AnomalyDetectionConfig", () => {
 
       expect(tierStates(wrapper)).toEqual(["off", "off", "off"]);
       expect((percentileInput(wrapper).element as HTMLInputElement).value).toBe("88");
-      expect(sensitivityHintText(wrapper)).toContain("p88");
+      expect(sensitivityHintText(wrapper)).toContain("88%");
     });
 
     it("typing a tier value lights that tier up", async () => {
@@ -591,7 +591,7 @@ describe("AnomalyDetectionConfig", () => {
 
       const hint = sensitivityHintText(wrapper);
       expect(hint).toBeDefined();
-      expect(hint).toContain("p97");
+      expect(hint).toContain("97%");
       expect(hint).toContain("training");
       for (const promise of ["per day", "per week", "about", "3%", "resolution"]) {
         expect(hint).not.toContain(promise);
@@ -764,6 +764,36 @@ describe("AnomalyDetectionConfig", () => {
     });
   });
 
+  describe("sensitivity — percentile row alignment", () => {
+    it("keeps the toggle bar and the percentile box on one centred row", async () => {
+      wrapper = mountConfig();
+      await flushPromises();
+
+      // A stacked label here would push the whole row below the Sensitivity heading.
+      // Walk up from the input to the row that also holds the tier toggle: that
+      // shared ancestor is the one whose cross-axis alignment sets the row's top.
+      let row: HTMLElement | null = wrapper.find('[data-test="anomaly-sensitivity-percentile"]')
+        .element as HTMLElement;
+      while (row && !row.querySelector('[data-test="anomaly-sensitivity-tier"]')) {
+        row = row.parentElement;
+      }
+      expect(row).not.toBeNull();
+      expect(row?.className).toContain("items-center");
+      expect(row?.className).not.toContain("items-end");
+    });
+
+    it("keeps the percentile label out of the narrow numeric column", async () => {
+      wrapper = mountConfig();
+      await flushPromises();
+
+      // The label renders as a sibling span, never inside OInput's own field column.
+      expect(wrapper.find('[data-test="anomaly-sensitivity-percentile"] label').exists()).toBe(
+        false,
+      );
+      expect(wrapper.find('[data-test="anomaly-sensitivity-percentile-info"]').exists()).toBe(true);
+    });
+  });
+
   // Wire contract: `alert_budget_per_day` absent/invalid = percentile mode; while set, `threshold` is API-derived and must never render or be written.
   describe("sensitivity — budget mode", () => {
     it("a config with no budget renders the percentile controls only", async () => {
diff --git a/web/src/components/anomaly_detection/steps/AnomalyDetectionConfig.vue b/web/src/components/anomaly_detection/steps/AnomalyDetectionConfig.vue
index 52347cc49be..5ec013a9cac 100644
--- a/web/src/components/anomaly_detection/steps/AnomalyDetectionConfig.vue
+++ b/web/src/components/anomaly_detection/steps/AnomalyDetectionConfig.vue
@@ -186,6 +186,14 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
             >
               {{ t("alerts.detectionFunction") }}
               <span class="text-status-error-text ms-1">*</span>
+              <OIcon name="info" size="sm" class="text-icon-color ms-1 cursor-pointer">
+                <OTooltip
+                  side="right"
+                  align="center"
+                  max-width="18.75rem"
+                  :content="t('alerts.anomaly.detectionFunctionTooltip')"
+                />
+              </OIcon>
             </div>
             <!-- items-start, not items-center: the field select renders its
                  validation message inside its own column (OSelect's root is
@@ -550,7 +558,7 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                 />
               </div>
             </div>
-            <div v-else class="flex items-start gap-3">
+            <div v-else class="flex items-center gap-3">
               <OFormToggleGroup
                 name="threshold"
                 :aria-label="t('alerts.sensitivity')"
@@ -568,16 +576,38 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
                   {{ tier.label }}
                 </OToggleGroupItem>
               </OFormToggleGroup>
-              <OFormInput
-                name="threshold"
-                type="number"
-                :model-modifiers="{ number: true }"
-                :label="t('alerts.anomaly.percentile')"
-                class="max-w-21.75 min-w-21.75"
-                data-test="anomaly-sensitivity-percentile"
-              >
-                <template #error />
-              </OFormInput>
+              <!-- Inline, not OFormInput's `label` prop: a stacked label would push the
+                   whole control row a label-height below the Sensitivity heading. -->
+              <div class="flex items-center gap-2">
+                <span
+                  class="o-input-label text-compact text-input-label-text flex items-center gap-1 leading-tight font-medium whitespace-nowrap"
+                >
+                  {{ t("alerts.anomaly.percentile") }}
+                  <OIcon
+                    name="info-outline"
+                    size="sm"
+                    class="cursor-help"
+                    data-test="anomaly-sensitivity-percentile-info"
+                  >
+                    <OTooltip
+                      side="right"
+                      align="center"
+                      max-width="18.75rem"
+                      :content="t('alerts.anomaly.sensitivityNotDataPercentile')"
+                    />
+                  </OIcon>
+                </span>
+                <OFormInput
+                  name="threshold"
+                  type="number"
+                  :model-modifiers="{ number: true }"
+                  :aria-label="t('alerts.anomaly.percentile')"
+                  class="max-w-21.75 min-w-21.75"
+                  data-test="anomaly-sensitivity-percentile"
+                >
+                  <template #error />
+                </OFormInput>
+              </div>
             </div>
             <div
               v-if="sensitivityError"
diff --git a/web/src/components/common/IacRegistryLinks.spec.ts b/web/src/components/common/IacRegistryLinks.spec.ts
index 9c7b836c105..aeb89bb4b3e 100644
--- a/web/src/components/common/IacRegistryLinks.spec.ts
+++ b/web/src/components/common/IacRegistryLinks.spec.ts
@@ -23,12 +23,17 @@ import IacRegistryLinks from "./IacRegistryLinks.vue";
 const i18n = createI18n({
   locale: "en",
   messages: {
-    en: { common: { openProviderOnRegistry: "OpenObserve provider on the {registry}" } },
+    en: {
+      common: {
+        openProviderOnRegistry: "OpenObserve provider on the {registry}",
+      },
+    },
   },
 });
 
-function mountLinks(theme: "light" | "dark") {
+function mountLinks(theme: "light" | "dark", props: { compact?: boolean } = {}) {
   return mount(IacRegistryLinks, {
+    props,
     global: { plugins: [createStore({ state: { theme } }), i18n] },
   });
 }
@@ -81,4 +86,54 @@ describe("IacRegistryLinks", () => {
     expect(src("light")).toContain("%234040b2");
     expect(src("dark")).toBe(src("light"));
   });
+
+  // The affordance gap this component shipped with: the marks carried an
+  // aria-label and a tooltip, so nothing was visible until you hovered.
+  it("labels each link visibly, without hovering", () => {
+    const wrapper = mountLinks("light");
+    const text = wrapper.text();
+
+    expect(text).toContain("Terraform");
+    expect(text).toContain("OpenTofu");
+  });
+
+  // Must be visually identical to the Import button it sits beside, which is
+  // variant="outline" — anything else reads as stray text next to a real control.
+  it("renders each registry as an outline button, matching Import", () => {
+    const wrapper = mountLinks("light");
+    const tf = wrapper.find('[data-test="iac-registry-links-terraform"]');
+
+    expect(tf.exists()).toBe(true);
+    expect(tf.element.tagName).toBe("A");
+    expect(tf.classes().join(" ")).toContain("border-button-outline-border");
+    expect(tf.classes().join(" ")).not.toContain("bg-button-secondary");
+  });
+
+  it("derives each link's data-test from dataTest", () => {
+    const wrapper = mount(IacRegistryLinks, {
+      props: { dataTest: "slos-slolist-iac-registries" },
+      global: { plugins: [createStore({ state: { theme: "light" } }), i18n] },
+    });
+
+    expect(wrapper.find('[data-test="slos-slolist-iac-registries-terraform"]').exists()).toBe(true);
+    expect(wrapper.find('[data-test="slos-slolist-iac-registries-opentofu"]').exists()).toBe(true);
+  });
+
+  it("drops the button text when compact, keeping both links", () => {
+    const wrapper = mountLinks("light", { compact: true });
+
+    expect(wrapper.text()).not.toContain("Terraform");
+    expect(wrapper.findAll("a")).toHaveLength(2);
+  });
+
+  // The caption is an addition, never a replacement: hover-free labelling must
+  // not cost the per-destination name each link already carried.
+  it("keeps each link's per-registry aria-label alongside the caption", () => {
+    const links = mountLinks("light").findAll("a");
+
+    expect(links[0].attributes("aria-label")).toBe(
+      "OpenObserve provider on the Terraform Registry",
+    );
+    expect(links[1].attributes("aria-label")).toBe("OpenObserve provider on the OpenTofu Registry");
+  });
 });
diff --git a/web/src/components/common/IacRegistryLinks.vue b/web/src/components/common/IacRegistryLinks.vue
index 86d49a69f42..814e39869ca 100644
--- a/web/src/components/common/IacRegistryLinks.vue
+++ b/web/src/components/common/IacRegistryLinks.vue
@@ -19,10 +19,17 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
   OpenObserve provider on that registry. Two separate logos rather than one
   combined badge: they are two distinct destinations, and the pair is also what
   tells a reader the exported configuration applies to either tool.
+
+  ONE caption labels the pair rather than one label per mark: two logos beside a
+  primary CTA have to stay quieter than it, and naming each one doubles the width
+  of a secondary affordance while saying the same thing twice. It names the
+  ARTIFACT ("Terraform provider" — what OpenTofu consumes too), never a registry,
+  since a registry name would mislabel whichever of the two marks it omits.
 -->
 <script setup lang="ts">
 import { computed } from "vue";
 
+import OButton from "@/lib/core/Button/OButton.vue";
 import OIcon from "@/lib/core/Icon/OIcon.vue";
 import OTooltip from "@/lib/overlay/Tooltip/OTooltip.vue";
 import opentofuLogo from "@/assets/images/common/opentofu.svg";
@@ -32,7 +39,13 @@ import { useTheme } from "@/composables/useTheme";
 import { OPENTOFU_REGISTRY_URL, TERRAFORM_REGISTRY_URL } from "@/utils/terraform/provider";
 import { raw, useI18nTyped } from "@/types/i18n";
 
-withDefaults(defineProps<{ dataTest?: string }>(), { dataTest: "iac-registry-links" });
+// `compact` drops the button text where the caller has no width for it; only the
+// call site knows, since the same pair sits inline on one page and inside the
+// header's "More" dropdown on another.
+const props = withDefaults(defineProps<{ dataTest?: string; compact?: boolean }>(), {
+  dataTest: "iac-registry-links",
+  compact: false,
+});
 
 const { t } = useI18nTyped();
 const { isDark } = useTheme();
@@ -48,13 +61,15 @@ const { isDark } = useTheme();
 const REGISTRIES = computed(() => [
   {
     key: "terraform",
-    name: raw("Terraform Registry"),
+    name: raw("Terraform"),
+    registry: raw("Terraform Registry"),
     icon: `img:${terraformLogo}`,
     url: TERRAFORM_REGISTRY_URL,
   },
   {
     key: "opentofu",
-    name: raw("OpenTofu Registry"),
+    name: raw("OpenTofu"),
+    registry: raw("OpenTofu Registry"),
     icon: `img:${isDark.value ? opentofuLogoDark : opentofuLogo}`,
     url: OPENTOFU_REGISTRY_URL,
   },
@@ -63,22 +78,28 @@ const REGISTRIES = computed(() => [
 
 <template>
   <div class="flex items-center gap-2" :data-test="dataTest">
-    <a
+    <OButton
       v-for="registry in REGISTRIES"
       :key="registry.key"
+      :class="props.compact ? 'min-w-0! px-2! py-0!' : ''"
+      variant="outline"
+      size="sm"
+      as="a"
       :href="registry.url"
       target="_blank"
       rel="noopener noreferrer"
-      :aria-label="t('common.openProviderOnRegistry', { registry: registry.name })"
-      class="rounded-default hover:bg-surface-subtle-hover flex items-center p-1 opacity-80 transition-opacity hover:opacity-100"
+      :aria-label="t('common.openProviderOnRegistry', { registry: registry.registry })"
       :data-test="`${dataTest}-${registry.key}`"
     >
-      <OIcon :name="registry.icon" size="md" />
+      <!-- Slot, not `icon-left`: that prop is typed to the icon enum and these are brand art. -->
+      <template #icon-left><OIcon :name="registry.icon" size="sm" /></template>
+      <template v-if="!props.compact">{{ registry.name }}</template>
       <OTooltip
-        :content="t('common.openProviderOnRegistry', { registry: registry.name })"
+        v-if="props.compact"
+        :content="t('common.openProviderOnRegistry', { registry: registry.registry })"
         side="bottom"
         align="end"
       />
-    </a>
+    </OButton>
   </div>
 </template>
diff --git a/web/src/lib/core/Button/OButton.spec.ts b/web/src/lib/core/Button/OButton.spec.ts
index a01e3e95eeb..8328d4a453f 100644
--- a/web/src/lib/core/Button/OButton.spec.ts
+++ b/web/src/lib/core/Button/OButton.spec.ts
@@ -216,6 +216,15 @@ describe("OButton", () => {
     expect(wrapper.element.tagName.toLowerCase()).toBe("a");
   });
 
+  // base-elements.css underlines every a:hover, which would make a link-button
+  // read as prose text rather than a control.
+  it('never underlines when as="a"', () => {
+    const wrapper = mount(OButton, { props: { as: "a" } });
+    const classes = wrapper.classes().join(" ");
+    expect(classes).toContain("no-underline");
+    expect(classes).toContain("hover:no-underline");
+  });
+
   it('does not set type attribute when as="a"', () => {
     const wrapper = mount(OButton, { props: { as: "a" } });
     expect(wrapper.attributes("type")).toBeUndefined();
diff --git a/web/src/lib/core/Button/OButton.vue b/web/src/lib/core/Button/OButton.vue
index a31576c9caa..abdba64d2be 100644
--- a/web/src/lib/core/Button/OButton.vue
+++ b/web/src/lib/core/Button/OButton.vue
@@ -323,6 +323,8 @@ const classes = computed<string[]>(() => [
   // taller than a borderless ghost one of the same size.
   "relative box-border",
   "whitespace-nowrap",
+  // `as="a"` renders a real anchor, which base-elements.css underlines on hover.
+  "no-underline hover:no-underline",
   // Medium (500) keeps button labels calm/simple — heavier weights read as shouty.
   "font-medium transition-[color,background-color,border-color,text-decoration-color,fill,stroke,box-shadow] duration-150",
   "outline-none",
diff --git a/web/src/locales/anomalySensitivityParity.spec.ts b/web/src/locales/anomalySensitivityParity.spec.ts
index d5cdffdc4db..f8c10a7d395 100644
--- a/web/src/locales/anomalySensitivityParity.spec.ts
+++ b/web/src/locales/anomalySensitivityParity.spec.ts
@@ -185,112 +185,112 @@ const PINNED_COPY: Record<string, { tooltip: string; budgetTooltip: string; hint
       "يحدد حد الدرجات من درجات تدريب النموذج عند هذا المئين — القيمة 97 تضع الحد عند أعلى 3٪ من درجات التدريب. قد تختلف الدرجات الفعلية عن درجات التدريب، لذا فهذه ليست نسبة من الفترات القادمة وليست معدل تنبيهات. القيمة الأقل تخفض الحد وترسل عادة تنبيهات أكثر؛ وتباعد فترة التهدئة التنبيهات المتكررة.",
     budgetTooltip:
       "الحد الأقصى لعدد التنبيهات التي يمكن لهذا الإعداد إرسالها، ويُفرض عند الإرسال. يستمر الكشف في تقييم كل فترة؛ وعند استنفاد الميزانية تُمنع التنبيهات الإضافية حتى تتجدد.",
-    hint: "تُقيَّم الفترات مقابل علامة المئين {percentile} من درجات تدريب النموذج — وعدد التنبيهات الناتج يعتمد على بياناتك.",
+    hint: "تُطلق النافذة تنبيهًا عندما تبدو أكثر غرابة من {percentile}% مما رآه النموذج أثناء التدريب. ويعتمد عدد التنبيهات الناتجة على بياناتك.",
   },
   "de-DE": {
     tooltip:
       "Legt die Score-Schwelle aus den Trainingswerten des Modells bei diesem Perzentil fest — 97 setzt die Schwelle bei den obersten 3 % der Trainingswerte. Live-Werte können vom Training abweichen; dies ist also kein Anteil künftiger Buckets und keine Alarmrate. Ein niedrigerer Wert senkt die Schwelle und sendet in der Regel mehr Warnmeldungen; eine Abklingzeit begrenzt Wiederholungen.",
     budgetTooltip:
       "Die maximale Zahl von Warnmeldungen, die diese Konfiguration zustellen darf — bei der Zustellung erzwungen. Die Erkennung bewertet weiterhin jeden Bucket; ist das Budget aufgebraucht, werden weitere Warnmeldungen unterdrückt, bis es sich auffüllt.",
-    hint: "Buckets werden am p{percentile}-Wert der Trainingswerte des Modells gemessen — wie viele Warnmeldungen das ergibt, hängt von Ihren Daten ab.",
+    hint: "Ein Bucket löst eine Warnmeldung aus, wenn er ungewöhnlicher erscheint als {percentile} % dessen, was das Modell im Training gesehen hat. Wie viele Warnmeldungen das bedeutet, hängt von Ihren Daten ab.",
   },
   "en-US": {
     tooltip:
       "Sets the score bar from the model's training scores at this percentile — 97 puts the bar at the top 3% of training scores. Live scores can differ from training, so this is not a share of future buckets and not an alert rate. A lower value lowers the bar and generally sends more alerts; a cooldown spaces out repeats.",
     budgetTooltip:
       "The maximum number of alerts this configuration may deliver, enforced at delivery. Detection still scores every bucket; once the budget is spent, further alerts are suppressed until it refills.",
-    hint: "Buckets are judged against the p{percentile} mark of the model's training scores — how many alerts that yields depends on your data.",
+    hint: "A bucket alerts when it looks more unusual than {percentile}% of what the model saw in training. How many alerts that means depends on your data.",
   },
   "es-ES": {
     tooltip:
       "Fija el listón de puntuación a partir de las puntuaciones de entrenamiento del modelo en este percentil: 97 sitúa el listón en el 3 % superior de las puntuaciones de entrenamiento. Las puntuaciones reales pueden diferir del entrenamiento, así que esto no es una proporción de buckets futuros ni una tasa de alertas. Un valor más bajo baja el listón y suele enviar más alertas; un periodo de enfriamiento espacia las repeticiones.",
     budgetTooltip:
       "El número máximo de alertas que esta configuración puede entregar, aplicado en la entrega. La detección sigue puntuando cada bucket; agotado el presupuesto, las alertas adicionales se suprimen hasta que se repone.",
-    hint: "Los buckets se comparan con la marca p{percentile} de las puntuaciones de entrenamiento del modelo; cuántas alertas produce depende de tus datos.",
+    hint: "Un intervalo genera una alerta cuando parece más inusual que el {percentile}% de lo que el modelo vio durante el entrenamiento. Cuántas alertas supone eso depende de tus datos.",
   },
   "fr-FR": {
     tooltip:
       "Définit la barre de score à partir des scores d'entraînement du modèle à ce percentile — 97 place la barre au niveau des 3 % les plus élevés des scores d'entraînement. Les scores réels peuvent différer de l'entraînement : ce n'est donc ni une part des compartiments futurs ni un taux d'alertes. Une valeur plus basse abaisse la barre et envoie en général davantage d'alertes ; un délai de refroidissement espace les répétitions.",
     budgetTooltip:
       "Le nombre maximal d'alertes que cette configuration peut délivrer, appliqué à la livraison. La détection continue de noter chaque compartiment ; une fois le budget épuisé, les alertes supplémentaires sont supprimées jusqu'à ce qu'il se reconstitue.",
-    hint: "Les compartiments sont évalués par rapport au repère p{percentile} des scores d'entraînement du modèle — le nombre d'alertes qui en résulte dépend de vos données.",
+    hint: "Un intervalle déclenche une alerte lorsqu'il paraît plus inhabituel que {percentile}% de ce que le modèle a vu à l'entraînement. Le nombre d'alertes que cela représente dépend de vos données.",
   },
   "it-IT": {
     tooltip:
       "Imposta l'asticella del punteggio dai punteggi di addestramento del modello a questo percentile: 97 colloca l'asticella nel 3 % più alto dei punteggi di addestramento. I punteggi reali possono differire dall'addestramento, quindi questa non è una quota dei bucket futuri né un tasso di avvisi. Un valore più basso abbassa l'asticella e in genere invia più avvisi; un periodo di attesa distanzia le ripetizioni.",
     budgetTooltip:
       "Il numero massimo di avvisi che questa configurazione può recapitare, applicato al recapito. Il rilevamento continua a valutare ogni bucket; esaurito il budget, gli avvisi ulteriori vengono soppressi finché non si ricarica.",
-    hint: "I bucket sono valutati rispetto al riferimento p{percentile} dei punteggi di addestramento del modello: quanti avvisi ne derivino dipende dai tuoi dati.",
+    hint: "Un intervallo genera un avviso quando appare più insolito del {percentile}% di ciò che il modello ha visto durante l'addestramento. Quanti avvisi comporti dipende dai tuoi dati.",
   },
   "ja-JP": {
     tooltip:
       "モデルのトレーニングスコアのこのパーセンタイルからスコアの基準線を設定します。97 はトレーニングスコアの上位 3% に基準線を置きます。実際のスコアはトレーニングと異なることがあるため、これは将来のバケットの割合でもアラート率でもありません。値を下げると基準線が下がり、通常はアラートが増えます。クールダウンにより連続するアラートは間隔が空きます。",
     budgetTooltip:
       "この設定が配信できるアラートの上限で、配信時に強制されます。検出はすべてのバケットをスコアリングし続けます。予算を使い切ると、回復するまで追加のアラートは抑制されます。",
-    hint: "バケットはモデルのトレーニングスコアの p{percentile} 基準で判定されます。アラート件数はデータ次第です。",
+    hint: "バケットは、モデルがトレーニングで見たものの {percentile}% よりも異常に見える場合にアラートを発します。それが何件のアラートになるかはデータ次第です。",
   },
   "ko-KR": {
     tooltip:
       "모델 훈련 점수의 이 백분위수에서 점수 기준선을 설정합니다. 97은 훈련 점수 상위 3%에 기준선을 둡니다. 실제 점수는 훈련과 다를 수 있으므로 이는 미래 버킷의 비율도, 알림 비율도 아닙니다. 값을 낮추면 기준선이 낮아져 보통 더 많은 알림이 전송되며, 쿨다운이 반복 알림의 간격을 벌립니다.",
     budgetTooltip:
       "이 구성이 전달할 수 있는 알림의 최대 개수로, 전달 시점에 강제됩니다. 감지는 모든 버킷을 계속 채점하며, 예산이 소진되면 회복될 때까지 추가 알림이 억제됩니다.",
-    hint: "버킷은 모델 훈련 점수의 p{percentile} 기준에 따라 판정됩니다. 알림 수는 데이터에 따라 달라집니다.",
+    hint: "버킷이 학습 중 모델이 확인한 데이터의 {percentile}%보다 더 이례적으로 보이면 알림이 발생합니다. 알림이 몇 건 발생하는지는 데이터에 따라 다릅니다.",
   },
   "nl-NL": {
     tooltip:
       "Stelt de scorelat in op basis van de trainingsscores van het model bij dit percentiel — 97 legt de lat bij de hoogste 3% van de trainingsscores. Live scores kunnen afwijken van de training, dus dit is geen aandeel van toekomstige buckets en geen meldingsfrequentie. Een lagere waarde verlaagt de lat en verstuurt doorgaans meer meldingen; een afkoelperiode spreidt herhalingen.",
     budgetTooltip:
       "Het maximale aantal meldingen dat deze configuratie mag bezorgen, afgedwongen bij bezorging. Detectie blijft elke bucket scoren; is het budget op, dan worden verdere meldingen onderdrukt tot het zich aanvult.",
-    hint: "Buckets worden beoordeeld tegen het p{percentile}-punt van de trainingsscores van het model — hoeveel meldingen dat oplevert, hangt af van je gegevens.",
+    hint: "Een bucket geeft een alarm wanneer deze ongewoner lijkt dan {percentile}% van wat het model tijdens de training zag. Hoeveel alarmen dat oplevert, hangt af van uw gegevens.",
   },
   "pl-PL": {
     tooltip:
       "Ustawia próg punktowy na podstawie wyników treningowych modelu przy tym percentylu — 97 umieszcza próg przy górnych 3% wyników treningowych. Wyniki na żywo mogą różnić się od treningowych, więc nie jest to udział przyszłych przedziałów ani częstość alertów. Niższa wartość obniża próg i zwykle wysyła więcej alertów; okres wyciszenia rozdziela powtórzenia.",
     budgetTooltip:
       "Maksymalna liczba alertów, jaką ta konfiguracja może dostarczyć, egzekwowana przy dostarczaniu. Wykrywanie nadal ocenia każdy przedział; po wyczerpaniu budżetu kolejne alerty są wstrzymywane, aż budżet się odnowi.",
-    hint: "Przedziały są oceniane względem punktu p{percentile} wyników treningowych modelu — liczba alertów zależy od Twoich danych.",
+    hint: "Przedział generuje alert, gdy wygląda na bardziej nietypowy niż {percentile}% tego, co model widział podczas uczenia. To, ile to oznacza alertów, zależy od Twoich danych.",
   },
   "pt-PT": {
     tooltip:
       "Define a fasquia de pontuação a partir das pontuações de treino do modelo neste percentil — 97 coloca a fasquia nos 3 % superiores das pontuações de treino. As pontuações reais podem diferir do treino, pelo que isto não é uma fração dos buckets futuros nem uma taxa de alertas. Um valor mais baixo desce a fasquia e normalmente envia mais alertas; um período de espera espaça as repetições.",
     budgetTooltip:
       "O número máximo de alertas que esta configuração pode entregar, aplicado na entrega. A deteção continua a pontuar todos os buckets; esgotado o orçamento, os alertas adicionais são suprimidos até ele se repor.",
-    hint: "Os buckets são avaliados face à marca p{percentile} das pontuações de treino do modelo — quantos alertas isso gera depende dos seus dados.",
+    hint: "Um bucket gera alerta quando parece mais incomum do que {percentile}% do que o modelo viu no treinamento. Quantos alertas isso significa depende dos seus dados.",
   },
   "ru-RU": {
     tooltip:
       "Задаёт планку оценки по обучающим оценкам модели на этом процентиле — 97 ставит планку на верхних 3% обучающих оценок. Реальные оценки могут отличаться от обучающих, поэтому это не доля будущих бакетов и не частота оповещений. Меньшее значение опускает планку и обычно отправляет больше оповещений; период охлаждения разрежает повторы.",
     budgetTooltip:
       "Максимальное число оповещений, которое эта конфигурация может доставить; ограничение применяется при доставке. Обнаружение продолжает оценивать каждый бакет; когда бюджет исчерпан, дальнейшие оповещения подавляются, пока он не восстановится.",
-    hint: "Бакеты оцениваются относительно отметки p{percentile} обучающих оценок модели — сколько оповещений это даст, зависит от ваших данных.",
+    hint: "Интервал срабатывает, когда выглядит необычнее, чем {percentile}% увиденного моделью при обучении. Сколько это даст оповещений, зависит от ваших данных.",
   },
   "tr-TR": {
     tooltip:
       "Puan çıtasını, modelin eğitim puanlarının bu yüzdelik dilimine göre belirler — 97, çıtayı eğitim puanlarının en üst %3'üne yerleştirir. Canlı puanlar eğitimden farklı olabilir; bu nedenle bu, gelecekteki aralıkların bir payı ya da bir uyarı oranı değildir. Daha düşük bir değer çıtayı düşürür ve genellikle daha fazla uyarı gönderir; bekleme süresi tekrarları seyrekleştirir.",
     budgetTooltip:
       "Bu yapılandırmanın teslim edebileceği en fazla uyarı sayısı; teslimde uygulanır. Algılama her aralığı puanlamaya devam eder; bütçe tükenince, yenilenene kadar ek uyarılar bastırılır.",
-    hint: "Aralıklar, modelin eğitim puanlarının p{percentile} işaretine göre değerlendirilir — kaç uyarı çıkacağı verilerinize bağlıdır.",
+    hint: "Bir zaman aralığı, modelin eğitimde gördüklerinin %{percentile}'inden daha sıra dışı göründüğünde uyarı verir. Bunun kaç uyarıya karşılık geldiği verinize bağlıdır.",
   },
   "vi-VN": {
     tooltip:
       "Đặt mức chuẩn điểm từ điểm huấn luyện của mô hình tại phân vị này — 97 đặt mức chuẩn ở 3% điểm huấn luyện cao nhất. Điểm thực tế có thể khác với huấn luyện, nên đây không phải tỷ lệ các bucket tương lai và không phải tần suất cảnh báo. Giá trị thấp hơn hạ mức chuẩn và thường gửi nhiều cảnh báo hơn; thời gian chờ giãn cách các cảnh báo lặp lại.",
     budgetTooltip:
       "Số cảnh báo tối đa mà cấu hình này được phép gửi, được áp đặt khi gửi. Việc phát hiện vẫn chấm điểm mọi bucket; khi ngân sách cạn, các cảnh báo tiếp theo bị chặn cho đến khi ngân sách hồi phục.",
-    hint: "Các bucket được so với mốc p{percentile} của điểm huấn luyện mô hình — số cảnh báo tạo ra tùy thuộc vào dữ liệu của bạn.",
+    hint: "Một khung thời gian sẽ phát cảnh báo khi nó trông bất thường hơn {percentile}% những gì mô hình đã thấy trong quá trình huấn luyện. Số lượng cảnh báo cụ thể phụ thuộc vào dữ liệu của bạn.",
   },
   "zh-CN": {
     tooltip:
       "根据模型训练分数在该百分位处设定分数门槛——97 将门槛设在训练分数最高的 3% 处。实际分数可能与训练不同，因此这既不是未来数据桶的占比，也不是告警频率。取值越低门槛越低，通常发送的告警越多；冷却时间会拉开重复告警的间隔。",
     budgetTooltip:
       "此配置可投递告警的上限，在投递时强制执行。检测仍会为每个数据桶评分；预算用尽后，多余的告警将被抑制，直到预算恢复。",
-    hint: "数据桶按模型训练分数的 p{percentile} 标记来判定——产生多少告警取决于你的数据。",
+    hint: "当某个时间桶看起来比模型在训练中见到的 {percentile}% 更异常时，就会触发告警。具体会产生多少条告警取决于你的数据。",
   },
   "zh-TW": {
     tooltip:
       "依模型訓練分數在此百分位處設定分數門檻——97 將門檻設在訓練分數最高的 3% 處。實際分數可能與訓練不同，因此這既不是未來區間的占比，也不是警示頻率。數值越低門檻越低，通常發送的警示越多；冷卻時間會拉開重複警示的間隔。",
     budgetTooltip:
       "此設定可傳送警示的上限，於傳送時強制執行。偵測仍會為每個區間評分；預算用盡後，多餘的警示會被抑制，直到預算回復。",
-    hint: "區間依模型訓練分數的 p{percentile} 標記判定——產生多少警示取決於你的資料。",
+    hint: "當某個時間桶看起來比模型在訓練中見過的 {percentile}% 更不尋常時，就會觸發警示。這代表多少個警示取決於你的資料。",
   },
 };
 
@@ -449,4 +449,49 @@ describe("anomaly sensitivity locale parity", () => {
       }
     }
   });
+
+  // "Percentile" named two unrelated things: the Sensitivity number and the
+  // Detection Function's p50/p95/p99. The label is what a reader sees first, so
+  // it must not reach for the Detection Function's vocabulary to name itself.
+  it("en-US labels the sensitivity number without detection-function vocabulary", () => {
+    const label = String(at(en, "alerts.anomaly.percentile")).toLowerCase();
+    expect(label).not.toContain("percentile");
+    expect(label).not.toMatch(/\bp\d{2}\b/);
+    // Still a label, not a sentence — it sits in a 21.75rem-wide field column.
+    expect(label.split(/\s+/).length).toBeLessThanOrEqual(3);
+  });
+
+  // The collision is only resolved if the tooltip on the number says, in words,
+  // that it is not the Detection Function's percentile.
+  it("en-US disowns the detection-function meaning beside the sensitivity number", () => {
+    const tooltip = String(at(en, "alerts.anomaly.sensitivityNotDataPercentile")).toLowerCase();
+    expect(tooltip).toContain("detection function");
+    expect(tooltip).toContain("unrelated");
+    // It has to name what it ranks instead, or "unrelated" tells the reader nothing.
+    expect(tooltip).toMatch(/model's confidence|model's scores/);
+  });
+
+  // The other half of the same collision: the Detection Function's p-levels rank
+  // the FIELD's values, and must never read as an alerting/sensitivity control.
+  it("en-US keeps the detection-function tooltip in data space, not alert space", () => {
+    const tooltip = String(at(en, "alerts.anomaly.detectionFunctionTooltip")).toLowerCase();
+    expect(tooltip).toMatch(/\bp50\b/);
+    expect(tooltip).toMatch(/\bp95\b/);
+    // Names the thing being measured, so p95 reads as "of a field", not "of scores".
+    expect(tooltip).toMatch(/field/);
+    for (const claim of ["sensitivity", "how unusual", "training score", "alerts when"]) {
+      expect(tooltip, `detectionFunctionTooltip must not contain "${claim}"`).not.toContain(claim);
+    }
+  });
+
+  // Anomalies are marked in score space by a trained model, so they cannot appear
+  // on the config-time value preview. The caption has to send the reader somewhere
+  // REAL — pinned against the rendered label, not a phrase no screen shows.
+  it("en-US points the preview reader at a destination the app actually renders", () => {
+    const caption = String(at(en, "alerts.anomaly.previewCaption")).toLowerCase();
+    expect(caption).toContain("training");
+    const destination = String(at(en, "alerts.anomaly.detectionCharts")).toLowerCase();
+    expect(destination.length).toBeGreaterThan(0);
+    expect(caption).toContain(destination);
+  });
 });
diff --git a/web/src/locales/languages/ar-SA.json b/web/src/locales/languages/ar-SA.json
index 5a3e2755fa6..67aa7d689ad 100644
--- a/web/src/locales/languages/ar-SA.json
+++ b/web/src/locales/languages/ar-SA.json
@@ -7472,6 +7472,7 @@
       "checkEveryTooltip": "كم مرة يتم تشغيل مهمة الكشف. يمكن أن تكون أكبر من فترة العينة (مثلا عينة كل 5 أمتار، وتشغيل الكشف كل ساعة).",
       "destinationRequired": "هناك وجهة واحدة على الأقل مطلوبة!",
       "detectionCharts": "نتائج الكشف",
+      "detectionFunctionTooltip": "ما يجب قياسه في كل نافذة زمنية. يراقب count عدد السجلات الواردة؛ وتراقب avg وmin وmax وsum حقلًا رقميًا. أما p50 وp95 وp99 فتراقب توزيع ذلك الحقل — فعلى سبيل المثال، يكشف p95 الخاص بـ response_time_ms عن تدهور زمن الاستجابة في أبطأ الطلبات حتى عندما يبقى حجم الحركة ثابتًا.",
       "detectionResolution": "دقة الكشف",
       "detectionResolutionTooltip": "مدى دقة كل نقطة بيانة. مثلا، \"5m\" تعني أن الشذوذات تكتشف بدقة 5 دقائق.",
       "deviationChart": "التجاوز فوق العتبة",
@@ -7495,7 +7496,8 @@
       "noFieldsFound": "لم يتم العثور على حقول",
       "notifications": "الإشعارات",
       "notificationsTooltip": "عند تفعيلها، سيتم إرسال إشعار إلى الوجهة المحددة كلما تم اكتشاف شذوذ.",
-      "percentile": "المئين",
+      "percentile": "المستوى",
+      "previewCaption": "عينة من البيانات التي سيراقبها هذا التنبيه، لتتحقق من صحة الاستعلام قبل الحفظ. تُعلَّم الحالات الشاذة بعد انتهاء تدريب النموذج — راجع نتائج الكشف.",
       "queryMode": "وضع الاستعلام",
       "retrainEvery": "أعد تدريب كل",
       "retrainEveryTooltip": "كم مرة يجب إعادة تدريب النموذج تلقائيا. \"أبدا\" تعني تدريب مرة واحدة والاحتفاظ بالنموذج حتى يتم إعادة تدريبه يدويا.",
@@ -7512,7 +7514,8 @@
       "sensitivityBalanced": "متوازنة",
       "sensitivityBudgetTooltip": "الحد الأقصى لعدد التنبيهات التي يمكن لهذا الإعداد إرسالها، ويُفرض عند الإرسال. يستمر الكشف في تقييم كل فترة؛ وعند استنفاد الميزانية تُمنع التنبيهات الإضافية حتى تتجدد.",
       "sensitivityConservative": "متحفظة",
-      "sensitivityHintPercentile": "تُقيَّم الفترات مقابل علامة المئين {percentile} من درجات تدريب النموذج — وعدد التنبيهات الناتج يعتمد على بياناتك.",
+      "sensitivityHintPercentile": "تُطلق النافذة تنبيهًا عندما تبدو أكثر غرابة من {percentile}% مما رآه النموذج أثناء التدريب. ويعتمد عدد التنبيهات الناتجة على بياناتك.",
+      "sensitivityNotDataPercentile": "مدى غرابة النافذة اللازم لإطلاق التنبيه. كلما ارتفع، قلّت التنبيهات وزادت تطرفًا. يقيّم هذا المستوى ثقة النموذج لا قيم بياناتك، لذا لا علاقة له باختيار p95 كدالة الكشف.",
       "sensitivityRange": "أدخل عددا صحيحا بين 50 و99",
       "sensitivityTooltip": "يحدد حد الدرجات من درجات تدريب النموذج عند هذا المئين — القيمة 97 تضع الحد عند أعلى 3٪ من درجات التدريب. قد تختلف الدرجات الفعلية عن درجات التدريب، لذا فهذه ليست نسبة من الفترات القادمة وليست معدل تنبيهات. القيمة الأقل تخفض الحد وترسل عادة تنبيهات أكثر؛ وتباعد فترة التهدئة التنبيهات المتكررة.",
       "seriesAnomaly": "شذوذ",
@@ -7541,7 +7544,7 @@
       "summaryRetrainEveryDays": "كل {days}d",
       "summarySchedule": "✓ الجدول: كل {schedule}",
       "summaryThreshold": "✓ العتبة: {threshold}",
-      "summaryThresholdPercentile": "شريط الدرجة عند p{percentile}",
+      "summaryThresholdPercentile": "المستوى {percentile} من 99",
       "summaryTraining": "✓ التدريب: {days} ({seasonality})",
       "summaryTrainingDays": "{days} أيام",
       "timestampAliasBanned": "لا يمكن استخدام {column} كاسم مستعار للعمود. استخدم {timeBucket} بدلا من ذلك.",
diff --git a/web/src/locales/languages/de-DE.json b/web/src/locales/languages/de-DE.json
index def47559f52..c5563946b6f 100644
--- a/web/src/locales/languages/de-DE.json
+++ b/web/src/locales/languages/de-DE.json
@@ -7472,6 +7472,7 @@
       "checkEveryTooltip": "Wie oft der Erkennungsjob ausgeführt wird. Kann länger als der Probenzeitraum sein (z. B. alle 5 m messen, Erkennung alle 1 Stunde ausführen).",
       "destinationRequired": "Mindestens ein Ziel ist erforderlich!",
       "detectionCharts": "Erkennungsergebnisse",
+      "detectionFunctionTooltip": "Was in jedem Zeit-Bucket gemessen werden soll. count beobachtet, wie viele Datensätze eintreffen; avg, min, max und sum beobachten ein numerisches Feld. p50, p95 und p99 beobachten die Verteilung dieses Feldes – z. B. erkennt p95 von response_time_ms eine sich verschlechternde Latenz bei den langsamsten Anfragen, selbst wenn der Datenverkehr konstant bleibt.",
       "detectionResolution": "Auflösung der Erkennung",
       "detectionResolutionTooltip": "Wie detailliert jeder Datenpunkt ist. Beispielsweise bedeutet „5 m“, dass Anomalien mit einer Auflösung von 5 Minuten erkannt werden.",
       "deviationChart": "Abweichung über dem Schwellenwert",
@@ -7495,7 +7496,8 @@
       "noFieldsFound": "Keine Felder gefunden",
       "notifications": "Benachrichtigungen",
       "notificationsTooltip": "Wenn aktiviert, wird eine Benachrichtigung an das ausgewählte Ziel gesendet, sobald eine Anomalie erkannt wird.",
-      "percentile": "Perzentil",
+      "percentile": "Stufe",
+      "previewCaption": "Eine Stichprobe der Daten, die diese Warnmeldung überwacht, damit Sie prüfen können, ob die Abfrage korrekt aussieht, bevor Sie speichern. Anomalien werden markiert, sobald das Modell das Training abgeschlossen hat – siehe Erkennungsergebnisse.",
       "queryMode": "Abfragemodus",
       "retrainEvery": "Trainiere jeden neu",
       "retrainEveryTooltip": "Wie oft soll das Modell automatisch neu trainiert werden? „Nie“ bedeutet, das Modell einmal zu trainieren und das Modell so lange zu behalten, bis es manuell neu trainiert wird.",
@@ -7512,7 +7514,8 @@
       "sensitivityBalanced": "Ausgewogen",
       "sensitivityBudgetTooltip": "Die maximale Zahl von Warnmeldungen, die diese Konfiguration zustellen darf — bei der Zustellung erzwungen. Die Erkennung bewertet weiterhin jeden Bucket; ist das Budget aufgebraucht, werden weitere Warnmeldungen unterdrückt, bis es sich auffüllt.",
       "sensitivityConservative": "Konservativ",
-      "sensitivityHintPercentile": "Buckets werden am p{percentile}-Wert der Trainingswerte des Modells gemessen — wie viele Warnmeldungen das ergibt, hängt von Ihren Daten ab.",
+      "sensitivityHintPercentile": "Ein Bucket löst eine Warnmeldung aus, wenn er ungewöhnlicher erscheint als {percentile} % dessen, was das Modell im Training gesehen hat. Wie viele Warnmeldungen das bedeutet, hängt von Ihren Daten ab.",
+      "sensitivityNotDataPercentile": "Wie ungewöhnlich ein Bucket sein muss, bevor eine Warnmeldung ausgelöst wird. Höher bedeutet weniger, dafür extremere Warnmeldungen. Diese Stufe bewertet die Konfidenz des Modells, nicht die Werte Ihrer Daten, und steht daher in keinem Zusammenhang mit der Wahl von p95 als Erkennungsfunktion.",
       "sensitivityRange": "Geben Sie eine ganze Zahl zwischen 50 und 99 ein",
       "sensitivityTooltip": "Legt die Score-Schwelle aus den Trainingswerten des Modells bei diesem Perzentil fest — 97 setzt die Schwelle bei den obersten 3 % der Trainingswerte. Live-Werte können vom Training abweichen; dies ist also kein Anteil künftiger Buckets und keine Alarmrate. Ein niedrigerer Wert senkt die Schwelle und sendet in der Regel mehr Warnmeldungen; eine Abklingzeit begrenzt Wiederholungen.",
       "seriesAnomaly": "Anomalie",
@@ -7541,7 +7544,7 @@
       "summaryRetrainEveryDays": "alle {days} Tage",
       "summarySchedule": "✓ Zeitplan: alle {schedule}",
       "summaryThreshold": "✓ Schwellwert: {threshold}",
-      "summaryThresholdPercentile": "Wertbalken bei p{percentile}",
+      "summaryThresholdPercentile": "Stufe {percentile} von 99",
       "summaryTraining": "✓ Training: {days} ({seasonality})",
       "summaryTrainingDays": "{days} Tage",
       "timestampAliasBanned": "{column} kann nicht als Spaltenalias verwendet werden. Verwenden Sie stattdessen {timeBucket}.",
diff --git a/web/src/locales/languages/en-US.json b/web/src/locales/languages/en-US.json
index cb41d24bb09..e1118948d73 100644
--- a/web/src/locales/languages/en-US.json
+++ b/web/src/locales/languages/en-US.json
@@ -7472,6 +7472,7 @@
       "checkEveryTooltip": "How often the detection job runs. Can be larger than the sample period (e.g. sample every 5m, run detection every 1h).",
       "destinationRequired": "At least one destination is required!",
       "detectionCharts": "Detection results",
+      "detectionFunctionTooltip": "What to measure in each time bucket. count watches how many records arrive; avg, min, max and sum watch a numeric field. p50, p95 and p99 watch that field's distribution — e.g. p95 of response_time_ms catches latency getting worse for the slowest requests even when traffic stays flat.",
       "detectionResolution": "Detection Resolution",
       "detectionResolutionTooltip": "How granular each data point is. E.g. \"5m\" means anomalies are detected at 5-minute resolution.",
       "deviationChart": "Deviation by kind",
@@ -7495,7 +7496,8 @@
       "noFieldsFound": "No fields found",
       "notifications": "Notifications",
       "notificationsTooltip": "When enabled, a notification will be sent to the selected destination whenever an anomaly is detected.",
-      "percentile": "Percentile",
+      "percentile": "Level",
+      "previewCaption": "A sample of the data this alert will watch, so you can check the query looks right before saving. Anomalies are marked once the model finishes training — see Detection results.",
       "queryMode": "Query mode",
       "retrainEvery": "Retrain Every",
       "retrainEveryTooltip": "How often to automatically retrain the model. \"Never\" means train once and keep the model until manually retrained.",
@@ -7512,7 +7514,8 @@
       "sensitivityBalanced": "Balanced",
       "sensitivityBudgetTooltip": "The maximum number of alerts this configuration may deliver, enforced at delivery. Detection still scores every bucket; once the budget is spent, further alerts are suppressed until it refills.",
       "sensitivityConservative": "Conservative",
-      "sensitivityHintPercentile": "Buckets are judged against the p{percentile} mark of the model's training scores — how many alerts that yields depends on your data.",
+      "sensitivityHintPercentile": "A bucket alerts when it looks more unusual than {percentile}% of what the model saw in training. How many alerts that means depends on your data.",
+      "sensitivityNotDataPercentile": "How unusual a bucket must be before it alerts. Higher means fewer, more extreme alerts. This level scores the model's confidence, not your data's values, so it is unrelated to choosing p95 as the Detection Function.",
       "sensitivityRange": "Enter a whole number between 50 and 99",
       "sensitivityTooltip": "Sets the score bar from the model's training scores at this percentile — 97 puts the bar at the top 3% of training scores. Live scores can differ from training, so this is not a share of future buckets and not an alert rate. A lower value lowers the bar and generally sends more alerts; a cooldown spaces out repeats.",
       "seriesAnomaly": "Anomaly",
@@ -7541,7 +7544,7 @@
       "summaryRetrainEveryDays": "every {days}d",
       "summarySchedule": "✓ Schedule: every {schedule}",
       "summaryThreshold": "✓ Threshold: {threshold}",
-      "summaryThresholdPercentile": "score bar at p{percentile}",
+      "summaryThresholdPercentile": "level {percentile} of 99",
       "summaryTraining": "✓ Training: {days} ({seasonality})",
       "summaryTrainingDays": "{days} days",
       "timestampAliasBanned": "{column} cannot be used as a column alias. Use {timeBucket} instead.",
diff --git a/web/src/locales/languages/es-ES.json b/web/src/locales/languages/es-ES.json
index b63962e1082..bdd1e929138 100644
--- a/web/src/locales/languages/es-ES.json
+++ b/web/src/locales/languages/es-ES.json
@@ -7472,6 +7472,7 @@
       "checkEveryTooltip": "La frecuencia con la que se ejecuta el trabajo de detección. Puede ser mayor que el período de muestreo (por ejemplo, tomar una muestra cada 5 minutos, realizar la detección cada 1 hora).",
       "destinationRequired": "¡Se requiere al menos un destino!",
       "detectionCharts": "Resultados de detección",
+      "detectionFunctionTooltip": "Qué medir en cada intervalo de tiempo. count vigila cuántos registros llegan; avg, min, max y sum vigilan un campo numérico. p50, p95 y p99 vigilan la distribución de ese campo; p. ej., p95 de response_time_ms detecta que la latencia empeora para las solicitudes más lentas incluso cuando el tráfico se mantiene estable.",
       "detectionResolution": "Resolución de detección",
       "detectionResolutionTooltip": "Qué tan granular es cada punto de datos. Por ejemplo, «5 m» significa que las anomalías se detectan con una resolución de 5 minutos.",
       "deviationChart": "Desviación por encima del umbral",
@@ -7495,7 +7496,8 @@
       "noFieldsFound": "No se encontró ningún campo",
       "notifications": "Notificaciones",
       "notificationsTooltip": "Cuando esté habilitado, se enviará una notificación al destino seleccionado cuando se detecte una anomalía.",
-      "percentile": "Percentil",
+      "percentile": "Nivel",
+      "previewCaption": "Una muestra de los datos que vigilará esta alerta, para que puedas comprobar que la consulta es correcta antes de guardar. Las anomalías se marcan una vez que el modelo termina de entrenarse — consulta Resultados de detección.",
       "queryMode": "Modo de consulta",
       "retrainEvery": "Vuelva a entrenar cada",
       "retrainEveryTooltip": "Con qué frecuencia se debe volver a entrenar automáticamente el modelo. «Nunca» significa entrenar una vez y conservar el modelo hasta que se vuelva a entrenar manualmente.",
@@ -7512,7 +7514,8 @@
       "sensitivityBalanced": "Equilibrado",
       "sensitivityBudgetTooltip": "El número máximo de alertas que esta configuración puede entregar, aplicado en la entrega. La detección sigue puntuando cada bucket; agotado el presupuesto, las alertas adicionales se suprimen hasta que se repone.",
       "sensitivityConservative": "Conservador",
-      "sensitivityHintPercentile": "Los buckets se comparan con la marca p{percentile} de las puntuaciones de entrenamiento del modelo; cuántas alertas produce depende de tus datos.",
+      "sensitivityHintPercentile": "Un intervalo genera una alerta cuando parece más inusual que el {percentile}% de lo que el modelo vio durante el entrenamiento. Cuántas alertas supone eso depende de tus datos.",
+      "sensitivityNotDataPercentile": "Cuán inusual debe ser un intervalo para generar una alerta. Un valor más alto significa menos alertas y más extremas. Este nivel evalúa la confianza del modelo, no los valores de tus datos, por lo que no tiene relación con elegir p95 como la Función de detección.",
       "sensitivityRange": "Introduce un número entero entre 50 y 99",
       "sensitivityTooltip": "Fija el listón de puntuación a partir de las puntuaciones de entrenamiento del modelo en este percentil: 97 sitúa el listón en el 3 % superior de las puntuaciones de entrenamiento. Las puntuaciones reales pueden diferir del entrenamiento, así que esto no es una proporción de buckets futuros ni una tasa de alertas. Un valor más bajo baja el listón y suele enviar más alertas; un periodo de enfriamiento espacia las repeticiones.",
       "seriesAnomaly": "Anomalía",
@@ -7541,7 +7544,7 @@
       "summaryRetrainEveryDays": "cada {days}d",
       "summarySchedule": "✓ Horario: cada {schedule}",
       "summaryThreshold": "✓ Umbral: {threshold}",
-      "summaryThresholdPercentile": "barra de puntuación en p{percentile}",
+      "summaryThresholdPercentile": "nivel {percentile} de 99",
       "summaryTraining": "✓ Entrenamiento: {days} ({seasonality})",
       "summaryTrainingDays": "{days} días",
       "timestampAliasBanned": "{column} no se puede usar como alias de columna. Use {timeBucket} en su lugar.",
diff --git a/web/src/locales/languages/fr-FR.json b/web/src/locales/languages/fr-FR.json
index 256eb51fee1..b27386384c5 100644
--- a/web/src/locales/languages/fr-FR.json
+++ b/web/src/locales/languages/fr-FR.json
@@ -7472,6 +7472,7 @@
       "checkEveryTooltip": "Fréquence d'exécution de la tâche de détection. Peut être plus longue que la période d'échantillonnage (par exemple, échantillonnage tous les 5 m, exécution de la détection toutes les 1 h).",
       "destinationRequired": "Au moins une destination est requise !",
       "detectionCharts": "Résultats de détection",
+      "detectionFunctionTooltip": "Ce qu'il faut mesurer dans chaque intervalle de temps. count observe le nombre d'enregistrements qui arrivent ; avg, min, max et sum observent un champ numérique. p50, p95 et p99 observent la distribution de ce champ — par ex. le p95 de response_time_ms détecte une dégradation de la latence pour les requêtes les plus lentes même lorsque le trafic reste stable.",
       "detectionResolution": "Résolution de détection",
       "detectionResolutionTooltip": "Le degré de précision de chaque point de données. Par exemple, « 5 m » signifie que des anomalies sont détectées à une résolution de 5 minutes.",
       "deviationChart": "Écart au-dessus du seuil",
@@ -7495,7 +7496,8 @@
       "noFieldsFound": "Aucun champ n'a été trouvé",
       "notifications": "Notifications",
       "notificationsTooltip": "Lorsqu'elle est activée, une notification sera envoyée à la destination sélectionnée chaque fois qu'une anomalie est détectée.",
-      "percentile": "Percentile",
+      "percentile": "Niveau",
+      "previewCaption": "Un échantillon des données que cette alerte surveillera, afin de vérifier que la requête est correcte avant d'enregistrer. Les anomalies sont signalées une fois l'entraînement du modèle terminé — voir Résultats de détection.",
       "queryMode": "Mode de requête",
       "retrainEvery": "Reformer chaque",
       "retrainEveryTooltip": "À quelle fréquence réentraîner automatiquement le modèle. « Jamais » signifie s'entraîner une seule fois et conserver le modèle jusqu'à ce qu'il soit réentraîné manuellement.",
@@ -7512,7 +7514,8 @@
       "sensitivityBalanced": "Équilibré",
       "sensitivityBudgetTooltip": "Le nombre maximal d'alertes que cette configuration peut délivrer, appliqué à la livraison. La détection continue de noter chaque compartiment ; une fois le budget épuisé, les alertes supplémentaires sont supprimées jusqu'à ce qu'il se reconstitue.",
       "sensitivityConservative": "Conservateur",
-      "sensitivityHintPercentile": "Les compartiments sont évalués par rapport au repère p{percentile} des scores d'entraînement du modèle — le nombre d'alertes qui en résulte dépend de vos données.",
+      "sensitivityHintPercentile": "Un intervalle déclenche une alerte lorsqu'il paraît plus inhabituel que {percentile}% de ce que le modèle a vu à l'entraînement. Le nombre d'alertes que cela représente dépend de vos données.",
+      "sensitivityNotDataPercentile": "À quel point un intervalle doit être inhabituel avant de déclencher une alerte. Une valeur plus élevée signifie moins d'alertes, mais plus extrêmes. Ce niveau évalue la confiance du modèle, et non les valeurs de vos données ; il est donc sans rapport avec le choix de p95 comme fonction de détection.",
       "sensitivityRange": "Saisissez un nombre entier entre 50 et 99",
       "sensitivityTooltip": "Définit la barre de score à partir des scores d'entraînement du modèle à ce percentile — 97 place la barre au niveau des 3 % les plus élevés des scores d'entraînement. Les scores réels peuvent différer de l'entraînement : ce n'est donc ni une part des compartiments futurs ni un taux d'alertes. Une valeur plus basse abaisse la barre et envoie en général davantage d'alertes ; un délai de refroidissement espace les répétitions.",
       "seriesAnomaly": "Anomalie",
@@ -7541,7 +7544,7 @@
       "summaryRetrainEveryDays": "tous les {days} j",
       "summarySchedule": "✓ Planification : toutes les {schedule}",
       "summaryThreshold": "✓ Seuil : {threshold}",
-      "summaryThresholdPercentile": "barre de score à p{percentile}",
+      "summaryThresholdPercentile": "niveau {percentile} sur 99",
       "summaryTraining": "✓ Entraînement : {days} ({seasonality})",
       "summaryTrainingDays": "{days} jours",
       "timestampAliasBanned": "{column} ne peut pas être utilisé comme alias de colonne. Utilisez {timeBucket} à la place.",
diff --git a/web/src/locales/languages/it-IT.json b/web/src/locales/languages/it-IT.json
index 72b5eb8188f..6f529a21ddd 100644
--- a/web/src/locales/languages/it-IT.json
+++ b/web/src/locales/languages/it-IT.json
@@ -7472,6 +7472,7 @@
       "checkEveryTooltip": "Con quale frequenza viene eseguito il processo di rilevamento. Può essere superiore al periodo di campionamento (ad esempio campionamento ogni 5 m, rilevamento eseguito ogni 1 ora).",
       "destinationRequired": "È richiesta almeno una destinazione!",
       "detectionCharts": "Risultati del rilevamento",
+      "detectionFunctionTooltip": "Cosa misurare in ogni intervallo temporale. count osserva quanti record arrivano; avg, min, max e sum osservano un campo numerico. p50, p95 e p99 osservano la distribuzione di quel campo — ad esempio p95 di response_time_ms rileva un peggioramento della latenza per le richieste più lente anche quando il traffico rimane stabile.",
       "detectionResolution": "Risoluzione di rilevamento",
       "detectionResolutionTooltip": "Quanto è granulare ogni punto dati. Ad esempio, «5m» significa che le anomalie vengono rilevate con una risoluzione di 5 minuti.",
       "deviationChart": "Scostamento oltre la soglia",
@@ -7495,7 +7496,8 @@
       "noFieldsFound": "Nessun campo trovato",
       "notifications": "Notifiche",
       "notificationsTooltip": "Se abilitato, una notifica verrà inviata alla destinazione selezionata ogni volta che viene rilevata un'anomalia.",
-      "percentile": "Percentile",
+      "percentile": "Livello",
+      "previewCaption": "Un campione dei dati che questo avviso monitorerà, così puoi verificare che la query sia corretta prima di salvare. Le anomalie vengono contrassegnate quando il modello termina l'addestramento — vedi Risultati del rilevamento.",
       "queryMode": "Modalità query",
       "retrainEvery": "Riqualificare ogni",
       "retrainEveryTooltip": "Con quale frequenza riqualificare automaticamente il modello. «Mai» significa addestrare il modello una sola volta e conservare il modello fino a quando non viene riaddestrato manualmente.",
@@ -7512,7 +7514,8 @@
       "sensitivityBalanced": "Bilanciato",
       "sensitivityBudgetTooltip": "Il numero massimo di avvisi che questa configurazione può recapitare, applicato al recapito. Il rilevamento continua a valutare ogni bucket; esaurito il budget, gli avvisi ulteriori vengono soppressi finché non si ricarica.",
       "sensitivityConservative": "Conservativo",
-      "sensitivityHintPercentile": "I bucket sono valutati rispetto al riferimento p{percentile} dei punteggi di addestramento del modello: quanti avvisi ne derivino dipende dai tuoi dati.",
+      "sensitivityHintPercentile": "Un intervallo genera un avviso quando appare più insolito del {percentile}% di ciò che il modello ha visto durante l'addestramento. Quanti avvisi comporti dipende dai tuoi dati.",
+      "sensitivityNotDataPercentile": "Quanto deve essere insolito un intervallo prima di generare un avviso. Un valore più alto significa meno avvisi, ma più estremi. Questo livello valuta la confidenza del modello, non i valori dei tuoi dati, quindi non ha alcuna relazione con la scelta di p95 come Funzione di rilevamento.",
       "sensitivityRange": "Inserisci un numero intero compreso tra 50 e 99",
       "sensitivityTooltip": "Imposta l'asticella del punteggio dai punteggi di addestramento del modello a questo percentile: 97 colloca l'asticella nel 3 % più alto dei punteggi di addestramento. I punteggi reali possono differire dall'addestramento, quindi questa non è una quota dei bucket futuri né un tasso di avvisi. Un valore più basso abbassa l'asticella e in genere invia più avvisi; un periodo di attesa distanzia le ripetizioni.",
       "seriesAnomaly": "Anomalia",
@@ -7541,7 +7544,7 @@
       "summaryRetrainEveryDays": "ogni {days} giorni",
       "summarySchedule": "✓ Pianificazione: ogni {schedule}",
       "summaryThreshold": "✓ Soglia: {threshold}",
-      "summaryThresholdPercentile": "barra del punteggio a p{percentile}",
+      "summaryThresholdPercentile": "livello {percentile} di 99",
       "summaryTraining": "✓ Addestramento: {days} ({seasonality})",
       "summaryTrainingDays": "{days} giorni",
       "timestampAliasBanned": "{column} non può essere utilizzato come alias di colonna. Usa {timeBucket} invece.",
diff --git a/web/src/locales/languages/ja-JP.json b/web/src/locales/languages/ja-JP.json
index c335c3ba7ef..3371772c7d6 100644
--- a/web/src/locales/languages/ja-JP.json
+++ b/web/src/locales/languages/ja-JP.json
@@ -7472,6 +7472,7 @@
       "checkEveryTooltip": "検出ジョブの実行頻度。サンプル期間より長くてもかまいません (例:5 m ごとにサンプリングし、1 時間ごとに検出を実行する)。",
       "destinationRequired": "宛先が少なくとも1つ必要です！",
       "detectionCharts": "検出結果",
+      "detectionFunctionTooltip": "各時間バケットで何を測定するかを指定します。count は到着するレコード数を監視し、avg、min、max、sum は数値フィールドを監視します。p50、p95、p99 はそのフィールドの分布を監視します — 例えば response_time_ms の p95 は、トラフィックが横ばいでも最も遅いリクエストのレイテンシが悪化していることを捉えます。",
       "detectionResolution": "検出解像度",
       "detectionResolutionTooltip": "各データポイントの細かさ。たとえば、「5m」は、5 分の解像度で異常が検出されたことを意味します。",
       "deviationChart": "しきい値からの超過率",
@@ -7495,7 +7496,8 @@
       "noFieldsFound": "フィールドが見つかりません",
       "notifications": "通知",
       "notificationsTooltip": "有効にすると、異常が検出されるたびに、選択した宛先に通知が送信されます。",
-      "percentile": "パーセンタイル",
+      "percentile": "レベル",
+      "previewCaption": "このアラートが監視するデータのサンプルです。保存する前にクエリが正しいかを確認できます。異常はモデルのトレーニングが完了するとマークされます — 「検出結果」を参照してください。",
       "queryMode": "クエリモード",
       "retrainEvery": "毎回再トレーニング",
       "retrainEveryTooltip": "モデルを自動的に再トレーニングする頻度。「しない」とは、一度トレーニングを行って、手動で再トレーニングするまでモデルを維持することを意味します。",
@@ -7512,7 +7514,8 @@
       "sensitivityBalanced": "バランス",
       "sensitivityBudgetTooltip": "この設定が配信できるアラートの上限で、配信時に強制されます。検出はすべてのバケットをスコアリングし続けます。予算を使い切ると、回復するまで追加のアラートは抑制されます。",
       "sensitivityConservative": "保守的",
-      "sensitivityHintPercentile": "バケットはモデルのトレーニングスコアの p{percentile} 基準で判定されます。アラート件数はデータ次第です。",
+      "sensitivityHintPercentile": "バケットは、モデルがトレーニングで見たものの {percentile}% よりも異常に見える場合にアラートを発します。それが何件のアラートになるかはデータ次第です。",
+      "sensitivityNotDataPercentile": "バケットがアラートを発するために必要な異常度です。高いほどアラートは少なく、より極端になります。このレベルはデータの値ではなくモデルの信頼度をスコア化するものなので、検出関数として p95 を選ぶこととは無関係です。",
       "sensitivityRange": "50 から 99 までの整数を入力してください",
       "sensitivityTooltip": "モデルのトレーニングスコアのこのパーセンタイルからスコアの基準線を設定します。97 はトレーニングスコアの上位 3% に基準線を置きます。実際のスコアはトレーニングと異なることがあるため、これは将来のバケットの割合でもアラート率でもありません。値を下げると基準線が下がり、通常はアラートが増えます。クールダウンにより連続するアラートは間隔が空きます。",
       "seriesAnomaly": "異常",
@@ -7541,7 +7544,7 @@
       "summaryRetrainEveryDays": "{days}日ごと",
       "summarySchedule": "✓ スケジュール: {schedule}ごと",
       "summaryThreshold": "✓ しきい値: {threshold}",
-      "summaryThresholdPercentile": "スコアバーは p{percentile}",
+      "summaryThresholdPercentile": "レベル {percentile} / 99",
       "summaryTraining": "✓ 学習: {days}日 ({seasonality})",
       "summaryTrainingDays": "{days}日",
       "timestampAliasBanned": "{column}は列エイリアスとして使用できません。代わりに{timeBucket}を使用してください。",
diff --git a/web/src/locales/languages/ko-KR.json b/web/src/locales/languages/ko-KR.json
index 45db84a3934..e8840160fcf 100644
--- a/web/src/locales/languages/ko-KR.json
+++ b/web/src/locales/languages/ko-KR.json
@@ -7472,6 +7472,7 @@
       "checkEveryTooltip": "탐지 작업의 실행 빈도샘플 기간보다 길 수 있습니다 (예: 5m마다 샘플링, 1시간마다 탐지 실행).",
       "destinationRequired": "대상이 하나 이상 필요합니다!",
       "detectionCharts": "탐지 결과",
+      "detectionFunctionTooltip": "각 시간 버킷에서 측정할 항목입니다. count는 도착하는 레코드 수를 관찰하고, avg, min, max, sum은 숫자 필드를 관찰합니다. p50, p95, p99는 해당 필드의 분포를 관찰합니다. 예를 들어 response_time_ms의 p95는 트래픽이 일정하게 유지되더라도 가장 느린 요청의 지연이 악화되는 것을 잡아냅니다.",
       "detectionResolution": "감지 해상도",
       "detectionResolutionTooltip": "각 데이터 포인트가 얼마나 세분화되었는지.예를 들어 “5m”은 5분 해상도에서 이상 징후가 감지되었음을 의미합니다.",
       "deviationChart": "임계값 초과 편차",
@@ -7495,7 +7496,8 @@
       "noFieldsFound": "필드를 찾을 수 없음",
       "notifications": "알림",
       "notificationsTooltip": "활성화하면 이상이 감지될 때마다 선택한 대상으로 알림이 전송됩니다.",
-      "percentile": "백분위수",
+      "percentile": "레벨",
+      "previewCaption": "이 알림이 모니터링할 데이터의 샘플로, 저장하기 전에 쿼리가 올바른지 확인할 수 있습니다. 모델의 학습이 완료되면 이상 항목이 표시됩니다. 자세한 내용은 탐지 결과를 참조하세요.",
       "queryMode": "쿼리 모드",
       "retrainEvery": "모든 재교육",
       "retrainEveryTooltip": "모델을 자동으로 재학습시키는 빈도.“절대 안 함”이란 한 번 학습하고 수동으로 재훈련할 때까지 모델을 유지하는 것을 의미합니다.",
@@ -7512,7 +7514,8 @@
       "sensitivityBalanced": "균형",
       "sensitivityBudgetTooltip": "이 구성이 전달할 수 있는 알림의 최대 개수로, 전달 시점에 강제됩니다. 감지는 모든 버킷을 계속 채점하며, 예산이 소진되면 회복될 때까지 추가 알림이 억제됩니다.",
       "sensitivityConservative": "보수적",
-      "sensitivityHintPercentile": "버킷은 모델 훈련 점수의 p{percentile} 기준에 따라 판정됩니다. 알림 수는 데이터에 따라 달라집니다.",
+      "sensitivityHintPercentile": "버킷이 학습 중 모델이 확인한 데이터의 {percentile}%보다 더 이례적으로 보이면 알림이 발생합니다. 알림이 몇 건 발생하는지는 데이터에 따라 다릅니다.",
+      "sensitivityNotDataPercentile": "버킷이 알림을 발생시키려면 얼마나 이례적이어야 하는지를 나타냅니다. 값이 높을수록 알림은 더 적고 더 극단적입니다. 이 레벨은 데이터 값이 아니라 모델의 신뢰도를 평가하므로, 탐지 함수로 p95를 선택하는 것과는 무관합니다.",
       "sensitivityRange": "50에서 99 사이의 정수를 입력하세요",
       "sensitivityTooltip": "모델 훈련 점수의 이 백분위수에서 점수 기준선을 설정합니다. 97은 훈련 점수 상위 3%에 기준선을 둡니다. 실제 점수는 훈련과 다를 수 있으므로 이는 미래 버킷의 비율도, 알림 비율도 아닙니다. 값을 낮추면 기준선이 낮아져 보통 더 많은 알림이 전송되며, 쿨다운이 반복 알림의 간격을 벌립니다.",
       "seriesAnomaly": "이상",
@@ -7541,7 +7544,7 @@
       "summaryRetrainEveryDays": "매 {days}일",
       "summarySchedule": "✓ 일정: 매 {schedule}",
       "summaryThreshold": "✓ 임계값: {threshold}",
-      "summaryThresholdPercentile": "점수 기준선 p{percentile}",
+      "summaryThresholdPercentile": "레벨 {percentile} / 99",
       "summaryTraining": "✓ 학습: {days} ({seasonality})",
       "summaryTrainingDays": "{days}일",
       "timestampAliasBanned": "{column}은(는) 열 별칭으로 사용할 수 없습니다. 대신 {timeBucket}을(를) 사용하세요.",
diff --git a/web/src/locales/languages/nl-NL.json b/web/src/locales/languages/nl-NL.json
index 357aeb99d11..6d330468eff 100644
--- a/web/src/locales/languages/nl-NL.json
+++ b/web/src/locales/languages/nl-NL.json
@@ -7472,6 +7472,7 @@
       "checkEveryTooltip": "Hoe vaak de detectieopdracht wordt uitgevoerd. Kan groter zijn dan de steekproefperiode (bijv. monster elke 5 meter, detectie elke 1 uur uitvoeren).",
       "destinationRequired": "Minstens één bestemming is vereist!",
       "detectionCharts": "Detectieresultaten",
+      "detectionFunctionTooltip": "Wat er per tijdsbucket gemeten moet worden. count kijkt naar hoeveel records er binnenkomen; avg, min, max en sum kijken naar een numeriek veld. p50, p95 en p99 kijken naar de verdeling van dat veld — bijvoorbeeld p95 van response_time_ms signaleert een verslechterende latency voor de traagste requests, zelfs wanneer het verkeer gelijk blijft.",
       "detectionResolution": "Resolutie van de detectie",
       "detectionResolutionTooltip": "Hoe gedetailleerd elk gegevenspunt is. Bijvoorbeeld „\" 5m \"” betekent dat afwijkingen worden gedetecteerd met een resolutie van 5 minuten.”",
       "deviationChart": "Afwijking boven de drempel",
@@ -7495,7 +7496,8 @@
       "noFieldsFound": "Geen velden gevonden",
       "notifications": "Meldingen",
       "notificationsTooltip": "Wanneer ingeschakeld, wordt er een melding naar de geselecteerde bestemming gestuurd telkens wanneer een anomalie wordt gedetecteerd.",
-      "percentile": "Percentiel",
+      "percentile": "Niveau",
+      "previewCaption": "Een voorbeeld van de gegevens die dit alarm in de gaten houdt, zodat u kunt controleren of de query klopt voordat u opslaat. Anomalieën worden gemarkeerd zodra het model klaar is met trainen — zie Detectieresultaten.",
       "queryMode": "Querymodus",
       "retrainEvery": "Elke opnieuw trainen",
       "retrainEveryTooltip": "Hoe vaak moet het model automatisch worden bijgeschoold. „Nooit” betekent één keer trainen en het model bewaren totdat het handmatig wordt bijgeschoold.",
@@ -7512,7 +7514,8 @@
       "sensitivityBalanced": "Gebalanceerd",
       "sensitivityBudgetTooltip": "Het maximale aantal meldingen dat deze configuratie mag bezorgen, afgedwongen bij bezorging. Detectie blijft elke bucket scoren; is het budget op, dan worden verdere meldingen onderdrukt tot het zich aanvult.",
       "sensitivityConservative": "Conservatief",
-      "sensitivityHintPercentile": "Buckets worden beoordeeld tegen het p{percentile}-punt van de trainingsscores van het model — hoeveel meldingen dat oplevert, hangt af van je gegevens.",
+      "sensitivityHintPercentile": "Een bucket geeft een alarm wanneer deze ongewoner lijkt dan {percentile}% van wat het model tijdens de training zag. Hoeveel alarmen dat oplevert, hangt af van uw gegevens.",
+      "sensitivityNotDataPercentile": "Hoe ongewoon een bucket moet zijn voordat er een alarm wordt gegeven. Hoger betekent minder, maar extremere alarmen. Dit niveau beoordeelt het vertrouwen van het model, niet de waarden van uw gegevens, dus het staat los van de keuze voor p95 als Detectiefunctie.",
       "sensitivityRange": "Voer een geheel getal tussen 50 en 99 in",
       "sensitivityTooltip": "Stelt de scorelat in op basis van de trainingsscores van het model bij dit percentiel — 97 legt de lat bij de hoogste 3% van de trainingsscores. Live scores kunnen afwijken van de training, dus dit is geen aandeel van toekomstige buckets en geen meldingsfrequentie. Een lagere waarde verlaagt de lat en verstuurt doorgaans meer meldingen; een afkoelperiode spreidt herhalingen.",
       "seriesAnomaly": "Anomalie",
@@ -7541,7 +7544,7 @@
       "summaryRetrainEveryDays": "elke {days}d",
       "summarySchedule": "✓ Schema: elke {schedule}",
       "summaryThreshold": "✓ Drempel: {threshold}",
-      "summaryThresholdPercentile": "scorebalk op p{percentile}",
+      "summaryThresholdPercentile": "niveau {percentile} van 99",
       "summaryTraining": "✓ Training: {days} ({seasonality})",
       "summaryTrainingDays": "{days} dagen",
       "timestampAliasBanned": "{column} kan niet worden gebruikt als kolomalias. Gebruik in plaats daarvan {timeBucket}.",
diff --git a/web/src/locales/languages/pl-PL.json b/web/src/locales/languages/pl-PL.json
index d306410cdf6..80de8ce76d5 100644
--- a/web/src/locales/languages/pl-PL.json
+++ b/web/src/locales/languages/pl-PL.json
@@ -7472,6 +7472,7 @@
       "checkEveryTooltip": "Jak często uruchamiane jest zadanie wykrywania. Może być większe niż okres próbkowania (np. próbkowanie co 5 min, wykrywanie co 1 godz.).",
       "destinationRequired": "Co najmniej jeden cel jest wymagany!",
       "detectionCharts": "Wyniki wykrywania",
+      "detectionFunctionTooltip": "Co mierzyć w każdym przedziale czasowym. count śledzi, ile rekordów napływa; avg, min, max i sum śledzą pole liczbowe. p50, p95 i p99 śledzą rozkład tego pola — np. p95 dla response_time_ms wychwytuje rosnące opóźnienia najwolniejszych żądań, nawet gdy ruch pozostaje stały.",
       "detectionResolution": "Rozdzielczość wykrywania",
       "detectionResolutionTooltip": "Jak szczegółowy jest każdy punkt danych. Np. \"5m\" oznacza, że anomalie są wykrywane z rozdzielczością 5 minut.",
       "deviationChart": "Odchylenie powyżej progu",
@@ -7495,7 +7496,8 @@
       "noFieldsFound": "Nie znaleziono pól",
       "notifications": "Powiadomienia",
       "notificationsTooltip": "Po włączeniu powiadomienie zostanie wysłane do wybranego celu za każdym razem, gdy wykryta zostanie anomalia.",
-      "percentile": "Percentyl",
+      "percentile": "Poziom",
+      "previewCaption": "Próbka danych, które ten alert będzie obserwować, abyś przed zapisaniem mógł sprawdzić, czy zapytanie wygląda poprawnie. Anomalie są oznaczane po zakończeniu uczenia modelu — patrz Wyniki wykrywania.",
       "queryMode": "Tryb zapytania",
       "retrainEvery": "Ponowne uczenie co",
       "retrainEveryTooltip": "Jak często automatycznie przekwalifikowywać model. „Nigdy” oznacza trenowanie raz i zachowanie modelu do ręcznego przekwalifikowania.",
@@ -7512,7 +7514,8 @@
       "sensitivityBalanced": "Zrównoważona",
       "sensitivityBudgetTooltip": "Maksymalna liczba alertów, jaką ta konfiguracja może dostarczyć, egzekwowana przy dostarczaniu. Wykrywanie nadal ocenia każdy przedział; po wyczerpaniu budżetu kolejne alerty są wstrzymywane, aż budżet się odnowi.",
       "sensitivityConservative": "Konserwatywna",
-      "sensitivityHintPercentile": "Przedziały są oceniane względem punktu p{percentile} wyników treningowych modelu — liczba alertów zależy od Twoich danych.",
+      "sensitivityHintPercentile": "Przedział generuje alert, gdy wygląda na bardziej nietypowy niż {percentile}% tego, co model widział podczas uczenia. To, ile to oznacza alertów, zależy od Twoich danych.",
+      "sensitivityNotDataPercentile": "Jak nietypowy musi być przedział, zanim wygeneruje alert. Wyższa wartość oznacza mniej, ale bardziej skrajnych alertów. Ten poziom ocenia pewność modelu, a nie wartości Twoich danych, więc nie ma związku z wyborem p95 jako funkcji wykrywania.",
       "sensitivityRange": "Podaj liczbę całkowitą z zakresu od 50 do 99",
       "sensitivityTooltip": "Ustawia próg punktowy na podstawie wyników treningowych modelu przy tym percentylu — 97 umieszcza próg przy górnych 3% wyników treningowych. Wyniki na żywo mogą różnić się od treningowych, więc nie jest to udział przyszłych przedziałów ani częstość alertów. Niższa wartość obniża próg i zwykle wysyła więcej alertów; okres wyciszenia rozdziela powtórzenia.",
       "seriesAnomaly": "Anomalia",
@@ -7541,7 +7544,7 @@
       "summaryRetrainEveryDays": "co {days} dni",
       "summarySchedule": "✓ Harmonogram: co {schedule}",
       "summaryThreshold": "✓ Próg: {threshold}",
-      "summaryThresholdPercentile": "próg wyniku na poziomie p{percentile}",
+      "summaryThresholdPercentile": "poziom {percentile} z 99",
       "summaryTraining": "✓ Trenowanie: {days} ({seasonality})",
       "summaryTrainingDays": "{days} dni",
       "timestampAliasBanned": "{column} nie może być użyty jako alias kolumny. Użyj {timeBucket} zamiast tego.",
diff --git a/web/src/locales/languages/pt-PT.json b/web/src/locales/languages/pt-PT.json
index 08c9a61251d..c1f83bf4c8d 100644
--- a/web/src/locales/languages/pt-PT.json
+++ b/web/src/locales/languages/pt-PT.json
@@ -7472,6 +7472,7 @@
       "checkEveryTooltip": "Com que frequência o trabalho de detecção é executado. Pode ser maior do que o período de amostragem (por exemplo, amostra a cada 5m, detecção de execução a cada 1h).",
       "destinationRequired": "Pelo menos um destino é obrigatório!",
       "detectionCharts": "Resultados de deteção",
+      "detectionFunctionTooltip": "O que medir em cada bucket de tempo. count observa quantos registros chegam; avg, min, max e sum observam um campo numérico. p50, p95 e p99 observam a distribuição desse campo — por exemplo, o p95 de response_time_ms detecta a latência piorando para as requisições mais lentas mesmo quando o tráfego permanece estável.",
       "detectionResolution": "Resolução de detecção",
       "detectionResolutionTooltip": "Quão granular é cada ponto de dados. Por exemplo, “5m” significa que anomalias são detectadas com resolução de 5 minutos.",
       "deviationChart": "Desvio acima do limiar",
@@ -7495,7 +7496,8 @@
       "noFieldsFound": "Nenhum campo encontrado",
       "notifications": "Notificações",
       "notificationsTooltip": "Quando ativado, uma notificação será enviada ao destino selecionado sempre que uma anomalia for detectada.",
-      "percentile": "Percentil",
+      "percentile": "Nível",
+      "previewCaption": "Uma amostra dos dados que este alerta vai monitorar, para você verificar se a consulta está correta antes de salvar. As anomalias são marcadas quando o modelo termina o treinamento — consulte Resultados de detecção.",
       "queryMode": "Modo de consulta",
       "retrainEvery": "Treine novamente cada",
       "retrainEveryTooltip": "Com que frequência treinar automaticamente o modelo. “Nunca” significa treinar uma vez e manter o modelo até que seja retreinado manualmente.",
@@ -7512,7 +7514,8 @@
       "sensitivityBalanced": "Equilibrado",
       "sensitivityBudgetTooltip": "O número máximo de alertas que esta configuração pode entregar, aplicado na entrega. A deteção continua a pontuar todos os buckets; esgotado o orçamento, os alertas adicionais são suprimidos até ele se repor.",
       "sensitivityConservative": "Conservador",
-      "sensitivityHintPercentile": "Os buckets são avaliados face à marca p{percentile} das pontuações de treino do modelo — quantos alertas isso gera depende dos seus dados.",
+      "sensitivityHintPercentile": "Um bucket gera alerta quando parece mais incomum do que {percentile}% do que o modelo viu no treinamento. Quantos alertas isso significa depende dos seus dados.",
+      "sensitivityNotDataPercentile": "Quão incomum um bucket precisa ser antes de gerar alerta. Mais alto significa menos alertas, mais extremos. Este nível pontua a confiança do modelo, não os valores dos seus dados, portanto não tem relação com escolher p95 como Função de detecção.",
       "sensitivityRange": "Introduza um número inteiro entre 50 e 99",
       "sensitivityTooltip": "Define a fasquia de pontuação a partir das pontuações de treino do modelo neste percentil — 97 coloca a fasquia nos 3 % superiores das pontuações de treino. As pontuações reais podem diferir do treino, pelo que isto não é uma fração dos buckets futuros nem uma taxa de alertas. Um valor mais baixo desce a fasquia e normalmente envia mais alertas; um período de espera espaça as repetições.",
       "seriesAnomaly": "Anomalia",
@@ -7541,7 +7544,7 @@
       "summaryRetrainEveryDays": "a cada {days}d",
       "summarySchedule": "✓ Agendamento: a cada {schedule}",
       "summaryThreshold": "✓ Limite: {threshold}",
-      "summaryThresholdPercentile": "barra de pontuação em p{percentile}",
+      "summaryThresholdPercentile": "nível {percentile} de 99",
       "summaryTraining": "✓ Treinamento: {days} ({seasonality})",
       "summaryTrainingDays": "{days} dias",
       "timestampAliasBanned": "{column} não pode ser usado como alias de coluna. Use {timeBucket} em vez disso.",
diff --git a/web/src/locales/languages/ru-RU.json b/web/src/locales/languages/ru-RU.json
index 9a2c2432107..beabdc1c54c 100644
--- a/web/src/locales/languages/ru-RU.json
+++ b/web/src/locales/languages/ru-RU.json
@@ -7472,6 +7472,7 @@
       "checkEveryTooltip": "Как часто выполняется задание обнаружения. Может быть больше периода выборки (например, выборка каждые 5 мин, обнаружение каждый час).",
       "destinationRequired": "Требуется хотя бы один получатель!",
       "detectionCharts": "Результаты обнаружения",
+      "detectionFunctionTooltip": "Что измерять в каждом временном интервале. count отслеживает, сколько записей поступает; avg, min, max и sum отслеживают числовое поле. p50, p95 и p99 отслеживают распределение этого поля — например, p95 для response_time_ms замечает рост задержки у самых медленных запросов, даже когда трафик не меняется.",
       "detectionResolution": "Разрешение обнаружения",
       "detectionResolutionTooltip": "Насколько детальна каждая точка данных. Например, \"5m\" означает, что аномалии обнаруживаются с разрешением 5 минут.",
       "deviationChart": "Превышение порога",
@@ -7495,7 +7496,8 @@
       "noFieldsFound": "Поля не найдены",
       "notifications": "Уведомления",
       "notificationsTooltip": "При включении уведомление будет отправлено выбранному получателю при обнаружении аномалии.",
-      "percentile": "Процентиль",
+      "percentile": "Уровень",
+      "previewCaption": "Образец данных, за которыми будет следить это оповещение, чтобы вы могли проверить правильность запроса перед сохранением. Аномалии отмечаются после завершения обучения модели — см. «Результаты обнаружения».",
       "queryMode": "Режим запроса",
       "retrainEvery": "Переобучать каждые",
       "retrainEveryTooltip": "Как часто автоматически переобучать модель. «Никогда» — обучить один раз и сохранять модель до ручного переобучения.",
@@ -7512,7 +7514,8 @@
       "sensitivityBalanced": "Сбалансированная",
       "sensitivityBudgetTooltip": "Максимальное число оповещений, которое эта конфигурация может доставить; ограничение применяется при доставке. Обнаружение продолжает оценивать каждый бакет; когда бюджет исчерпан, дальнейшие оповещения подавляются, пока он не восстановится.",
       "sensitivityConservative": "Консервативная",
-      "sensitivityHintPercentile": "Бакеты оцениваются относительно отметки p{percentile} обучающих оценок модели — сколько оповещений это даст, зависит от ваших данных.",
+      "sensitivityHintPercentile": "Интервал срабатывает, когда выглядит необычнее, чем {percentile}% увиденного моделью при обучении. Сколько это даст оповещений, зависит от ваших данных.",
+      "sensitivityNotDataPercentile": "Насколько необычным должен быть интервал, чтобы сработало оповещение. Чем выше, тем меньше и экстремальнее оповещений. Этот уровень оценивает уверенность модели, а не значения ваших данных, поэтому он не связан с выбором p95 в качестве функции обнаружения.",
       "sensitivityRange": "Введите целое число от 50 до 99",
       "sensitivityTooltip": "Задаёт планку оценки по обучающим оценкам модели на этом процентиле — 97 ставит планку на верхних 3% обучающих оценок. Реальные оценки могут отличаться от обучающих, поэтому это не доля будущих бакетов и не частота оповещений. Меньшее значение опускает планку и обычно отправляет больше оповещений; период охлаждения разрежает повторы.",
       "seriesAnomaly": "Аномалия",
@@ -7541,7 +7544,7 @@
       "summaryRetrainEveryDays": "каждые {days} дн.",
       "summarySchedule": "✓ Расписание: каждые {schedule}",
       "summaryThreshold": "✓ Порог: {threshold}",
-      "summaryThresholdPercentile": "планка оценки на p{percentile}",
+      "summaryThresholdPercentile": "уровень {percentile} из 99",
       "summaryTraining": "✓ Обучение: {days} ({seasonality})",
       "summaryTrainingDays": "{days} дней",
       "timestampAliasBanned": "{column} не может использоваться как псевдоним столбца. Вместо этого используйте {timeBucket}.",
diff --git a/web/src/locales/languages/tr-TR.json b/web/src/locales/languages/tr-TR.json
index f399b755904..790620507b2 100644
--- a/web/src/locales/languages/tr-TR.json
+++ b/web/src/locales/languages/tr-TR.json
@@ -7472,6 +7472,7 @@
       "checkEveryTooltip": "Algılama işinin ne sıklıkta çalıştığı. Numune periyodundan daha büyük olabilir (örn. her 5m'de bir numune, her 1 saatte bir algılama çalıştırın).",
       "destinationRequired": "En az bir hedef gereklidir!",
       "detectionCharts": "Algılama sonuçları",
+      "detectionFunctionTooltip": "Her zaman aralığında neyin ölçüleceği. count kaç kaydın geldiğini izler; avg, min, max ve sum sayısal bir alanı izler. p50, p95 ve p99 o alanın dağılımını izler — örn. response_time_ms için p95, trafik sabit kalsa bile en yavaş isteklerde gecikmenin kötüleşmesini yakalar.",
       "detectionResolution": "Algılama Çözünürlüğü",
       "detectionResolutionTooltip": "Her veri noktası ne kadar ayrıntılı. Örneğin “5m”, anormalliklerin 5 dakikalık çözünürlükte tespit edildiği anlamına gelir.",
       "deviationChart": "Eşik üzerindeki sapma",
@@ -7495,7 +7496,8 @@
       "noFieldsFound": "Alan bulunamadı",
       "notifications": "Bildirimler",
       "notificationsTooltip": "Etkinleştirildiğinde, bir anormallik algılandığında seçilen hedefe bir bildirim gönderilir.",
-      "percentile": "Yüzdelik",
+      "percentile": "Seviye",
+      "previewCaption": "Bu uyarının izleyeceği verilerden bir örnek; böylece kaydetmeden önce sorgunun doğru göründüğünü kontrol edebilirsiniz. Model eğitimini tamamladıktan sonra anomaliler işaretlenir — bkz. Algılama sonuçları.",
       "queryMode": "Sorgu modu",
       "retrainEvery": "Her Şeyi Yeniden Eğitin",
       "retrainEveryTooltip": "Modelin otomatik olarak ne sıklıkta yeniden eğitileceği. “Asla”, bir kez antrenman yapmak ve manuel olarak yeniden eğitilene kadar modeli tutmak anlamına gelir.",
@@ -7512,7 +7514,8 @@
       "sensitivityBalanced": "Dengeli",
       "sensitivityBudgetTooltip": "Bu yapılandırmanın teslim edebileceği en fazla uyarı sayısı; teslimde uygulanır. Algılama her aralığı puanlamaya devam eder; bütçe tükenince, yenilenene kadar ek uyarılar bastırılır.",
       "sensitivityConservative": "Muhafazakâr",
-      "sensitivityHintPercentile": "Aralıklar, modelin eğitim puanlarının p{percentile} işaretine göre değerlendirilir — kaç uyarı çıkacağı verilerinize bağlıdır.",
+      "sensitivityHintPercentile": "Bir zaman aralığı, modelin eğitimde gördüklerinin %{percentile}'inden daha sıra dışı göründüğünde uyarı verir. Bunun kaç uyarıya karşılık geldiği verinize bağlıdır.",
+      "sensitivityNotDataPercentile": "Bir zaman aralığının uyarı vermeden önce ne kadar sıra dışı olması gerektiği. Daha yüksek değer, daha az sayıda ve daha uç uyarı anlamına gelir. Bu seviye modelin güvenini puanlar, verinizin değerlerini değil; dolayısıyla Algılama Fonksiyonu olarak p95 seçmekle ilgisi yoktur.",
       "sensitivityRange": "50 ile 99 arasında bir tam sayı girin",
       "sensitivityTooltip": "Puan çıtasını, modelin eğitim puanlarının bu yüzdelik dilimine göre belirler — 97, çıtayı eğitim puanlarının en üst %3'üne yerleştirir. Canlı puanlar eğitimden farklı olabilir; bu nedenle bu, gelecekteki aralıkların bir payı ya da bir uyarı oranı değildir. Daha düşük bir değer çıtayı düşürür ve genellikle daha fazla uyarı gönderir; bekleme süresi tekrarları seyrekleştirir.",
       "seriesAnomaly": "Anomali",
@@ -7541,7 +7544,7 @@
       "summaryRetrainEveryDays": "her {days}g",
       "summarySchedule": "✓ Zamanlama: her {schedule}",
       "summaryThreshold": "✓ Eşik: {threshold}",
-      "summaryThresholdPercentile": "puan çubuğu p{percentile} düzeyinde",
+      "summaryThresholdPercentile": "99 üzerinden {percentile} seviyesi",
       "summaryTraining": "✓ Eğitim: {days} ({seasonality})",
       "summaryTrainingDays": "{days} gün",
       "timestampAliasBanned": "{column} sütun takma adı olarak kullanılamaz. Bunun yerine {timeBucket} kullanın.",
diff --git a/web/src/locales/languages/vi-VN.json b/web/src/locales/languages/vi-VN.json
index 9eff342f01a..94b661d277b 100644
--- a/web/src/locales/languages/vi-VN.json
+++ b/web/src/locales/languages/vi-VN.json
@@ -7472,6 +7472,7 @@
       "checkEveryTooltip": "Tần suất chạy tác vụ phát hiện. Có thể lớn hơn chu kỳ lấy mẫu (ví dụ: lấy mẫu mỗi 5 phút, chạy phát hiện mỗi 1 giờ).",
       "destinationRequired": "Cần ít nhất một đích!",
       "detectionCharts": "Kết quả phát hiện",
+      "detectionFunctionTooltip": "Đo lường gì trong mỗi khung thời gian. count theo dõi số bản ghi đến; avg, min, max và sum theo dõi một trường số. p50, p95 và p99 theo dõi phân bố của trường đó — ví dụ p95 của response_time_ms phát hiện độ trễ tăng lên ở những yêu cầu chậm nhất ngay cả khi lưu lượng không đổi.",
       "detectionResolution": "Độ phân giải phát hiện",
       "detectionResolutionTooltip": "Mức độ chi tiết của mỗi điểm dữ liệu. Ví dụ: \"5m\" nghĩa là các bất thường được phát hiện ở độ phân giải 5 phút.",
       "deviationChart": "Độ lệch trên ngưỡng",
@@ -7495,7 +7496,8 @@
       "noFieldsFound": "Không tìm thấy trường nào",
       "notifications": "Thông báo",
       "notificationsTooltip": "Khi được bật, một thông báo sẽ được gửi đến đích đã chọn mỗi khi phát hiện bất thường.",
-      "percentile": "Phân vị",
+      "percentile": "Mức",
+      "previewCaption": "Một mẫu dữ liệu mà cảnh báo này sẽ theo dõi, để bạn kiểm tra truy vấn có đúng không trước khi lưu. Các bất thường được đánh dấu sau khi mô hình huấn luyện xong — xem Kết quả phát hiện.",
       "queryMode": "Chế độ truy vấn",
       "retrainEvery": "Huấn luyện lại mỗi",
       "retrainEveryTooltip": "Tần suất tự động huấn luyện lại mô hình. \"Không bao giờ\" nghĩa là huấn luyện một lần và giữ nguyên mô hình cho đến khi huấn luyện lại thủ công.",
@@ -7512,7 +7514,8 @@
       "sensitivityBalanced": "Cân bằng",
       "sensitivityBudgetTooltip": "Số cảnh báo tối đa mà cấu hình này được phép gửi, được áp đặt khi gửi. Việc phát hiện vẫn chấm điểm mọi bucket; khi ngân sách cạn, các cảnh báo tiếp theo bị chặn cho đến khi ngân sách hồi phục.",
       "sensitivityConservative": "Thận trọng",
-      "sensitivityHintPercentile": "Các bucket được so với mốc p{percentile} của điểm huấn luyện mô hình — số cảnh báo tạo ra tùy thuộc vào dữ liệu của bạn.",
+      "sensitivityHintPercentile": "Một khung thời gian sẽ phát cảnh báo khi nó trông bất thường hơn {percentile}% những gì mô hình đã thấy trong quá trình huấn luyện. Số lượng cảnh báo cụ thể phụ thuộc vào dữ liệu của bạn.",
+      "sensitivityNotDataPercentile": "Mức độ bất thường mà một khung thời gian phải đạt trước khi phát cảnh báo. Giá trị cao hơn nghĩa là ít cảnh báo hơn nhưng cực đoan hơn. Mức này đánh giá độ tin cậy của mô hình, chứ không phải giá trị dữ liệu của bạn, nên nó không liên quan đến việc chọn p95 làm Hàm phát hiện.",
       "sensitivityRange": "Nhập số nguyên từ 50 đến 99",
       "sensitivityTooltip": "Đặt mức chuẩn điểm từ điểm huấn luyện của mô hình tại phân vị này — 97 đặt mức chuẩn ở 3% điểm huấn luyện cao nhất. Điểm thực tế có thể khác với huấn luyện, nên đây không phải tỷ lệ các bucket tương lai và không phải tần suất cảnh báo. Giá trị thấp hơn hạ mức chuẩn và thường gửi nhiều cảnh báo hơn; thời gian chờ giãn cách các cảnh báo lặp lại.",
       "seriesAnomaly": "Bất thường",
@@ -7541,7 +7544,7 @@
       "summaryRetrainEveryDays": "mỗi {days} ngày",
       "summarySchedule": "✓ Lịch trình: mỗi {schedule}",
       "summaryThreshold": "✓ Ngưỡng: {threshold}",
-      "summaryThresholdPercentile": "ngưỡng điểm tại p{percentile}",
+      "summaryThresholdPercentile": "mức {percentile} trên 99",
       "summaryTraining": "✓ Đào tạo: {days} ({seasonality})",
       "summaryTrainingDays": "{days} ngày",
       "timestampAliasBanned": "{column} không thể được sử dụng làm bí danh cột. Sử dụng {timeBucket} thay thế.",
diff --git a/web/src/locales/languages/zh-CN.json b/web/src/locales/languages/zh-CN.json
index bbb84eb4a10..026e668f641 100644
--- a/web/src/locales/languages/zh-CN.json
+++ b/web/src/locales/languages/zh-CN.json
@@ -7472,6 +7472,7 @@
       "checkEveryTooltip": "检测任务的运行频率。可以大于采样周期（例如，每 5 分钟采样一次，每 1 小时运行一次检测）。",
       "destinationRequired": "至少需要一个目标！",
       "detectionCharts": "检测结果",
+      "detectionFunctionTooltip": "每个时间桶中要衡量的内容。count 统计到达的记录数；avg、min、max 和 sum 衡量某个数值字段。p50、p95 和 p99 衡量该字段的分布——例如，response_time_ms 的 p95 能发现最慢请求的延迟在恶化，即使流量保持平稳。",
       "detectionResolution": "探测分辨率",
       "detectionResolutionTooltip": "每个数据点的粒度如何。例如，“5m” 表示以 5 分钟的分辨率检测到异常。",
       "deviationChart": "按类型划分的偏差",
@@ -7495,7 +7496,8 @@
       "noFieldsFound": "未找到字段",
       "notifications": "通知",
       "notificationsTooltip": "启用后，只要检测到异常，就会向所选目标发送通知。",
-      "percentile": "百分位",
+      "percentile": "级别",
+      "previewCaption": "此告警将要监视的数据样本，方便你在保存前确认查询是否正确。模型训练完成后会标记异常——请参阅检测结果。",
       "queryMode": "查询模式",
       "retrainEvery": "对所有人进行重新训练",
       "retrainEveryTooltip": "自动重新训练模型的频率。“从不” 表示训练一次并保留模型直到手动重新训练。",
@@ -7512,7 +7514,8 @@
       "sensitivityBalanced": "平衡",
       "sensitivityBudgetTooltip": "此配置可投递告警的上限，在投递时强制执行。检测仍会为每个数据桶评分；预算用尽后，多余的告警将被抑制，直到预算恢复。",
       "sensitivityConservative": "保守",
-      "sensitivityHintPercentile": "数据桶按模型训练分数的 p{percentile} 标记来判定——产生多少告警取决于你的数据。",
+      "sensitivityHintPercentile": "当某个时间桶看起来比模型在训练中见到的 {percentile}% 更异常时，就会触发告警。具体会产生多少条告警取决于你的数据。",
+      "sensitivityNotDataPercentile": "时间桶必须有多异常才会触发告警。数值越高，告警越少、越极端。此级别评分的是模型的置信度，而非数据的值，因此与选择 p95 作为检测函数无关。",
       "sensitivityRange": "请输入 50 到 99 之间的整数",
       "sensitivityTooltip": "根据模型训练分数在该百分位处设定分数门槛——97 将门槛设在训练分数最高的 3% 处。实际分数可能与训练不同，因此这既不是未来数据桶的占比，也不是告警频率。取值越低门槛越低，通常发送的告警越多；冷却时间会拉开重复告警的间隔。",
       "seriesAnomaly": "异常",
@@ -7541,7 +7544,7 @@
       "summaryRetrainEveryDays": "每 {days}天",
       "summarySchedule": "✓ 调度：每 {schedule}",
       "summaryThreshold": "✓ 阈值：{threshold}",
-      "summaryThresholdPercentile": "分数门槛位于 p{percentile}",
+      "summaryThresholdPercentile": "级别 {percentile} / 99",
       "summaryTraining": "✓ 训练：{days}（{seasonality}）",
       "summaryTrainingDays": "{days} 天",
       "timestampAliasBanned": "{column} 不能用作列别名。请改用 {timeBucket}。",
diff --git a/web/src/locales/languages/zh-TW.json b/web/src/locales/languages/zh-TW.json
index 134eda9a570..735ba1fe8ed 100644
--- a/web/src/locales/languages/zh-TW.json
+++ b/web/src/locales/languages/zh-TW.json
@@ -7467,6 +7467,7 @@
       "checkEveryTooltip": "偵測工作的執行頻率。可以大於取樣週期 (例如每 5 分鐘取樣，每 1 小時執行偵測)。",
       "destinationRequired": "至少需要一個目標！",
       "detectionCharts": "偵測結果",
+      "detectionFunctionTooltip": "每個時間桶中要測量的內容。count 觀察有多少記錄到達；avg、min、max 和 sum 觀察某個數值欄位。p50、p95 和 p99 觀察該欄位的分布 — 例如 response_time_ms 的 p95 能捕捉到即使流量持平，最慢請求的延遲正在變差。",
       "detectionResolution": "偵測解析度",
       "detectionResolutionTooltip": "每個資料點的精細程度。例如「5m」表示以 5 分鐘的解析度偵測異常。",
       "deviationChart": "超出閾值的偏差",
@@ -7490,7 +7491,8 @@
       "noFieldsFound": "找不到欄位",
       "notifications": "通知",
       "notificationsTooltip": "啟用後，每當偵測到異常時，就會向選取的目標傳送通知。",
-      "percentile": "百分位",
+      "percentile": "層級",
+      "previewCaption": "此警示將監控的資料樣本，讓你在儲存前確認查詢是否正確。模型訓練完成後會標示異常 — 請參閱「偵測結果」。",
       "queryMode": "查詢模式",
       "retrainEvery": "重新訓練頻率",
       "retrainEveryTooltip": "自動重新訓練模型的頻率。「從不」表示訓練一次並保留模型直到手動重新訓練。",
@@ -7507,7 +7509,8 @@
       "sensitivityBalanced": "平衡",
       "sensitivityBudgetTooltip": "此設定可傳送警示的上限，於傳送時強制執行。偵測仍會為每個區間評分；預算用盡後，多餘的警示會被抑制，直到預算回復。",
       "sensitivityConservative": "保守",
-      "sensitivityHintPercentile": "區間依模型訓練分數的 p{percentile} 標記判定——產生多少警示取決於你的資料。",
+      "sensitivityHintPercentile": "當某個時間桶看起來比模型在訓練中見過的 {percentile}% 更不尋常時，就會觸發警示。這代表多少個警示取決於你的資料。",
+      "sensitivityNotDataPercentile": "時間桶必須多不尋常才會觸發警示。數值越高代表警示越少、越極端。這個層級評估的是模型的信心度，而非你的資料值，因此與選擇 p95 作為偵測函式無關。",
       "sensitivityRange": "請輸入 50 到 99 之間的整數",
       "sensitivityTooltip": "依模型訓練分數在此百分位處設定分數門檻——97 將門檻設在訓練分數最高的 3% 處。實際分數可能與訓練不同，因此這既不是未來區間的占比，也不是警示頻率。數值越低門檻越低，通常發送的警示越多；冷卻時間會拉開重複警示的間隔。",
       "seriesAnomaly": "異常",
@@ -7536,7 +7539,7 @@
       "summaryRetrainEveryDays": "每 {days} 天",
       "summarySchedule": "✓ 排程：每 {schedule}",
       "summaryThreshold": "✓ 閾值：{threshold}",
-      "summaryThresholdPercentile": "分數條柱位於 p{percentile}",
+      "summaryThresholdPercentile": "層級 {percentile}，共 99 級",
       "summaryTraining": "✓ 訓練：{days} 天（{seasonality}）",
       "summaryTrainingDays": "{days} 天",
       "timestampAliasBanned": "{column} 不能用作欄位別名。請改用 {timeBucket}。",
diff --git a/web/src/utils/alerts/anomalySqlBuilder.spec.ts b/web/src/utils/alerts/anomalySqlBuilder.spec.ts
index 5e655c3f389..44da35013f7 100644
--- a/web/src/utils/alerts/anomalySqlBuilder.spec.ts
+++ b/web/src/utils/alerts/anomalySqlBuilder.spec.ts
@@ -51,14 +51,13 @@ describe("toDetectionFunctionSql", () => {
     expect(toDetectionFunctionSql("p50", "duration")).toBe("approx_percentile_cont(duration, 0.5)");
   });
 
-  it("converts p75 to approx_percentile_cont", () => {
-    expect(toDetectionFunctionSql("p75", "duration")).toBe(
-      "approx_percentile_cont(duration, 0.75)",
-    );
+  // p75/p90 are not DetectionFunction variants, so they must not expand into a query the API rejects.
+  it("leaves p75 unexpanded — the backend has no such detection function", () => {
+    expect(toDetectionFunctionSql("p75", "duration")).toBe("p75(duration)");
   });
 
-  it("converts p90 to approx_percentile_cont", () => {
-    expect(toDetectionFunctionSql("p90", "duration")).toBe("approx_percentile_cont(duration, 0.9)");
+  it("leaves p90 unexpanded — the backend has no such detection function", () => {
+    expect(toDetectionFunctionSql("p90", "duration")).toBe("p90(duration)");
   });
 
   it("converts p95 to approx_percentile_cont", () => {
@@ -75,10 +74,8 @@ describe("toDetectionFunctionSql", () => {
 
   // ── Already-wrapped forms (API returns parenthesized) ─────────────────────
 
-  it("converts already-wrapped p90(duration) from API", () => {
-    expect(toDetectionFunctionSql("p90(duration)", "*")).toBe(
-      "approx_percentile_cont(duration, 0.9)",
-    );
+  it("leaves an already-wrapped p90(duration) alone", () => {
+    expect(toDetectionFunctionSql("p90(duration)", "*")).toBe("p90(duration)");
   });
 
   it("converts already-wrapped p95(latency) from API", () => {
@@ -105,8 +102,10 @@ describe("toDetectionFunctionSql", () => {
 
   // ── Case insensitivity ────────────────────────────────────────────────────
 
-  it("handles uppercase P90", () => {
-    expect(toDetectionFunctionSql("P90", "duration")).toBe("approx_percentile_cont(duration, 0.9)");
+  it("handles uppercase P99", () => {
+    expect(toDetectionFunctionSql("P99", "duration")).toBe(
+      "approx_percentile_cont(duration, 0.99)",
+    );
   });
 
   it("handles mixed-case P95(Duration) from API", () => {
@@ -134,13 +133,13 @@ describe("toDetectionFunctionSql", () => {
   });
 
   it("handles unknown percentile name like p99_9", () => {
-    // only p50/p75/p90/p95/p99 are in the map
+    // only p50/p95/p99 are in the map
     expect(toDetectionFunctionSql("p99_9", "duration")).toBe("p99_9(duration)");
   });
 
   it("handles percentile with complex field expression", () => {
-    expect(toDetectionFunctionSql("p90", "response_time_ms")).toBe(
-      "approx_percentile_cont(response_time_ms, 0.9)",
+    expect(toDetectionFunctionSql("p95", "response_time_ms")).toBe(
+      "approx_percentile_cont(response_time_ms, 0.95)",
     );
   });
 
@@ -153,10 +152,10 @@ describe("toDetectionFunctionSql", () => {
 
   // ── Match existing behavior in alertQueryBuilder.ts ───────────────────────
 
-  it("matches alertQueryBuilder format for p90", () => {
-    // alertQueryBuilder produces: approx_percentile_cont(column, 0.9)
-    const result = toDetectionFunctionSql("p90", "duration");
-    expect(result).toBe("approx_percentile_cont(duration, 0.9)");
+  it("matches alertQueryBuilder format for p95", () => {
+    // alertQueryBuilder produces: approx_percentile_cont(column, 0.95)
+    const result = toDetectionFunctionSql("p95", "duration");
+    expect(result).toBe("approx_percentile_cont(duration, 0.95)");
   });
 
   it("matches alertQueryBuilder format for p99", () => {
@@ -165,6 +164,40 @@ describe("toDetectionFunctionSql", () => {
   });
 });
 
+// ─── Detection function coverage ─────────────────────────────────────────────
+
+// The backend DetectionFunction enum is the contract; a name the dropdown cannot
+// offer must never be expanded into SQL, or the preview would show a query the
+// API rejects on save.
+describe("detection function coverage", () => {
+  const BACKEND_PERCENTILES: Record<string, string> = {
+    p50: "0.5",
+    p95: "0.95",
+    p99: "0.99",
+  };
+
+  it.each(Object.entries(BACKEND_PERCENTILES))(
+    "expands %s, which the backend accepts",
+    (fn, fraction) => {
+      expect(toDetectionFunctionSql(fn, "duration")).toBe(
+        `approx_percentile_cont(duration, ${fraction})`,
+      );
+    },
+  );
+
+  it.each(["p25", "p75", "p90", "p999"])(
+    "leaves %s unexpanded, since the backend rejects it",
+    (fn) => {
+      expect(toDetectionFunctionSql(fn, "duration")).toBe(`${fn}(duration)`);
+    },
+  );
+
+  it.each(["count", "avg", "sum", "min", "max"])("passes %s through as a plain aggregate", (fn) => {
+    const expected = fn === "count" ? "count(*)" : `${fn}(duration)`;
+    expect(toDetectionFunctionSql(fn, "duration")).toBe(expected);
+  });
+});
+
 // ─── buildAnomalyPreviewSql ─────────────────────────────────────────────────
 
 describe("buildAnomalyPreviewSql", () => {
@@ -266,16 +299,16 @@ describe("buildAnomalyPreviewSql", () => {
     expect(result).toContain("approx_percentile_cont(duration, 0.95) AS value");
   });
 
-  it("generates SQL with p90 as approx_percentile_cont", () => {
+  it("generates SQL with p50 as approx_percentile_cont", () => {
     const result = buildAnomalyPreviewSql({
       query_mode: "filters",
       stream_name: "metrics",
-      detection_function: "p90",
+      detection_function: "p50",
       detection_function_field: "latency",
       training_window_days: 7,
       histogram_interval: "1m",
     });
-    expect(result).toContain("approx_percentile_cont(latency, 0.9) AS value");
+    expect(result).toContain("approx_percentile_cont(latency, 0.5) AS value");
   });
 
   it("generates SQL with p99 as approx_percentile_cont", () => {
@@ -405,15 +438,15 @@ describe("buildAnomalyPreviewSql", () => {
     expect(result).toContain("count(*) AS value");
   });
 
-  it("handles already-wrapped p90(duration) from API", () => {
+  it("handles already-wrapped p99(duration) from API", () => {
     const result = buildAnomalyPreviewSql({
       query_mode: "filters",
       stream_name: "logs",
-      detection_function: "p90(duration)",
+      detection_function: "p99(duration)",
       training_window_days: 7,
       histogram_interval: "10m",
     });
-    expect(result).toContain("approx_percentile_cont(duration, 0.9) AS value");
+    expect(result).toContain("approx_percentile_cont(duration, 0.99) AS value");
   });
 
   // ── Full SQL structure ────────────────────────────────────────────────────
diff --git a/web/src/utils/alerts/anomalySqlBuilder.ts b/web/src/utils/alerts/anomalySqlBuilder.ts
index 38b49792b21..5e4cc1fdec5 100644
--- a/web/src/utils/alerts/anomalySqlBuilder.ts
+++ b/web/src/utils/alerts/anomalySqlBuilder.ts
@@ -18,19 +18,19 @@ import {
   operatorNeedsValue,
 } from "@/utils/alerts/anomalyFilterOperators";
 
+// Exactly the backend DetectionFunction percentile variants; p75/p90 would be rejected on save.
 const percentileMap: Record<string, number> = {
   p50: 0.5,
-  p75: 0.75,
-  p90: 0.9,
   p95: 0.95,
   p99: 0.99,
 };
 
 /**
  * Converts a detection function name + field to the SQL expression for the
- * histogram query. Percentile short-names (p50, p95, etc.) are expanded to
- * `approx_percentile_cont(field, percentile)` matching the regular alert
- * query builder behaviour.
+ * histogram query. The percentile short-names the backend accepts (p50, p95,
+ * p99) are expanded to `approx_percentile_cont(field, percentile)`; any other
+ * name is wrapped verbatim. Regular alerts accept a wider percentile set and
+ * build their own SQL in alertQueryBuilder.ts.
  */
 export const toDetectionFunctionSql = (rawFn: string, field: string): string => {
   // API may return already-wrapped forms like "p90(duration)" or "avg(size)"
diff --git a/web/src/utils/alerts/anomalySummaryGenerator.spec.ts b/web/src/utils/alerts/anomalySummaryGenerator.spec.ts
index 8bb8056ba57..3ecc0ac7f9b 100644
--- a/web/src/utils/alerts/anomalySummaryGenerator.spec.ts
+++ b/web/src/utils/alerts/anomalySummaryGenerator.spec.ts
@@ -49,21 +49,23 @@ const config = (overrides: Record<string, unknown> = {}) => ({
 });
 
 describe("generateAnomalySummary — sensitivity line", () => {
-  it("states the training-score percentile, never a live anomaly rate", () => {
-    // The percentile indexes TRAINING scores and promises nothing about live buckets.
+  it("states the stored level, never a live anomaly rate", () => {
+    // The stored number indexes TRAINING scores and promises nothing about live buckets.
     const summary = generateAnomalySummary(config(), [], t);
-    expect(summary).toContain("score bar at p97");
+    expect(summary).toContain("level 97");
     expect(summary).not.toContain("anomaly rate");
+    // A bare "97" beside "Threshold" would read as a count or a percentage of alerts.
+    expect(summary).not.toMatch(/Threshold:[^<]*<[^>]*>\s*97\s*</);
   });
 
-  it("reports the conservative tier as its percentile", () => {
-    expect(generateAnomalySummary(config({ threshold: 99 }), [], t)).toContain("score bar at p99");
+  it("reports the conservative tier as its level", () => {
+    expect(generateAnomalySummary(config({ threshold: 99 }), [], t)).toContain("level 99");
   });
 
-  it("a stored budget replaces the percentile with the enforced cap", () => {
+  it("a stored budget replaces the level with the enforced cap", () => {
     const summary = generateAnomalySummary(config({ alert_budget_per_day: 2 }), [], t);
     expect(summary).toContain("at most 2 alerts/day");
-    expect(summary).not.toContain("score bar");
+    expect(summary).not.toMatch(/\blevel \d/);
   });
 
   it("a sub-daily budget reads as alerts per week, singular at one", () => {
@@ -76,7 +78,7 @@ describe("generateAnomalySummary — sensitivity line", () => {
     expect(summary).toContain("at most 0.35 alerts/week");
   });
 
-  // The percentile input can be emptied, and the write-back passes "" through
+  // The level input can be emptied, and the write-back passes "" through
   // unchanged; a blank must not be numberified into a claim.
   it.each([
     ["", "empty string"],
@@ -85,7 +87,8 @@ describe("generateAnomalySummary — sensitivity line", () => {
     ["abc", "non-numeric"],
   ])("omits the sensitivity line entirely for %s (%s)", (threshold) => {
     const summary = generateAnomalySummary(config({ threshold }), [], t);
-    expect(summary).not.toContain("score bar");
+    expect(summary).not.toContain("Threshold:");
+    expect(summary).not.toMatch(/\blevel \d/);
     expect(summary).not.toContain("anomaly rate");
     // The rest of the summary still renders.
     expect(summary).toContain("14 days");
diff --git a/web/src/views/slos/SloList.vue b/web/src/views/slos/SloList.vue
index 32bee7c6a24..720e4ce7595 100644
--- a/web/src/views/slos/SloList.vue
+++ b/web/src/views/slos/SloList.vue
@@ -35,7 +35,7 @@
     <template #actions>
       <!-- The provider behind the Terraform export, which is otherwise only
            discoverable once the export dialog is already open. -->
-      <IacRegistryLinks data-test="slos-slolist-iac-registries" />
+      <IacRegistryLinks data-test="slos-slolist-iac-registries" :compact="isMobile" />
       <OButton variant="primary" size="sm-action" data-test="slos-slolist-new" @click="goToNew">
         {{ t("slos.new") }}
       </OButton>
@@ -491,6 +491,7 @@ import type { StatItem } from "@/lib/data/StatStrip/OStatStrip.types";
 import type { SloListItem } from "@/ts/interfaces/slo";
 import { toast } from "@/lib/feedback/Toast/useToast";
 import sloService from "@/services/slos";
+import useBreakpoint from "@/composables/useBreakpoint";
 import alertsService from "@/services/alerts";
 import { sloDetailRoute } from "@/utils/alerts/sloAlertRouting";
 import { slosToTerraform } from "@/utils/slos/sloTerraform";
@@ -509,6 +510,7 @@ import {
 } from "@/composables/useSloFormat";
 
 const { t } = useI18nTyped();
+const { isMobile } = useBreakpoint();
 const router = useRouter();
 const route = useRoute();
 const store = useStore();
