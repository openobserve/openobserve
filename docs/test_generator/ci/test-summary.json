[
  {
    "title": "WFF-UI-11: the folder survives the trip into the editor and back",
    "action": "extend",
    "verifies": "the editor shows the workflow's folder as static text (create-only folder picker hidden) when editing an existing workflow"
  },
  {
    "title": "WFF-UI-08: the trigger-type tab filters inside the active folder",
    "action": "extend",
    "verifies": "selecting the alert-fired trigger tab keeps the matching workflow and hides the incident_event workflow inside the active folder"
  },
  {
    "title": "WFF-UI-02: create a folder from the rail, and reject an empty name",
    "action": "extend",
    "verifies": "a newly created folder's rail tab navigates the list into that folder (not just a phantom store entry)"
  }
]
