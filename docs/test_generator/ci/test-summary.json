[
  {
    "title": "unauthenticated bootstrap exposes exactly the login-page keys and no version/instance",
    "action": "new",
    "verifies": "GET /config returns the exact 10-key bootstrap set with build_type=opensource and no version/license_expiry/instance"
  },
  {
    "title": "authenticated full config returns version and reserved keywords without instance",
    "action": "new",
    "verifies": "GET /api/{org}/config returns a non-empty version, build_type=opensource, non-empty sql_reserved_keywords, and no instance"
  },
  {
    "title": "login page renders from bootstrap; sign-in loads full config and About shows version",
    "action": "new",
    "verifies": "the login page renders from the unauthenticated bootstrap, sign-in reveals the nav rail, and About shows a non-empty version with build_type=opensource"
  },
  {
    "title": "bootstrap re-fire does not clobber the already-loaded full config",
    "action": "new",
    "verifies": "revisiting the login route and returning to About preserves the loaded full-config version"
  },
  {
    "title": "POST on the full-config endpoint is rejected with 405",
    "action": "new",
    "verifies": "POST /api/{org}/config returns 405 Method Not Allowed"
  },
  {
    "title": "config reload endpoint is 401 when unauthenticated",
    "action": "new",
    "verifies": "GET /config/reload returns 401 when unauthenticated"
  },
  {
    "title": "About page copy-commit-hash shows a success toast",
    "action": "new",
    "verifies": "copying the commit hash on the About page surfaces a success toast"
  },
  {
    "title": "Enterprise upgrade quota badge shows the configured ingestion quota — not wired: ingestion_quota never returned by backend (EnterpriseUpgradeDialog.vue:512)",
    "action": "new",
    "verifies": "the enterprise quota badge renders the configured ingestion_quota instead of the hardcoded 50 fallback (fixme, ENT only)"
  }
]
