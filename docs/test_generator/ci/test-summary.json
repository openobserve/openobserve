[
  {
    "title": "drillable cell shows both copy and drilldown in the same hover toolbar",
    "action": "append",
    "verifies": "a plain (drillable) X-axis table cell reveals both the copy button and the drilldown search icon in the unified hover toolbar"
  },
  {
    "title": "aggregated column shows only the copy button with no drilldown icon",
    "action": "append",
    "verifies": "an aggregated Y-axis count(code) cell reveals the copy button but no drilldown icon"
  }
]
