[
  {
    "title": "should preserve composite identity when cloned (not flattened to simple)",
    "action": "extend",
    "verifies": "the clone keeps its composite type and expression, and is created disabled even when the source was enabled"
  },
  {
    "title": "should clone a composite whose child alert was deleted — not wired: delete of a referenced child is refused 409 child_referenced (mod.rs:2157-2168) and alerts-api-helpers.js:233 swallows the error; a genuinely missing child is rejected at resolve_children (service.rs:462-466, ChildNotAccessible)",
    "action": "extend",
    "verifies": "the stale-child clone scenario is surfaced as a feature gap (fixme) instead of a false-green pass"
  }
]
