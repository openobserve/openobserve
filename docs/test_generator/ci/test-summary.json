[
  {
    "title": "CDN variant shows O2 globals and pinned 0.4.3 bundle URLs",
    "action": "append",
    "verifies": "the CDN install/init snippets render the renamed O2_RUM/O2_LOGS globals and the pinned 0.4.3 CDN bundle URLs"
  },
  {
    "title": "CDN variant contains no OO_RUM / OO_LOGS / 0.3.4 anywhere",
    "action": "append",
    "verifies": "the old OO_RUM/OO_LOGS globals and the 0.3.4 version are fully absent from the rendered card"
  },
  {
    "title": "switching back to NPM restores the default install command",
    "action": "append",
    "verifies": "switching the shared pkg variant group back to NPM restores the default npm install command"
  }
]
