[
  {
    "title": "Community Slack Invite dialog never renders on the OSS build",
    "action": "new",
    "verifies": "the Community Slack Invite popup is structurally absent (count 0) on first paint and after a full page reload on the OSS build"
  },
  {
    "title": "Day-2 dismiss snoozes the invite for 7 days — not wired: CommunitySlackInvite.vue:73 render gate blocks dismiss() on OSS",
    "action": "new",
    "verifies": "dismissing the day-2 popup keeps it snoozed (status pending_day2, dismissCount incremented) instead of permanently due"
  },
  {
    "title": "Join Slack resolves the invite for good — not wired: CommunitySlackInvite.vue:73 render gate blocks joinSlack() on OSS",
    "action": "new",
    "verifies": "clicking Join Slack resolves the invite record so the popup never re-asks"
  },
  {
    "title": "Member-count caption renders (qualitative vs count) — not wired: CommunitySlackInvite.vue:73 render gate blocks the dialog on OSS",
    "action": "new",
    "verifies": "the social-proof member-count caption renders non-empty text when the dialog opens"
  }
]
