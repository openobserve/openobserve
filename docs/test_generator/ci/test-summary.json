[
  {
    "title": "the percentile control is labeled Level and disowns the detection-function meaning",
    "action": "append",
    "verifies": "the percentile control is labeled 'Level' (not 'Percentile') and its info tooltip explains it scores model confidence, unrelated to the Detection Function"
  },
  {
    "title": "the preview caption appears once a stream is chosen and points at Detection results",
    "action": "append",
    "verifies": "the data-preview chart shows a caption only once a stream is selected, and the caption points at 'Detection results'"
  },
  {
    "title": "budget mode does not render the percentile info tooltip",
    "action": "append",
    "verifies": "a budget-mode config renders the budget tiers and hides the percentile 'Level' info tooltip"
  },
  {
    "title": "the Detection Function label explains p50/p95/p99 measure a field, not sensitivity",
    "action": "append",
    "verifies": "the Detection Function info tooltip explains p50/p95/p99 measure a field's distribution and never mentions sensitivity"
  }
]
