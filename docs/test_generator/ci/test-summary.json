[
  { "title": "Valid URL — no inline error, save proceeds", "action": "new", "verifies": "Typing a valid https:// URL shows no error and the template saves successfully" },
  { "title": "Disallowed scheme — inline error + save block", "action": "new", "verifies": "javascript:alert(1) shows 'Unsupported URL scheme' inline error and blocks save with toast" },
  { "title": "Template variable accepted — no error, save succeeds", "action": "new", "verifies": "{alert_url} passes both inline and save-time validation" },
  { "title": "\"Not a URL\" rejected — inline error + save block", "action": "new", "verifies": "Bare words trigger 'Enter a valid URL' message inline and block save" },
  { "title": "Relative path accepted — no error, save succeeds", "action": "new", "verifies": "/web/logs?stream=app passes validation" },
  { "title": "Empty URL — no error (incomplete, not invalid)", "action": "new", "verifies": "Empty URL field shows no error; filling valid URL after also shows no error" },
  { "title": "mailto: with valid email accepted", "action": "new", "verifies": "mailto:ops@example.com passes validation and save succeeds" },
  { "title": "mailto: without valid address rejected", "action": "new", "verifies": "mailto: alone is rejected as NOT_A_URL" },
  { "title": "Control-character bypass rejected (whitespace smuggling)", "action": "new", "verifies": "Tab character in scheme is stripped and the URL is rejected" },
  { "title": "`{x}javascript:alert(1)` — partial template var with hostile tail rejected", "action": "new", "verifies": "Partial template variable with literal hostile tail is rejected" },
  { "title": "`http:` / `https://` (empty host) rejected", "action": "new", "verifies": "Allowed scheme with empty host is rejected as structurally invalid" },
  { "title": "Multiple link rows — each validated independently", "action": "new", "verifies": "Valid URL on row 0 and bad scheme on row 1 both show correct statuses; save blocks" },
  { "title": "`data:text/html,...` scheme rejected", "action": "new", "verifies": "data: URI scheme is rejected" },
  { "title": "`JavaScript:` (mixed case) rejected", "action": "new", "verifies": "Scheme comparison is case-insensitive" },
  { "title": "`java%73cript:` (URL-encoded scheme) rejected", "action": "new", "verifies": "URL-encoded hostile schemes are decoded and rejected" },
  { "title": "Custom mode bypasses link validation", "action": "new", "verifies": "Switching to Custom mode hides the links section entirely" }
]
