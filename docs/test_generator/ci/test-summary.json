[
  { "title": "should hide the Profiles tab in the Streams type filter when the flag is off", "action": "new", "verifies": "the LogStream stream-type filter omits the Profiles tab (sibling logs/metrics/traces/metadata tabs still render) when profiling_enabled is off" },
  { "title": "should hide the Profiles tab on the Ingestion Custom page when the flag is off", "action": "new", "verifies": "the Ingestion Custom route omits the Profiles tab while Logs/Metrics/Traces render when profiling_enabled is off" },
  { "title": "should hide the left-nav Profiles menu item when the flag is off", "action": "new", "verifies": "the left-nav Profiles item is absent when profiling_enabled is off" },
  { "title": "should show the Profiles tab in the Streams type filter and select it on click", "action": "new", "verifies": "with profiling_enabled on, the Profiles tab renders in the stream-type filter and becomes the selected tab on click" },
  { "title": "should show the Profiles tab on the Ingestion Custom page and route to the profiles module", "action": "new", "verifies": "with profiling_enabled on, the Ingestion Profiles tab renders and clicking it routes to /ingestion/custom/profiles" },
  { "title": "should show the left-nav Profiles menu item when the flag is on", "action": "new", "verifies": "with profiling_enabled on, the left-nav Profiles item appears (proves the config mock propagated into zoConfig)" },
  { "title": "should show the Home no-data Profiles chip when the flag is on and the org has no data (needs empty org: UsageTab.vue:539)", "action": "new", "verifies": "the Home empty-state Profiles chip renders and routes to profiles (fixme: no empty-org provisioning in the suite)" },
  { "title": "should hide the Home no-data Profiles chip when the flag is off (needs empty org: UsageTab.vue:539)", "action": "new", "verifies": "the Home empty state renders without the Profiles chip while the non-gated OpenTelemetry chip stays visible (fixme: no empty-org provisioning in the suite)" }
]
