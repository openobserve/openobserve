[
  {
    "title": "should render the role summary strip for the root org",
    "action": "append",
    "verifies": "the root and total role tiles render alongside the root user's row"
  },
  {
    "title": "should skip the custom-roles fetch in OSS (diff regression guard)",
    "action": "append",
    "verifies": "no request fires to the custom-roles or batched-role endpoints in OSS"
  },
  {
    "title": "should narrow and restore the users list via the role facet filter",
    "action": "append",
    "verifies": "clicking a role tile filters the table to that role and the total tile restores it"
  },
  {
    "title": "should not render the custom-role select in the Add-user dialog (OSS)",
    "action": "append",
    "verifies": "the enterprise/cloud-only custom-role field never renders in OSS"
  },
  {
    "title": "should render a role tile for a created admin user",
    "action": "append",
    "verifies": "the role strip enumerates tiles from data, not a hardcoded list"
  },
  {
    "title": "should render custom roles — not wired: enterprise/cloud + rbac_enabled required (web/src/aws-exports.ts:30; web/src/stores/index.ts:100)",
    "action": "append",
    "verifies": "custom roles render only in an enterprise/cloud build with rbac_enabled (parked as fixme)"
  }
]
