[
  { "title": "should render checklist with Task 1 active and Tasks 2 & 3 locked on deep-link entry", "action": "new", "verifies": "deep-link shortcut renders the checklist with Task 1 active (blue 1, install CTA), Tasks 2 & 3 locked (gray, lock icon), skip link visible, and Open Record button disabled" },
  { "title": "should complete Task 1 via attestation checkbox and unlock Task 2", "action": "new", "verifies": "checking the install attestation completes Task 1 (green check + Undo button) and unlocks Task 2 (incognito checkbox visible, Task 3 stays locked)" },
  { "title": "should complete Task 2 via attestation checkbox and unlock Task 3 in waiting state", "action": "new", "verifies": "after both attestations, Task 2 done (green check), Task 3 shows waiting spinner with refresh button, Open Record button stays disabled" },
  { "title": "should relock Tasks 2 and 3 when undo is clicked on Task 1 install attestation", "action": "new", "verifies": "undoing Task 1 returns it to active and relocks downstream Tasks 2 and 3 (sequential gating in reverse)" },
  { "title": "should enter Journey editor after clicking Skip from Context A checklist", "action": "new", "verifies": "clicking Skip exits the checklist and renders the Journey editor step with Record/Replay toolbar buttons" },
  { "title": "should open ExtensionSetupDialog on Record click with disabled Continue and visible Skip", "action": "new", "verifies": "Record button opens dialog with checklist, Continue button disabled, Skip link visible, progress badge at 0 of 3" },
  { "title": "should show Task 3 waiting and keep Continue disabled after completing Tasks 1 & 2 in dialog", "action": "new", "verifies": "completing Tasks 1&2 inside the dialog unlocks Task 3 in waiting state but Continue stays disabled (requires extension connection)" },
  { "title": "should update progress badge reactively as tasks complete in dialog", "action": "new", "verifies": "progress badge increments 0→1→2 of 3 as tasks are completed, never reaches 3 without extension connection" },
  { "title": "should show Skip link for record action and hide it for replay action in dialog", "action": "new", "verifies": "Skip link renders for record action dialog but is absent from replay action dialog" },
  { "title": "should close dialog via skip link and return to Journey editor without starting a recording", "action": "new", "verifies": "dialog Skip closes the dialog and returns user to Journey editor step with no side effects" },
  { "title": "should keep Open Record button disabled when Task 3 is in waiting state (Tasks 1 & 2 done)", "action": "new", "verifies": "Open Record button stays disabled even after completing Tasks 1&2 because setupAllDone requires extension connection" },
  { "title": "should relock only Task 3 when undo is clicked on Task 2 incognito attestation, leaving Task 1 done", "action": "new", "verifies": "undoing Task 2 returns only it to active and relocks Task 3; Task 1 remains done (selective reverse gating)" },
  { "title": "should open Chrome Web Store in a new tab when Install button is clicked", "action": "new", "verifies": "clicking Install from CWS button opens a new browser tab pointing to the Chrome Web Store URL" },
  { "title": "should show contextual blocking hint text below disabled Continue button in dialog", "action": "new", "verifies": "hint text changes contextually: install hint → incognito hint → connect hint as tasks are completed" },
  { "title": "should open ExtensionSetupDialog on Replay click without the Skip link", "action": "new", "verifies": "Replay button opens dialog but without Skip link; Continue is still disabled" },
  { "title": "should render Task 2 step-by-step instructions list when active", "action": "new", "verifies": "when Task 2 is active, an ordered list of Chrome settings instructions is rendered" },
  { "title": "should close dialog via Escape key and return to Journey editor", "action": "new", "verifies": "pressing Escape closes the dialog and returns to the Journey editor step" },
  { "title": "Task 1 auto-completes via extension probe — UNWIRED (no extension in CI)", "action": "new", "verifies": "placeholder: Task 1 auto-completes with detected-automatically badge when extension probe succeeds" },
  { "title": "Task 3 completes with green check when extension connected — UNWIRED (no extension in CI)", "action": "new", "verifies": "placeholder: Task 3 completes with green check when extension bridge connects" },
  { "title": "Continue button enables when all three tasks done in dialog — UNWIRED (no extension in CI)", "action": "new", "verifies": "placeholder: Continue button enables when connected + incognito both true" },
  { "title": "Open Record button enables and starts recording — UNWIRED (no extension in CI)", "action": "new", "verifies": "placeholder: Open Record button enables when setupAllDone is true and transitions to editor with auto-record" },
  { "title": "Incognito attestation triggers extension re-probe in dialog — UNWIRED (no extension in CI)", "action": "new", "verifies": "placeholder: incognito ack triggers verify event causing parent to re-probe extension connection" },
  { "title": "Install attestation superseded by live probe — UNWIRED (no extension in CI)", "action": "new", "verifies": "placeholder: when extension connects, the attestation checkbox is replaced by detected-automatically badge" }
]
