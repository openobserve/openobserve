[
  {
    "title": "should display timeline chip with timezone label and formatted time range",
    "action": "new",
    "verifies": "the schema drawer timeline chip renders with a non-empty timezone label and formatted doc_time range (not raw epoch values)"
  },
  {
    "title": "should display browser timezone fallback when store timezone is unset",
    "action": "new",
    "verifies": "when no explicit timezone is set in the Vuex store, the timezone label falls back to the browser's Intl.DateTimeFormat resolved timezone"
  },
  {
    "title": "should persist timeline chip across tab switches",
    "action": "new",
    "verifies": "the timeline chip (timezone label + doc_time range) remains visible and unchanged when switching between schema drawer tabs (Settings ↔ Configuration)"
  },
  {
    "title": "Timezone change reflects on schema drawer re-open — not wired: analyst needs to validate combined cross-page flow",
    "action": "new",
    "verifies": "placeholder test (fixme) for verifying that changing the timezone on the Logs page and re-opening the schema drawer shows the updated timezone and re-formatted time range"
  }
]
