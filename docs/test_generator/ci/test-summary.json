[
  {
    "title": "should show an error toast and refuse patterns when multiple streams are selected @P0 @smoke",
    "action": "new",
    "verifies": "the multi-stream patterns guard fires an error toast before any API call when two streams are selected"
  },
  {
    "title": "should not show the multi-stream patterns toast for a single stream @P1",
    "action": "new",
    "verifies": "the guard is skipped and no multi-stream error toast is emitted for a single selected stream"
  }
]
