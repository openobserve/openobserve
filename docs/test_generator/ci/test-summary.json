[
  {
    "title": "should render the four OSS alert-type tabs and hide the enterprise-only Anomaly tab",
    "action": "new",
    "verifies": "the migrated OToggleGroup renders all/scheduled/realTime/composite tabs via alert-list-tab-* selectors and the enterprise Anomaly tab is absent on OSS"
  },
  {
    "title": "should set a tab active and filter rows to the matching alert type",
    "action": "new",
    "verifies": "clicking a tab sets data-state=on and the row set changes to match (all -> composite -> realtime), including the composite badge"
  },
  {
    "title": "should exclude Composite from Scheduled and show only real-time rows on Realtime",
    "action": "new",
    "verifies": "Scheduled excludes composite+realtime rows and Realtime shows strictly is_real_time === true rows"
  },
  {
    "title": "should render the header subtitle and navigate the four section tabs",
    "action": "new",
    "verifies": "the header title+subtitle render and the section switcher navigates all four sibling routes with the subtitle persisting"
  },
  {
    "title": "should toggle the search scope between This folder and All folders",
    "action": "new",
    "verifies": "the search-scope toggle flips active state (data-state=on) between the two folder scopes"
  },
  {
    "title": "should show the OTable empty state when switching tabs on an empty folder",
    "action": "new",
    "verifies": "switching tabs on an empty folder renders the OTable empty state without crashing"
  }
]
