[
  { "title": "OSS - Domain to Organization mappings section is hidden (cloud-gated)", "action": "new", "verifies": "the cloud gate hides the Domain to Organization mappings section on an OSS build" },
  { "title": "Cloud - Domain to Organization mappings section renders and Add works", "action": "new", "verifies": "the section renders in the meta org and the add happy-path produces a list row (clean-skip on OSS)" },
  { "title": "Edit mapping — not wired: DomainOrgMappings.vue:173-177 (section cloud-gated, OrganizationSettings.vue:146)", "action": "new", "verifies": "editing a mapping persists the changed target org in place" },
  { "title": "Delete mapping — not wired: DomainOrgMappings.vue:196-206 (section cloud-gated, OrganizationSettings.vue:146)", "action": "new", "verifies": "deleting a mapping removes the row and reveals the empty state" },
  { "title": "Duplicate-domain rejection — not wired: DomainOrgMappingDialog.vue:159-164 (section cloud-gated)", "action": "new", "verifies": "a duplicate domain surfaces an error toast and keeps the dialog open" },
  { "title": "Backend persists domain_org_mappings on host Save — not wired: settings.rs:157-207 (#[cfg(feature = \"cloud\")] only)", "action": "new", "verifies": "the host Save button persists domain_org_mappings and shows a success toast" },
  { "title": "Domain validation — not wired: DomainOrgMappings.schema.ts + DomainManagement.schema.ts isValidDomain", "action": "new", "verifies": "an invalid domain format shows a validation error" },
  { "title": "allowed_user role displays as \"User\" badge — not wired: DomainOrgMappings.vue badgeRole()", "action": "new", "verifies": "the allowed_user base role renders the localized \"User\" badge" },
  { "title": "Optional user_group dropped from payload when empty — not wired: DomainOrgMappingDialog.vue onSubmit normalization", "action": "new", "verifies": "an empty user_group renders no group tag on the row" },
  { "title": "Sign-in auto-add via domain match — not wired: jwt.rs:837-838 process_domain_org_mapping (#[cfg(feature = \"cloud\")] only)", "action": "new", "verifies": "a mapped domain round-trips through /api/_meta/settings (prerequisite for sign-in auto-add)" }
]
