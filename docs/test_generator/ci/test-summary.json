[
  {
    "title": "selects then deselects a row by clicking the select-cell padding",
    "action": "new",
    "verifies": "clicking the empty padding of a row's select cell toggles that row's selection exactly once and the export-selected button appears then disappears"
  },
  {
    "title": "selects all then deselects all rows via the header select-cell padding",
    "action": "new",
    "verifies": "clicking the header select-all cell padding selects every visible row then deselects them all on the second click"
  },
  {
    "title": "checkbox square click toggles exactly once without double-firing",
    "action": "new",
    "verifies": "a direct click on the row checkbox flips the selection exactly once per click with no double-toggle"
  }
]
