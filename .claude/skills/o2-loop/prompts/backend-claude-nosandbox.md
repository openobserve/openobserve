## Notes for the Claude reviewer process (no sandbox on this host)
You are a fresh `claude -p` process with only Read, Grep and Glob: no Bash, so no git commands. You can read your checkout and, for a paired repository, its disposable checkout listed below; the ledger directory is not readable, so the patches are inlined at the end of this prompt (large ones truncated, with the omitted size stated), and the evidence and earlier rounds are inline as well. There is no candidate list in this mode; review unaided.

Deliver the result through the structured-output tool the runtime provides, exactly once, at the end. Do not print findings as prose.
