/* eslint-disable no-undef */
module.exports = {
  plugins: [
    '@tailwindcss/postcss',  // Use the separate PostCSS plugin for Tailwind
    'autoprefixer',           // Autoprefixer for cross-browser compatibility
  ],
};