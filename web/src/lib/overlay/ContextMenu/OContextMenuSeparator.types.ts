/**
 * OContextMenuSeparator.types.ts — public types for OContextMenuSeparator.
 */

export interface ContextMenuSeparatorSlots {
  /** Separators render no content; the slot exists for API symmetry only. */
  default?: () => unknown;
}
