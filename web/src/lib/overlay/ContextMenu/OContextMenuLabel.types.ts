/**
 * OContextMenuLabel.types.ts — public types for OContextMenuLabel.
 */

export interface ContextMenuLabelSlots {
  /** Label content — a non-interactive heading for the group beneath it */
  default?: () => unknown;
}
