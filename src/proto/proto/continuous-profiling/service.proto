syntax = "proto3";
package myservice;

// Changed service name to match backend
service MyService {
    rpc HandleRequest (Request) returns (Response);
}

message Request {
    bytes data = 1;
}

message Response {
    bytes result = 1;
} 