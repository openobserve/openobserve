# OpenObserve instance where mobile RUM data lands (the SDK's customEndpoint host)
OO_URL=https://dev.o2aks1.internal.zinclabs.dev
OO_ORG=3GldGL2anfrpTHgdLsuPpnqy4eI

# Credentials for the OpenObserve Search API + dashboard UI login
OO_USER=you@openobserve.ai
# QUOTE the password if it contains '#' (dotenv treats unquoted # as a comment)
OO_PASS="changeme"

# The app under test (React Native / Android)
RN_ANDROID_APP_ID=com.o2rumtester
RN_SERVICE=o2-rum-tester
RN_ENV=testing
