# Mobile RUM SDK — CI notes

How `.github/workflows/mobile-sdk-e2e.yml` runs. **Model B: self-contained, no secrets** — each
device job stands up its own OpenObserve on the runner and points the app at it, exactly like
`playwright.yml` does for the web tests.

## Why self-contained (and why it's possible)

- **RUM ingestion is OSS** — a plain OpenObserve accepts the data, so no special/enterprise cluster.
- The other test workflows already run a throwaway OpenObserve on the runner with the well-known
  defaults (`root@example.com` / `Complexpass#123` / org `default` / `localhost:5080`) as plain
  values — **no secrets**. This workflow does the same.
- Result: **every run is isolated** (its own instance), so the Android and iOS jobs run in
  **parallel** with no collision — this removes the shared-service problem entirely.

## How the app gets pointed at the local instance (build-time config injection)

Each app reads its RUM target from a build-time-overridable config. The committed value is the dev
cluster (so local dev is unchanged); CI regenerates it to point at the local instance:

| App | Mechanism | Regenerated by |
|---|---|---|
| RN (o2-rum-tester) | `rum.config.json` (imported by `App.tsx`) | `scripts/gen-config.js` (postinstall, reads `O2_RUM_*`) |
| Android-native | `BuildConfig.OO_*` fields | `build.gradle.kts` reads `O2_RUM_*` via `System.getenv` |
| iOS-native | `Sources/GeneratedConfig.swift` | `gen-config.sh` (reads `O2_RUM_*`) |

Env the CI job sets before building: `O2_RUM_HOST`, `O2_RUM_ORG`, `O2_RUM_TOKEN`, `O2_RUM_ENV`.
With none set, every generator writes the **committed defaults** — so running them locally is a no-op.

### Networking

- **Android emulator** can't see the host's `localhost`; it reaches the host at **`10.0.2.2`**. So the
  app posts to `http://10.0.2.2:5080` while the suite's API/UI checks (on the host) use `localhost:5080`.
- **iOS simulator** shares the host network → app and suite both use `localhost:5080`.
- Cleartext HTTP to the local instance: Android manifests set `usesCleartextTraffic="true"`; the RN
  iOS build adds an ATS `NSAllowsLocalNetworking` key at build time (committed source stays HTTPS).

## Testing the latest SDK (version injection)

The fixture apps **pin** the SDK version (RN `alpha.6`, android/iOS `alpha.5`) so ordinary runs are
reproducible. But on a **release** we want to test the *new* version, not the frozen pin. So the SDK
version is a build input, defaulting to the pin:

| App | Override mechanism | Env |
|---|---|---|
| RN | workflow `npm install @openobserve/mobile-react-native@<ver>` before build | `O2_RN_SDK_VERSION` |
| Android-native | `build.gradle.kts`: `System.getenv("O2_ANDROID_SDK_VERSION") ?: "<pin>"` | `O2_ANDROID_SDK_VERSION` |
| iOS-native | workflow `sed` of `project.yml` `exactVersion` before xcodegen | `O2_IOS_SDK_VERSION` |

The **`mobile-sdk-release` dispatch** carries `{sdk, version}`; the "Resolve SDK version override" step
maps it to the right env so a release **builds + tests against the latest SDK**. A manual run can do the
same via the `sdk` + `sdk_version` inputs. Unset → the committed pin (reproducible).

## Triggers

| Trigger | Fires when | Runs |
|---|---|---|
| `repository_dispatch` (`mobile-sdk-release`) | an SDK repo publishes a release (cross-repo) | preflight **+ device jobs** |
| `pull_request` (paths) | a PR touches `tests/mobile-testing/**` or the workflow | preflight only |
| `workflow_dispatch` | manual; `run_full_suite=true` to include devices | preflight (+ devices) |

### The cross-repo trigger

The SDK source lives in **other repos**, so an SDK change never appears as a PR here. Each SDK repo
sends a dispatch on release (needs a PAT with `repo` scope on `openobserve/openobserve`):

```yaml
- name: Trigger OpenObserve mobile E2E
  run: |
    curl -sf -X POST \
      -H "Authorization: Bearer ${{ secrets.OO_DISPATCH_TOKEN }}" \
      -H "Accept: application/vnd.github+json" \
      https://api.github.com/repos/openobserve/openobserve/dispatches \
      -d '{"event_type":"mobile-sdk-release","client_payload":{"sdk":"ios","version":"${{ github.ref_name }}"}}'
```

The three SDKs to wire: `openobserve-sdk-ios`, the Android SDK (Maven `ai.openobserve:o2-sdk-android-*`),
and `@openobserve/mobile-react-native` (npm).

## Runners

- **Android** → `ubuntu-latest` + KVM emulator + OpenObserve via Docker. No macOS.
- **iOS** → `macos-latest` + simulator + OpenObserve via the macOS release binary (Docker isn't
  available on macOS runners). **Free on this public repo** (macOS runners bill only on private repos).

## First-run shakeout (authored, not yet executed on GitHub runners)

The workflow is complete but these bits can't be validated from a laptop — expect to tune them on the
first `workflow_dispatch → run_full_suite=true`:

1. **OpenObserve image/binary** — `OO_IMAGE` (`public.ecr.aws/zinclabs/openobserve:latest`) and the
   macOS release-asset name; pin both once green.
2. **`/rumtoken` response shape** — the token parse (`.data.rum_token // .rum_token // .token`) falls
   back to a static token if it can't parse; confirm ingestion accepts it (or fix the parse).
3. **iOS ATS for iOS-native** — the RN iOS build gets an ATS tweak; the native iOS app may also need
   `NSAllowsLocalNetworking` (via `project.yml` info properties) if it can't reach `localhost`.
4. **Xcode pin** — `latest-stable`; replace with a hard version once the SDK's Xcode is confirmed.

## Validated locally

- Suite parses (`playwright --list`): 29 tests, 4 platforms.
- `gen-config.js` / `gen-config.sh` write the committed defaults when no env is set (backward compat).
- The RN app **Metro-bundles cleanly** with the new `rum.config.json` import.
- Android/iOS native config wiring is in place (`BuildConfig.OO_*`, `GeneratedConfig`).

## Optional later

- Host next to the enterprise/alpha pipeline (mobile RUM is enterprise-flavored) — but note macOS is
  free here (public) and billed there (private).
- A per-run RUM `run-id` attribute is **no longer needed** for isolation (each run has its own
  instance); only relevant if you ever move back to a shared cluster.
