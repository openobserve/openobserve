{
  "queries": [
    {
      "id": "Q113",
      "sql": "SELECT _timestamp, COALESCE(facility_zone, 'UNKNOWN_ZONE') AS safe_zone, CONCAT(sorter_model, ' @ ', control_center) AS sorter_op_label FROM \"query_agent_test_v2\" ORDER BY _timestamp ASC LIMIT 12",
      "category": "string_functions",
      "time_range": {
        "start": 1780314717000000,
        "end": 1780314885000000
      },
      "expected": {
        "row_count": 12,
        "columns": [
          "_timestamp",
          "safe_zone",
          "sorter_op_label"
        ],
        "note": "COALESCE + CONCAT combining sorter and facility zone"
      }
    },
    {
      "id": "Q114",
      "sql": "SELECT _timestamp, re_match(log, 'warehouse|event') AS matched, split_part(log, ' ', 1) AS first_word FROM \"query_agent_test_v2\" WHERE log IS NOT NULL ORDER BY _timestamp DESC LIMIT 12",
      "category": "string_functions",
      "time_range": {
        "start": 1780314777000000,
        "end": 1780314945000000
      },
      "expected": {
        "row_count": 12,
        "columns": [
          "_timestamp",
          "matched",
          "first_word"
        ],
        "note": "re_match + split_part on log field"
      }
    },
    {
      "id": "Q115",
      "sql": "SELECT _timestamp, UPPER(pallet_id) AS pallet_upper, LOWER(conveyor_lane) AS lane_lower, CONCAT(sorter_model, '-', operation_mode) AS sorter_op, TRIM(COALESCE(build_signature, '')) AS clean_build, LENGTH(CONCAT(facility_zone, '-', control_center)) AS label_len FROM \"query_agent_test_v2\" WHERE pallet_id IS NOT NULL ORDER BY _timestamp DESC LIMIT 12",
      "category": "string_functions",
      "time_range": {
        "start": 1780314837000000,
        "end": 1780315005000000
      },
      "expected": {
        "row_count": 12,
        "columns": [
          "_timestamp",
          "pallet_upper",
          "lane_lower",
          "sorter_op",
          "clean_build",
          "label_len"
        ],
        "note": "5 string functions: UPPER, LOWER, CONCAT, TRIM+COALESCE, LENGTH+CONCAT"
      }
    },
    {
      "id": "Q116",
      "sql": "SELECT _timestamp, pallet_id, re_match(pallet_id, 'PL-00[1-4]') AS is_front_pallet, split_part(pallet_id, '-', 1) AS prefix, split_part(pallet_id, '-', 2) AS suffix_num FROM \"query_agent_test_v2\" WHERE re_match(pallet_id, 'PL-00[1-8]') = TRUE ORDER BY suffix_num ASC",
      "category": "string_functions",
      "time_range": {
        "start": 1780314897000000,
        "end": 1780315065000000
      },
      "expected": {
        "row_count": 28,
        "columns": [
          "_timestamp",
          "pallet_id",
          "is_front_pallet",
          "prefix",
          "suffix_num"
        ],
        "note": "re_match with regex character class + split_part on dash delimiter"
      }
    },
    {
      "id": "Q117",
      "sql": "SELECT control_center, STRING_AGG(DISTINCT sorter_model, ' | ' ORDER BY sorter_model ASC) AS sorter_models, STRING_AGG(DISTINCT operation_mode, ', ') AS op_modes, COUNT(*) AS event_cnt FROM \"query_agent_test_v2\" WHERE control_center IS NOT NULL GROUP BY control_center HAVING COUNT(*) >= 2 ORDER BY event_cnt DESC",
      "category": "string_functions",
      "time_range": {
        "start": 1780314957000000,
        "end": 1780315125000000
      },
      "expected": {
        "row_count": 4,
        "columns": [
          "control_center",
          "sorter_models",
          "op_modes",
          "event_cnt"
        ],
        "note": "STRING_AGG with DISTINCT + ORDER BY, second STRING_AGG for another column"
      }
    },
    {
      "id": "Q118",
      "sql": "SELECT _timestamp, pallet_id, build_signature, SUBSTR(build_signature, 1, 8) AS build_prefix, SUBSTR(build_signature, 10, 2) AS build_suffix, REPLACE(build_signature, 'BS-', 'BUILD_') AS replaced_sig FROM \"query_agent_test_v2\" WHERE build_signature LIKE 'BS-2026-%' ORDER BY build_signature ASC, _timestamp ASC",
      "category": "string_functions",
      "time_range": {
        "start": 1780315017000000,
        "end": 1780315185000000
      },
      "expected": {
        "row_count": 23,
        "columns": [
          "_timestamp",
          "pallet_id",
          "build_signature",
          "build_prefix",
          "build_suffix",
          "replaced_sig"
        ],
        "note": "SUBSTR (two positions) + REPLACE + LIKE pattern filter"
      }
    },
    {
      "id": "Q119",
      "sql": "SELECT _timestamp, pallet_id, conveyor_lane, facility_zone, LOWER(CONCAT(SUBSTR(conveyor_lane, 1, 4), '-', SUBSTR(facility_zone, 5, 1))) AS short_code, CASE WHEN re_match(CONCAT(conveyor_lane, '-', facility_zone), 'LANE-[AB]-ZONE-[12]') THEN 'core' ELSE 'edge' END AS location_type FROM \"query_agent_test_v2\" WHERE conveyor_lane IS NOT NULL AND facility_zone IS NOT NULL ORDER BY location_type ASC, short_code ASC",
      "category": "string_functions",
      "time_range": {
        "start": 1780315077000000,
        "end": 1780315245000000
      },
      "expected": {
        "row_count": 28,
        "columns": [
          "_timestamp",
          "pallet_id",
          "conveyor_lane",
          "facility_zone",
          "short_code",
          "location_type"
        ],
        "note": "Nested: LOWER(CONCAT(SUBSTR, SUBSTR)) + CASE with re_match on CONCAT result"
      }
    },
    {
      "id": "Q120",
      "sql": "SELECT _timestamp, pallet_id, sorter_model, COALESCE(sorter_model, 'UNKNOWN_SORTER') AS safe_sorter, NULLIF(sorter_model, 'SORT-Q5') AS exclude_q5, COALESCE(NULLIF(sorter_model, 'SORT-Q5'), 'FILTERED') AS filtered_sorter FROM \"query_agent_test_v2\" ORDER BY _timestamp ASC LIMIT 12",
      "category": "string_functions",
      "time_range": {
        "start": 1780315137000000,
        "end": 1780315305000000
      },
      "expected": {
        "row_count": 12,
        "columns": [
          "_timestamp",
          "pallet_id",
          "sorter_model",
          "safe_sorter",
          "exclude_q5",
          "filtered_sorter"
        ],
        "note": "COALESCE + NULLIF + chained COALESCE(NULLIF(...))"
      }
    },
    {
      "id": "Q121",
      "sql": "SELECT _timestamp, log, pallet_id, re_match(log, 'warehouse|event') AS has_keyword, re_match(log, '[0-9]+') AS has_digit, LENGTH(log) AS msg_length, POSITION('record' IN log) AS record_pos FROM \"query_agent_test_v2\" WHERE re_match(log, 'warehouse') = TRUE ORDER BY msg_length DESC LIMIT 12",
      "category": "string_functions",
      "time_range": {
        "start": 1780315197000000,
        "end": 1780315365000000
      },
      "expected": {
        "row_count": 12,
        "columns": [
          "_timestamp",
          "log",
          "pallet_id",
          "has_keyword",
          "has_digit",
          "msg_length",
          "record_pos"
        ],
        "note": "re_match (two patterns) + LENGTH + POSITION on log field"
      }
    },
    {
      "id": "Q122",
      "sql": "SELECT facility_zone, STRING_AGG(DISTINCT CONCAT(operation_mode, ':', CAST(ROUND(throughput_rate, 0) AS VARCHAR)), '; ' ORDER BY throughput_rate DESC) AS mode_tput_list, COUNT(DISTINCT operation_mode) AS distinct_modes FROM \"query_agent_test_v2\" WHERE throughput_rate IS NOT NULL GROUP BY facility_zone ORDER BY distinct_modes DESC",
      "category": "string_functions",
      "time_range": {
        "start": 1780315257000000,
        "end": 1780315425000000
      },
      "expected": {
        "row_count": 0,
        "columns": [
          "facility_zone",
          "mode_tput_list",
          "distinct_modes"
        ],
        "note": "STRING_AGG with CONCAT of mode + rounded throughput, ORDER BY inside STRING_AGG"
      }
    }
  ],
  "last_query_id": 122
}